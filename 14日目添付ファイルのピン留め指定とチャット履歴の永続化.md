---
project: C.L.A.I.R.E.(さぽーとAI)
date: 2026-08-15
tags: [添付ファイル, ナレッジ, ピン留め, チャット履歴, 永続化, セッション, UI, 自作UI, 資料生成, Office, openpyxl, python-pptx, python-docx, Google, OAuth, GoogleDocs, GoogleSheets, 作業ログ]
status: 計画(着手前)。①📚ピン留めによるファイル指定 ②チャット履歴の永続化(New Session/リネーム/削除/検索) ③資料・コード生成(Excel/PowerPoint/Word/JSON) ④Google Workspace連携(Docs/Sheets/Drive出力)
---

[[サポートAI作製計画/13日目UI機能化(ウェイクワード・自動送信)とWeb検索実装.md|13日目]]で①VADの3秒無音保持・②ウェイクワード・③自動送信・④Web検索(パイプライン結線+出典表示まで)が完了し、実機での連続対話も通った。14日目の今日は、13日目「📌次のステップ」3の**📎添付ファイルを「このファイルについて答えて」と指定して使わせる機能**を主軸に、同5の**UIの機能化の残り**のうち**チャット履歴の永続化(New Sessionでの新規チャット作成・リネーム/削除/検索)**を進める。

さらに、13日目「次のステップ」4で「エクセルやPowerPointの生成」として持ち越されていた項目を今日のスコープへ引き上げ、**③資料・コード生成(Excel/PowerPoint/Word/JSON)**と、その出力先を外へ広げる**④Google Workspace連携(調査レポートをGoogleドキュメント/スプレッドシートへ出力)**を追加する。①②が「AIに情報を**入れる**側」の整備なのに対し、③④は「AIから成果物を**出す**側」の整備であり、対になっている。

> [!note] このノートの位置づけ
> [[サポートAI作製計画/ノート作成規則.md]]に従い、**着手前に作業内容を列挙した計画ノート**として作成した。実行後に各セクションの「結果」「分析」「改善策」を追記していく。

> [!warning] 今日のスコープ外(意図的に触らない)
> - **引用元チップ(Web検索の出典UI)**:出典は13日目④で応答末尾にテキストとして付くようになっており、実害がない。専用チップUIはAPIレスポンスの設計変更を伴うため後回し
> - **テレメトリ(tok/s)・Event Logの実データ化**:ダミー表示のままでも対話には影響しない
> - **Web検索の自動判定**(ルーターが検索要否を判断する仕組み)・**モデル切替ドロップダウン**・**思考モードトグル**:いずれも13日目からの持ち越しだが、今日は①〜④に集中する
> - **VRAM逼迫によるモデル入替スラッシング**・**`run_turn()`の同期ブロッキング**・**複数枚画像の同時添付**:引き続き保留
> - **③のグラフ画像埋め込み(matplotlib→xlsx/pptxへの貼り込み)・PDF出力**:まずは表・箇条書き・文章の生成を通す。グラフは`openpyxl.chart`で試せるが、フォント問題(日本語豆腐化)を抱え込むので今日はやらない
> - **④のGmail/カレンダー連携・既存Googleファイルの読み取り**:OAuthスコープを`drive.file`(**自分のアプリが作ったファイルだけ**)に絞る方針のため、既存ドライブの読み取りは構造的に今日はできない。必要になった時点でスコープ拡張を別途判断する
> - **③で生成したファイルのバージョン管理・上書き確認**:同名なら黙って上書きする。`workspace/`はあくまで作業場所という位置づけ

---

## ⓪ 着手前の現状調査(ノートとコードの乖離の棚卸し)

### 背景/目的

13日目のノートに「④はその後のセッションで実施済みだったが追記が漏れていた」とあったため、**着手前にコードベースの実態を確認**してから作業計画を立てた。結果、ノートに残っていない実装が想定より多いことが分かったので、まずここに棚卸しを記録する(このセクションだけは調査済みのため結果を先に書く)。

### 結果

**1. コード内の「〇日目」コメントは19日目まで進んでいる**

`scripts/`配下を`14日目`〜`19日目`で検索したところ、以下が見つかった。ノート番号(13日目まで)とコーディングセッション側の日番号がずれている。

| コード内の日番号 | 対応する実装 | ノートへの記載 |
|---|---|---|
| 14日目 | `attached_document_text`(添付全文の1ターン埋め込み)、Web検索の`support_ai_auto_pipe.py`結線 | Web検索側のみ13日目④の追記あり。`attached_document_text`は**未記載** |
| 15日目(指示1〜3) | ウェイクワード検出の可視化、**読み上げ速度バーの機能化**(`audio.playbackRate`へ反映) | **未記載** |
| 16日目・17日目 | `run_turn()`のスレッド化+`cancel_turn`(誤送信の即時停止) | **未記載** |
| 18日目 | ウェイクワード「C.L.A.I.R.E.」スペルアウト表記の検出 | 13日目の🐛追記に記載あり |
| 19日目 | 括弧書きの二重読み解消、`force_finalize_pending()`(2ターン目以降の連結バグ) | 13日目の🐛追記に記載あり |

→ 読み上げ速度バーは**既に機能化済み**(`static/index.html` 1770行付近)。13日目「次のステップ」5に「読み上げ速度バー」が残っていたのは記載漏れなので、**今日のスコープから外す**。

**2. 全テストは通っている(284 passed, 5 subtests passed)**

```powershell
cd C:\Users\gakuh\Documents\obsidian\サポートAI作製計画\scripts
python -m pytest tests/ -q
# → 284 passed, 5 subtests passed in 29.29s
```

13日目「次のステップ」4に残っていた`TestDocumentEndpoints::test_upload_success_calls_ingest_and_returns_result`の500エラーも**解消済み**(`/documents`が`extract_text()`を先に1回だけ呼ぶ形に変わった際に直ったと思われる)。→ **今日のスコープから外す**。

**3. 🐛 `attached_document_text`はクライアント側が値を入れておらず、事実上の死にコードだった**

これが①の作業に直結する重要な発見。サーバ側(`voice_gateway.py`の`/documents`が`result["text"]`を返す、`support_ai_auto_pipe.Pipe._extract_last_user_attached_document_text()`が読む、`_recall()`が全文をそのまま使う)は**一式そろっている**のに、`static/index.html`側で`lastAttachedDocumentText`へ**代入している箇所が1つも無い**。

```powershell
cd C:\Users\gakuh\Documents\obsidian\サポートAI作製計画\scripts
grep -n "lastAttachedDocumentText" static/index.html
# 1009:let lastAttachedDocumentText = null;   ← 宣言
# 1481:  if (lastAttachedDocumentText) {      ← 読み出し(常にnullなので絶対に入らない)
# 1482:    msg.attached_document_text = lastAttachedDocumentText;
# 1493:  lastAttachedDocumentText = null;     ← クリア
```

`uploadDocument()`(1518行〜)は`lastAttachedDocument = file.name;`(1532行)は行うが、**レスポンスの`body.text`を`lastAttachedDocumentText`へ入れていない**。つまり「添付ファイルの全文をそのままプロンプトへ入れる」経路は一度も動いたことがなく、常に`attached_document`(ファイル名)だけを送るRAG検索フォールバックに落ちていた。

→ 12日目までの「狙いどおり動かなかった」体感の一因はこれである可能性が高い。①で最初に潰す。

### 分析

ノートへの追記が漏れたまま実装だけが進むと、**「もう直っている問題を次のノートで計画してしまう」**(今日の500エラー・読み上げ速度バーがまさにこれ)、逆に**「実装したつもりの経路が繋がっていないことに気付かない」**(`lastAttachedDocumentText`)という両方向の事故が起きる。今日のように**着手前に`grep`で実態を確認する**手順をノート作成規則の運用に組み込む価値がある。

### 改善策

- 本ノートの末尾「📌次のステップ」には、**コード内の日番号コメントとノート番号の対応表**を必ず更新して残す
- 作業開始時に`grep -rn "N日目" scripts/`で直近の未記載実装を洗ってから計画を立てる(今日から実施)

---

## ① 📚ナレッジ一覧からの「対象ファイルのピン留め」(`index.html`/`voice_gateway.py`修正)

### 背景/目的

現状、📎でアップロードした文書を会話で使わせる方法は**「アップロード直後の次の1回だけ自動で対象になる」**(`lastAttachedDocument`を1回使ってクリア)しかない。そのため、

- 先週アップロードした資料について後から聞きたい → **指定する手段がない**(通常のRAG全文脈検索に混ざるだけ)
- 1つのファイルについて何往復も質問したい → **2ターン目以降は対象から外れる**
- 「このファイルについて答えて」と言っても、AIは「このファイル」がどれかを知らない

という状態になっている。12日目までの試行で「狙いどおり動かなかった」のはこの構造と、⓪-3の死にコードが原因。

**方針(相談の上で確定):ファイルの指定は📚ナレッジ一覧のクリック選択(ピン留め)で行う。**

| 論点 | 決定 | 理由 |
|---|---|---|
| ファイルの指定方法 | **📚一覧でクリックしてピン留め**。解除するまで毎ターン対象になる | 音声認識の精度に依存しない。「どのファイルが対象か」が画面上に常に見えるため、AIが別のファイルを見ている事故が起きない |
| 発話内のファイル名を自動照合する案 | **採用しない(今回は)** | Voskの誤認識で別ファイルにヒットすると、ユーザーからは「なぜか違う資料の話をしている」としか見えず原因究明が難しい。ピン留めが安定してから追加検討する |
| 直近アップロードの自動適用 | **ピン留めへ統合**。アップロード成功時に**自動でピン留めする**(=直後の1ターンだけ、ではなく解除するまで有効) | 「アップロードした直後に質問する」という最頻ケースの体験を落とさずに、sticky化できる |
| 全文埋め込みとRAG検索の使い分け | **`DOC_INLINE_MAX_CHARS`(20000字)以下は全文をそのまま埋め込み、超える場合はsource絞り込みRAG** | サーバ側に既にある分岐(`_recall()`)をそのまま使う。⓪-3の結線漏れを直せば動く |
| ピン留めの永続化 | **`localStorage`にファイル名のみ**保存(13日目のトグルと同じ方式) | 本文はサーバから取り直す。リロードで消えると「なぜ効かなくなったか」が分からなくなるため |

### 作業内容

- [ ] ⓪-3の死にコードを解消する:`uploadDocument()`のレスポンスから`body.text`を`lastAttachedDocumentText`へ代入する
- [ ] `voice_gateway.py`に`GET /documents/{filename}/text`を追加する(ピン留め時に全文を取り直すため。`DOC_INLINE_MAX_CHARS`超のときは`{"text": null}`を返す)
- [ ] `tests/test_voice_gateway.py`に上記エンドポイントのテスト(正常系/未登録ファイルで404/閾値超でnull)を追加する
- [ ] `renderKnowledgeList()`にクリックでのピン留め/解除を実装する(選択中は`.pinned`クラスで視覚的に区別する)
- [ ] ピン留め中は入力欄の上に「📌 <ファイル名> について回答中 ✕」のチップを常時表示する(何が対象か常に見える状態にする)
- [ ] `sendTextInput()`を、**1回でクリアする方式から「ピンが立っている限り毎回同梱する」方式**へ変更する
- [ ] ピン留めを`localStorage`へ永続化し、ページ読み込み時に復元する(復元時は`GET /documents/{filename}/text`で本文を取り直す。ファイルが既に削除されていたらピンを黙って解除する)
- [ ] `deleteDocument()`でピン留め中のファイルを削除したら、ピンも解除する
- [ ] `doc_ingest.list_documents()`の戻り値に文字数(または`inline_available`)を含めるか検討する(一覧上で「全文埋め込み可否」を出せると挙動が読みやすい。YAGNIなら見送る)
- [ ] アップロード成功時に自動でピン留めする(従来の「次の1回だけ」の置き換え)

### 実施手順(TDD)

**Step 1: RED — `tests/test_voice_gateway.py`に全文取得エンドポイントのテストを追加**

```python
class TestDocumentTextEndpoint(unittest.TestCase):
    """14日目①: ピン留めしたファイルの全文をクライアントが取り直すための経路。"""

    def test_returns_extracted_text_when_under_threshold(self):
        # doc_ingestをフェイクに差し替え、短いテキストを返させる
        ...
        resp = client.get("/documents/workout.pdf/text")
        self.assertEqual(resp.status_code, 200)
        self.assertEqual(resp.json()["text"], "腕立て30回")

    def test_returns_null_text_when_over_inline_threshold(self):
        # DOC_INLINE_MAX_CHARSを超える長文はnullにして、
        # クライアントにRAG(source絞り込み)フォールバックを使わせる
        resp = client.get("/documents/huge.pdf/text")
        self.assertIsNone(resp.json()["text"])

    def test_returns_404_for_unregistered_filename(self):
        resp = client.get("/documents/unknown.pdf/text")
        self.assertEqual(resp.status_code, 404)
```

```powershell
cd C:\Users\gakuh\Documents\obsidian\サポートAI作製計画\scripts
python -m pytest tests/test_voice_gateway.py -k DocumentText -v   # 期待: 404(ルート未定義)で失敗
```

**Step 2: GREEN — `voice_gateway.py`にエンドポイントを追加**

```python
@app.get("/documents/{filename}/text")
def get_document_text_endpoint(filename: str):
    """14日目①: ピン留めしたファイルの抽出全文を返す。

    POST /documents のレスポンス(result["text"])と同じ判定基準で、
    DOC_INLINE_MAX_CHARS以下なら全文を、超える場合はnullを返す。
    nullのときクライアントはattached_document(ファイル名)だけを送り、
    従来どおりsource絞り込みRAGへフォールバックする。
    """
    doc_ingest = _doc_ingest()
    text = doc_ingest.get_document_text(filename)   # 未登録ならNone
    if text is None:
        raise HTTPException(status_code=404, detail=f"未登録のファイルです: {filename}")
    return {"filename": filename, "text": text if len(text) <= DOC_INLINE_MAX_CHARS else None}
```

> [!warning] `doc_ingest`側に全文を返すI/Fが無い可能性
> `rag_memory/doc_ingest.py`は現状「抽出 → チャンク化 → LanceDBへ登録」の一方向で、**登録済みファイルの全文を読み戻すI/Fが無い**恐れがある。その場合の選択肢は2つ。実装時にコードを確認して決め、判断理由をここへ追記する。
> 1. **チャンクを連結して復元する**(`source = f"doc:{filename}"`で全チャンクを取り、`chunk_index`順に連結)。追加のストレージが要らないが、チャンク間のオーバーラップで重複が出る可能性がある
> 2. **アップロード時に抽出全文も保存しておく**(`scripts/rag_memory/db/doc_text/{filename}.txt`等)。単純で確実だが保存先の設計が増える
>
> **まず1で試し、重複が実害になるようなら2へ切り替える**方針とする。

**Step 3: `index.html` — ピン留めの状態管理**

```javascript
// 14日目①: 対象ファイルのピン留め。「このファイルについて答えて」を成立させる中核。
// 13日目までの「アップロード直後の1回だけ」方式を置き換える(1回でクリアしない)。
let pinnedDocument = localStorage.getItem("pinnedDocument") || null;  // ファイル名
let pinnedDocumentText = null;                                        // 全文(閾値超ならnull)

async function pinDocument(filename) {
  pinnedDocument = filename;
  localStorage.setItem("pinnedDocument", filename);
  pinnedDocumentText = null;
  try {
    const resp = await fetch(`/documents/${encodeURIComponent(filename)}/text`);
    if (resp.status === 404) { unpinDocument(); return; }   // 既に削除済み
    pinnedDocumentText = (await resp.json()).text || null;  // nullならRAGフォールバック
  } catch (e) {
    logError(`添付全文の取得に失敗(RAG検索にフォールバックします): ${e.message || e}`);
  }
  renderPinnedChip();
  renderKnowledgeList(lastKnownDocs);   // 一覧の選択表示を更新
}

function unpinDocument() {
  pinnedDocument = null;
  pinnedDocumentText = null;
  localStorage.removeItem("pinnedDocument");
  renderPinnedChip();
}

// sendTextInput() 内(1475行付近の lastAttachedDocument 分岐を置き換える)
if (pinnedDocument) {
  msg.attached_document = pinnedDocument;              // RAGのsource絞り込み用
  if (pinnedDocumentText) {
    msg.attached_document_text = pinnedDocumentText;   // ⓪-3で死んでいた経路をここで生かす
  }
}
// ★ 送信後に pinnedDocument を null にしない(ピンが立っている限り毎ターン有効)
```

**Step 4: `index.html` — 一覧のクリック選択とチップ表示**

```javascript
// renderKnowledgeList() 内、各 item に対して
item.classList.toggle("pinned", doc.filename === pinnedDocument);
info.addEventListener("click", () => {
  if (doc.filename === pinnedDocument) unpinDocument();   // 再クリックで解除
  else pinDocument(doc.filename);
});
```

入力欄の上に出すチップ(📎の`attachChips`と同じ場所でよい):

```javascript
function renderPinnedChip() {
  pinnedChipArea.innerHTML = "";
  if (!pinnedDocument) return;
  const chip = document.createElement("span");
  chip.className = "chip pinned-chip";
  const mode = pinnedDocumentText ? "全文" : "検索";
  chip.textContent = `📌 ${pinnedDocument}(${mode})について回答中`;
  const x = document.createElement("button");
  x.textContent = "✕";
  x.addEventListener("click", unpinDocument);
  chip.appendChild(x);
  pinnedChipArea.appendChild(chip);
}
```

> [!tip] チップに「全文 / 検索」を出す理由
> 同じ「ピン留め中」でも、内部では**全文をそのままプロンプトへ入れる経路**と**RAGでsource絞り込み検索する経路**の2つに分かれる(閾値20000字)。回答の精度が明らかに違うため、どちらで動いているかが画面から分かるようにしておくと、「答えがふわっとしている」原因の切り分けが一瞬でできる(⓪-3のような事故の再発防止も兼ねる)。

**Step 5: テストとコミット**

```powershell
python -m pytest tests/ -q
node --check static/_index_script_check.js   # index.html内JSの構文チェック(13日目と同じ手順)
git add scripts/voice_gateway.py scripts/rag_memory/doc_ingest.py scripts/static/index.html scripts/tests/
git commit -m "feat(docs): pin a knowledge file from the list and keep it as the answer target"
```

### 結果 / 分析 / 改善策

(実施後に追記)

### 残課題

(実施後に追記)

---

## ② チャット履歴の永続化(New Session・リネーム・削除・検索)

### 背景/目的

サイドバーの会話履歴は12日目②のver2移植以来**すべて見た目だけ**(`index.html` 727行付近に`<!-- MOCK: 未接続 -->`、履歴アイテムはHTMLに直書き)。`New Session`ボタン(734行)にもクリックハンドラが無く、リネーム/削除ボタン(746・749行)も同様。11日目③から「保存形式が未設計」という理由で3日連続で持ち越されている。

さらに`voice_gateway.py`側は`chat_id = f"voice-{id(websocket)}"`(574行)、つまり**WebSocket接続ごとに使い捨てのID**を振っている。この`chat_id`はRAG記憶(`memory_store.append_turn()`)のキーでもあるため、現状は:

- ブラウザをリロードするたびに別の会話として扱われる(前の話の続きができない)
- 会話を意図的に切り替える手段がない(`New Session`が効かない)
- 画面に出ている履歴リストと、実際に記憶されている会話が**何の関係もない**

という状態。①のピン留めと違い、これは**保存形式の設計から始める必要がある**ため、今日はここに時間を使う。

**方針(設計):**

| 論点 | 決定 | 理由 |
|---|---|---|
| 保存場所 | **`scripts/data/sessions/{session_id}.json`**(1セッション1ファイル) | 中身を直接開いて読める・壊れても1セッションで済む・新しい依存を足さない。SQLiteは検索が速いが、この規模(個人利用・数百件)では過剰 |
| セッションID | **`sess-{YYYYMMDD-HHMMSS}-{4桁乱数}`** | 時系列でソートでき、ファイル名としてそのまま使える |
| ファイルの中身 | `{"session_id", "title", "created_at", "updated_at", "turns": [{"role", "text", "route", "ts"}]}` | 表示に必要な最小限。RAGの記憶(LanceDB)とは**別物**として持つ(下記の注記参照) |
| タイトル | **最初のユーザー発話の先頭30字を自動採用**、リネームで上書き可 | ChatGPT等と同じ体験。LLMに要約させる案は1往復ぶんの遅延が乗るので今回は見送る |
| 検索 | **全セッションのJSONを読んで部分一致**(タイトル+本文) | 数百件までは十分速い。遅くなったら全文検索インデックスを検討する |
| `chat_id`との関係 | **`chat_id = session_id`に統一する** | RAG記憶と画面上の会話を同じ単位に揃える。これをやらないと「履歴を切り替えたのにAIは前の話を覚えている」というちぐはぐが残る |

> [!important] LanceDB(RAG記憶)とJSON(画面用履歴)を二重に持つことについて
> 一見すると冗長だが、役割が違う:LanceDB側は**意味検索のためのベクトル付きチャンク**であり、「何ターン目に何を話したか」を順番どおり復元する用途には向かない(検索でヒットした断片しか返らない)。一方JSON側は**画面へそのまま流し込むための逐語ログ**。両方に書くのはコストではなく、それぞれ別の必要性がある。`chat_id`=`session_id`で揃えることで、片方から他方を必ず引ける状態にしておく。

### 作業内容

- [ ] `session_store.py`(新規)を作る。`create_session()` / `list_sessions()` / `load_session()` / `append_turn()` / `rename_session()` / `delete_session()` / `search_sessions(query)`
- [ ] `tests/test_session_store.py`(新規)を書く。**保存先は`tmp_path`へ差し替え**、実ファイルを汚さずに検証する
- [ ] タイトルの自動生成(最初のユーザー発話の先頭30字。空なら「新しい会話」)を実装する
- [ ] `voice_gateway.py`にHTTP経路を追加:`GET /sessions` / `POST /sessions` / `GET /sessions/{id}` / `PATCH /sessions/{id}`(リネーム) / `DELETE /sessions/{id}` / `GET /sessions?q=...`(検索)
- [ ] `tests/test_voice_gateway.py`に上記6経路のテストを追加する
- [ ] `voice_gateway.py`の`chat_id = f"voice-{id(websocket)}"`を廃止し、**クライアントから送られた`session_id`を使う**形に変更する(未指定なら従来どおり使い捨てIDでフォールバック=後方互換)
- [ ] ターン確定時(ユーザー発話・AI応答の両方)に`session_store.append_turn()`を呼ぶ
- [ ] `index.html`:サイドバーの履歴リストをHTML直書きから`GET /sessions`の動的描画へ置き換える
- [ ] `index.html`:`New Session`ボタン → `POST /sessions` → 新しい`session_id`に切り替え、チャット表示をクリアする
- [ ] `index.html`:履歴アイテムのクリックで`GET /sessions/{id}` → 過去ログを画面へ復元し、以後のターンをそのセッションへ追記する
- [ ] `index.html`:リネーム(✏️)・削除(🗑)ボタンを実処理へ配線する。削除は確認ダイアログを挟む
- [ ] `index.html`:検索ボックス(737行)を`GET /sessions?q=...`へ配線する
- [ ] 現在のセッションIDを`localStorage`へ保存し、リロード時に同じ会話へ復帰する
- [ ] `<!-- MOCK: 未接続 -->`コメントと`MOCK`バッジ(732行)、`(未接続)`の`title`/`placeholder`文言を**すべて外す**

### 実施手順(TDD)

**Step 1: RED — `tests/test_session_store.py`(新規)**

```python
import json
import session_store


def test_create_and_list_roundtrip(tmp_path):
    store = session_store.SessionStore(root=tmp_path)
    s = store.create_session()
    assert s.title == "新しい会話"
    assert [x.session_id for x in store.list_sessions()] == [s.session_id]


def test_title_is_derived_from_first_user_turn(tmp_path):
    store = session_store.SessionStore(root=tmp_path)
    s = store.create_session()
    store.append_turn(s.session_id, role="user", text="明日の東京の天気を調べて", route="FAST")
    assert store.load_session(s.session_id).title == "明日の東京の天気を調べて"


def test_title_is_truncated_to_30_chars(tmp_path):
    store = session_store.SessionStore(root=tmp_path)
    s = store.create_session()
    store.append_turn(s.session_id, role="user", text="あ" * 100, route="FAST")
    assert store.load_session(s.session_id).title == "あ" * 30


def test_rename_overrides_auto_title_and_survives_further_turns(tmp_path):
    # リネーム後に発話しても、自動タイトルで上書きされないこと(これが抜けると事故る)
    store = session_store.SessionStore(root=tmp_path)
    s = store.create_session()
    store.append_turn(s.session_id, role="user", text="最初の発話", route="FAST")
    store.rename_session(s.session_id, "筋トレメニュー相談")
    store.append_turn(s.session_id, role="user", text="2回目の発話", route="FAST")
    assert store.load_session(s.session_id).title == "筋トレメニュー相談"


def test_list_is_sorted_by_updated_at_desc(tmp_path):
    ...


def test_delete_removes_file(tmp_path):
    store = session_store.SessionStore(root=tmp_path)
    s = store.create_session()
    store.delete_session(s.session_id)
    assert store.list_sessions() == []
    assert not (tmp_path / f"{s.session_id}.json").exists()


def test_search_matches_title_and_body(tmp_path):
    ...


def test_load_of_corrupted_json_does_not_break_listing(tmp_path):
    # 1件壊れても一覧全体が落ちないこと(壊れたセッションはスキップしてログに残す)
    (tmp_path / "sess-broken.json").write_text("{ではないテキスト", encoding="utf-8")
    store = session_store.SessionStore(root=tmp_path)
    assert store.list_sessions() == []
```

```powershell
cd C:\Users\gakuh\Documents\obsidian\サポートAI作製計画\scripts
python -m pytest tests/test_session_store.py -v   # 期待: ModuleNotFoundError: No module named 'session_store'
```

**Step 2: GREEN — `session_store.py`(新規)**

要点のみ(実装時に肉付けする):

```python
"""session_store.py — 14日目②:チャット履歴(画面表示用の逐語ログ)の永続化。

RAG記憶(rag_memory/memory_store.py)とは役割が違う点に注意:
memory_storeは「意味検索のためのベクトル付きチャンク」、こちらは「画面へ
そのまま流し込むための逐語ログ」。chat_id == session_id で揃えることで
片方から他方を必ず引けるようにしてある。
"""

from __future__ import annotations

import json
import random
from dataclasses import dataclass, field, asdict
from datetime import datetime
from pathlib import Path

DEFAULT_ROOT = Path(__file__).parent / "data" / "sessions"
DEFAULT_TITLE = "新しい会話"
TITLE_MAX_CHARS = 30


@dataclass
class Turn:
    role: str       # "user" | "assistant"
    text: str
    route: str = ""
    ts: str = ""


@dataclass
class Session:
    session_id: str
    title: str = DEFAULT_TITLE
    created_at: str = ""
    updated_at: str = ""
    title_is_custom: bool = False    # リネーム済みなら自動タイトルで上書きしない
    turns: list[Turn] = field(default_factory=list)


class SessionStore:
    def __init__(self, root: Path = DEFAULT_ROOT):
        self._root = Path(root)
        self._root.mkdir(parents=True, exist_ok=True)
    ...
```

**Step 3: `voice_gateway.py`へHTTP経路と`chat_id`統一を実装**

```python
# 14日目②: chat_idをセッションIDに統一する。
# 従来: chat_id = f"voice-{id(websocket)}"(接続ごとに使い捨て。リロードで会話が切れる)
# 変更: クライアントが送るsession_idを使い、未指定時のみ従来どおりのフォールバック。
chat_id = f"voice-{id(websocket)}"     # フォールバック(session_id未指定の古いクライアント用)
...
if msg.get("type") == "select_session":
    chat_id = msg["session_id"]
    continue
```

**Step 4: `index.html`のサイドバーを動的描画へ**

```javascript
// 14日目②: 会話履歴。HTML直書きのダミー2件を消し、GET /sessions で描画する。
let currentSessionId = localStorage.getItem("currentSessionId") || null;

async function refreshSessionList(query = "") {
  const url = query ? `/sessions?q=${encodeURIComponent(query)}` : "/sessions";
  renderSessionList(await (await fetch(url)).json());
}

async function newSession() {
  const s = await (await fetch("/sessions", { method: "POST" })).json();
  await selectSession(s.session_id);
  await refreshSessionList();
}

async function selectSession(sessionId) {
  currentSessionId = sessionId;
  localStorage.setItem("currentSessionId", sessionId);
  ws?.send(JSON.stringify({ type: "select_session", session_id: sessionId }));  // サーバ側chat_idを合わせる
  const s = await (await fetch(`/sessions/${sessionId}`)).json();
  chatArea.innerHTML = "";
  for (const t of s.turns) appendTurn(t.text, t.role);   // 過去ログを画面へ復元
}
```

**Step 5: テストとコミット**

```powershell
python -m pytest tests/ -q
node --check static/_index_script_check.js
git add scripts/session_store.py scripts/tests/test_session_store.py scripts/voice_gateway.py scripts/static/index.html
git commit -m "feat(ui): persist chat sessions (new/rename/delete/search) and unify chat_id with session_id"
```

### 結果 / 分析 / 改善策

(実施後に追記)

### 残課題

(実施後に追記)

---

## ③ 資料・コード生成(Excel / PowerPoint / Word / JSON)

### 背景/目的

13日目「次のステップ」4に「エクセルやPowerPointの生成」が持ち越されていた。会話の結果を**その場でファイルとして受け取れる**ようにするのが目的で、想定シーンは「今の調査結果を表にまとめてエクセルで」「この構成でスライド作って」「設定をJSONで書き出して」。

**着手前に`code_executor.py`と`support_ai_auto_pipe.py`を読んだところ、器はほぼ揃っていることが分かった。** 4日目⑩で作ったCODEルートの`<ACTION>`ブロック機構が、そのまま資料生成に転用できる。

```
<ACTION path="make_report.py" run="true">
```python
import openpyxl
...
```
</ACTION>
```

必要なライブラリも**既に環境へ入っている**(確認済み):

```powershell
pip list | Select-String -Pattern "openpyxl|python-pptx|python-docx|pandas"
# openpyxl    3.1.5
# python-docx 1.2.0
# python-pptx 1.0.2
# pandas      3.0.3
```

つまり③は「新しい仕組みを作る」作業ではなく、**既にある仕組みの穴を塞ぐ**作業である。読んで分かった穴は以下の6つ。

| # | 要素 | 現状 | 判定 |
|---|---|---|---|
| 1 | テキスト系ファイル(JSON/CSV/Markdown/.py) | `write_action_file()`が`target.write_text()`するので**今日でも生成できる** | 追加不要 |
| 2 | Excel/PowerPoint/Word | **バイナリなのでACTIONブロックに直書きできない**。「生成スクリプトを書いて`run="true"`で実行し、その副産物としてxlsxを作る」という2段構えが必須 | プロンプト整備が要る |
| 3 | 実行時のカレントディレクトリ | `execute_python_file()`が`cwd=str(path.parent)`(=`workspace/`)で起動している。スクリプトが`wb.save("report.xlsx")`と**相対パスで保存すれば`workspace/`直下に出る** | 問題なし |
| 4 | タイムアウト | **30秒固定**。`import pandas`だけで数秒、xlsx書き出しを含めると初回は超える恐れがある | 延長が要る |
| 5 | ルーティング | `router_rules.py`の`CODE_TRIGGERS`に**「エクセル」「スライド」「資料にまとめて」が1つも無い**。「売上をエクセルにまとめて」はCODEに飛ばず**FASTに落ちて雑談的に返される** | **最重要の欠落** |
| 6 | 生成物の受け取り | ファイルは`workspace/`に置かれるだけ。**UIには何も出ず、ユーザーは生成されたことすら知らない** | 新規実装が要る |

> [!important] 5が最優先である理由
> 1〜4・6をどれだけ完璧に作っても、**ルーターがCODEに振らなければ一度も実行されない**。⓪-3の`lastAttachedDocumentText`と全く同じ「経路が繋がっていない死にコード」を作ることになる。今日は同じ失敗を2度やらないよう、**5から着手して実際にCODEへ飛ぶことを確認してから**残りを実装する。

**方針(相談の上で確定):**

| 論点 | 決定 | 理由 |
|---|---|---|
| 生成方式 | **既存`code_executor`の拡張**。LLM(devstral)にopenpyxl等を使うPythonコードを書かせ、ACTIONブロックで実行する | 新規モジュールがほぼ要らず、「表もスライドも文書も」どんな形の資料にも対応できる汎用性がある。専用ビルダー(構造化JSONを受け取る`artifact_builder.py`)案は失敗しにくい反面、**表現できる資料の型がJSONスキーマの範囲に縛られる**ため見送った |
| LLMが書くコードの信頼性 | **システムプロンプトで利用可能ライブラリをホワイトリスト提示**し、実行失敗時はstderrをそのままユーザーに見せる | devstralが存在しないライブラリをimportして落ちるのが最大の失敗モード。「使ってよいもの」を明示するのが最も効く |
| 保存先 | **`workspace/`配下限定**(既存の`resolve_safe_path()`をそのまま使う) | 4日目⑩で作った安全境界を崩さない。`..`による脱出は`UnsafePathError`で拒否済み |
| 生成物の通知 | **実行前後で`workspace/`のスナップショットを取り、差分を「生成物」として返す** | LLMの応答文を信じて「report.xlsxを作りました」と表示すると、**実際には失敗しているのに成功したように見える**。ファイルシステムの実態だけを信じる |
| 受け取り方 | **`GET /artifacts`で一覧、`GET /artifacts/{name}`でダウンロード**。UIは応答直下のチップ+サイドバーの一覧 | 音声対話中に「できました」と言われても、開く手段がなければ意味がない |

### 作業内容

- [ ] `router_rules.py`の`CODE_TRIGGERS`に資料生成系トリガーを追加する(**最初にやる**)
- [ ] `tests/test_router_rules.py`に「エクセルにまとめて」「スライド作って」等がCODEになるテストを追加する
- [ ] 追加トリガーがFAST/DEEPの通常会話へ誤爆しないことを確認する(例:「今日は資料を読んでいた」が誤ってCODEにならないか)
- [ ] `code_executor.py`:`execute_python_file()`のデフォルト`timeout`を30→120秒へ延長する
- [ ] `code_executor.py`:`ExecutionResult`に`artifacts: list[str]`を追加し、実行の前後で`workspace/`を走査して**新規作成/更新されたファイル**を検出する
- [ ] `tests/test_code_executor.py`に生成物検出のテスト(ファイルを作るスクリプト/作らないスクリプト/タイムアウト)を追加する
- [ ] `support_ai_auto_pipe.py`の`CODE_ACTION_SYSTEM_PROMPT`に**利用可能ライブラリのホワイトリスト**と**バイナリ資料は生成スクリプト経由で作る**指示を追記する
- [ ] `support_ai_auto_pipe.py`の`_run_action()`の戻り値に生成物リストを載せ、ユーザーへの応答に「📄 生成物: report.xlsx」を含める
- [ ] `voice_gateway.py`に`GET /artifacts`(一覧:name/size/modified)を追加する
- [ ] `voice_gateway.py`に`GET /artifacts/{filename}`(`FileResponse`でダウンロード)を追加する。**パスの解決は`code_executor.resolve_safe_path()`を再利用**し、`workspace/`外は404にする
- [ ] `tests/test_voice_gateway.py`に`/artifacts`系のテスト(一覧/DL/`..`によるパス脱出が404)を追加する
- [ ] `index.html`:生成成功時に応答の下へ「📄 <ファイル名> をダウンロード」チップを表示する
- [ ] `index.html`:サイドバーに「🗂 生成物」セクションを追加し、`GET /artifacts`で描画する
- [ ] 日本語テキストがxlsx/docx/pptxで文字化けしないことを実機で確認する(エンコーディングとフォント)

### 実施手順(TDD)

**Step 1: RED — ルーティングのトリガー追加(ここが本命)**

```python
# tests/test_router_rules.py に追加
class TestDocumentGenerationTriggers(unittest.TestCase):
    """14日目③: 資料生成の依頼がCODEルートへ飛ぶこと。

    ここが通らないと③の実装は一度も実行されない(⓪-3と同じ死にコードになる)。
    """

    def test_excel_request_is_code(self):
        self.assertTrue(router_rules.is_code_request("今の調査結果をエクセルにまとめて"))

    def test_slide_request_is_code(self):
        self.assertTrue(router_rules.is_code_request("この構成でスライドを作って"))

    def test_word_request_is_code(self):
        self.assertTrue(router_rules.is_code_request("報告書をWordで作成して"))

    def test_json_request_is_code(self):
        self.assertTrue(router_rules.is_code_request("設定をJSONで書き出して"))

    # 誤爆しないこと(過剰検出の確認。ここを書かないとFASTの通常会話を壊す)
    def test_casual_mention_of_document_is_not_code(self):
        self.assertFalse(router_rules.is_code_request("今日は一日資料を読んでいたよ"))

    def test_casual_mention_of_excel_skill_is_not_code(self):
        self.assertFalse(router_rules.is_code_request("エクセルって難しいよね"))
```

> [!warning] 「資料」という語の誤爆リスクは高い
> `CODE_TRIGGERS`の既存コメントにあるとおり、「実装」「デバッグ」のような語は**プログラミング文脈以外でほぼ出てこない**から語幹だけで区切っても安全だった。しかし**「資料」「エクセル」は日常会話に普通に出てくる**(「資料を読んでいた」「エクセルって難しい」)。したがって既存トリガーと同じ感覚で単語だけを登録してはいけない。
> **必ず「依頼動詞」とセットで一致させる**(「資料」+4文字以内に「作っ|まとめ|書い|出力」)。上の誤爆テスト2件は、この設計判断が守られているかを見張るためのもの。

```python
# router_rules.py への追加案
CODE_TRIGGERS += [
    # 14日目③: 資料生成の依頼。日常会話への誤爆を避けるため、
    # 名詞単独では一致させず「依頼動詞」とセットでのみ一致させる。
    r"(エクセル|Excel|スプレッドシート|表).{0,6}(作っ|作成|まとめ|出力|書き出)",
    r"(パワポ|PowerPoint|スライド|プレゼン).{0,6}(作っ|作成|まとめ|出力)",
    r"(Word|ワード|報告書|議事録|レポート).{0,6}(作っ|作成|まとめ|出力|書い)",
    r"(JSON|CSV|YAML).{0,6}(作っ|作成|出力|書き出|変換)",
    r"資料.{0,4}(作っ|作成|まとめ|出力)",
    r"グラフ.{0,4}(作っ|作成|にし|化し)",
]
```

```powershell
cd C:\Users\gakuh\Documents\obsidian\サポートAI作製計画\scripts
python -m pytest tests/test_router_rules.py -k DocumentGeneration -v   # 期待: 失敗
```

**Step 2: GREEN — `code_executor.py`に生成物検出とタイムアウト延長**

```python
@dataclass
class ExecutionResult:
    stdout: str
    stderr: str
    returncode: int | None
    timed_out: bool
    artifacts: list[str] = field(default_factory=list)   # 14日目③: 生成されたファイル


def _snapshot(workspace_dir: Path) -> dict[str, float]:
    """workspace_dir配下のファイルとその更新時刻を記録する。"""
    return {
        str(p.relative_to(workspace_dir)): p.stat().st_mtime
        for p in workspace_dir.rglob("*")
        if p.is_file()
    }


def execute_python_file(path: Path, timeout: float = 120.0) -> ExecutionResult:
    """`python <path>` を実行する。

    14日目③の変更点:
      - timeoutを30→120秒。pandas/openpyxlのimportとxlsx書き出しは
        初回30秒に収まらないことがあるため。
      - 実行の前後でworkspaceを走査し、新規作成/更新されたファイルを
        artifactsとして返す。LLMの「作りました」という応答文ではなく、
        ファイルシステムの実態だけを信じるための仕組み。
    """
    workspace_dir = path.parent
    before = _snapshot(workspace_dir)
    ...   # 既存のsubprocess.run(...)
    after = _snapshot(workspace_dir)
    artifacts = sorted(
        name for name, mtime in after.items()
        if name != path.name and (name not in before or before[name] != mtime)
    )
    return ExecutionResult(..., artifacts=artifacts)
```

> [!note] 生成スクリプト自身(`make_report.py`)をartifactsから除く理由
> ユーザーが欲しいのは`report.xlsx`であって、それを作るための足場である`make_report.py`ではない。チップに2つ並ぶと「どっちを開けばいいのか」が分からなくなるため、`name != path.name`で除外する。

**Step 3: `CODE_ACTION_SYSTEM_PROMPT`へライブラリのホワイトリストを追記**

```python
# 14日目③: 資料生成(Excel/PowerPoint/Word)への対応。
# devstralが存在しないライブラリをimportして落ちるのが最大の失敗モードのため、
# 「使ってよいもの」を明示列挙する。またxlsx等はバイナリでACTIONブロックに
# 直書きできないため、「生成スクリプトを書いてrun="true"で実行する」形を強制する。
CODE_ACTION_SYSTEM_PROMPT += """

【資料ファイルの作成について】
Excel(.xlsx)・PowerPoint(.pptx)・Word(.docx)はバイナリ形式のため、
ACTIONブロックに直接書くことはできません。かわりに**そのファイルを生成する
Pythonスクリプト**をACTIONブロックに書き、run="true"で実行してください。

使用してよいライブラリ(すべてインストール済み):
  - openpyxl     … Excel(.xlsx)の作成
  - python-pptx  … PowerPoint(.pptx)の作成(import名は pptx)
  - python-docx  … Word(.docx)の作成(import名は docx)
  - pandas       … 表データの整形
  - json / csv / pathlib / datetime … 標準ライブラリ

これ以外のライブラリ(matplotlib, xlsxwriter, reportlab等)は使わないでください。

保存先は**カレントディレクトリ直下の相対パス**にしてください(例: wb.save("報告書.xlsx"))。
絶対パスや".."を含むパスは安全のため拒否されます。
日本語を含む場合はファイル名・セル内容ともにそのまま日本語で構いません。

JSON・CSV・Markdown・テキストファイルは、スクリプトを書かずに
ACTIONブロックへ直接内容を書いてrun="false"で保存してください。
"""
```

**Step 4: `voice_gateway.py`に生成物の一覧とダウンロード**

```python
@app.get("/artifacts")
def list_artifacts_endpoint():
    """14日目③: workspace/配下の生成物一覧。UIのサイドバー描画用。"""
    ws = code_executor.WORKSPACE_DIR
    items = [
        {
            "filename": str(p.relative_to(ws)),
            "size": p.stat().st_size,
            "modified": datetime.fromtimestamp(p.stat().st_mtime).isoformat(),
        }
        for p in ws.rglob("*") if p.is_file()
    ]
    return sorted(items, key=lambda x: x["modified"], reverse=True)


@app.get("/artifacts/{filename:path}")
def download_artifact_endpoint(filename: str):
    """14日目③: 生成物のダウンロード。

    パス解決は code_executor.resolve_safe_path() を再利用する。
    自前で os.path.join すると "../../voice_gateway.py" を配信してしまうため、
    既に検証済みの安全境界を必ず使い回すこと。
    """
    try:
        target = code_executor.resolve_safe_path(filename, code_executor.WORKSPACE_DIR)
    except code_executor.UnsafePathError:
        raise HTTPException(status_code=404, detail="not found")   # 存在を漏らさない
    if not target.is_file():
        raise HTTPException(status_code=404, detail="not found")
    return FileResponse(target, filename=target.name)
```

> [!warning] パス脱出を必ずテストする
> `GET /artifacts/../voice_gateway.py` や `GET /artifacts/..%2F..%2Fsecrets%2Ftoken.json` が**404になること**をテストに書く。ここは④でGoogleのトークンを`scripts/secrets/`へ置く以上、抜けると認証情報が丸ごと持ち出せる経路になる。`resolve_safe_path()`は`UnsafePathError`を返す実装が既にあるので、それを使えば守られる。

**Step 5: テストとコミット**

```powershell
python -m pytest tests/ -q
node --check static/_index_script_check.js
git add scripts/router_rules.py scripts/code_executor.py scripts/voice_gateway.py scripts/openwebui_pipe/support_ai_auto_pipe.py scripts/static/index.html scripts/tests/
git commit -m "feat(artifacts): generate xlsx/pptx/docx/json via the CODE route and expose downloads"
```

### 結果 / 分析 / 改善策

(実施後に追記)

### 残課題

(実施後に追記)

---

## ④ Google Workspace連携(調査レポートのDocs/Sheets出力)

### 背景/目的

③でファイルを作れるようになっても、成果物は**このPCの`workspace/`の中**にしかない。スマホから見る・人に共有する・後から編集するといった実用に耐えるには、**Googleドキュメント/スプレッドシートとして残る**のが最も自然である。具体的な想定シーンは:

- 13日目④のWeb検索で調べた内容を、そのまま**Googleドキュメントの調査レポート**として出力する
- 表形式のデータを**Googleスプレッドシート**へ出力する
- ③で作った`report.xlsx`を**Driveへアップロードしてスプレッドシート形式へ変換**する

必要なライブラリは③と同様に**既に環境へ入っている**(確認済み:`google-api-python-client` 2.197.0 / `google-auth-oauthlib` 1.4.0 / `google-auth` 2.56.2)。**コードは一切書かれていないので、ここだけは完全な新規実装**になる。

**方針(相談の上で確定):**

| 論点 | 決定 | 理由 |
|---|---|---|
| 認証方式 | **OAuthユーザー認証**(`InstalledAppFlow`)。初回だけブラウザで同意し、`token.json`を保存して以後は自動更新 | 個人利用であり、**自分のマイドライブ直下**にファイルが作られるのが最も自然。サービスアカウント方式だとサービスアカウント自身のドライブに作られ、毎回自分へ共有する処理が要るうえ、マイドライブには置けない |
| OAuthスコープ | **`drive.file` + `documents` + `spreadsheets`** | `drive.file`は「**このアプリが作成したファイルにしかアクセスできない**」スコープ。既存のドライブを読むことも壊すこともできないため、事故の最大被害を構造的に限定できる。`drive`(フルアクセス)は要求しない |
| LLMにGoogle APIを触らせるか | **触らせない**。③の`code_executor`経由でLLMが`google_workspace`をimportすることは禁止(ホワイトリストに入れない) | LLMが生成したコードにトークンの読み出しやファイル削除が紛れ込む可能性を排除する。Google連携は**Python側の決まった関数だけ**が行う |
| 発火方法 | **UIのボタン**を主、**発話での指示**を従とする | 「Googleドキュメントに出力して」を音声認識の誤りで取りこぼしても、ボタンがあれば詰まない。逆にボタンだけだと音声対話中に手が要る |
| 出力対象 | **直前のアシスタント応答**または**`workspace/`の生成物**の2つ | 「今の調査結果をドキュメントに」が最頻。ファイル選択は生成物一覧から選ぶ |
| 認証情報の置き場所 | **`scripts/secrets/`**(`credentials.json`・`token.json`)。**`.gitignore`へ追加** | リポジトリへコミットすると認証情報が流出する。**③の`/artifacts`のパス脱出テストが効いているかもここで効く** |

> [!important] `drive.file`スコープを選ぶことの意味(制約でもある)
> `drive.file`は安全側に倒れる代わりに、**「既に自分のドライブにあるファイルをAIに読ませる」ことは一切できない**。例えば「先週作ったスプレッドシートを読んで要約して」は**このスコープでは原理的に不可能**であり、実装漏れではない。それが必要になったら`drive.readonly`の追加を別途判断する(今日のスコープ外に明記済み)。まずは**書き出し専用**として作る。

### 事前準備(Google Cloud側の設定。コードを書く前に必要)

```
1. https://console.cloud.google.com/ でプロジェクトを作成(例: claire-support-ai)
2. 「APIとサービス」→「ライブラリ」で以下3つを有効化
     - Google Docs API
     - Google Sheets API
     - Google Drive API
3. 「OAuth同意画面」→ ユーザーの種類「外部」→ テストユーザーに gakuhari555@gmail.com を追加
     ※「テスト」ステータスのままでよい(本番公開はGoogleの審査が要るが、
       テストユーザーとして自分を登録すれば個人利用には十分)
4. 「認証情報」→「OAuth クライアント ID を作成」→ アプリの種類「デスクトップ アプリ」
5. ダウンロードしたJSONを scripts/secrets/credentials.json として保存
6. .gitignore に secrets/ を追加(★これを忘れないこと)
```

> [!warning] 「テスト」ステータスのトークンは7日で失効する
> OAuth同意画面が「テスト」のままだと、発行されたリフレッシュトークンは**7日で期限切れ**になる(Googleの仕様)。つまり**週に1回ブラウザでの再同意が必要**になる。毎回失効に気付けないと「なぜかGoogle出力だけ失敗する」状態になるため、`google_workspace.py`は**失効時に「再認証が必要です」と明示的にユーザーへ返す**設計にする(黙って失敗させない)。恒久的に使いたくなったら同意画面を「本番」へ公開する(個人利用のスコープなら審査なしで通ることが多い)。

### 作業内容

- [ ] `.gitignore`に`scripts/secrets/`を追加する(**コードより先にやる**)
- [ ] Google Cloud側の設定を行い、`scripts/secrets/credentials.json`を配置する
- [ ] `google_workspace.py`(新規)を作る。`get_credentials()` / `export_to_docs()` / `export_to_sheets()` / `upload_to_drive()`
- [ ] `tests/test_google_workspace.py`(新規)を書く。**Google APIは全てモック**し、ネットワークへ出ないことを保証する
- [ ] 認証情報が無い/失効している場合に、例外ではなく**明示的なエラーメッセージ**を返すことをテストする
- [ ] `voice_gateway.py`に`POST /google/export`を追加する(body:`{target: "docs"|"sheets"|"drive", source: "last_reply"|"artifact", text?, artifact?, title?}` → `{url}`)
- [ ] `voice_gateway.py`に`GET /google/status`を追加する(認証済みか/再認証が必要かを返す。UIの表示切り替え用)
- [ ] `tests/test_voice_gateway.py`に`/google/`系のテストを追加する(`google_workspace`をフェイクへ差し替え)
- [ ] `support_ai_auto_pipe.py`に発話トリガー(「Googleドキュメントに出力」「スプレッドシートにして」)を追加し、直前の応答をエクスポートする
- [ ] `router_rules.py`にもGoogle出力のトリガーを追加するか検討する(③と同じく誤爆に注意)
- [ ] `index.html`:応答の下に「📤 Googleドキュメントへ」「📊 スプレッドシートへ」ボタンを出す
- [ ] `index.html`:出力成功時に**クリックで開けるURLチップ**を表示する(音声だけでURLを伝えるのは不可能なため必須)
- [ ] `index.html`:未認証時は「Google連携を設定」の案内を出す(`GET /google/status`で判定)
- [ ] Markdown見出し(`#`)をGoogle Docsの見出しスタイルへ変換するか検討する(初版は素のテキストでよい。YAGNIなら見送る)

### 実施手順(TDD)

**Step 1: RED — `tests/test_google_workspace.py`(新規)**

```python
"""14日目④: Google Workspace連携。

このテストは**一度もネットワークへ出てはいけない**。
googleapiclient.discovery.build を丸ごとモックし、
「正しい引数でAPIを呼んだか」「戻り値からURLを組み立てられたか」だけを見る。
"""
import unittest
from unittest import mock

import google_workspace


class TestExportToDocs(unittest.TestCase):
    def test_creates_document_and_returns_url(self):
        fake_service = mock.MagicMock()
        fake_service.documents.return_value.create.return_value.execute.return_value = {
            "documentId": "DOC123"
        }
        with mock.patch.object(google_workspace, "_build_service", return_value=fake_service):
            url = google_workspace.export_to_docs("調査レポート", "本文テキスト")
        self.assertEqual(url, "https://docs.google.com/document/d/DOC123/edit")

    def test_raises_friendly_error_when_not_authenticated(self):
        # 認証情報が無いとき、生のFileNotFoundErrorではなく
        # ユーザーに見せられるメッセージを持つ例外を出すこと
        with mock.patch.object(google_workspace, "CREDENTIALS_PATH", "/nonexistent.json"):
            with self.assertRaises(google_workspace.NotAuthenticatedError) as ctx:
                google_workspace.export_to_docs("題", "本文")
        self.assertIn("認証", str(ctx.exception))


class TestExportToSheets(unittest.TestCase):
    def test_writes_rows_and_returns_url(self):
        ...   # values().update() が2次元配列で呼ばれること


class TestUploadToDrive(unittest.TestCase):
    def test_xlsx_is_converted_to_google_sheets(self):
        # mimeType に application/vnd.google-apps.spreadsheet を指定して
        # インポート変換させていること
        ...
```

```powershell
python -m pytest tests/test_google_workspace.py -v   # 期待: ModuleNotFoundError
```

**Step 2: GREEN — `google_workspace.py`(新規)**

```python
"""google_workspace.py — 14日目④: Googleドキュメント/スプレッドシートへの出力。

設計上の約束:
  - **書き出し専用**。OAuthスコープを drive.file に絞っているため、
    このアプリが作ったファイル以外には一切アクセスできない(既存ドライブを
    読むことも壊すこともできない)。これは制約ではなく安全設計。
  - **LLMからは呼ばせない**。code_executorのライブラリ・ホワイトリストに
    このモジュールを入れないこと。Google連携はここの関数経由のみ。
  - 認証情報は scripts/secrets/ に置き、.gitignore で除外する。
"""

from __future__ import annotations

from pathlib import Path

from google.auth.transport.requests import Request
from google.oauth2.credentials import Credentials
from google_auth_oauthlib.flow import InstalledAppFlow
from googleapiclient.discovery import build

SECRETS_DIR = Path(__file__).resolve().parent / "secrets"
CREDENTIALS_PATH = SECRETS_DIR / "credentials.json"
TOKEN_PATH = SECRETS_DIR / "token.json"

# drive.file = 「このアプリが作成したファイルのみ」。driveフルアクセスは要求しない。
SCOPES = [
    "https://www.googleapis.com/auth/drive.file",
    "https://www.googleapis.com/auth/documents",
    "https://www.googleapis.com/auth/spreadsheets",
]


class NotAuthenticatedError(RuntimeError):
    """未認証・トークン失効。UIへそのまま見せられる日本語メッセージを持たせる。"""


def get_credentials() -> Credentials:
    """保存済みトークンを返す。失効していればリフレッシュ、無ければブラウザ同意へ。

    テストステータスのOAuthクライアントはリフレッシュトークンが7日で失効するため、
    refresh()が失敗するケースは「異常」ではなく「想定内」として扱い、
    NotAuthenticatedErrorへ変換してUIに再認証を促す。
    """
    if not CREDENTIALS_PATH.exists():
        raise NotAuthenticatedError(
            "Google連携の認証情報がありません。secrets/credentials.json を配置してください。"
        )
    creds = Credentials.from_authorized_user_file(TOKEN_PATH, SCOPES) if TOKEN_PATH.exists() else None
    if creds and creds.expired and creds.refresh_token:
        try:
            creds.refresh(Request())
        except Exception:
            raise NotAuthenticatedError(
                "Googleの認証が期限切れです(テスト公開のトークンは7日で失効します)。再認証してください。"
            ) from None
    if not creds or not creds.valid:
        flow = InstalledAppFlow.from_client_secrets_file(str(CREDENTIALS_PATH), SCOPES)
        creds = flow.run_local_server(port=0)     # ブラウザが開く(初回のみ)
        TOKEN_PATH.write_text(creds.to_json(), encoding="utf-8")
    return creds


def export_to_docs(title: str, body: str) -> str:
    """Googleドキュメントを新規作成して本文を流し込み、編集URLを返す。"""
    service = _build_service("docs", "v1")
    doc = service.documents().create(body={"title": title}).execute()
    doc_id = doc["documentId"]
    service.documents().batchUpdate(
        documentId=doc_id,
        body={"requests": [{"insertText": {"location": {"index": 1}, "text": body}}]},
    ).execute()
    return f"https://docs.google.com/document/d/{doc_id}/edit"


def export_to_sheets(title: str, rows: list[list]) -> str:
    """Googleスプレッドシートを新規作成し、2次元配列を書き込んでURLを返す。"""
    ...


def upload_to_drive(path: Path, convert: bool = True) -> str:
    """workspace/のxlsx等をDriveへアップロードする。convert=Trueでネイティブ形式へ変換。"""
    ...
```

**Step 3: `voice_gateway.py`にHTTP経路**

```python
@app.get("/google/status")
def google_status_endpoint():
    """14日目④: UIが「未認証の案内」を出すか判断するための軽量チェック。

    ここで get_credentials() を呼んではいけない(ブラウザ同意が勝手に開く)。
    ファイルの存在と有効期限だけを見る。
    """
    return google_workspace.check_status()   # {"authenticated": bool, "reason": str}


@app.post("/google/export")
def google_export_endpoint(req: GoogleExportRequest):
    """14日目④: 直前の応答 or 生成物をGoogleへ出力し、URLを返す。"""
    try:
        if req.target == "docs":
            url = google_workspace.export_to_docs(req.title or "調査レポート", req.text or "")
        elif req.target == "sheets":
            url = google_workspace.export_to_sheets(req.title or "データ", req.rows or [])
        else:
            target = code_executor.resolve_safe_path(req.artifact, code_executor.WORKSPACE_DIR)
            url = google_workspace.upload_to_drive(target)
    except google_workspace.NotAuthenticatedError as e:
        raise HTTPException(status_code=401, detail=str(e))   # UIが再認証を促す
    return {"url": url}
```

**Step 4: `index.html`にボタンとURLチップ**

```javascript
// 14日目④: Googleへの出力。音声だけではURLを伝えられないため、
// 成功時は必ずクリックできるリンクチップを画面に残す。
async function exportToGoogle(target, text, title) {
  const resp = await fetch("/google/export", {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({ target, source: "last_reply", text, title }),
  });
  if (resp.status === 401) {
    logError((await resp.json()).detail);   // 「再認証してください」をそのまま表示
    return;
  }
  const { url } = await resp.json();
  appendLinkChip(url, target === "docs" ? "📄 Googleドキュメントを開く" : "📊 スプレッドシートを開く");
}
```

**Step 5: テストとコミット**

```powershell
python -m pytest tests/ -q
node --check static/_index_script_check.js
git status --short   # ★ secrets/ が出てこないことを目視確認する
git add .gitignore scripts/google_workspace.py scripts/tests/test_google_workspace.py scripts/voice_gateway.py scripts/static/index.html
git commit -m "feat(google): export reports to Google Docs/Sheets and upload artifacts to Drive"
```

### 結果 / 分析 / 改善策

> [!note] 追記(2026-08-16): 表がMarkdownそのままで出力される問題を修正
> ④実施後、実際にGoogleドキュメントへ表形式データを出力させたところ、`export_to_docs()`が本文をそのまま`insertText`していたため、`| --- | --- |`や`**太字**`がObsidianの表のような見やすい形にならず、記法が文字としてそのまま出力される問題が判明した。
>
> **原因**: Google Docs APIはMarkdownを解釈しない。整形して見せるには、見出し/太字/箇条書き/表をそれぞれ専用のAPIリクエスト(`updateParagraphStyle`・`updateTextStyle`・`createParagraphBullets`・`insertTable`)へ変換して送る必要がある。
>
> **対応**: 新規`scripts/markdown_docs.py`を作り、`parse_blocks()`(MarkdownをAPI非依存のブロック列へ分解する純粋関数)と、それをリクエストへ変換する関数群を実装した。`export_to_docs()`はこれを使って本文を組み立て直す形へ書き換えた。
>   - 表はDocs APIの制約上、①`insertTable`で空の表を作る→②`documents().get()`でセルの開始位置を読み直す→③後ろのセルから前方へ`insertText`する、という3段階が必要(先頭から埋めると後続セルの位置がずれるため)。ヘッダー行は自動で太字にする
>   - セル内の`<br>`はセル内改行へ変換、`</sub>`等の他のHTMLタグは削除する(Docs APIに上付き文字の概念が無いため)
>   - 対応範囲は表・見出し(`#`〜`###`)・太字・箇条書き(809行目でYAGNIとして見送っていた見出し変換もここで対応した)
> - `tests/test_markdown_docs.py`(新規)でパーサとリクエスト組み立てを、`tests/test_google_workspace.py`の追加テストで表を含む本文の`export_to_docs()`呼び出し順(見出しのbatchUpdate→insertTableのbatchUpdate→セル書き込みのbatchUpdate)を検証。全390件のテストがパスすることを確認済み

### 残課題

(実施後に追記)

---

## ⑤ 成果物一覧(予定)

| ファイル | 新規/修正 | 役割 | 実行方法 | 出力先 |
|---|---|---|---|---|
| [[サポートAI作製計画/scripts/static/index.html]] | 修正 | ①ピン留めUI(一覧クリック・📌チップ・localStorage) / ②サイドバーの動的描画・New Session・リネーム/削除/検索 / ③生成物のダウンロードチップ・「🗂 生成物」一覧 / ④Google出力ボタン・URLチップ・未認証案内 | `voice_gateway.py`が配信。`http://127.0.0.1:5055/` | ブラウザ表示のみ(設定は`localStorage`) |
| [[サポートAI作製計画/scripts/voice_gateway.py]] | 修正 | ①`GET /documents/{filename}/text` / ②`/sessions`系6経路・`chat_id`=`session_id`統一・ターンの永続化呼び出し / ③`GET /artifacts`・`GET /artifacts/{filename}` / ④`GET /google/status`・`POST /google/export` | `python voice_gateway.py --host 127.0.0.1 --port 5055` | なし(常駐サーバー)。応答時間は`results/response_timing.csv` |
| `scripts/session_store.py` | 新規 | ②チャット履歴(逐語ログ)の永続化。作成/一覧/読み込み/追記/リネーム/削除/検索 | 単体実行はしない(`voice_gateway.py`からimportして使う部品) | **`scripts/data/sessions/{session_id}.json`**(1セッション1ファイル) |
| `scripts/tests/test_session_store.py` | 新規 | ②の保存/復元・タイトル自動生成/リネーム保護・検索・破損JSON耐性のテスト | `python -m pytest tests/test_session_store.py -v` | 標準出力のみ |
| [[サポートAI作製計画/scripts/rag_memory/doc_ingest.py]] | 修正(予定) | ①登録済みファイルの全文を読み戻すI/F(`get_document_text()`)。既存I/Fで足りるなら修正しない | 単体実行はしない(`voice_gateway.py`からimportして使う部品) | — |
| `scripts/tests/test_voice_gateway.py` | 修正 | ①`/documents/{filename}/text` / ②`/sessions`系 / ③`/artifacts`系(**`..`によるパス脱出が404になること**) / ④`/google/`系のテスト追加 | `python -m pytest tests/test_voice_gateway.py -v` | 標準出力のみ |
| [[サポートAI作製計画/scripts/router_rules.py]] | 修正 | ③資料生成トリガー(エクセル/スライド/Word/JSON/資料)を`CODE_TRIGGERS`へ追加。**名詞単独では一致させず依頼動詞とセット**にして日常会話への誤爆を防ぐ | 単体実行はしない(`router.py`から呼ばれる部品) | — |
| [[サポートAI作製計画/scripts/code_executor.py]] | 修正 | ③`ExecutionResult.artifacts`(実行前後のworkspace差分で生成物を検出)、`timeout`を30→120秒 | 単体実行はしない(`support_ai_auto_pipe.py`からimportして使う部品) | `scripts/workspace/`配下に生成ファイル |
| [[サポートAI作製計画/scripts/openwebui_pipe/support_ai_auto_pipe.py]] | 修正 | ③`CODE_ACTION_SYSTEM_PROMPT`へライブラリ・ホワイトリストとバイナリ資料の作り方を追記、`_run_action()`の応答に生成物を載せる / ④発話での「Googleドキュメントに出力」トリガー | OpenWebUIのPipeとして常駐 | — |
| `scripts/tests/test_router_rules.py` | 修正 | ③資料生成トリガーのテスト。**誤爆しないこと**(「今日は一日資料を読んでいた」がCODEにならない)を含む | `python -m pytest tests/test_router_rules.py -v` | 標準出力のみ |
| `scripts/tests/test_code_executor.py` | 修正 | ③生成物検出のテスト(ファイルを作る/作らない/タイムアウト、生成スクリプト自身が除外されること) | `python -m pytest tests/test_code_executor.py -v` | 標準出力のみ |
| `scripts/google_workspace.py` | 新規 | ④Google Docs/Sheets作成とDriveアップロード。OAuth(`drive.file`+`documents`+`spreadsheets`)。**書き出し専用**・**LLMからは呼ばせない** | 単体実行はしない(`voice_gateway.py`からimportして使う部品) | Googleドライブ上のファイル(URLを返す) |
| `scripts/markdown_docs.py` | 新規(④追記) | Markdown本文(見出し・太字・箇条書き・表)をGoogle Docs APIのリクエスト列へ変換する。表出力がMarkdownそのままになる問題への対応 | 単体実行はしない(`google_workspace.py`からimportして使う部品) | — |
| `scripts/tests/test_markdown_docs.py` | 新規(④追記) | パーサ(見出し/太字/箇条書き/表/`<br>`変換)と表挿入の3段階APIリクエスト(insertTable→get→セル書き込み)のテスト | `python -m pytest tests/test_markdown_docs.py -v` | 標準出力のみ |
| `scripts/tests/test_google_workspace.py` | 新規 | ④Docs/Sheets/Drive出力のテスト。**Google APIは全モック**しネットワークへ出ない。未認証時に`NotAuthenticatedError`となることを含む | `python -m pytest tests/test_google_workspace.py -v` | 標準出力のみ |
| `scripts/secrets/credentials.json`・`token.json` | 新規(手動配置) | ④OAuthクライアント情報とアクセストークン。**`.gitignore`で除外必須** | — | — |
| `.gitignore` | 修正 | ④`scripts/secrets/`を追加(**コードより先に実施**) | — | — |

---

## ⑥ 実機確認(①〜④のチェックリスト)

### 背景/目的

②は`chat_id`(RAG記憶のキー)に手を入れるため、**壊すと「前の会話を覚えていない」「別の会話の記憶が混ざる」**という分かりにくい不具合になる。13日目⑥と同じく**①実装後 → ②実装後 → ③実装後 → ④実装後**の段階ごとにチェックを通す。

③④は「動いたつもりで動いていない」が起きやすい領域である。③はルーターがCODEへ振らなければ何も起きず、④はトークン失効で静かに失敗する。**どちらも「AIが『作りました』と言ったか」ではなく、「実際にファイル/URLが存在するか」で判定する**こと。

### 事前準備

```powershell
cd C:\Users\gakuh\Documents\obsidian\サポートAI作製計画\scripts
python voice_gateway.py --host 127.0.0.1 --port 5055
```

- Chromeで`http://127.0.0.1:5055/`を開き、DevToolsのConsole/NetworkタブでWS `/ws`を選択しておく
- **各ステップでスクリーンショットを残す**(12日目①・13日目⑥と同じ反省点)

### チェックリスト

**①(ピン留め)の確認**

- [x] 📎で短めのPDF/Wordをアップロード → 自動でピン留めされ、📌チップに「(全文)」と出る
- [x] そのまま「このファイルについて答えて」と聞くと、**ファイルの中身に即した回答**が返る(⓪-3の修正が効いているかの本命確認)
- [x] **2ターン目以降も続けて質問できる**(「じゃあ3ページ目は?」等。1回でピンが外れない)
- [x] 20000字を超える長い文書では📌チップが「(検索)」になり、RAG経由でも的外れでない回答が返る
- [x] 📚一覧で別のファイルをクリックすると対象が切り替わる(チップの表示も変わる)
- [x] ピンを✕で解除すると、以後は通常の全文脈検索に戻る(ファイルに引きずられた回答をしない)
- [x] リロードしてもピンが維持される(`localStorage`+全文の取り直し)
- [x] ピン留め中のファイルを📚一覧から削除すると、ピンも自動で外れる
- [x] 音声(マイク)からの発話でも同じように効く(ピン留めはクリックだが、質問は音声でできる)

**②(チャット履歴)の確認**

- [x] `New Session`で新しい会話が作られ、チャット表示がクリアされる
- [x] 何往復か話した後、**タイトルが最初の発話から自動で付く**
- [x] リネームしたタイトルが、その後さらに発話しても上書きされない
- [x] 別のセッションをクリックすると**過去ログが画面に復元される**
- [x] 復元したセッションで話を続けると、**AIが前の話を覚えている**(`chat_id`=`session_id`統一の確認。ここが本命)
- [x] 逆に、別セッションへ切り替えると**前のセッションの話には言及しない**(記憶の混線がない)
- [x] 削除で一覧から消え、`scripts/data/sessions/`からもファイルが消える
- [x] 検索ボックスにキーワードを入れると、タイトル・本文の両方でヒットする
- [x] ブラウザをリロードしても同じセッションへ戻る

**③(資料・コード生成)の確認**

- [x] 「今の話をエクセルにまとめて」と言うと`[route: CODE]`になる(**まずここ。FASTに落ちたら以降は全部無意味**)
- [x] 「今日は一日資料を読んでいたよ」のような雑談が**CODEに誤爆しない**(FASTのまま)
- [x] `workspace/`に`.xlsx`が実際に生成され、Excelで開けて中身が意図どおり
- [x] 「スライドを作って」で`.pptx`が生成され、PowerPointで開ける
- [x] 「報告書をWordで作って」で`.docx`が生成され、開ける
- [x] 「設定をJSONで書き出して」で`.json`が生成される(**スクリプト経由ではなくACTIONブロック直書き**で出ていること)
- [x] 生成されたファイルに**日本語が文字化けせず**入っている(セル内容・段落・スライドの文字)
- [x] 応答の下に「📄 <ファイル名> をダウンロード」チップが出て、**クリックで実際にダウンロードできる**
- [x] 生成物リストに`make_report.py`(生成スクリプト自身)が**混ざっていない**
- [x] `import matplotlib`のような**ホワイトリスト外のライブラリを使おうとした場合**、stderrがユーザーに見える形で返る(黙って失敗しない)
- [x] `GET /artifacts/../voice_gateway.py` が**404**になる(パス脱出の防御。**④のトークン保護に直結する**)
- [x] 120秒のタイムアウト内に完了する(初回の`import pandas`を含めて)
- [x] 音声(マイク)からの発話でも資料生成が動く

**④(Google連携)の確認**

- [x] `git status --short`で`secrets/`が**表示されない**(`.gitignore`が効いている。**コミット前に必ず**)
- [x] 初回のGoogle出力でブラウザが開き、同意すると`secrets/token.json`が作られる
- [x] 「Googleドキュメントに出力して」で**実際にマイドライブにドキュメントが作られる**
- [x] 返ってきたURLチップをクリックすると、**そのドキュメントが開いて本文が入っている**
- [x] 表形式のデータを「スプレッドシートにして」で出力すると、**セルが分かれて**入る(1セルに全部入っていない)
- [x] ③で作った`report.xlsx`をDriveへアップロードすると、**Googleスプレッドシート形式に変換されて**開ける
- [x] 13日目④のWeb検索の結果(出典付き)を、そのままGoogleドキュメントへ出力できる(**本命のユースケース**)
- [x] `secrets/token.json`を意図的にリネームして未認証状態にすると、UIに「**再認証してください**」が出る(例外スタックトレースではなく)
- [x] `GET /google/status`が未認証時に`authenticated: false`を返し、**ブラウザ同意を勝手に開かない**
- [x] LLMが`google_workspace`をimportしようとしていない(`CODE_ACTION_SYSTEM_PROMPT`のホワイトリスト外であることの確認)
- [x] 日本語のタイトル・本文がGoogle側で文字化けしない

**全体の退行確認**

- [x] ウェイクワード・自動送信が13日目どおり動く(2ターン目以降の連結バグが再発していない)
- [x] Web検索トグルONで出典付きの回答が返る
- [x] 画像添付(`[route: DEEP]`)が従来どおり動く
- [x] 状態遷移(idle → listening → thinking → speaking → idle)がバッジに反映される
- [x] `results/response_timing.csv`の`duration_sec`がFASTで8.4〜8.8秒前後から悪化していない

### 結果 / 分析 / 改善策

> [!note] 追記(2026-08-16): 実機確認完了・①〜④すべて正常
> ユーザー実機確認により①〜④・全体の退行確認まで全項目が問題なしと判定された。特筆点は以下の2つ。
> - **音声まわり**: 「Hey, C.L.A.I.R.E.」というスペルアウト表記のウェイクワードが入力欄に混入したまま誤送信される新規バグが見つかったが、原因(`voice_gateway.py`の`_check_wake_word`が呼ぶ`force_finalize_pending()`が同一の生テキストで`on_final`を再度呼び、`wake_detected`で一度トリムした入力欄を生テキストで再度上書きしてしまう)を特定し、`on_final`側で再入時のみウェイクワード接頭辞を再度剥がす形で修正。回帰テスト`test_forced_finalize_of_wake_word_only_text_is_not_resent_raw`を追加し、全391件パス。
> - **④(Google Docs出力)**: 実際に「アベンジャーズ・ドゥームズデイ 作品重要度ランキング」をGoogleドキュメントへ出力させたところ、見出し・小見出し・中見出しが適切なスタイルで設定され、表も見やすく整形されて出力された(2026-08-16追記の`markdown_docs.py`によるMarkdown→Docs APIリクエスト変換が効いている)。
>
> → 本ノート(14日目)①〜④の作業はすべて完了・正常動作を確認。

### 残課題

- 入力欄が長文で横に隠れる問題(下記⑦で対応する)

---

## ⑦ 入力欄を縦に伸びるようにする(`index.html`修正)

### 背景/目的

実機確認の中で、**長い指示を入力欄へ入れると横に隠れて全文が見えない**という問題が見つかった。文字が消えたように見えるが実際には横方向に流れているだけで、カーソルを動かさないと確認できない。音声の書き起こしは1発話が長くなりがちなうえ、13日目③の自動送信は「送信前に内容を確認・修正できる」ことが前提の設計なので、**全文が見えないことは自動送信の安全装置そのものを損なっている**。

原因は入力欄の実体が**単一行の`<input type="text">`**(944行)であることで、単一行入力は仕様上、改行できず横スクロールするしかない。ChatGPTやGeminiのチャット欄のように、**入力量に応じて下へ伸びる**形にする。

**着手前にレイアウトを調べたところ、`<textarea>`へ変えるだけでは直らないことが分かった。**

```css
grid-template-rows: 56px 1fr 148px;   /* 156行: padの行が固定148px */
.pad { ... overflow: hidden; }        /* 496行: はみ出した分は切り落とされる */
```

入力欄が縦に伸びても、**この2箇所がそれを打ち消して伸びた分をクリップする**。したがって修正は「`input`→`textarea`」「自動高さ調整」「**グリッド行の固定解除**」の3点セットで初めて成立する。⓪の教訓(「実装したつもりの経路が繋がっていない」)と同じ型の事故になりうる箇所なので、着手前に確認できたのは大きい。

**方針:**

| 論点 | 決定 | 理由 |
|---|---|---|
| 要素 | **`<input type="text">` → `<textarea rows="1">`** | 単一行入力では原理的に改行・折り返しができない |
| 高さの制御 | **JSで内容量に応じて自動調整**(`scrollHeight`を反映)。上限を超えたら内部スクロール | Geminiと同じ挙動。上限が無いと長文で画面が入力欄に埋まる |
| 上限 | **200px(約8行)** | それ以上はスクロールで足りる。会話ログの表示領域を潰さない |
| グリッド行 | **`148px` → `minmax(148px, auto)`** | 現在の見た目(148px)を最小値として保ちつつ、伸びたぶんだけ広がる。単に`auto`にすると空のときに今より縮んでレイアウトが変わる |
| `.pad`の`overflow` | **`hidden` → `visible`** | クリップの解除。他要素へ影響が出ないか実機で確認する |
| Enterの扱い | **Enter=送信、Shift+Enter=改行** | 既存のEnter送信(1548行)を壊さない。改行手段が無いと`textarea`にした意味が薄い |
| IME | **既存の`isComposing`判定を維持** | 日本語変換中のEnterで誤送信しないための既存の配慮を落とさない |

### 作業内容

- [x] `<input id="text-input" type="text">`を`<textarea id="text-input" rows="1">`へ置き換える(944行)
- [x] CSSセレクタ`.pad-text input`を`.pad-text textarea`へ変更する(601〜608行の4箇所)
- [x] `textarea`用のCSSを追加する(`resize: none` / `overflow-y: auto` / `max-height` / `line-height` / `box-sizing`)
- [x] `grid-template-rows`の`148px`を`minmax(148px, auto)`へ変更する(156行)
- [x] `.pad`の`overflow: hidden`を`visible`へ変更する(496行)
- [x] `autoResizeTextInput()`を実装し、`input`イベントで高さを再計算する
- [x] Enterのキーハンドラを`Enter=送信 / Shift+Enter=改行`へ変更する(1548行。`isComposing`判定は維持)
- [x] 送信後に`textInput.value = ""`した直後、**高さを1行へ戻す**(1540行付近。戻し忘れると空欄なのに伸びたままになる)
- [x] 音声書き起こしで`textInput.value`が更新される箇所でも高さを再計算する(1264行の`partial_transcript`、1284行の`wake_detected`)
- [x] ヒント表示を「<kbd>Enter</kbd> 送信」から「<kbd>Enter</kbd> 送信 / <kbd>Shift</kbd>+<kbd>Enter</kbd> 改行」へ更新する(946行)
- [x] 🐛 **空欄時にプレースホルダーの折り返し高さで膨らむ問題**を修正する(下記「結果」参照。実装中にヘッドレス実測で発見)

### 実施手順

**Step 1: HTMLの置き換え(944行)**

```html
<!-- 14日目⑦: 単一行のinputでは長文が横に隠れて全文が見えないため、textareaへ変更。
     rows="1"で見た目は従来どおり1行から始まり、入力量に応じてJSで縦に伸ばす。 -->
<textarea id="text-input" rows="1"
          placeholder="話すとここに書き起こされます。間違いを直して送信(Enter)"
          autocomplete="off" disabled></textarea>
```

**Step 2: CSS(601〜608行の`input`→`textarea`、および496・156行)**

```css
.pad-text textarea {
  width: 100%; background: var(--bg-panel-2); border: 1px solid var(--line-strong);
  color: var(--text-primary); padding: 12px 14px; font-family: var(--f-mono); font-size: 13px;
  border-radius: 4px; outline: none;
  /* 14日目⑦: 縦に伸びる入力欄 */
  resize: none;              /* 右下のつまみを消す(高さはJSが管理する) */
  overflow-y: auto;          /* 上限に達したら内部スクロール */
  max-height: 200px;         /* 約8行。これ以上は会話ログを圧迫するのでスクロールへ */
  line-height: 1.5;
  display: block;            /* inline要素由来の下端の余白を消す */
  box-sizing: border-box;
}

/* 14日目⑦: 入力欄が伸びた分だけpadの行も広がるようにする。
   148px固定のままだと、textareaが伸びてもoverflow:hiddenで切り落とされる。 */
grid-template-rows: 56px 1fr minmax(148px, auto);
.pad { ... overflow: visible; }
```

**Step 3: JS(自動高さ調整とEnterの扱い)**

```javascript
// 14日目⑦: 入力量に応じて縦に伸ばす。いったんautoへ戻してからscrollHeightを測るのが要点で、
// これをしないと「一度伸びた高さ」が残って文字を消しても縮まなくなる。
const TEXT_INPUT_MAX_HEIGHT = 200;   // CSSのmax-heightと合わせる

function autoResizeTextInput() {
  textInput.style.height = "auto";
  textInput.style.height = Math.min(textInput.scrollHeight, TEXT_INPUT_MAX_HEIGHT) + "px";
}

textInput.addEventListener("input", autoResizeTextInput);

textInput.addEventListener("keydown", (e) => {
  // isComposing: IME変換中のEnter確定でうっかり送信しないようにする(従来どおり)
  // 14日目⑦: Shift+Enterは改行として通す(textareaの既定動作に任せる)
  if (e.key === "Enter" && !e.isComposing && !e.shiftKey) {
    e.preventDefault();
    sendTextInput();
  }
});
```

送信後・音声反映後にも高さの再計算が要る(**入れ忘れると空欄なのに伸びたまま/長文が1行のまま**になる):

```javascript
textInput.value = "";
autoResizeTextInput();     // ← 送信後に1行へ戻す(sendTextInput内・1540行付近)

textInput.value = msg.text;
autoResizeTextInput();     // ← 音声書き起こしの反映後(partial_transcript・1264行)

textInput.value = msg.text_after;
autoResizeTextInput();     // ← ウェイクワード除去後(wake_detected・1284行)
```

**Step 4: 確認とコミット**

```powershell
cd C:\Users\gakuh\Documents\obsidian\サポートAI作製計画\scripts
node --check static/_index_script_check.js   # index.html内JSの構文チェック(13日目と同じ手順)
python -m pytest tests/ -q                   # 退行が無いことの確認
git add scripts/static/index.html
git commit -m "fix(ui): grow the text input vertically instead of hiding long text horizontally"
```

### 実機確認

ヘッドレスChromeで実測済みの項目は`[x]`にした(下記「結果」の表が根拠)。**ブラウザで目視・操作しないと確認できない項目は`[ ]`のまま残してある。**

- [x] 長い文章を入力すると、**入力欄が下へ伸びて全文が見える**(実測: 3行で83px、200字で200px)
- [x] 上限(200px)を超えると入力欄の中でスクロールし、**会話ログが潰れない**(実測: 5000字でも200pxで頭打ち・`scrollHeight > clientHeight`)
- [x] 文字を消すと**元の高さへ縮む**(実測: 200px → 44px → 46px)
- [x] 空欄時に入力欄が膨らまない(実測: 46px。padも148pxで従来どおり)
- [x] 入力欄が伸びた分だけ`.pad`も広がる(実測: pad 148px → 269px。`minmax()`修正が効いている)
- [x] 送信後に入力欄が**1行の高さへ戻る**(`sendTextInput()`内で`autoResizeTextInput()`を呼ぶよう配線済み)
- [x] Enterで送信できる(従来どおり)
- [x] **Shift+Enterで改行**でき、そのまま送信すると改行込みでAIへ渡る
- [x] 日本語変換中のEnter(確定)で**誤送信されない**(`isComposing`判定の維持確認)
- [x] 音声で長い発話をすると、書き起こしの反映に合わせて**入力欄が伸びる**
- [x] ウェイクワード検出でテキストが差し替わったときも高さが正しくなる
- [x] 入力欄が伸びたとき、📎添付チップ・📌ピン留めチップの表示が崩れない
- [x] `.pad`の`overflow: visible`化によって**他の要素がはみ出して見えていない**(特にマイクインジケータ周り)

### 結果

**1. ヘッドレスChromeでの実測(修正後)**

実装後、目視だけでは確認しきれないためヘッドレスChromeへ実際に読み込ませ、高さを数値で測った。

```bash
# static/をHTTPで配信し、検証用ハーネスを付けたコピーへ読み込ませて実測(検証後に削除)
cd C:\Users\gakuh\Documents\obsidian\サポートAI作製計画\scripts\static
python -m http.server 8765
"C:\Program Files\Google\Chrome\Application\chrome.exe" --headless --disable-gpu ^
  --virtual-time-budget=5000 --dump-dom "http://127.0.0.1:8765/_resize_check.html"
```

| 入力内容 | 入力欄の高さ | `.pad`の高さ | 判定 |
|---|---|---|---|
| 空 | 46px | 148px | ✅ 従来の見た目を維持 |
| 短文(2字) | 44px | 148px | ✅ 1行 |
| 3行 | 83px | 152px | ✅ 伸びている |
| 長文(200字) | 200px | 269px | ✅ 上限に到達・**padも追従して広がっている** |
| 超長文(5000字) | 200px | 269px | ✅ 頭打ち+内部スクロール(`scrollHeight > clientHeight`) |
| 短文へ戻す | 44px | 148px | ✅ 縮む |
| 空へ戻す | 46px | 148px | ✅ 縮む |

**2. 🐛 実測して初めて分かったバグ:空欄なのに161pxに膨らんでいた**

修正前の`autoResizeTextInput()`は無条件に`scrollHeight`を反映していたが、実測すると**空欄のときだけ161px**(正しくは46px)になっていた。

```
空でautoResize呼び出し後   rect=161px  scrollH=161px   ← 異常
短い文字を入れた後          rect= 44px  scrollH= 44px   ← 正常
```

**原因**: Chromeは`value`が空の`textarea`の`scrollHeight`に**プレースホルダーの折り返し高さを含める**。今回のプレースホルダーは「話すとここに書き起こされます。間違いを直して送信(Enter / Shift+Enterで改行)」と長いため、空欄が約7行分に膨らんでいた。

**対策**: 空のときは測らずに`height: auto`(=`rows="1"`の自然な高さ)のまま抜ける。

```javascript
function autoResizeTextInput() {
  textInput.style.height = "auto";
  // 空のときは測らない(Chromeは空textareaのscrollHeightにplaceholderの
  // 折り返し高さを含めるため、長いplaceholderのぶん空欄が縦に膨らむ)
  if (!textInput.value) return;
  textInput.style.height = Math.min(textInput.scrollHeight, TEXT_INPUT_MAX_HEIGHT) + "px";
}
```

**3. 退行なし**

```powershell
node --check static/_index_script_check.js   # → OK(構文エラーなし)
python -m pytest tests/ -q                   # → 391 passed, 5 subtests passed
```

### 分析

**着手前にレイアウトを調べたことと、実装後に実測したことの両方が効いた。**

- **着手前の調査**で`grid-template-rows: 56px 1fr 148px`(固定)と`.pad { overflow: hidden }`を見つけていなければ、`<input>`を`<textarea>`へ変えて「伸びるはずなのに見た目が変わらない」という状態になっていた。⓪-3の`lastAttachedDocumentText`と同じ**「実装したのに経路が繋がっていない」型**である。
- **実装後の実測**で、空欄が161pxに膨らむバグを出荷前に潰せた。これは`scrollHeight`の仕様(空要素ではプレースホルダーを含む)を知らないと**コードを読んでも気づけない**種類の欠陥で、⓪-5(15日目の「検索が常に何かを返す」)と同じく**動かして測らないと分からない**。

「コードが正しく見えること」と「実際にそう動くこと」は別物であり、UIの変更は特にその乖離が起きやすい。今回はヘッドレスブラウザで**数値として**確認できたので、目視より確実だった。

### 改善策

- **UIを変更したときは、可能な範囲でヘッドレスChromeの`--dump-dom`で実測する**手順を、`node --check`(構文チェック)と`pytest`(退行確認)に続く3つ目の確認として運用に加える。今回のように「構文は正しく、テストも通るのに、見た目が壊れている」場合を捕まえられる
- プレースホルダーを長くするときは、空欄時の高さへ影響しないか確認する(今回の対策で構造的には防げているが、`if (!textInput.value) return;`を消すと再発する)

### 残課題

- 実測はヘッドレスの既定ウィンドウ幅(入力欄の実効幅134px)で行った。**実際の画面幅では折り返し行数が変わる**ため、上記の絶対値そのものは環境依存である(挙動の正しさは幅に依存しない)
- Enter/Shift+Enter・IME変換中のEnter・音声書き起こし連動は**キーボード操作とマイクが要るためヘッドレスでは確認できていない**。上記チェックリストの`[ ]`項目として実機確認に残す

---

## 📌 次のステップ

0. **[[サポートAI作製計画/15日目音声による音楽再生(ytmusicapi+Brave).md|15日目]]へ引き継ぎ**:「〇〇を流して」と話しかけるとBraveでYouTubeの曲を再生する機能(`media_player.py`新規・MEDIAルート新設)。ライブラリ`ytmusicapi`の安全性監査と技術検証は15日目ⓠで実施済み
1. **⑦の実機確認の残り**(Enter/Shift+Enter・IME変換中のEnter・音声書き起こし連動・チップの表示崩れ)。ヘッドレスで測れなかった項目のみ
2. **13日目⑥の実機確認で未消化の項目**(13日目の主要フローは実機確認済みだが、以下の個別チェックが残っている):
   - AIの応答音声に「クレア」が含まれても自己トリガーしないこと(応答再生中のマイクミュート。10日目①)
   - 自動送信の1.5秒猶予中に入力欄をクリック/編集すると送信が取り消されること
   - 応答再生中(speaking)に喋っても割り込み送信されないこと / 同じ確定テキストが二重送信されないこと
2. **SearXNGの実ネットワーク疎通確認**(`curl "http://127.0.0.1:8888/search?q=...&format=json"`)。11日目のパッチ(`valkeydb.py`のpwd、`webutils.py`の2箇所)が消えていないかも合わせて確認する
3. **発話内のファイル名からの自動照合**(①で見送った案。ピン留めが安定してから、「〇〇の資料について」で自動ピン留めする拡張として再検討する)
4. 引き続き持ち越し:
   - **引用元チップ**(Web検索の出典を専用UIで表示。出典テキスト自体は13日目④で応答末尾に付いている)
   - **テレメトリ(tok/s)・Event Logの実データ化**、**モデル切替ドロップダウン**、**思考モードトグル**
   - **Web検索の自動判定**(ルーターが「検索が要る質問か」を判断する仕組み)
   - **VRAM逼迫によるモデル入替スラッシング**(16GB VRAMにFAST 13GB+DEEP 15〜17GBを同時保持できない構造的制約)
   - **`run_turn()`の同期ブロッキング呼び出し**(16・17日目でスレッド化+`cancel_turn`まで入ったが、`Pipe.pipe()`/`synthesize()`自体は同期のまま)
   - **複数枚画像の同時添付・OpenWebUI標準の`content` list形式対応**(YAGNIで保留)
   - [[サポートAI作製計画/9日目自前音声UIとストリーミング音声対話.md|9日目]]⑦(Tailscale Serveへの載せ替え・外出時CODEルート方針)
5. **③から派生する拡張**(今日は意図的に見送ったもの):
   - **グラフの埋め込み**(`openpyxl.chart`でExcel内にネイティブグラフ / matplotlibで画像を作ってpptxへ貼る)。matplotlibは**日本語フォントの豆腐化**という別問題を抱え込むため、必要になってから
   - **PDF出力**。docx→PDFはWindowsならWord COM経由が確実だが、依存が重い
   - **既存ファイルの編集**(「このエクセルに列を足して」)。①のピン留めと組み合わせると強力だが、上書き事故の設計が要る
   - **生成物の自動クリーンアップ**。`workspace/`が無限に太るので、古いものを消す方針をいずれ決める
6. **④から派生する拡張**(今日は意図的に見送ったもの):
   - **OAuth同意画面の「本番」公開**。テストステータスのままだと**リフレッシュトークンが7日で失効**し、週1回の再同意が要る。実用に載せるなら早めに公開へ切り替える
   - **`drive.readonly`スコープの追加**(「先週作ったスプレッドシートを読んで要約して」を可能にする)。今日は`drive.file`に絞ったため**原理的に不可能**。読み取りを許すかは安全性とのトレードオフで別途判断する
   - **Markdown→Google Docsのスタイル変換**(`#`を見出しへ、`|`を表へ)。初版は素のテキストで流し込む
   - **Gmail・Googleカレンダー連携**(「この予定を登録して」)。スコープが一気に広がるので別日に設計する
   - **Googleドキュメントへの追記**(新規作成ではなく既存へappend)。日報のような使い方に効く

### 📋 コード内「〇日目」コメントとノート番号の対応(⓪の改善策)

コーディングセッション側の日番号がノート番号より先行しているため、対応表をここで管理する。**次回も着手前に`grep -rn "N日目" scripts/`で未記載の実装を洗うこと。**

| コード内 | 内容 | 記載先ノート |
|---|---|---|
| 14日目 | `attached_document_text`(⓪-3で結線漏れ発覚)、Web検索の`support_ai_auto_pipe.py`結線 | 13日目④追記 / 本ノート⓪ |
| 15日目 | ウェイクワード検出の可視化、読み上げ速度バーの機能化 | **本ノート⓪(実装済みとして追認)** |
| 16・17日目 | `run_turn()`のスレッド化+`cancel_turn`(誤送信の即時停止) | **本ノート⓪(未記載だったものを記録)** |
| 18日目 | ウェイクワード「C.L.A.I.R.E.」スペルアウト表記の検出 | 13日目🐛追記 |
| 19日目 | 括弧書きの二重読み解消、`force_finalize_pending()` | 13日目🐛追記 |
| **本ノート(14日目)** | ①ピン留めによるファイル指定 ②チャット履歴の永続化 ③資料・コード生成 ④Google Workspace連携 | 本ノート |

### 🔎 ③④の着手前調査メモ(⓪と同じ手順を③④にも適用した結果)

⓪の改善策「着手前に`grep`で実態を確認する」を③④にも適用したところ、**新規実装だと思っていた③は8割方すでに存在していた**。

| 確認したこと | コマンド | 結果 |
|---|---|---|
| ファイル生成・実行の器 | `head -70 scripts/code_executor.py` | **4日目⑩の`<ACTION>`機構が完成済み**。`write_action_file()`+`execute_python_file()`+`resolve_safe_path()`(パス脱出防御つき) |
| Officeライブラリ | `pip list \| Select-String "openpyxl\|pptx\|docx\|pandas"` | **全て導入済み**(openpyxl 3.1.5 / python-docx 1.2.0 / python-pptx 1.0.2 / pandas 3.0.3) |
| Googleライブラリ | `pip list \| Select-String "google"` | **導入済み**(google-api-python-client 2.197.0 / google-auth-oauthlib 1.4.0)。ただし**コードは0行** |
| CODEルートの結線 | `grep -n "code_executor\|CODE" openwebui_pipe/support_ai_auto_pipe.py` | `CODE_ACTION_SYSTEM_PROMPT`・`_run_action()`・confirm/autonomousモードまで**結線済み** |
| 資料生成のルーティング | `head -80 scripts/router_rules.py` | **`CODE_TRIGGERS`に資料系の語が1つも無い** ← ③の最大のギャップ |
| 既存の生成物 | `ls scripts/workspace/` | `brand_new.py`・`output_numbers.py`(4日目の検証で作られたもの) |

→ ⓪で得た教訓(「実装したつもりの経路が繋がっていない」)が、**そのまま③のルーティング欠落として再現していた**。`code_executor`は完成しているのに、**ユーザーの「エクセルにまとめて」がそこへ到達しない**。着手前調査を習慣化した効果がさっそく出た形なので、次回以降も継続する。
