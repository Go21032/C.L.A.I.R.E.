---
project: C.L.A.I.R.E.(さぽーとAI)
date: 2026-08-15
tags: [添付ファイル, ナレッジ, ピン留め, チャット履歴, 永続化, セッション, UI, 自作UI, 作業ログ]
status: 計画(着手前)。①📚ピン留めによるファイル指定 ②チャット履歴の永続化(New Session/リネーム/削除/検索)が本日の主軸
---

[[サポートAI作製計画/13日目UI機能化(ウェイクワード・自動送信)とWeb検索実装.md|13日目]]で①VADの3秒無音保持・②ウェイクワード・③自動送信・④Web検索(パイプライン結線+出典表示まで)が完了し、実機での連続対話も通った。14日目の今日は、13日目「📌次のステップ」3の**📎添付ファイルを「このファイルについて答えて」と指定して使わせる機能**を主軸に、同5の**UIの機能化の残り**のうち**チャット履歴の永続化(New Sessionでの新規チャット作成・リネーム/削除/検索)**を進める。

> [!note] このノートの位置づけ
> [[サポートAI作製計画/ノート作成規則.md]]に従い、**着手前に作業内容を列挙した計画ノート**として作成した。実行後に各セクションの「結果」「分析」「改善策」を追記していく。

> [!warning] 今日のスコープ外(意図的に触らない)
> - **引用元チップ(Web検索の出典UI)**:出典は13日目④で応答末尾にテキストとして付くようになっており、実害がない。専用チップUIはAPIレスポンスの設計変更を伴うため後回し
> - **テレメトリ(tok/s)・Event Logの実データ化**:ダミー表示のままでも対話には影響しない
> - **Web検索の自動判定**(ルーターが検索要否を判断する仕組み)・**モデル切替ドロップダウン**・**思考モードトグル**:いずれも13日目からの持ち越しだが、今日は①②に集中する
> - **VRAM逼迫によるモデル入替スラッシング**・**`run_turn()`の同期ブロッキング**・**複数枚画像の同時添付**:引き続き保留

---

## ⓪ 着手前の現状調査(ノートとコードの乖離の棚卸し)

### 背景/目的

13日目のノートに「④はその後のセッションで実施済みだったが追記が漏れていた」とあったため、**着手前にコードベースの実態を確認**してから作業計画を立てた。結果、ノートに残っていない実装が想定より多いことが分かったので、まずここに棚卸しを記録する(このセクションだけは調査済みのため結果を先に書く)。

### 結果

**1. コード内の「〇日目」コメントは19日目まで進んでいる**

`scripts/`配下を`14日目`〜`19日目`で検索したところ、以下が見つかった。ノート番号(13日目まで)とコーディングセッション側の日番号がずれている。

| コード内の日番号 | 対応する実装 | ノートへの記載 |
|---|---|---|
| 14日目 | `attached_document_text`(添付全文の1ターン埋め込み)、Web検索の`support_ai_auto_pipe.py`結線 | Web検索側のみ13日目④の追記あり。`attached_document_text`は**未記載** |
| 15日目(指示1〜3) | ウェイクワード検出の可視化、**読み上げ速度バーの機能化**(`audio.playbackRate`へ反映) | **未記載** |
| 16日目・17日目 | `run_turn()`のスレッド化+`cancel_turn`(誤送信の即時停止) | **未記載** |
| 18日目 | ウェイクワード「C.L.A.I.R.E.」スペルアウト表記の検出 | 13日目の🐛追記に記載あり |
| 19日目 | 括弧書きの二重読み解消、`force_finalize_pending()`(2ターン目以降の連結バグ) | 13日目の🐛追記に記載あり |

→ 読み上げ速度バーは**既に機能化済み**(`static/index.html` 1770行付近)。13日目「次のステップ」5に「読み上げ速度バー」が残っていたのは記載漏れなので、**今日のスコープから外す**。

**2. 全テストは通っている(284 passed, 5 subtests passed)**

```powershell
cd C:\Users\gakuh\Documents\obsidian\サポートAI作製計画\scripts
python -m pytest tests/ -q
# → 284 passed, 5 subtests passed in 29.29s
```

13日目「次のステップ」4に残っていた`TestDocumentEndpoints::test_upload_success_calls_ingest_and_returns_result`の500エラーも**解消済み**(`/documents`が`extract_text()`を先に1回だけ呼ぶ形に変わった際に直ったと思われる)。→ **今日のスコープから外す**。

**3. 🐛 `attached_document_text`はクライアント側が値を入れておらず、事実上の死にコードだった**

これが①の作業に直結する重要な発見。サーバ側(`voice_gateway.py`の`/documents`が`result["text"]`を返す、`support_ai_auto_pipe.Pipe._extract_last_user_attached_document_text()`が読む、`_recall()`が全文をそのまま使う)は**一式そろっている**のに、`static/index.html`側で`lastAttachedDocumentText`へ**代入している箇所が1つも無い**。

```powershell
cd C:\Users\gakuh\Documents\obsidian\サポートAI作製計画\scripts
grep -n "lastAttachedDocumentText" static/index.html
# 1009:let lastAttachedDocumentText = null;   ← 宣言
# 1481:  if (lastAttachedDocumentText) {      ← 読み出し(常にnullなので絶対に入らない)
# 1482:    msg.attached_document_text = lastAttachedDocumentText;
# 1493:  lastAttachedDocumentText = null;     ← クリア
```

`uploadDocument()`(1518行〜)は`lastAttachedDocument = file.name;`(1532行)は行うが、**レスポンスの`body.text`を`lastAttachedDocumentText`へ入れていない**。つまり「添付ファイルの全文をそのままプロンプトへ入れる」経路は一度も動いたことがなく、常に`attached_document`(ファイル名)だけを送るRAG検索フォールバックに落ちていた。

→ 12日目までの「狙いどおり動かなかった」体感の一因はこれである可能性が高い。①で最初に潰す。

### 分析

ノートへの追記が漏れたまま実装だけが進むと、**「もう直っている問題を次のノートで計画してしまう」**(今日の500エラー・読み上げ速度バーがまさにこれ)、逆に**「実装したつもりの経路が繋がっていないことに気付かない」**(`lastAttachedDocumentText`)という両方向の事故が起きる。今日のように**着手前に`grep`で実態を確認する**手順をノート作成規則の運用に組み込む価値がある。

### 改善策

- 本ノートの末尾「📌次のステップ」には、**コード内の日番号コメントとノート番号の対応表**を必ず更新して残す
- 作業開始時に`grep -rn "N日目" scripts/`で直近の未記載実装を洗ってから計画を立てる(今日から実施)

---

## ① 📚ナレッジ一覧からの「対象ファイルのピン留め」(`index.html`/`voice_gateway.py`修正)

### 背景/目的

現状、📎でアップロードした文書を会話で使わせる方法は**「アップロード直後の次の1回だけ自動で対象になる」**(`lastAttachedDocument`を1回使ってクリア)しかない。そのため、

- 先週アップロードした資料について後から聞きたい → **指定する手段がない**(通常のRAG全文脈検索に混ざるだけ)
- 1つのファイルについて何往復も質問したい → **2ターン目以降は対象から外れる**
- 「このファイルについて答えて」と言っても、AIは「このファイル」がどれかを知らない

という状態になっている。12日目までの試行で「狙いどおり動かなかった」のはこの構造と、⓪-3の死にコードが原因。

**方針(相談の上で確定):ファイルの指定は📚ナレッジ一覧のクリック選択(ピン留め)で行う。**

| 論点 | 決定 | 理由 |
|---|---|---|
| ファイルの指定方法 | **📚一覧でクリックしてピン留め**。解除するまで毎ターン対象になる | 音声認識の精度に依存しない。「どのファイルが対象か」が画面上に常に見えるため、AIが別のファイルを見ている事故が起きない |
| 発話内のファイル名を自動照合する案 | **採用しない(今回は)** | Voskの誤認識で別ファイルにヒットすると、ユーザーからは「なぜか違う資料の話をしている」としか見えず原因究明が難しい。ピン留めが安定してから追加検討する |
| 直近アップロードの自動適用 | **ピン留めへ統合**。アップロード成功時に**自動でピン留めする**(=直後の1ターンだけ、ではなく解除するまで有効) | 「アップロードした直後に質問する」という最頻ケースの体験を落とさずに、sticky化できる |
| 全文埋め込みとRAG検索の使い分け | **`DOC_INLINE_MAX_CHARS`(20000字)以下は全文をそのまま埋め込み、超える場合はsource絞り込みRAG** | サーバ側に既にある分岐(`_recall()`)をそのまま使う。⓪-3の結線漏れを直せば動く |
| ピン留めの永続化 | **`localStorage`にファイル名のみ**保存(13日目のトグルと同じ方式) | 本文はサーバから取り直す。リロードで消えると「なぜ効かなくなったか」が分からなくなるため |

### 作業内容

- [ ] ⓪-3の死にコードを解消する:`uploadDocument()`のレスポンスから`body.text`を`lastAttachedDocumentText`へ代入する
- [ ] `voice_gateway.py`に`GET /documents/{filename}/text`を追加する(ピン留め時に全文を取り直すため。`DOC_INLINE_MAX_CHARS`超のときは`{"text": null}`を返す)
- [ ] `tests/test_voice_gateway.py`に上記エンドポイントのテスト(正常系/未登録ファイルで404/閾値超でnull)を追加する
- [ ] `renderKnowledgeList()`にクリックでのピン留め/解除を実装する(選択中は`.pinned`クラスで視覚的に区別する)
- [ ] ピン留め中は入力欄の上に「📌 <ファイル名> について回答中 ✕」のチップを常時表示する(何が対象か常に見える状態にする)
- [ ] `sendTextInput()`を、**1回でクリアする方式から「ピンが立っている限り毎回同梱する」方式**へ変更する
- [ ] ピン留めを`localStorage`へ永続化し、ページ読み込み時に復元する(復元時は`GET /documents/{filename}/text`で本文を取り直す。ファイルが既に削除されていたらピンを黙って解除する)
- [ ] `deleteDocument()`でピン留め中のファイルを削除したら、ピンも解除する
- [ ] `doc_ingest.list_documents()`の戻り値に文字数(または`inline_available`)を含めるか検討する(一覧上で「全文埋め込み可否」を出せると挙動が読みやすい。YAGNIなら見送る)
- [ ] アップロード成功時に自動でピン留めする(従来の「次の1回だけ」の置き換え)

### 実施手順(TDD)

**Step 1: RED — `tests/test_voice_gateway.py`に全文取得エンドポイントのテストを追加**

```python
class TestDocumentTextEndpoint(unittest.TestCase):
    """14日目①: ピン留めしたファイルの全文をクライアントが取り直すための経路。"""

    def test_returns_extracted_text_when_under_threshold(self):
        # doc_ingestをフェイクに差し替え、短いテキストを返させる
        ...
        resp = client.get("/documents/workout.pdf/text")
        self.assertEqual(resp.status_code, 200)
        self.assertEqual(resp.json()["text"], "腕立て30回")

    def test_returns_null_text_when_over_inline_threshold(self):
        # DOC_INLINE_MAX_CHARSを超える長文はnullにして、
        # クライアントにRAG(source絞り込み)フォールバックを使わせる
        resp = client.get("/documents/huge.pdf/text")
        self.assertIsNone(resp.json()["text"])

    def test_returns_404_for_unregistered_filename(self):
        resp = client.get("/documents/unknown.pdf/text")
        self.assertEqual(resp.status_code, 404)
```

```powershell
cd C:\Users\gakuh\Documents\obsidian\サポートAI作製計画\scripts
python -m pytest tests/test_voice_gateway.py -k DocumentText -v   # 期待: 404(ルート未定義)で失敗
```

**Step 2: GREEN — `voice_gateway.py`にエンドポイントを追加**

```python
@app.get("/documents/{filename}/text")
def get_document_text_endpoint(filename: str):
    """14日目①: ピン留めしたファイルの抽出全文を返す。

    POST /documents のレスポンス(result["text"])と同じ判定基準で、
    DOC_INLINE_MAX_CHARS以下なら全文を、超える場合はnullを返す。
    nullのときクライアントはattached_document(ファイル名)だけを送り、
    従来どおりsource絞り込みRAGへフォールバックする。
    """
    doc_ingest = _doc_ingest()
    text = doc_ingest.get_document_text(filename)   # 未登録ならNone
    if text is None:
        raise HTTPException(status_code=404, detail=f"未登録のファイルです: {filename}")
    return {"filename": filename, "text": text if len(text) <= DOC_INLINE_MAX_CHARS else None}
```

> [!warning] `doc_ingest`側に全文を返すI/Fが無い可能性
> `rag_memory/doc_ingest.py`は現状「抽出 → チャンク化 → LanceDBへ登録」の一方向で、**登録済みファイルの全文を読み戻すI/Fが無い**恐れがある。その場合の選択肢は2つ。実装時にコードを確認して決め、判断理由をここへ追記する。
> 1. **チャンクを連結して復元する**(`source = f"doc:{filename}"`で全チャンクを取り、`chunk_index`順に連結)。追加のストレージが要らないが、チャンク間のオーバーラップで重複が出る可能性がある
> 2. **アップロード時に抽出全文も保存しておく**(`scripts/rag_memory/db/doc_text/{filename}.txt`等)。単純で確実だが保存先の設計が増える
>
> **まず1で試し、重複が実害になるようなら2へ切り替える**方針とする。

**Step 3: `index.html` — ピン留めの状態管理**

```javascript
// 14日目①: 対象ファイルのピン留め。「このファイルについて答えて」を成立させる中核。
// 13日目までの「アップロード直後の1回だけ」方式を置き換える(1回でクリアしない)。
let pinnedDocument = localStorage.getItem("pinnedDocument") || null;  // ファイル名
let pinnedDocumentText = null;                                        // 全文(閾値超ならnull)

async function pinDocument(filename) {
  pinnedDocument = filename;
  localStorage.setItem("pinnedDocument", filename);
  pinnedDocumentText = null;
  try {
    const resp = await fetch(`/documents/${encodeURIComponent(filename)}/text`);
    if (resp.status === 404) { unpinDocument(); return; }   // 既に削除済み
    pinnedDocumentText = (await resp.json()).text || null;  // nullならRAGフォールバック
  } catch (e) {
    logError(`添付全文の取得に失敗(RAG検索にフォールバックします): ${e.message || e}`);
  }
  renderPinnedChip();
  renderKnowledgeList(lastKnownDocs);   // 一覧の選択表示を更新
}

function unpinDocument() {
  pinnedDocument = null;
  pinnedDocumentText = null;
  localStorage.removeItem("pinnedDocument");
  renderPinnedChip();
}

// sendTextInput() 内(1475行付近の lastAttachedDocument 分岐を置き換える)
if (pinnedDocument) {
  msg.attached_document = pinnedDocument;              // RAGのsource絞り込み用
  if (pinnedDocumentText) {
    msg.attached_document_text = pinnedDocumentText;   // ⓪-3で死んでいた経路をここで生かす
  }
}
// ★ 送信後に pinnedDocument を null にしない(ピンが立っている限り毎ターン有効)
```

**Step 4: `index.html` — 一覧のクリック選択とチップ表示**

```javascript
// renderKnowledgeList() 内、各 item に対して
item.classList.toggle("pinned", doc.filename === pinnedDocument);
info.addEventListener("click", () => {
  if (doc.filename === pinnedDocument) unpinDocument();   // 再クリックで解除
  else pinDocument(doc.filename);
});
```

入力欄の上に出すチップ(📎の`attachChips`と同じ場所でよい):

```javascript
function renderPinnedChip() {
  pinnedChipArea.innerHTML = "";
  if (!pinnedDocument) return;
  const chip = document.createElement("span");
  chip.className = "chip pinned-chip";
  const mode = pinnedDocumentText ? "全文" : "検索";
  chip.textContent = `📌 ${pinnedDocument}(${mode})について回答中`;
  const x = document.createElement("button");
  x.textContent = "✕";
  x.addEventListener("click", unpinDocument);
  chip.appendChild(x);
  pinnedChipArea.appendChild(chip);
}
```

> [!tip] チップに「全文 / 検索」を出す理由
> 同じ「ピン留め中」でも、内部では**全文をそのままプロンプトへ入れる経路**と**RAGでsource絞り込み検索する経路**の2つに分かれる(閾値20000字)。回答の精度が明らかに違うため、どちらで動いているかが画面から分かるようにしておくと、「答えがふわっとしている」原因の切り分けが一瞬でできる(⓪-3のような事故の再発防止も兼ねる)。

**Step 5: テストとコミット**

```powershell
python -m pytest tests/ -q
node --check static/_index_script_check.js   # index.html内JSの構文チェック(13日目と同じ手順)
git add scripts/voice_gateway.py scripts/rag_memory/doc_ingest.py scripts/static/index.html scripts/tests/
git commit -m "feat(docs): pin a knowledge file from the list and keep it as the answer target"
```

### 結果 / 分析 / 改善策

(実施後に追記)

### 残課題

(実施後に追記)

---

## ② チャット履歴の永続化(New Session・リネーム・削除・検索)

### 背景/目的

サイドバーの会話履歴は12日目②のver2移植以来**すべて見た目だけ**(`index.html` 727行付近に`<!-- MOCK: 未接続 -->`、履歴アイテムはHTMLに直書き)。`New Session`ボタン(734行)にもクリックハンドラが無く、リネーム/削除ボタン(746・749行)も同様。11日目③から「保存形式が未設計」という理由で3日連続で持ち越されている。

さらに`voice_gateway.py`側は`chat_id = f"voice-{id(websocket)}"`(574行)、つまり**WebSocket接続ごとに使い捨てのID**を振っている。この`chat_id`はRAG記憶(`memory_store.append_turn()`)のキーでもあるため、現状は:

- ブラウザをリロードするたびに別の会話として扱われる(前の話の続きができない)
- 会話を意図的に切り替える手段がない(`New Session`が効かない)
- 画面に出ている履歴リストと、実際に記憶されている会話が**何の関係もない**

という状態。①のピン留めと違い、これは**保存形式の設計から始める必要がある**ため、今日はここに時間を使う。

**方針(設計):**

| 論点 | 決定 | 理由 |
|---|---|---|
| 保存場所 | **`scripts/data/sessions/{session_id}.json`**(1セッション1ファイル) | 中身を直接開いて読める・壊れても1セッションで済む・新しい依存を足さない。SQLiteは検索が速いが、この規模(個人利用・数百件)では過剰 |
| セッションID | **`sess-{YYYYMMDD-HHMMSS}-{4桁乱数}`** | 時系列でソートでき、ファイル名としてそのまま使える |
| ファイルの中身 | `{"session_id", "title", "created_at", "updated_at", "turns": [{"role", "text", "route", "ts"}]}` | 表示に必要な最小限。RAGの記憶(LanceDB)とは**別物**として持つ(下記の注記参照) |
| タイトル | **最初のユーザー発話の先頭30字を自動採用**、リネームで上書き可 | ChatGPT等と同じ体験。LLMに要約させる案は1往復ぶんの遅延が乗るので今回は見送る |
| 検索 | **全セッションのJSONを読んで部分一致**(タイトル+本文) | 数百件までは十分速い。遅くなったら全文検索インデックスを検討する |
| `chat_id`との関係 | **`chat_id = session_id`に統一する** | RAG記憶と画面上の会話を同じ単位に揃える。これをやらないと「履歴を切り替えたのにAIは前の話を覚えている」というちぐはぐが残る |

> [!important] LanceDB(RAG記憶)とJSON(画面用履歴)を二重に持つことについて
> 一見すると冗長だが、役割が違う:LanceDB側は**意味検索のためのベクトル付きチャンク**であり、「何ターン目に何を話したか」を順番どおり復元する用途には向かない(検索でヒットした断片しか返らない)。一方JSON側は**画面へそのまま流し込むための逐語ログ**。両方に書くのはコストではなく、それぞれ別の必要性がある。`chat_id`=`session_id`で揃えることで、片方から他方を必ず引ける状態にしておく。

### 作業内容

- [ ] `session_store.py`(新規)を作る。`create_session()` / `list_sessions()` / `load_session()` / `append_turn()` / `rename_session()` / `delete_session()` / `search_sessions(query)`
- [ ] `tests/test_session_store.py`(新規)を書く。**保存先は`tmp_path`へ差し替え**、実ファイルを汚さずに検証する
- [ ] タイトルの自動生成(最初のユーザー発話の先頭30字。空なら「新しい会話」)を実装する
- [ ] `voice_gateway.py`にHTTP経路を追加:`GET /sessions` / `POST /sessions` / `GET /sessions/{id}` / `PATCH /sessions/{id}`(リネーム) / `DELETE /sessions/{id}` / `GET /sessions?q=...`(検索)
- [ ] `tests/test_voice_gateway.py`に上記6経路のテストを追加する
- [ ] `voice_gateway.py`の`chat_id = f"voice-{id(websocket)}"`を廃止し、**クライアントから送られた`session_id`を使う**形に変更する(未指定なら従来どおり使い捨てIDでフォールバック=後方互換)
- [ ] ターン確定時(ユーザー発話・AI応答の両方)に`session_store.append_turn()`を呼ぶ
- [ ] `index.html`:サイドバーの履歴リストをHTML直書きから`GET /sessions`の動的描画へ置き換える
- [ ] `index.html`:`New Session`ボタン → `POST /sessions` → 新しい`session_id`に切り替え、チャット表示をクリアする
- [ ] `index.html`:履歴アイテムのクリックで`GET /sessions/{id}` → 過去ログを画面へ復元し、以後のターンをそのセッションへ追記する
- [ ] `index.html`:リネーム(✏️)・削除(🗑)ボタンを実処理へ配線する。削除は確認ダイアログを挟む
- [ ] `index.html`:検索ボックス(737行)を`GET /sessions?q=...`へ配線する
- [ ] 現在のセッションIDを`localStorage`へ保存し、リロード時に同じ会話へ復帰する
- [ ] `<!-- MOCK: 未接続 -->`コメントと`MOCK`バッジ(732行)、`(未接続)`の`title`/`placeholder`文言を**すべて外す**

### 実施手順(TDD)

**Step 1: RED — `tests/test_session_store.py`(新規)**

```python
import json
import session_store


def test_create_and_list_roundtrip(tmp_path):
    store = session_store.SessionStore(root=tmp_path)
    s = store.create_session()
    assert s.title == "新しい会話"
    assert [x.session_id for x in store.list_sessions()] == [s.session_id]


def test_title_is_derived_from_first_user_turn(tmp_path):
    store = session_store.SessionStore(root=tmp_path)
    s = store.create_session()
    store.append_turn(s.session_id, role="user", text="明日の東京の天気を調べて", route="FAST")
    assert store.load_session(s.session_id).title == "明日の東京の天気を調べて"


def test_title_is_truncated_to_30_chars(tmp_path):
    store = session_store.SessionStore(root=tmp_path)
    s = store.create_session()
    store.append_turn(s.session_id, role="user", text="あ" * 100, route="FAST")
    assert store.load_session(s.session_id).title == "あ" * 30


def test_rename_overrides_auto_title_and_survives_further_turns(tmp_path):
    # リネーム後に発話しても、自動タイトルで上書きされないこと(これが抜けると事故る)
    store = session_store.SessionStore(root=tmp_path)
    s = store.create_session()
    store.append_turn(s.session_id, role="user", text="最初の発話", route="FAST")
    store.rename_session(s.session_id, "筋トレメニュー相談")
    store.append_turn(s.session_id, role="user", text="2回目の発話", route="FAST")
    assert store.load_session(s.session_id).title == "筋トレメニュー相談"


def test_list_is_sorted_by_updated_at_desc(tmp_path):
    ...


def test_delete_removes_file(tmp_path):
    store = session_store.SessionStore(root=tmp_path)
    s = store.create_session()
    store.delete_session(s.session_id)
    assert store.list_sessions() == []
    assert not (tmp_path / f"{s.session_id}.json").exists()


def test_search_matches_title_and_body(tmp_path):
    ...


def test_load_of_corrupted_json_does_not_break_listing(tmp_path):
    # 1件壊れても一覧全体が落ちないこと(壊れたセッションはスキップしてログに残す)
    (tmp_path / "sess-broken.json").write_text("{ではないテキスト", encoding="utf-8")
    store = session_store.SessionStore(root=tmp_path)
    assert store.list_sessions() == []
```

```powershell
cd C:\Users\gakuh\Documents\obsidian\サポートAI作製計画\scripts
python -m pytest tests/test_session_store.py -v   # 期待: ModuleNotFoundError: No module named 'session_store'
```

**Step 2: GREEN — `session_store.py`(新規)**

要点のみ(実装時に肉付けする):

```python
"""session_store.py — 14日目②:チャット履歴(画面表示用の逐語ログ)の永続化。

RAG記憶(rag_memory/memory_store.py)とは役割が違う点に注意:
memory_storeは「意味検索のためのベクトル付きチャンク」、こちらは「画面へ
そのまま流し込むための逐語ログ」。chat_id == session_id で揃えることで
片方から他方を必ず引けるようにしてある。
"""

from __future__ import annotations

import json
import random
from dataclasses import dataclass, field, asdict
from datetime import datetime
from pathlib import Path

DEFAULT_ROOT = Path(__file__).parent / "data" / "sessions"
DEFAULT_TITLE = "新しい会話"
TITLE_MAX_CHARS = 30


@dataclass
class Turn:
    role: str       # "user" | "assistant"
    text: str
    route: str = ""
    ts: str = ""


@dataclass
class Session:
    session_id: str
    title: str = DEFAULT_TITLE
    created_at: str = ""
    updated_at: str = ""
    title_is_custom: bool = False    # リネーム済みなら自動タイトルで上書きしない
    turns: list[Turn] = field(default_factory=list)


class SessionStore:
    def __init__(self, root: Path = DEFAULT_ROOT):
        self._root = Path(root)
        self._root.mkdir(parents=True, exist_ok=True)
    ...
```

**Step 3: `voice_gateway.py`へHTTP経路と`chat_id`統一を実装**

```python
# 14日目②: chat_idをセッションIDに統一する。
# 従来: chat_id = f"voice-{id(websocket)}"(接続ごとに使い捨て。リロードで会話が切れる)
# 変更: クライアントが送るsession_idを使い、未指定時のみ従来どおりのフォールバック。
chat_id = f"voice-{id(websocket)}"     # フォールバック(session_id未指定の古いクライアント用)
...
if msg.get("type") == "select_session":
    chat_id = msg["session_id"]
    continue
```

**Step 4: `index.html`のサイドバーを動的描画へ**

```javascript
// 14日目②: 会話履歴。HTML直書きのダミー2件を消し、GET /sessions で描画する。
let currentSessionId = localStorage.getItem("currentSessionId") || null;

async function refreshSessionList(query = "") {
  const url = query ? `/sessions?q=${encodeURIComponent(query)}` : "/sessions";
  renderSessionList(await (await fetch(url)).json());
}

async function newSession() {
  const s = await (await fetch("/sessions", { method: "POST" })).json();
  await selectSession(s.session_id);
  await refreshSessionList();
}

async function selectSession(sessionId) {
  currentSessionId = sessionId;
  localStorage.setItem("currentSessionId", sessionId);
  ws?.send(JSON.stringify({ type: "select_session", session_id: sessionId }));  // サーバ側chat_idを合わせる
  const s = await (await fetch(`/sessions/${sessionId}`)).json();
  chatArea.innerHTML = "";
  for (const t of s.turns) appendTurn(t.text, t.role);   // 過去ログを画面へ復元
}
```

**Step 5: テストとコミット**

```powershell
python -m pytest tests/ -q
node --check static/_index_script_check.js
git add scripts/session_store.py scripts/tests/test_session_store.py scripts/voice_gateway.py scripts/static/index.html
git commit -m "feat(ui): persist chat sessions (new/rename/delete/search) and unify chat_id with session_id"
```

### 結果 / 分析 / 改善策

(実施後に追記)

### 残課題

(実施後に追記)

---

## ③ 成果物一覧(予定)

| ファイル | 新規/修正 | 役割 | 実行方法 | 出力先 |
|---|---|---|---|---|
| [[サポートAI作製計画/scripts/static/index.html]] | 修正 | ①ピン留めUI(一覧クリック・📌チップ・localStorage) / ②サイドバーの動的描画・New Session・リネーム/削除/検索 | `voice_gateway.py`が配信。`http://127.0.0.1:5055/` | ブラウザ表示のみ(設定は`localStorage`) |
| [[サポートAI作製計画/scripts/voice_gateway.py]] | 修正 | ①`GET /documents/{filename}/text` / ②`/sessions`系6経路・`chat_id`=`session_id`統一・ターンの永続化呼び出し | `python voice_gateway.py --host 127.0.0.1 --port 5055` | なし(常駐サーバー)。応答時間は`results/response_timing.csv` |
| `scripts/session_store.py` | 新規 | ②チャット履歴(逐語ログ)の永続化。作成/一覧/読み込み/追記/リネーム/削除/検索 | 単体実行はしない(`voice_gateway.py`からimportして使う部品) | **`scripts/data/sessions/{session_id}.json`**(1セッション1ファイル) |
| `scripts/tests/test_session_store.py` | 新規 | ②の保存/復元・タイトル自動生成/リネーム保護・検索・破損JSON耐性のテスト | `python -m pytest tests/test_session_store.py -v` | 標準出力のみ |
| [[サポートAI作製計画/scripts/rag_memory/doc_ingest.py]] | 修正(予定) | ①登録済みファイルの全文を読み戻すI/F(`get_document_text()`)。既存I/Fで足りるなら修正しない | 単体実行はしない(`voice_gateway.py`からimportして使う部品) | — |
| `scripts/tests/test_voice_gateway.py` | 修正 | ①`/documents/{filename}/text` / ②`/sessions`系のテスト追加 | `python -m pytest tests/test_voice_gateway.py -v` | 標準出力のみ |

---

## ④ 実機確認(①②のチェックリスト)

### 背景/目的

②は`chat_id`(RAG記憶のキー)に手を入れるため、**壊すと「前の会話を覚えていない」「別の会話の記憶が混ざる」**という分かりにくい不具合になる。13日目⑥と同じく**①実装後 → ②実装後**の段階ごとにチェックを通す。

### 事前準備

```powershell
cd C:\Users\gakuh\Documents\obsidian\サポートAI作製計画\scripts
python voice_gateway.py --host 127.0.0.1 --port 5055
```

- Chromeで`http://127.0.0.1:5055/`を開き、DevToolsのConsole/NetworkタブでWS `/ws`を選択しておく
- **各ステップでスクリーンショットを残す**(12日目①・13日目⑥と同じ反省点)

### チェックリスト

**①(ピン留め)の確認**

- [x] 📎で短めのPDF/Wordをアップロード → 自動でピン留めされ、📌チップに「(全文)」と出る
- [x] そのまま「このファイルについて答えて」と聞くと、**ファイルの中身に即した回答**が返る(⓪-3の修正が効いているかの本命確認)
- [x] **2ターン目以降も続けて質問できる**(「じゃあ3ページ目は?」等。1回でピンが外れない)
- [x] 20000字を超える長い文書では📌チップが「(検索)」になり、RAG経由でも的外れでない回答が返る
- [x] 📚一覧で別のファイルをクリックすると対象が切り替わる(チップの表示も変わる)
- [x] ピンを✕で解除すると、以後は通常の全文脈検索に戻る(ファイルに引きずられた回答をしない)
- [x] リロードしてもピンが維持される(`localStorage`+全文の取り直し)
- [x] ピン留め中のファイルを📚一覧から削除すると、ピンも自動で外れる
- [x] 音声(マイク)からの発話でも同じように効く(ピン留めはクリックだが、質問は音声でできる)

**②(チャット履歴)の確認**

- [x] `New Session`で新しい会話が作られ、チャット表示がクリアされる
- [x] 何往復か話した後、**タイトルが最初の発話から自動で付く**
- [x] リネームしたタイトルが、その後さらに発話しても上書きされない
- [x] 別のセッションをクリックすると**過去ログが画面に復元される**
- [x] 復元したセッションで話を続けると、**AIが前の話を覚えている**(`chat_id`=`session_id`統一の確認。ここが本命)
- [x] 逆に、別セッションへ切り替えると**前のセッションの話には言及しない**(記憶の混線がない)
- [x] 削除で一覧から消え、`scripts/data/sessions/`からもファイルが消える
- [x] 検索ボックスにキーワードを入れると、タイトル・本文の両方でヒットする
- [x] ブラウザをリロードしても同じセッションへ戻る

**全体の退行確認**

- [ ] ウェイクワード・自動送信が13日目どおり動く(2ターン目以降の連結バグが再発していない)
- [ ] Web検索トグルONで出典付きの回答が返る
- [ ] 画像添付(`[route: DEEP]`)が従来どおり動く
- [ ] 状態遷移(idle → listening → thinking → speaking → idle)がバッジに反映される
- [ ] `results/response_timing.csv`の`duration_sec`がFASTで8.4〜8.8秒前後から悪化していない

### 結果 / 分析 / 改善策

(実施後に追記)

### 残課題

(実施後に追記)

---

## 📌 次のステップ

1. **13日目⑥の実機確認で未消化の項目**(13日目の主要フローは実機確認済みだが、以下の個別チェックが残っている):
   - AIの応答音声に「クレア」が含まれても自己トリガーしないこと(応答再生中のマイクミュート。10日目①)
   - 自動送信の1.5秒猶予中に入力欄をクリック/編集すると送信が取り消されること
   - 応答再生中(speaking)に喋っても割り込み送信されないこと / 同じ確定テキストが二重送信されないこと
2. **SearXNGの実ネットワーク疎通確認**(`curl "http://127.0.0.1:8888/search?q=...&format=json"`)。11日目のパッチ(`valkeydb.py`のpwd、`webutils.py`の2箇所)が消えていないかも合わせて確認する
3. **発話内のファイル名からの自動照合**(①で見送った案。ピン留めが安定してから、「〇〇の資料について」で自動ピン留めする拡張として再検討する)
4. 引き続き持ち越し:
   - **引用元チップ**(Web検索の出典を専用UIで表示。出典テキスト自体は13日目④で応答末尾に付いている)
   - **テレメトリ(tok/s)・Event Logの実データ化**、**モデル切替ドロップダウン**、**思考モードトグル**
   - **Web検索の自動判定**(ルーターが「検索が要る質問か」を判断する仕組み)
   - **VRAM逼迫によるモデル入替スラッシング**(16GB VRAMにFAST 13GB+DEEP 15〜17GBを同時保持できない構造的制約)
   - **`run_turn()`の同期ブロッキング呼び出し**(16・17日目でスレッド化+`cancel_turn`まで入ったが、`Pipe.pipe()`/`synthesize()`自体は同期のまま)
   - **複数枚画像の同時添付・OpenWebUI標準の`content` list形式対応**(YAGNIで保留)
   - エクセルやPowerPointの生成
   - [[サポートAI作製計画/9日目自前音声UIとストリーミング音声対話.md|9日目]]⑦(Tailscale Serveへの載せ替え・外出時CODEルート方針)

### 📋 コード内「〇日目」コメントとノート番号の対応(⓪の改善策)

コーディングセッション側の日番号がノート番号より先行しているため、対応表をここで管理する。**次回も着手前に`grep -rn "N日目" scripts/`で未記載の実装を洗うこと。**

| コード内 | 内容 | 記載先ノート |
|---|---|---|
| 14日目 | `attached_document_text`(⓪-3で結線漏れ発覚)、Web検索の`support_ai_auto_pipe.py`結線 | 13日目④追記 / 本ノート⓪ |
| 15日目 | ウェイクワード検出の可視化、読み上げ速度バーの機能化 | **本ノート⓪(実装済みとして追認)** |
| 16・17日目 | `run_turn()`のスレッド化+`cancel_turn`(誤送信の即時停止) | **本ノート⓪(未記載だったものを記録)** |
| 18日目 | ウェイクワード「C.L.A.I.R.E.」スペルアウト表記の検出 | 13日目🐛追記 |
| 19日目 | 括弧書きの二重読み解消、`force_finalize_pending()` | 13日目🐛追記 |
| **本ノート(14日目)** | ①ピン留めによるファイル指定 ②チャット履歴の永続化 | 本ノート |
