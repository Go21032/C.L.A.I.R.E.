
# モデル動作検証

[[サポートAI作製計画/1日目設計とモデル選定.md|1日目:設計とモデル選定]]で選んだ4モデル(Gemma 4 26B-A4B / gpt-oss-20b / Devstral Small 2 24B / Phi-4-mini)をOllamaで実際にダウンロードした。ここではダウンロード後に行うべき動作検証の作業内容をまとめる。

---

## ① 事前準備

- [x] `ollama list` で4モデルが正しく取得できているか確認
- [x] `ollama --version` / GPUドライバ(NVIDIA)のバージョン確認
- [x] タスクマネージャー or `nvidia-smi` を別ウィンドウで常時監視できるようにしておく(VRAM使用量・GPU使用率を見るため)
---

## ② モデル単体の起動確認(4モデル共通)

各モデルについて、以下を1体ずつ順番に検証する(同時ロードはVRAM都合上不可のため)。

| 確認項目        | 内容                                            |
| ----------- | --------------------------------------------- |
| 起動可否        | `ollama run <model>` で正常に起動するか、エラーが出ないか       |
| VRAM使用量     | `nvidia-smi` でロード時の実測VRAM(GB)を記録し、設計時の見積もりと比較 |
| 初回応答速度      | プロンプト投入から最初のトークンが返るまでの時間(コールドスタート)            |
| 生成速度        | tokens/sec の実測値(体感速度の確認)                      |
| CPU/GPU併用有無 | 意図せずCPUオフロードが発生していないか(特にDevstral Q4量子化時)      |
| 終了・アンロード確認  | `ollama stop <model>` 後にVRAMが解放されるか           |

### 通常時のVRAM使用量
| 項目      | 該当箇所             | 今回の値                 | 意味                       |
| ------- | ---------------- | -------------------- | ------------------------ |
| VRAM使用量 | `Memory-Usage` 列 | `2163MiB / 16303MiB` | 現在使用中VRAM / 総VRAM(16GB)  |
| GPU使用率  | `GPU-Util` 列     | `3%`                 | GPUコアの稼働率(推論中に跳ね上がるか確認用) |
### モデル別の検証観点

- **Gemma 4 26B-A4B(計画・深い相談)**
  - 研究スケジュールや資格勉強の計画立案など、複数ステップの論理的なタスクを実際に投げて応答の一貫性を確認
  - 長めのコンテキスト(過去の会話を含む想定)でも破綻しないか

- **gpt-oss-20b(速度・雑談・数学)**
  - 雑談的な質問への応答速度(体感の速さ)を確認
  - 簡単な数学問題(暗算〜中学レベル)を数問投げて正答率を確認

- **Devstral Small 2 24B(コーディング専任)**
  - 実際の研究用コード(避難シミュレーション関連など)の一部を渡し、実装・デバッグ提案の質を確認
  - Q4量子化時の精度劣化が体感で問題ないか

- **Phi-4-mini(ルーター)**
  - CPUのみでの起動・応答速度を確認(常駐前提のため)
  - 「この質問はどのモデルに振るべきか」という簡易的な分類プロンプトを与え、意図通りの判定ができるか(振り分けロジック本実装の前段階のスモークテスト)

---

## ③ モデル切り替え(スワップ)検証

設計上、Gemma4・gpt-oss・Devstralは「どれか1つのみVRAMにロード」する運用のため、切り替え動作を検証する。

- [ ] モデルA起動 → 停止 → モデルB起動、という手順でVRAMが正しく解放・再確保されるか
- [ ] 切り替えにかかる時間(体感の待ち時間)を計測し、実運用で許容できるか判断
- [ ] 連続して切り替えテストを行い、メモリリークやエラー蓄積が起きないか確認

---

## ④ 記録・まとめ

### 測定方法

手動で`nvidia-smi`を目視して数値をメモする方式は、`GPU-Util`が推論中の一瞬しか跳ね上がらないためタイミングを逃しやすく、記録のたびに数値がブレる(ガス化する)問題があった。そのため **Pythonスクリプトで全項目を自動計測してCSVに記録する方式** に切り替えた。

#### 自動計測スクリプト([[サポートAI作製計画/scripts/monitor_ollama.py]])

`ollama run <model> --verbose "<prompt>"` をサブプロセスとして実行しつつ、裏スレッドで `nvidia-smi` を1秒間隔でポーリングし、以下を自動で行う。

- VRAM使用量・GPU使用率を1秒ごとにサンプリングし、実行中の**ピーク値・平均値**を算出
- `--verbose`出力(`total/load/prompt eval/eval`の各duration・rate)を正規表現でパース
- 初回応答速度(TTFT) = `load duration + prompt eval duration` を自動計算
- `--repeat 3` のように指定すれば同一プロンプトを複数回試行し、平均値も自動で1行追記(手動で3回試行→平均する作業が不要になる)
- 結果は2種類のCSVに保存される(3日目のスワップ計測モード追加に伴い、通常の動作検証結果は`scripts/results/Functional Testing/`フォルダにまとめるよう整理した)
  - `scripts/results/Functional Testing/summary.csv` … モデル横断で比較する集計1行(このNoteの実測値記録表に転記する元データ)
  - `scripts/results/Functional Testing/<label>_<timestamp>_samples.csv` … 1秒ごとの生データ(VRAM/GPU-Util推移。異常検知や裏付け用)

**使い方:**

```bash
cd サポートAI作製計画/scripts
python monitor_ollama.py --model gemma4:26b --prompt "簡単な自己紹介をして" --label "Gemma4-初回" --repeat 3
```

- `--model`: `ollama list`に表示されるモデル名
- `--prompt`: 投げるプロンプト(モデル別の検証観点で使う実際の質問文をそのまま指定)
- `--label`: 記録上の識別名(例: `Gemma4-2回目` `gptoss-数学問題1`)
- `--repeat`: 試行回数(省略時1回。ノートの推奨通り3回試行するなら`--repeat 3`)
- `--interval`: `nvidia-smi`のポーリング間隔(秒、デフォルト1秒)

動作確認済み(phi4-miniで実行し、summary.csvへの追記・samples.csvの生成・`--repeat`平均行の追記まで確認済み)。標準ライブラリのみで動くため追加インストール不要。

#### CSVの見方・単位の補足

- `vram_used_peak_mib` / `vram_used_avg_mib` … `nvidia-smi`の`Memory-Usage`欄相当。**MiB表示**(GBではない)。GiBに換算したい場合は `MiB ÷ 1024`
- `gpu_util_peak_pct` / `gpu_util_avg_pct` … `GPU-Util`欄相当。推論中のサンプルのみ含まれるため「跳ね上がった瞬間」を取りこぼしにくい
- `ttft_ms` … 初回応答速度(ms換算済み)
- `eval_rate_tps` … 生成速度(tokens/sec)。`--repeat`使用時は平均行(`label`が`_avg(N回)`で終わる行)を実測値記録表に転記する

---

### チェックリスト

- [x] 各モデルの実測値を自動計測スクリプトで記録完了(`scripts/results/Functional Testing/summary.csv` / まとめは `scripts/results/Functional Testing/summary_report.xlsx`)
	- [x] 見積もりと乖離が大きい項目がないか確認(VRAM特に)→ Phi-4-miniの乖離を発見・原因特定・対処済み(詳細は下記「分析・考察」)
- [ ] スワップ時のオーバーヘッド(切り替え時間)を計測・記録
- [ ] 問題なければ「② Phi-4-miniの振り分けロジック(プロンプト設計)」のステップへ進む(→ Devstralの実コード検証・③スワップ検証が残っているため保留中)

---

## 分析・考察

`scripts/results/Functional Testing/summary.csv`の実測データをもとに、4モデルの検証結果について考察した内容をまとめる(詳細な数値・色分けは [[サポートAI作製計画/scripts/results/Functional Testing/summary_report.xlsx]] 参照)。

### ① keep_alive(モデルのアイドル保持時間)の方針

手動で`nvidia-smi`を都度叩く方式から自動計測に切り替えた過程で、「モデルはアイドル状態が何分続くとVRAMから解放されるか」というOllamaの`keep_alive`設定が実運用の体感速度に直結することが分かった。

- Ollamaはデフォルトで**アイドル5分**経過すると自動的にモデルをアンロードする。5分後だろうと10時間後だろうと、一度アンロードされれば次回は前回の状態を引き継がず、初回と同じコールドスタート(ディスクからの再読み込み)が発生する。
- **Gemma4 / gpt-oss / Devstral(重量級・排他ロード運用)** → デフォルトの5分のままでよい。設計上どのみち手動で1つずつ切り替える運用のため、使い終われば自動でVRAMが解放される方が都合が良い。
- **Phi-4-mini(ルーター・常駐前提)** → 無期限(`keep_alive: -1`)に設定する。CPU実行(下記②)にしたことでVRAMをほぼ使わないため、無期限常駐させても他モデルのスワップの妨げにならない。

**無期限設定の実際のコマンド(検証済み):**
```powershell
# CLIでの単発テスト。-1 だけだと "missing unit in duration" エラーになるため単位付きで -1s と指定する
ollama run phi4-mini-cpu --keepalive -1s "テスト"
# ollama ps の UNTIL 欄が "Forever" になることを確認済み
```
本実装(Pythonから振り分けロジックとしてAPI経由で呼ぶ場合)は、`/api/generate`等のリクエストボディに `"keep_alive": -1` を含める形になる。

### ② Phi-4-miniのCPU優先化

Ollamaは何も指定しないとVRAMに空きがあれば自動的にGPUへオフロードするため、「ルーターはCPU優先で動かす」という設計意図は明示的に設定しない限り反映されない。`num_gpu 0`を指定したModelfileでCPU固定版(`phi4-mini-cpu`)を作成し、`monitor_ollama.py`で実測比較した。

| 項目 | 元(GPU自動オフロード) | CPU固定版(`num_gpu 0`) |
|---|---|---|
| VRAM使用量 | 約4880MiB | **約1981MiB**(設計見積もり2560MiBに近い) |
| GPU使用率 | 30〜80% | ほぼ0〜1% |
| 生成速度 | 約206〜217 tokens/s | **約21.6 tokens/s(約1/10)** |
| TTFT | 約200〜400ms | 約1084ms |

**PowerShellでの作成手順(検証済み):**
```powershell
@'
FROM phi4-mini:latest
PARAMETER num_gpu 0
'@ | Set-Content -Encoding utf8 Modelfile.phi4-cpu

ollama create phi4-mini-cpu -f Modelfile.phi4-cpu
```

生成速度は約1/10に低下するが、ルーターの役割(「どのモデルに振るか」を判定する軽い分類タスクのみ)であれば体感上気になるレベルではないと判断。**速度低下への追加対処は保留**とし、振り分けロジック実装時に体感で再判断する。

### ③ Phi-4-mini VRAM異常値(15547MiB)の原因

②の検証以前に「Phi-4-miniロード時、初回だけVRAM使用量が15547MiBまで跳ね上がる」という異常値が観測されていた。原因を調査した結果:

- 異常値`15547MiB`は、**直前に実行していたDevstral(実測15538〜15623MiB)の値とほぼ一致**していた。
- Devstral試行終了からPhi-4-mini試行開始までの間隔はわずか47秒。Ollamaのデフォルトkeep_alive(5分)以内のため、Devstralがまだ(部分的に)VRAM上に残っていたと考えられる。
- 十分な間隔を空けて再測定したところ(6回試行・3回試行の計9回)、異常値は一度も再現せず、約4880〜5025MiBで安定して推移した。

→ **結論: Phi-4-mini自体のロード挙動に問題があったのではなく、直前モデルの残留VRAMによる一時的な計測汚染だった。** 実際の課題は異常値そのものではなく、②で対処した「そもそもCPUではなくGPUに乗っていた」という設計との乖離だった。原因特定・対処ともに完了。

### ④ Devstralの生成速度(24 tokens/s前後)について

自己紹介プロンプト(出力94〜136token程度の短文)での実測では、Devstralの生成速度は約24 tokens/sと、他モデル(gpt-oss 150 tokens/s、Gemma4 82 tokens/s、Phi-4-mini 200 tokens/s台)と比べて明確に遅い。

ただし短文生成の実測値だけでは、実際のコーディング支援(数百〜千token規模の出力)での体感速度を正しく判断できない。ノート②の「モデル別の検証観点」に元々予定していた**「実際の研究用コードの一部を渡し、実装・デバッグ提案の質を確認」を実施する際に、`monitor_ollama.py`で速度も同時計測し、実コードベースの数値で改めて判断する**方針とし、**現時点での対処は保留**とする。

---

## 📌 次のステップ(1日目からの引き継ぎ)
1. ~~4モデルをOllamaで実際にダウンロード・動作検証~~ ← 今回はこの「動作検証」部分
2. Phi-4-miniの振り分けロジック(プロンプト設計)
3. RAGの記憶DB構築
4. Cloudflare Tunnelのセットアップ
5. STT/TTSパイプラインの組み立て

---

## 今日のまとめ(2026-08-02)

- 4モデル(Gemma4 26B-A4B / gpt-oss-20b / Devstral Small 2 24B / Phi-4-mini)の単体起動確認・実測値取得を実施。
- 手動での`nvidia-smi`目視記録は精度・再現性に難があったため、[[サポートAI作製計画/scripts/monitor_ollama.py|monitor_ollama.py]]を新規作成し、VRAM・GPU使用率・応答速度の計測を完全自動化(CSV記録・複数回試行の平均算出まで対応)。
- 実測データの比較・色分けを行う[[サポートAI作製計画/scripts/build_report.py|build_report.py]]も追加し、`summary_report.xlsx`でモデル横断の比較と所見出力を自動化。
- 実測の結果、**Phi-4-mini(ルーター)がCPUではなくGPUにロードされていた**という設計との乖離を発見。原因(直前モデルの残留VRAMによる誤認と、Ollamaのデフォルト挙動がGPU優先であること)を特定し、`num_gpu 0`によるCPU固定化で対処・実測比較まで完了。
- Phi-4-mini用に`keep_alive`を無期限にする運用方針・具体的なコマンドも確定。
- Devstralの生成速度の遅さ、および③モデル切り替え(スワップ)検証は、実コードでの再検証待ちとして次回以降に持ち越し。

→ 次回は「③ モデル切り替え検証」と「Devstralへの実コード投入テスト」を優先し、それらが完了次第「Phi-4-miniの振り分けロジック実装」へ進む。
