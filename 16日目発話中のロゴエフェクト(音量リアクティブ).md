---
project: C.L.A.I.R.E.(さぽーとAI)
date: 2026-08-18
tags: [UI, エフェクト, WebAudio, AnalyserNode, CSS, ARC CORE, ロゴ, 発話中, フロントエンド, 作業ログ]
status: 実装完了・実機確認待ち。①AnalyserNodeによる実音量の取得 ②rAFでCSS変数へ流し込み ③ロゴの発光+微振動(すべて`index.html`1ファイル内)
---

[[サポートAI作製計画/15日目音声による音楽再生(ytmusicapi+Brave).md|15日目]]で「AIにPC上のソフトを操作させる」最初の一歩(MEDIAルートによる音楽再生)を通した。16日目の今日は方向を変えて**UIの表現力**に取り組む。具体的には、**クレアが喋っている間だけ「ARC CORE」の左隣にあるロゴが発光し、微かに振動する**ようにして、「今このAIが話している」ことを画面からも感じ取れるようにする。

> [!note] このノートの位置づけ
> [[サポートAI作製計画/ノート作成規則.md]]に従うが、15日目までと違い**実装を先に済ませてから記録した事後ノート**である。作業内容のチェックボックスは実施済みのため`[x]`で記載している。ただし**音と光を目と耳で確かめる部分(⑤実機確認)は未実施**であり、そこだけは着手前の状態で残している。

> [!warning] 今日のスコープ外(意図的に触らない)
> - **サーバー側(`voice_gateway.py`)の変更**:既存の`audio`イベントだけで実現できるため一切触っていない。WSプロトコルに新イベントを増やしていない
> - **大きい方のARC CORE / 背景演出**:今回光らせるのは**ヘッダー左の64pxロゴ(`.arc-mini`)のみ**。ステージ全体の演出には広げない
> - **ユーザーの発話(マイク入力)への反応**:今日は「クレアが話している間」だけ。マイク入力レベルの可視化は別途(`micIndicator`が既に存在する)
> - **エフェクトのON/OFFトグル**:右パネルのControlsへ設定項目を増やすことはしない。まず出してみて、うるさければ強度を調整する

---

## ⓪ 着手前調査(何に同期させるか)

### 背景/目的

「話している間」をUIで表現するには、**何をもって「話している」とするか**を決める必要がある。既存コードには候補が2つあった。

### 実施方法

`index.html`と`voice_gateway.py`の該当箇所を読み、状態がどう流れているかを追った。

```bash
cd C:\Users\gakuh\Documents\obsidian\サポートAI作製計画\scripts
grep -n "speaking\|isPlaying\|playNext" static/index.html
grep -n '"type": "state"' voice_gateway.py
```

### 結果

**候補1: `state:speaking`(サーバー由来のイベント)**

```python
# voice_gateway.py:1032
await send_json({"type": "final_transcript", "text": text})
await send_json({"type": "state", "value": "speaking"})
```

**候補2: `isPlaying`(クライアント側の再生キューの状態)**

```javascript
// index.html playNext(): 実際に <audio> が鳴っている区間だけ true
isPlaying = true;
```

| 論点 | `state:speaking` | `isPlaying` |
|---|---|---|
| 立つタイミング | **音声キューより先**(LLM生成〜TTS合成の待ち時間を含む) | 実際に音が出た瞬間 |
| 落ちるタイミング | サーバーが`state:idle`を送った時 | 再生キューが空になった時 |
| 「無音なのに光る」区間 | **ある**(合成待ちの数百ms〜数秒) | ない |

**🔑 発見:`state:speaking`と「実際に音が鳴っている区間」は一致しない。**

### 分析

`state:speaking`で切り替えると、**TTS合成待ちの無音区間もロゴが光り続ける**。これは10日目①でマイクミュートの範囲を`isPlaying`だけでなく`state:thinking/speaking`まで広げたのと**ちょうど逆の要求**である。あちらは「音が鳴る前から塞ぎたい」(安全側に倒す)、今回は「音が鳴っている間だけ光らせたい」(正確さを取る)なので、同じ状態を使い回してはいけない。

さらに、単に「鳴っている/鳴っていない」の2値でループアニメを回すと、**声の抑揚とロゴの動きが無関係**になり「ただ点滅している飾り」に見える。相談の上、**実際の音量(振幅)に反応させる**方針を採った。

### 改善策

- 同期対象を **`isPlaying`(実再生)** に固定し、さらに`AnalyserNode`で**実音量**を取り出して強度に反映する。
- ただし後述①の地雷(グラフへ引き込むと音が消える)があるため、**解析への接続は必ず失敗してもよい設計**にする。

---

## ① 実音量の取得(`index.html` 修正)

### 背景/目的

⓪の結論に沿って、再生中の`<audio>`から振幅を取り出す。ブラウザの Web Audio API で `MediaElementAudioSourceNode → AnalyserNode → destination` と繋げば、**再生を止めずに**波形を覗ける。

**方針(相談の上で確定):**

| 論点 | 決定 | 理由 |
|---|---|---|
| 同期対象 | **`isPlaying`(実再生)+ 実音量** | ⓪のとおり。`state:speaking`は無音区間を含む |
| AudioContext | **読み上げ専用に新規生成**(`ttsCtx`)。マイク用の`audioCtx`は再利用しない | `audioCtx`は`startMic()`内でしか作られない。**マイクを起動していなくてもテキスト入力の読み上げは鳴る**ため、ライフサイクルを独立させないとエフェクトが出ないケースが生まれる |
| 解析失敗時 | **エフェクトを諦めて素の再生にフォールバック** | エフェクトは飾り。**飾りのために読み上げを死なせない**(⓪の改善策) |
| ノードの後始末 | `ended`/`error`/`play()`失敗のすべてで`disconnect()` | `playNext()`は**文ごとに`new Audio()`**を作るので、放置するとノードが溜まる |

### 作業内容

- [x] `ttsCtx` / `ttsAnalyser` を遅延生成する`ensureTtsAnalyser()`を追加する
- [x] `<audio>`を解析グラフへ繋ぐ`connectAudioToAnalyser()`を追加する(**全体をtry/catchで包む**)
- [x] `AudioContext`が`suspended`で生成された場合に`resume()`する
- [x] `playNext()`から呼び出し、`ended`/`error`/`play()`失敗の全経路で`disconnect()`する
- [x] 解析に失敗しても再生処理が一切変わらないことを、コードパスを追って確認する

### 実施方法

**`scripts/static/index.html`**(修正)。`enqueueAudio()`の直前へ以下を追加した。

```javascript
// === 16日目: 発話中のロゴエフェクト(実音量リアクティブ) ======================
// 読み上げ中の<audio>を AnalyserNode に通して振幅(RMS)を取り出し、CSS変数
// `--voice-level`(0〜1)として .arc-mini(ARC COREの左隣のロゴ)へ毎フレーム流し込む。
//
// 設計上の約束:
//   - **エフェクトのために読み上げを死なせない**。解析への接続はすべてtry/catchで囲み、
//     失敗したらエフェクトを諦めて素の再生にフォールバックする。
//   - マイク用の`audioCtx`(startMic内で生成)は**再利用しない**。マイクを起動して
//     いなくてもテキスト入力の読み上げは鳴るため、ライフサイクルを独立させる。
const arcMiniEl = document.querySelector(".arc-mini");
let ttsCtx = null;        // 読み上げ解析専用のAudioContext
let ttsAnalyser = null;
let ttsLevelRaf = 0;      // requestAnimationFrameのID(0 = 停止中)
let ttsLevel = 0;         // 平滑化後のレベル 0〜1

function ensureTtsAnalyser() {
  if (ttsAnalyser) return ttsAnalyser;
  const Ctx = window.AudioContext || window.webkitAudioContext;
  if (!Ctx) return null;  // 非対応ブラウザではエフェクトなしで動かす
  ttsCtx = new Ctx();
  ttsAnalyser = ttsCtx.createAnalyser();
  ttsAnalyser.fftSize = 1024;
  ttsAnalyser.smoothingTimeConstant = 0.6;
  // ★最重要: createMediaElementSource()で<audio>をグラフへ引き込んだ後、
  //   destinationまで繋がっていないと音が「完全に無音」になる。ここを忘れないこと。
  ttsAnalyser.connect(ttsCtx.destination);
  return ttsAnalyser;
}

// <audio>を解析グラフへ接続する。成功したらMediaElementSourceNodeを返す(失敗時はnull)。
function connectAudioToAnalyser(audio) {
  try {
    const analyser = ensureTtsAnalyser();
    if (!analyser) return null;
    // 自動再生ポリシーによりsuspendedで生成されることがある(この状態だと無音になる)
    if (ttsCtx.state === "suspended") ttsCtx.resume();
    // createMediaElementSource()は1要素につき1回しか呼べない。playNext()は文ごとに
    // new Audio()を作るので、ここで毎回作って ended 時にdisconnectする。
    const source = ttsCtx.createMediaElementSource(audio);
    source.connect(analyser);
    return source;
  } catch (e) {
    console.warn("[16日目] 音量解析へ接続できませんでした(エフェクトなしで再生します):", e);
    return null;
  }
}
```

`playNext()`の Before/After は次のとおり。

| 項目 | 修正前 | 修正後 | 狙い |
|---|---|---|---|
| `ended`ハンドラ | `audio.addEventListener("ended", playNext)` | `releaseAnalyser()` してから `playNext()` | 文ごとに作られるノードを確実に解放する |
| `error`ハンドラ | `logError()` → `playNext()` | 先頭に `releaseAnalyser()` を追加 | 失敗時もノードを残さない |
| `play()`失敗 | `logError()` → `playNext()` | 先頭に `releaseAnalyser()` を追加 | 同上 |
| 再生開始時 | (なし) | `connectAudioToAnalyser()` → 成功時のみ `startVoiceLevelLoop()` | **失敗したら何も起きないだけ**で、再生には影響しない |

```javascript
  const analyserSource = connectAudioToAnalyser(audio);
  if (analyserSource) startVoiceLevelLoop();
  const releaseAnalyser = () => {
    if (!analyserSource) return;
    try { analyserSource.disconnect(); } catch (e) { /* noop */ }
  };
```

### 結果

`node --check`によるJS構文検査を通過(下記③の結果を参照)。実際に音が鳴るかは⑤の実機確認で判定する。

### 分析

**この作業で最も危険だったのは `createMediaElementSource()` の副作用**である。この関数を呼んだ瞬間、その`<audio>`の音声出力は**要素から切り離されて Web Audio のグラフへ移る**。`destination`まで繋いでいないと、**エラーも警告も一切出ないまま完全な無音になる**。「エフェクトを足したら読み上げが聞こえなくなった」という、原因が極めて分かりにくい事故になり得た。

これは15日目⓪-5の「`search()`は見つからなくても何かを返す」と同じ**黙って壊れる**型の落とし穴である。だからこそ、

1. `ensureTtsAnalyser()`内で`connect(ttsCtx.destination)`を**★コメント付きで**明示した
2. 接続処理全体を`try/catch`で囲み、**失敗時は解析を諦めて素の再生に戻す**

の2段構えにした。

### 改善策

- 結果が良かったので改善不要。ただし**⑤の実機確認で「音が普通に聞こえること」を最優先項目に置く**(エフェクトの見栄えより先に確認する)。

### 残課題

- `cancelTurn()`/`stopSpeechOnly()`で`pause()`された`<audio>`の`MediaElementSourceNode`は`disconnect()`されずに残る。要素自体が破棄されGCされるため実害は小さいが、**長時間の連続使用でノードが溜まらないか**は⑤で様子を見る。

---

## ② レベルの算出とCSSへの流し込み(`index.html` 修正)

### 背景/目的

①で取れた波形を、**目に心地よい数値**へ変換する必要がある。生のRMSをそのまま使うと、母音の谷ごとにロゴが真っ暗になり「チカチカした安っぽい点滅」になる。

### 作業内容

- [x] `getByteTimeDomainData`からRMSを計算する
- [x] 会話音量が0〜1に収まるようゲインを掛ける
- [x] **アタックを速く・リリースを遅く**した非対称の平滑化を入れる
- [x] `.arc-mini`の CSS変数 `--voice-level` へ毎フレーム書き込む
- [x] 再生が終わったら **rAFループを止める**(常時回しっぱなしにしない)
- [x] `cancelTurn()`/`stopSpeechOnly()`(`pause()`のため`ended`が飛ばない経路)からも停止する

### 実施方法

```javascript
function startVoiceLevelLoop() {
  if (ttsLevelRaf || !ttsAnalyser || !arcMiniEl) return;
  const buf = new Uint8Array(ttsAnalyser.fftSize);
  arcMiniEl.classList.add("speaking");
  const tick = () => {
    ttsAnalyser.getByteTimeDomainData(buf);
    let sum = 0;
    for (let i = 0; i < buf.length; i++) {
      const v = (buf[i] - 128) / 128;
      sum += v * v;
    }
    const rms = Math.sqrt(sum / buf.length);
    const target = Math.min(1, rms * 3.2); // VOICEVOXの通常音量が0〜1に収まるゲイン
    // アタックは速く・リリースは遅く。子音の立ち上がりに追従しつつ、
    // 母音の谷でロゴが毎回真っ暗になる「チカチカ」を防ぐ。
    ttsLevel += (target - ttsLevel) * (target > ttsLevel ? 0.55 : 0.12);
    arcMiniEl.style.setProperty("--voice-level", ttsLevel.toFixed(3));
    ttsLevelRaf = requestAnimationFrame(tick);
  };
  ttsLevelRaf = requestAnimationFrame(tick);
}

// 再生キューが空になったとき・中断されたときに必ず呼ぶ(rAFを回しっぱなしにしない)。
function stopVoiceLevelLoop() {
  if (ttsLevelRaf) {
    cancelAnimationFrame(ttsLevelRaf);
    ttsLevelRaf = 0;
  }
  ttsLevel = 0;
  if (arcMiniEl) {
    arcMiniEl.classList.remove("speaking");
    arcMiniEl.style.setProperty("--voice-level", "0");
  }
}
```

停止処理は**3か所すべて**へ入れた(ここが抜けると「喋り終わったのにロゴが光ったまま」「rAFが永久に回り続ける」になる)。

| 呼び出し元 | 状況 | 追加した行 |
|---|---|---|
| `playNext()` のキュー空 | 正常に読み上げが終わった | `stopVoiceLevelLoop();` |
| `cancelTurn()` | 応答中断ボタン | `stopVoiceLevelLoop(); // pause()では"ended"が飛ばない` |
| `stopSpeechOnly()` | 読み上げ停止ボタン | `stopVoiceLevelLoop();` |

### 分析

**`pause()`では`ended`イベントが飛ばない**点が、見落とすと必ずバグになる箇所だった。`playNext()`のキュー空だけに停止処理を置いていたら、**中断ボタンを押したときにロゴが光ったまま固まり、裏でrAFが回り続ける**。既存の`cancelTurn()`/`stopSpeechOnly()`が`isPlaying = false`をそれぞれ手で書いていた(=停止の責務が3か所に散っている)構造だったため、同じ3か所へ横並びに足す形にした。

平滑化を非対称(アタック0.55 / リリース0.12)にしたのは、**人間の声は立ち上がりが速く減衰が緩やか**だからである。対称にすると、速い方に合わせればチカチカし、遅い方に合わせれば喋り出しに追従しない。

### 改善策

- 停止の責務が3か所に散っているのは既存構造の問題で、今回はそれに合わせた。**将来`isPlaying`の更新を1つの関数に集約する**なら、そのときに`stopVoiceLevelLoop()`もそこへ寄せる(今回のスコープでは触らない)。

### 残課題

- ゲイン`3.2`・アタック`0.55`・リリース`0.12`はすべて**机上で決めた初期値**。⑤の実機確認で「弱すぎ/強すぎ」を見て調整する。

---

## ③ 見た目(CSS)

### 背景/目的

`--voice-level`を実際の**発光と振動**に変換する。相談の上、振動は**控えめ**(1〜2px程度、発光が主体)に決めた。

### 作業内容

- [x] `--voice-level`の初期値を`.arc-mini`へ定義する
- [x] `drop-shadow`のぼかし半径と不透明度をレベルで変調する
- [x] `scale`で最大+4%の拡縮を付ける
- [x] 微振動のkeyframesを追加し、**振幅もレベルで変調**する
- [x] `prefers-reduced-motion: reduce`で振動を無効化する
- [x] **レイアウトを動かすプロパティを一切使わない**ことを確認する

### 実施方法

**`scripts/static/index.html`**(修正)。既存の`.arc-mini`定義の直後へ追加した。

```css
  /* --- 16日目: クレアの発話中エフェクト(実音量リアクティブな発光+微振動) ---
     `--voice-level`(0〜1)は playNext() 側のAnalyserNodeから requestAnimationFrame で
     毎フレーム書き込まれる。state:speaking ではなく「実際に音が鳴っている区間」に同期する
     (state:speakingは音声キューより先に立つため、無音の間も光り続けてしまう)。

     ★意図的に transform / filter だけで表現している。width/margin/top等を動かすと
       隣の「ARC CORE」テキストやtok/s統計までガタつく(レイアウトを揺らさない)。 */
  .arc-mini { --voice-level: 0; }
  .arc-mini svg {
    transform: scale(calc(1 + 0.04 * var(--voice-level)));
    transform-origin: 50% 50%;
    filter: drop-shadow(0 0 calc(2px + 10px * var(--voice-level))
                        rgba(94, 234, 255, calc(0.12 + 0.55 * var(--voice-level))));
    will-change: transform, filter;
  }
  /* 振動は「控えめ」方針。最大でも約±1.2pxで、かつ音量が小さい間は自動的にほぼ止まる */
  .arc-mini.speaking { animation: arc-jitter 0.12s steps(1, end) infinite; }
  @keyframes arc-jitter {
    0%   { transform: translate(calc(var(--voice-level) *  1.2px), calc(var(--voice-level) * -0.7px)); }
    25%  { transform: translate(calc(var(--voice-level) * -0.9px), calc(var(--voice-level) *  1.0px)); }
    50%  { transform: translate(calc(var(--voice-level) *  0.6px), calc(var(--voice-level) *  0.8px)); }
    75%  { transform: translate(calc(var(--voice-level) * -1.1px), calc(var(--voice-level) * -0.5px)); }
    100% { transform: translate(calc(var(--voice-level) *  0.8px), calc(var(--voice-level) *  0.6px)); }
  }
  /* 動きに酔う人・OS側で動きを減らす設定の人には振動を出さず、発光だけ残す */
  @media (prefers-reduced-motion: reduce) {
    .arc-mini.speaking { animation: none; }
  }
```

**設計の要点:**

| 要素 | 適用先 | 内容 |
|---|---|---|
| 発光 | `.arc-mini svg` | ぼかし2→12px、不透明度0.12→0.67。既存のシアン`#5EEAFF`に合わせた |
| 拡縮 | `.arc-mini svg` | 最大+4%。「息をしている」程度に留める |
| 振動 | `.arc-mini`(**親**) | 子と親で別要素に分けることで、`scale`と`translate`が上書きし合わない |
| 振幅の変調 | keyframes内の`calc()` | **無音時は自動的に0px**になるため、アニメが回っていても静止して見える |

### 結果

**1. JS構文検査:2ブロックともパス**

```bash
cd C:\Users\gakuh\Documents\obsidian\サポートAI作製計画\scripts
# index.html内のインラインscriptを切り出して node --check にかける
```

```
main(1009-2352) -> OK
deco(2360-2488) -> OK
```

**2. バックエンドの退行テスト:全件パス**

```powershell
cd C:\Users\gakuh\Documents\obsidian\サポートAI作製計画\scripts
python -m pytest tests/ -q
```

```
425 passed, 5 subtests passed in 49.64s
```

サーバー側は無変更なので当然ではあるが、**「触っていないことの証拠」として記録**しておく。

### 分析

`transform`と`filter`だけに絞ったのは、**ヘッダーが横並びのflexレイアウト**(`.core-head`は`display:flex`)だからである。`width`や`margin`を揺らすと、隣の「ARC CORE」タイトル・「C.L.A.I.R.E. 音声対話」・右端のtok/s統計まで巻き込んで毎フレーム再レイアウトが走り、**画面全体がガタつくうえに重くなる**。`transform`/`filter`はコンポジット層だけで完結するため、レイアウトにもペイントにも影響しない。

振動を**親要素の`.arc-mini`**、拡縮を**子の`svg`**に分けたのも同じ理由で、CSSの`transform`は1要素につき1プロパティしか持てず、同じ要素に両方書くと後勝ちで一方が消える。

**⚠️ 自動テストで守れていない領域である点は正直に記しておく。** このプロジェクトのフロントエンドはインラインJSで、単体テストの基盤が無い。今回検証できたのは「**構文が壊れていないこと**」と「**バックエンドが無傷なこと**」だけで、**見た目と音は⑤の実機確認でしか判定できない**。

### 改善策

- 12日目②・13日目③のUI変更と同じく、⑤の実機確認チェックリストで担保する。
- 将来UIのロジックが増えるなら、`static/`から純粋関数(今回で言えばRMS→レベル変換)を切り出して`.js`ファイル化し、テストできる形にすることを検討する(**今日はスコープ外**)。

### 残課題

- `calc()`を`scale()`・`rgba()`のアルファ・keyframes内で使っている。Chromium系(Brave含む)では動作するが、**実機で意図通りに解釈されているか**を⑤で目視確認する。

---

## ④ 成果物一覧

| ファイル | 新規/修正 | 役割 | 実行方法 | 出力先 |
|---|---|---|---|---|
| [[サポートAI作製計画/scripts/static/index.html]] | 修正 | ①`ensureTtsAnalyser()`/`connectAudioToAnalyser()`の追加、②`startVoiceLevelLoop()`/`stopVoiceLevelLoop()`の追加と`playNext()`・`cancelTurn()`・`stopSpeechOnly()`への結線、③`.arc-mini`のCSS(発光・拡縮・微振動) | `voice_gateway.py`が配信。`http://127.0.0.1:5055/` | ブラウザ表示のみ(**ファイルには何も保存されない**) |
| [[サポートAI作製計画/scripts/voice_gateway.py]] | **無変更** | 既存の`audio`イベントで足りるため一切触っていない | `python voice_gateway.py --host 127.0.0.1 --port 5055` | なし(常駐サーバー) |

> [!note] 今回はテストファイルを1つも追加していない
> フロントエンドのインラインJSには単体テストの基盤が無いため(③の分析参照)。**「テストを書いていない」ことを成果物一覧にも明記しておく**。15日目までは新機能に必ずテストを添えていたので、今回だけ方針が違う点を後から見て分かるようにする。

---

## ⑤ 実機確認(チェックリスト)

### 背景/目的

③の分析のとおり、**このタスクは自動テストでほぼ守れていない**。特に①の`createMediaElementSource()`は失敗すると**エラーを出さずに無音になる**ため、「エフェクトが綺麗か」より先に「**そもそも声が聞こえるか**」を確認する。

### 事前準備

```powershell
cd C:\Users\gakuh\Documents\obsidian\サポートAI作製計画\scripts
python voice_gateway.py --host 127.0.0.1 --port 5055
# ブラウザで http://127.0.0.1:5055/ を開く(Ctrl+Shift+R でキャッシュを破棄してから)
```

### チェックリスト

**最優先:読み上げが壊れていないこと(ここが×なら以降は全部無意味)**

- [x] テキスト入力で質問し、**クレアの声がこれまで通り聞こえる**(無音になっていない)
- [x] 複数文の応答で、**文と文の間が途切れず連続再生される**
- [x] DevToolsのConsoleに`[16日目] 音量解析へ接続できませんでした`が**出ていない**(出ていたらフォールバック動作=エフェクトなしで再生されている)

**エフェクトの確認**

- [x] 喋っている間、`ARC CORE`左隣のロゴが**シアンに発光する**
- [x] 発光の強さが**声の抑揚に合わせて変化する**(一定の点滅ではない)
- [x] ロゴが**微かに振動する**(±1px程度。うるさく感じないか)
- [x] 喋り終わると**発光・振動がきちんと止まる**(光ったまま固まらない)
- [x] 隣の「ARC CORE」テキスト・tok/s統計が**一緒に揺れていない**(レイアウトを揺らしていない)

**停止経路の確認(②の分析。`ended`が飛ばない経路)**

- [ ] **読み上げ停止ボタン**を押すと、音と同時にロゴのエフェクトも即座に止まる
- [ ] **応答中断ボタン**を押すと、同じくエフェクトが止まる
- [ ] 停止直後に次の質問をすると、**再びエフェクトが出る**(2回目以降も動く)

**マイク未起動の経路(①でAudioContextを分けた理由)**

- [ ] **マイクを一度も起動せず**テキスト入力だけで質問しても、エフェクトが出る
- [ ] マイクを起動→停止した後でも、エフェクトが出続ける

**退行確認**

- [ ] **読み上げ速度スライダー**が引き続き効く(15日目・指示3の機能。`playbackRate`)
- [ ] 読み上げ中にマイクが**ミュートされる**(9日目⑥・10日目①のエコー対策)
- [ ] ウェイクワード・自動送信が引き続き動く
- [ ] 15日目の音楽再生(「〇〇を流して」)が引き続き動く
- [ ] 長い会話を続けてもブラウザが重くならない(rAFとノードの後始末。①②の残課題)

### 結果 / 分析 / 改善策

(実施後に追記)

### 残課題

(実施後に追記)

---

## 📌 次のステップ

1. **⑤の実機確認を実施し、パラメータを詰める**。特にゲイン`3.2`・アタック`0.55`・リリース`0.12`・振動幅`±1.2px`は机上の初期値なので、実際の声を見て調整する(②③の残課題)
2. **ユーザーの発話への反応**。今日は「クレアが話している間」だけを扱った。マイク入力レベルでもロゴを反応させれば、「聞いている/話している」が一目で分かる。既存の`micIndicator`と`micProcessor.onaudioprocess`(既に生の`Float32Array`が流れている)を使えば、①のような`createMediaElementSource()`の地雷なしに実装できる
3. **状態による色の出し分け**。`thinking`中は別の色でゆっくり脈打たせる等。⓪で「`state:speaking`は無音区間を含む」と切り捨てた情報を、**逆にそこで活かす**(合成待ちの間も「考えている」ことを表現できる)
4. **エフェクトのON/OFFトグル**。うるさいと感じた場合に備え、右パネルのControlsへ追加する(まず出してみてから判断する)
5. **フロントエンドのテスト基盤**。純粋関数を`.js`へ切り出せば、今回のような「構文チェックしかできない」状況を脱せる(③の改善策)
6. 15日目からの持ち越し:
   - **音楽再生中の読み上げの扱い**(音楽とAIの声が重なる問題)
   - **停止・曲送りへの拡張**、**ローカル音楽ファイルの再生**、**汎用ブラウザ起動**
   - **`ytmusicapi`更新時の再監査**
   - 13日目⑥の未消化項目、**SearXNGの実ネットワーク疎通確認**、**発話内のファイル名からの自動照合**
   - **引用元チップ**、**テレメトリ/Event Logの実データ化**、**モデル切替**、**思考モードトグル**、**Web検索の自動判定**
   - **VRAM逼迫によるモデル入替スラッシング**、**`Pipe.pipe()`/`synthesize()`の同期呼び出し**
   - **資料生成のグラフ埋め込み・PDF出力・`workspace/`の自動クリーンアップ**
   - **OAuth同意画面の本番公開**(テストのままだとトークンが7日で失効)、**Gmail/カレンダー連携**

### 📋 コード内「〇日目」コメントとノート番号の対応(14日目⓪の改善策を継続)

> [!warning] ⚠️ 「16日目」というラベルがコード内で**2つの別作業**を指している
> `index.html`には以前から`// 16日目 修正2:`(応答中断・読み上げ停止ボタンの有効/無効切替)というコメントがあり、これは[[サポートAI作製計画/14日目添付ファイルのピン留め指定とチャット履歴の永続化.md|14日目ノート]]に記載された作業である。今日追加したコメントも`// 16日目:`で始まるため、**同じ番号が別の作業を指す状態**になった。
> 見分け方は下表のとおり。**今後はコメントを「作業した日」ではなく「記載先ノート番号」で採番する**ことを改善策とする。

| コード内 | 内容 | 記載先ノート |
|---|---|---|
| 14日目 | `attached_document_text`、Web検索の`support_ai_auto_pipe.py`結線 | 13日目④追記 / 14日目⓪ |
| 15日目 | ウェイクワード検出の可視化、読み上げ速度バーの機能化 | 14日目⓪(実装済みとして追認) |
| **16日目 修正2** | `run_turn()`のスレッド化+`cancel_turn`(応答中断ボタン) | [[サポートAI作製計画/14日目添付ファイルのピン留め指定とチャット履歴の永続化.md|14日目]]⓪ |
| 17日目 | 同上(サーバー側の`cancel_turn`送信) | 14日目⓪ |
| 18日目 | ウェイクワード「C.L.A.I.R.E.」スペルアウト表記の検出 | 13日目🐛追記 |
| 19日目 | 括弧書きの二重読み解消、`force_finalize_pending()` | 13日目🐛追記 |
| 20日目 | 「Hey, C.L.A.I.R.E.」がforce_finalize再入時に誤送信される問題の修正 | 14日目⑥追記 |
| 15日目(ノート) | ①`media_player.py` ②MEDIAルート ③再生チップ | [[サポートAI作製計画/15日目音声による音楽再生(ytmusicapi+Brave).md|15日目]] |
| **16日目(本ノート)** | ①AnalyserNodeによる実音量取得 ②レベル算出とCSS変数への流し込み ③ロゴの発光・微振動 | 本ノート |

### 🔎 実装で参照した情報源

- [AnalyserNode — MDN](https://developer.mozilla.org/ja/docs/Web/API/AnalyserNode)
- [AudioContext.createMediaElementSource() — MDN](https://developer.mozilla.org/ja/docs/Web/API/AudioContext/createMediaElementSource)
- [prefers-reduced-motion — MDN](https://developer.mozilla.org/ja/docs/Web/CSS/@media/prefers-reduced-motion)
- [CSS Triggers(どのプロパティがlayout/paint/compositeを起こすか)](https://csstriggers.com/)
