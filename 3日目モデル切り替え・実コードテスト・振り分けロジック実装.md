[[サポートAI作製計画/2日目モデル動作検証.md|2日目:モデル動作検証]]で4モデルの単体動作確認と実測値の自動計測は完了した。積み残しだった「① モデル切り替え(スワップ)検証」と「② Devstralの実コードテスト」を今回で片付ける。「Phi-4-miniの振り分けロジック実装」は[[サポートAI作製計画/4日目Phi4ロジック設計.md|4日目]]に持ち越す。

---

## ① モデル切り替え(スワップ)検証

設計上、Gemma4・gpt-oss・Devstralは「どれか1つのみVRAMにロード」する運用(Phi-4-miniはCPU固定・無期限常駐で別枠)。実際に切り替えた際の挙動・所要時間を計測する。

### 作業内容

- [x] モデルA起動 → `ollama stop` → モデルB起動、という手順でVRAMが正しく解放・再確保されるか`nvidia-smi`で確認
- [x] 切り替えにかかる時間(体感の待ち時間)を計測し、実運用で許容できるか判断
  - `ollama stop <model>` 完了(VRAM解放)までの時間
  - 次モデルの `load duration`(`--verbose`で取得)
  - 合計の「切り替え待ち時間」を実測値として記録
- [x] 3モデル総当たり(Gemma4→gpt-oss、gpt-oss→Devstral、Devstral→Gemma4 など)で切り替えパターンを一通り試す
- [x] 連続して切り替えテストを行い、メモリリークやエラー蓄積が起きないか確認(5〜10回連続切り替え後もVRAM使用量が初期値に戻るか)
- [ ] Phi-4-mini(CPU固定・無期限常駐)が裏で動いていても、上記3モデルのスワップに影響を与えないか確認(VRAM圧迫がないか) ※常駐なし版の比較データが未取得。詳細は末尾「①のまとめ」参照

### 計測方法(自動化済み)

手動で`nvidia-smi`を見ながらスワップ時間を記録する方式は、切り替えの瞬間を見逃すリスクがあるため、[[サポートAI作製計画/scripts/monitor_ollama.py]]に**スワップ計測モード**を追加して自動化した。

**使い方:**

```bash
cd サポートAI作製計画/scripts
# 3モデル総当たり(Gemma4→gpt-oss→Devstral→Gemma4)を1周
python monitor_ollama.py --swap-models "gemma4:26b,gpt-oss:20b,devstral-small-2:24b,gemma4:26b" \
    --prompt "簡単な自己紹介をして" --label "スワップ総当たり" --cycles 1

# 5〜10回連続切り替えのリーク確認(3モデルをそのままcyclesで繰り返せば、総当たり性を保ったまま回数を稼げる。
#  3モデル×cycles4周=11回切り替え、cycles3周=8回切り替え。「5〜10回」の目安なのでどちらでも可)
python monitor_ollama.py --swap-models "gemma4:26b,gpt-oss:20b,devstral-small-2:24b" \
    --prompt "簡単な自己紹介をして" --label "スワップ総当たり連続" --cycles 4
```

- `--swap-models`: 切り替え順序をカンマ区切りで指定(2つ以上必須。3つ以上でもOK)。これを指定すると自動的にスワップ計測モードになり、`--model`は不要。
- 2モデルに絞る必要はない: `--swap-models`はリストをそのまま`--cycles`回繰り返す仕組みなので、3モデルのリストのまま`--cycles`を増やせば「総当たり」と「連続切り替えのリーク確認」を1回のコマンドで同時に満たせる(例: 3モデル×cycles4周=11回切り替え)。CSVには`from_model`/`to_model`が1行ごとに残るため、後からどのパターンが何回出たかも集計できる。
- `--cycles`: リスト全体を繰り返す回数(デフォルト1)。連続切り替えのリーク確認をする場合はこれで回数を稼ぐ。
- `--release-timeout`: `ollama stop`後、VRAMが安定するまで待つ最大秒数(デフォルト20秒)。
- `--prompt` / `--label` / `--interval` は通常モードと共通。

**事前準備:** 「Phi-4-miniが裏で動いていても影響しないか」も同時に確認したい場合は、計測開始前に別タブで以下を1回実行し、Phi-4-miniを`keep_alive -1s`で常駐させた状態にしておく(ワンショットコマンドなので、実行後はタブを閉じてもモデルはOllamaサーバー側に常駐し続ける。`ollama ps`のUNTIL欄が`Forever`になっていれば確認OK。スクリプト側での自動起動はしない)。

```powershell
ollama run phi4-mini-cpu --keepalive -1s "テスト"
```

比較のため、Phi-4-mini常駐あり/なしの両パターンでスワップ計測を実行し(`--label`を変えて区別)、`vram_peak_during_run_mib`に有意な差がないか突き合わせるとよい。

**動作:** リスト先頭のモデルをロード→計測 → 以降「`ollama stop <前モデル>` → nvidia-smiをポーリングしVRAMが安定するまでの時間を計測 → `ollama run <次モデル> --verbose`で計測」をリストの並び通りに繰り返す。全transition終了後、最後のモデルも停止してアイドルVRAMを確認し、計測開始前の値と比較してリーク疑いがあれば注記する。

**結果の保存先:** `scripts/results/Swap Verification/{label}_{timestamp}_swap.csv` に1 transitionごと1行で保存される(手記入の「記録表」は廃止し、このCSVで代替する)。

**CSVでどの列を見ればよいか:**

| 列                                                                    | 見るポイント                                                                                     |
| -------------------------------------------------------------------- | ------------------------------------------------------------------------------------------ |
| `from_model` / `to_model`                                            | どの切り替えパターンか(`from_model`が空欄の行は初回ロード=スワップなし)                                                |
| `stop_duration_s`                                                    | `ollama stop`〜VRAM解放(安定)までの時間。ここが長い/バラつく場合は切り替えの体感待ちに直結                                    |
| `load_duration_s`                                                    | 次モデルの読み込み時間(`--verbose`実測値)                                                                |
| `swap_total_wait_s`                                                  | `stop_duration_s + load_duration_s`。**これが「切り替え待ち時間」の実測値**。実運用で許容できるか(目安: 数秒〜十数秒以内)をここで判断する |
| `vram_before_stop_mib` / `vram_after_stop_mib` / `vram_released_mib` | VRAMが正しく解放されているか(`vram_released_mib`が0や極端に小さい場合は解放漏れの疑い)                                   |
| `vram_peak_during_run_mib` / `vram_avg_during_run_mib`               | 切り替え後の実行時VRAM(2日目の単体検証値と大きく違わないか)                                                          |
| 最終行(`to_model`が「(全停止・リーク確認)」の行)の`note`                               | 開始前アイドルVRAM vs 全スワップ終了後アイドルVRAMの差分。**500MiB以上の増加でリーク疑いフラグ**が自動で付く。連続切り替えテストで特に確認する         |

**判断基準(このNoteの作業項目に対応):**
- `swap_total_wait_s`が実運用のテンポとして許容できるか(体感の待ち時間として許容範囲か)
- 3モデル総当たりの全パターンで`swap_total_wait_s`・`vram_released_mib`に異常な値がないか
- 5〜10回連続切り替え後、最終行の`note`にリーク疑いフラグが出ていないか
- Phi-4-miniを常駐させた状態と、させていない状態で`vram_peak_during_run_mib`に有意な差がないか(裏で動いていても圧迫していないか)

---

## ② Devstralへの実コード投入テスト

2日目の検証では「簡単な自己紹介をして」という短文プロンプトでしか計測しておらず、生成速度が約24 tokens/sと他モデルより明確に遅いことが分かっている(他モデルは80〜200 tokens/s台)。実際のコーディング支援での体感を確認するため、研究用コード(避難シミュレーション関連)を使って再検証する。

### 作業内容

- [ ] 実際の研究コードから、デバッグ・改修が必要な一部(100〜300行程度)を用意する
- [ ] 以下の観点でプロンプトを複数パターン用意し、それぞれ`monitor_ollama.py --model devstral-small-2:24b --repeat 3`で計測
  - [ ] コードの説明・レビューを依頼するプロンプト
  - [ ] バグを含むコードを渡して修正案を依頼するプロンプト
  - [ ] 機能追加(新しい関数の実装)を依頼するプロンプト
- [ ] 出力トークン数と生成速度(eval_rate_tps)の関係を確認(出力が長くなるほど遅くなる傾向がないか)
- [ ] 提案されたコードの質(実装が正しいか、デバッグ提案が的確か)を主観評価(〇/△/×)
- [ ] Q4量子化による精度劣化が体感で問題ないか確認

### 判断基準

- 体感の待ち時間が「コーディング相談のテンポ」として許容できるか(目安: 1回のやり取りで数十秒以内に収まるか)
- 許容できない場合の代替案を検討
  - 量子化レベルの変更(Q4→Q5/Q6でVRAMと引き換えに精度・速度を調整、または逆にさらに軽量化)
  - プロンプトを分割して短い応答を往復させる運用に変更
  - Devstralの役割を「大きな実装」ではなく「レビュー・方針相談」中心に絞り、実装自体はGemma4やgpt-oss(高速)に振る運用も検討

### 実施手順(避難シミュレーションコードの代わりにScratchの実プロジェクトで代用)

避難シミュレーションのコードはLinux側のシミュレーター環境にしかなく、Windows側の本テストでは扱えない。
代わりに、就活先(KISプログラミング教室)の実案件コードである
[[就活/kisプログラミング/先生のオリジナル/タイピングクエスト仕様書.docx|タイピングクエスト仕様書]]と
`タイピングクエスト.sb3`(スプライトのみ作成済みでロジック=blocksは0個の未実装状態)を「実コード」の題材として使う。

> ⚠️ 注意: Scratchの`project.json`はブロックIDで相互参照するグラフ構造であり、LLMに丸ごと生成させると
> ID不整合で壊れたsb3になるリスクが高い。そのため本テストは「LLMに丸ごと生成させて即採用」ではなく、
> 「説明・レビュー / バグ修正案 / 小さな新規スクリプトの提案」をさせ、人間が`project.json`に反映する運用とする。

**0. 仕様書のテキスト化(完了)**
`タイピングクエスト仕様書.docx`から`タイピングクエスト仕様書.txt`を抽出済み(同フォルダに保存)。

**1. 対象コードの解凍(完了)**
`Unfreeze.py`で`タイピングクエスト.sb3` → `タイピングクエスト解凍/project.json`に展開済み。
現状確認: Stageは変数`変数`(デフォルトの未使用変数)のみ・blocks 0個。キャラ3体(ネコ=攻撃特化, ナノ=ミス許容特化,
ピコ=回復特化)もvariables/blocksともに0個(コスチュームのみ用意済み)。仕様書7.1のグローバル変数は未実装。

**2. プロンプト3パターンを用意(完了・下書き済み)**
`サポートAI作製計画/scripts/prompts/`配下に作成済み。いずれも実データ(仕様書抜粋・実際のproject.json)を使用。

| ファイル | 種別 | 内容 |
|---|---|---|
| `devstral_typingquest_1_review.txt` | 説明・レビュー依頼 | 仕様書7.1のグローバル変数と、現状の空のvariables定義を比較させ、不足点・命名方針をレビューさせる |
| `devstral_typingquest_2_bugfix.txt` | バグ修正依頼 | `タイピングゲーム【完成版】.sb3`のStageブロック(実JSON)にバグを1箇所仕込み、原因ブロックの特定と修正案を出させる |
| `devstral_typingquest_3_feature.txt` | 機能追加依頼 | 仕様書7.2フェーズ1(難易度・キャラ選択)を、CLAUDE.mdの命名規則に沿った新規blocks JSONとして設計させる |

**3. `monitor_ollama.py`を改修(完了)**
長文プロンプトをコマンドライン引数でそのまま渡すのは非現実的(クォート崩れ・文字数制限)なため、
`--prompt-file`オプションを追加した(`--prompt`との排他ではなく、指定時は`--prompt-file`が優先)。

```powershell
cd サポートAI作製計画/scripts

python monitor_ollama.py --model devstral-small-2:24b `
    --prompt-file prompts/devstral_typingquest_2_bugfix.txt `
    --label "Devstral_実コード_バグ修正" --repeat 3

python monitor_ollama.py --model devstral-small-2:24b `
    --prompt-file prompts/devstral_typingquest_3_feature.txt `
    --label "Devstral_実コード_機能追加" --repeat 3
```

**4. 出力の反映・動作確認(未実施)**
- ③(機能追加)で得られたJSON片を、目視でブロックID重複がないか確認したうえで`タイピングクエスト解凍/project.json`に追記
- `Unfreeze.py`→2で再圧縮し、Scratchで実際に開いて動作確認(小学生向け教材のため最終チェックは人間が必須)

**5. 評価・記録(一部実施: バグ修正プロンプト・機能追加プロンプトの2本が完了、説明・レビュープロンプトが残り)**
- `eval_rate_tps`と出力トークン数の関係
- 3プロンプトそれぞれの提案の質を〇/△/×で主観評価
  - 追加観点: 一般的なコードと違いScratchのブロックグラフ構造はLLMに馴染みが薄いドメインのため、
    「JSONとして構文的に正しいか」「親子(parent/next)のID参照が破綻していないか」も評価軸に含める
- 体感の待ち時間が「コーディング相談のテンポ」として許容できるか

#### 結果: `devstral_typingquest_2_bugfix.txt`(バグ修正依頼)

**速度・VRAM(`summary.csv`、3回平均)**

| 指標 | run1 | run2 | run3 |
|---|---|---|---|
| load_duration_s | 15.17(初回ロード込み) | 0.32 | 0.22 |
| ttft_ms | 16741(ロード込み) | 357 | 261 |
| eval_count(出力トークン数) | 506 | 638 | 464 |
| eval_rate_tps | 27.42 | 27.29 | 27.04 |
| vram_used_peak_mib | 15843 | 15838 | 15806 |

- `eval_rate_tps`は27.0〜27.4tpsで3回ともほぼ一致し、2日目の単体検証値(24〜30tps台)と整合。出力トークン数が464〜638と変動しても速度自体は安定しており、「出力が長くなるほど遅くなる」傾向は今回は見られなかった。
- run1のみ`load_duration_s`/`ttft_ms`が突出しているのはモデル未ロード状態からの初回ロード分が乗っているためで異常ではない。ロード済みのrun2・run3(`ttft_ms` 260〜360ms)が実運用でのレスポンス初動の目安になる。
- VRAM・GPU使用率もサンプル単位で確認したが停滞や異常値なし。

**提案の質: 〇**

意図的に仕込んだバグ(「ゲームオーバー」受信時の`sound_setvolumeto`が本来0にすべきところ100のままになっている)を、以下の通り正確に指摘・修正できた。

- バグの該当ブロックID(`y[GTP]K/lYs:)5xk[$59`)を正しく特定
- 仕様(ゲームオーバー効果音とBGMが重ならないよう音量0にする)との食い違いを正しく説明
- 修正後のJSONも構文的に正しく、`parent`/`opcode`/`inputs`の形式を壊さずに出力
- 他のブロック(ステージクリア=0、ゲームスタート=100、ゲームクリア)への誤検知もなく、影響範囲の確認も的確

Scratchのブロックグラフ構造という馴染みの薄いドメインでも、JSON形式を崩さずピンポイントで1ブロックだけを差分修正できており、②の判断基準(コーディング相談のテンポとして許容できるか)についても、ロード済み状態なら数秒〜十数秒程度の応答時間で「レビュー・修正案の相談」用途としては十分実用的と判断できる。

<details>
<summary>ollama run 生出力(参考。monitor_ollama.pyは統計のみ保存し本文は保存しない仕様のため、ここに手動転記)</summary>

```
### 1. バグの指摘

**バグがあるブロックID:** "y[GTP]K/lYs:)5xk[$59"

**問題点:**
- 「ゲームオーバー」を受け取ったときのスクリプトでは、sound_setvolumetoブロックで音量が100に設定されています。
- これは本来の仕様と逆で、「ゲームオーバー」時には音量を0にする必要があります（ゲームオーバー効果音とBGMが重ならないようにするため）。
- 現在の設定では、BGMが最大音量で再生され続け、ゲームオーバー効果音と重なることになります。

### 2. 修正後のブロックJSON

{
  "y[GTP]K/lYs:)5xk[$59": {
    "opcode": "sound_setvolumeto",
    "next": null,
    "parent": "1v2Gm,5f%9FDZcX0;h(~",
    "inputs": { "VOLUME": [1, [4, "0"]] },
    "fields": {},
    "shadow": false,
    "topLevel": false
  }
}

### 3. 他のブロックへの影響確認

**確認結果:**
- 「ステージクリア」を受け取ったときのスクリプト（"l$Cz}15b9?j1Z;I3?g(_"）は正しく音量0に設定されています。
- 「ゲームスタート」を受け取ったときのスクリプト（"xwj{-VzgL75}.Nl:R1#O"）は正しく音量100に設定されています。
- 「ゲームクリア」を受け取ったときのスクリプト（"eC21T~$vhHkvuy?@e4#E"）は正しく動作しています。

**結論:**
バグは「ゲームオーバー」時のみに存在し、他のブロックには同様の問題はありません。修正後は正常に動作するはずです。
```

</details>

**残作業:** プロンプト①(説明・レビュー)③(機能追加)の結果記録、Phi-4-mini常駐なし版との比較(①の残タスク)、機能追加分のproject.jsonへの反映と動作確認。

#### 結果: `devstral_typingquest_3_feature.txt`(機能追加依頼)

**速度・VRAM(`summary.csv`、3回)**

| 指標 | run1 | run2 | run3 |
|---|---|---|---|
| total_duration_s | 1m0.21564s(下記バグにより`None`) | 52.88 | 1m37.0748739s(下記バグにより`None`) |
| load_duration_s | 0.23 | 0.23 | 0.21 |
| ttft_ms | 866 | 264 | 252 |
| eval_count(出力トークン数) | 1460 | 1428 | 2306 |
| eval_duration_s | 59.14 | 52.40 | 1m36.80933s(下記バグにより`None`) |
| eval_rate_tps | 24.69 | 27.25 | 23.82 |
| vram_used_peak_mib | 15814 | 15804 | 15818 |

- `eval_rate_tps`は23.8〜27.3tpsで、バグ修正プロンプト(27前後)よりやや遅くばらつきも大きい。出力トークン数が1428〜2306と長くなるほど微妙に速度が落ちる傾向が見える(run3は出力が最長の2306トークンで最も遅い23.82tps)。②の確認項目「出力が長くなるほど遅くなる傾向がないか」への回答としては「顕著ではないが、わずかにその傾向あり」。
- VRAMは15804〜15818MiBで安定、リーク兆候なし。

**⚠️ 計測スクリプトのバグを発見・修正**

run1・run3で`total_duration_s`と`eval_duration_s`が`None`として記録されていた。原因は`monitor_ollama.py`の`parse_verbose_output`が、ollamaのverbose出力の時間表記(Goの`time.Duration`文字列)のうち`"52.8766542s"`のような単発の秒/ミリ秒表記しか正規表現でマッチできておらず、実行時間が1分を超えたときの`"1m37.0748739s"`(分+秒の複合表記)を拾えていなかったこと。run1(`1m0.21564s`)・run3(`1m37.0748739s`/`1m36.80933s`)はいずれも1分超えで発生、1分未満だったrun2だけ偶然正しくパースできていた。

`parse_go_duration()`関数を新設し、`h/m/s/ms/µs`の単位トークンを分解して合算する方式に修正済み(`monitor_ollama.py`)。修正後にテストした結果、`"1m37.0748739s"`→97.07秒のように正しく変換されることを確認。**過去に取得済みのこの3件のCSVはNoneのまま**(再実行はしていない)なので、上表のtotal_duration_s/eval_duration_s(run1, run3)はollama生出力から手動で書き戻した値。

**提案の質: ×(設計パートは〇、JSON実装パートは3回とも実際のScratch形式と不一致)**

- 変数命名・難易度別の値・キャラクター補正方針などの**設計・説明パートは3回とも良好**。CLAUDE.mdの命名規則(ひらがな・カタカナの日本語名)に沿って`ゆるすミスかいすう`(Max_Miss)、`たいりょく`(Base_Damage)、`かいふくりょう/かいふくち`(Heal_Amount)、`じょうたい`(state)といった変数名を提案し、難易度別の値(かんたん20/ふつう10/むずかしい5/デスモード0)も仕様書通り。
- しかし**肝心のJSON実装パートが、3回とも実際のScratch `project.json`形式と一致しない**。
  - run1: `"variables"`が`{"ゆるすミスかいすう": 0, ...}`という単純な連想配列。実際は`{"変数ID": ["表示名", 初期値]}`という**ID→[名前,値]のタプル形式**が必須で、この形式ではScratchが変数として認識できない。`"blocks"`も配列(`[...]`)になっているが、実際は**ブロックIDをキーとしたオブジェクト**(`{"ブロックID": {opcode, next, parent, ...}}`)である必要がある。
  - run2: `"blockType"`, `"proccode"`, `"protocolSnippet"`, `"extensionId"`など実在しない/カスタムブロック専用のフィールドを一般ブロックに誤用。`"variables"`も`[{"name":..., "value":...}]`という配列形式で、これも不一致。
  - run3: 最も崩れている。存在しない`"children"`という入れ子構造(Scratchのblocksはツリーではなく`next`/`parent`のIDで繋ぐフラットなオブジェクト)を作り出し、`"control_startasclock"`という**実在しないopcode**(正しくは`control_start_as_clone`)まで登場。
- つまり3回とも「そのまま`project.json`に貼り付けても動作しない/認識されない」レベルで、②で懸念していた「Scratchのブロックグラフ構造はLLMに馴染みが薄いドメイン」という点が実測で裏付けられた形。
- バグ修正プロンプト(②)では実在のJSONを渡して1ブロックだけ差分修正させたときは形式を崩さず正確だった一方、機能追加プロンプト(③)のようにゼロから新規のブロックJSONを組み立てさせると形式が崩れる、という差が明確に出た。**「既存コードの差分修正」は強いが「新規のScratch project.json生成」は弱い**、という役割分担の示唆。

<details>
<summary>ollama run 出力(抜粋。run1の一部。全文は長大なため設計パートと崩れの一例のみ抜粋)</summary>

```
### 1. フェーズ1に必要なグローバル変数の設計

| 日本語名 | 英語対応 | 初期値 | 説明 |
|----------|----------|--------|-------|
| ゆるすミスかいすう | Max_Miss | 0 | ミス許容回数 |
| たいりょく | Base_Damage | 20 | 基本攻撃力 |
| かいふくりょう | Heal_Amount | 30 | 回復量 |
| じょうたい | state | "" | 状態管理（フェーズ遷移用） |

### 2. 「旗が押されたとき」の初期化スクリプト (Stage側)

{
  "blocks": [
    {
      "opcode": "じゅんび1_変数初期化",
      "next": null,
      "parent": "",
      ...
      "topLevel": true
    }
  ],
  "variables": {
    "ゆるすミスかいすう": 0,
    ...
  }
}
```

(↑ `variables`が`{名前: 値}`形式、`blocks`が配列形式になっており、いずれも実際のScratch `project.json`形式とは異なる。run3では`"children"`という架空の入れ子構造や実在しない`opcode: "control_startasclock"`も出現。詳細は上記「提案の質」参照)

</details>

**残作業:** プロンプト①(説明・レビュー)の結果記録、Phi-4-mini常駐なし版との比較(①の残タスク)。機能追加分は現状JSONとして未完成のため、`project.json`への反映は保留(そのまま貼ると壊れるため、人間が実際の形式に書き直すか、正しい形式の実例をプロンプトに追加した再テストが必要)。

---

## ① のまとめ(スワップ検証 結果)

**実施日:** 2026-08-03
**使用データ:**
- `scripts/results/Swap Verification/スワップ総当たり_Phi4常駐時_20260803_142138_swap.csv`(3モデル1周、Phi4-mini常駐状態)
- `scripts/results/Swap Verification/スワップ総当たり連続_20260803_212439_swap.csv`(3モデル×cycles4=11回切り替え、Phi4-mini常駐状態)

**結果:**

| 判断項目 | 結果 |
|---|---|
| VRAM解放 | 全遷移で`vram_released_mib`は12000〜14250MiB前後。解放漏れ(極端に小さい値)なし |
| 切り替え待ち時間(`swap_total_wait_s`) | 11〜22秒程度(devstral→gemma4が最長)。実運用のテンポとして許容範囲と判断 |
| 3モデル総当たり | gemma4⇄gpt-oss⇄devstralの全パターンを2ファイル合計で複数回実施。`stop_duration_s`・`vram_released_mib`とも異常値なし |
| 連続切り替え(11回)後のリーク | 最終行の差分は**-341MiB/-389MiB(いずれも減少)**で、リーク疑いフラグ(500MiB以上増加)は出ていない → リークなし |
| eval_rate_tps | gemma4 76〜86、gpt-oss 134〜151、devstral 28〜30と、いずれも2日目の単体検証値と一致。スワップ後の性能劣化なし |
| Phi-4-mini常駐時の圧迫確認 | ⚠️ **未確定。** 2回のテストは両方ともPhi4-mini常駐状態で実施されており、「常駐なし」との比較データが取得できていない。参考として`vram_peak_during_run_mib`は各モデルとも通常検証時と大差ない範囲(gemma4 15600台、gpt-oss 13800〜14000、devstral 15400〜15700)に収まっており、目立った圧迫の兆候はないが、正式な比較検証は完了していない |

**結論:**
VRAM解放・切り替え待ち時間・3モデル総当たり・連続切り替えのリーク確認の4項目は問題なく完了。ただし「Phi-4-mini常駐あり/なし」の比較検証だけは、常駐なし状態でのテストを別途1回実行できておらず未完了。この1点を除けば③モデル切り替え(スワップ)検証は完了とみなせる。

**残作業(任意):** Phi-4-miniを`ollama stop phi4-mini-cpu`で一旦停止した状態で同じ3モデルスワップテストを1回実行し、`vram_peak_during_run_mib`を上記常駐時データと突き合わせれば①は完全クローズとなる。優先度が低ければ、「常駐時でも実測上は問題なし」という参考情報のまま次工程(②Devstral実コードテスト、③振り分けロジック実装)に進んでも支障はないと考えられる。

---

## 📌 次のステップ(2日目からの引き継ぎ)
1. ~~4モデルをOllamaで実際にダウンロード・動作検証~~
2. Phi-4-miniの振り分けロジック(プロンプト設計・実装) → [[サポートAI作製計画/4日目Phi4ロジック設計.md|4日目]]に持ち越し
3. RAGの記憶DB構築
4. Cloudflare Tunnelのセットアップ
5. STT/TTSパイプラインの組み立て
