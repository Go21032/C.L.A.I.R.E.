サポートAI作製計画の日次ノート([[サポートAI作製計画/3日目モデル切り替え・実コードテスト・振り分けロジック実装.md|3日目]]・[[サポートAI作製計画/4日目Phi4ロジック設計.md|4日目]])で実際に運用してみて有効だった書き方を、今後のノート作成時の規則としてまとめる。

---

## ① ノート全体の構成(必須の流れ)

3日目・4日目のノートは、読み返したときに「① 今日やることの全体像 → ② 実際にどう作業したか → ③ 結果はどうだったか → ④ 悪かった場合どう改善したか」を追える構成になっていた。この流れを毎回のノートで踏襲する。

**1. 冒頭の要約(ノート全体の書き出し)**

本文に入る前に、`[[前回ノート]]`へのリンクを添えつつ、「前回どこまで終わっていて」「今回は何を片付けるか」を1〜2文で簡潔に書く(3日目冒頭・4日目冒頭を参照)。長々と経緯を書き直す必要はない。

**2. 各作業セクション(①②③…)の中の順序**

1つの作業テーマにつき、次の順で書く。すべての項目を毎回フル装備で書く必要はないが、**「作業内容」「結果」「分析」「改善策」は特に省略しない**(この4つがこれまで抜けがちだった)。

| 順番 | 項目 | 内容 |
|---|---|---|
| 1 | 背景/目的 | なぜこの作業をするか(前回からの積み残し・設計上の理由など)を1〜2文 |
| 2 | 作業内容 | これから何をするかを**着手前にチェックボックスの箇条書きで列挙**(4日目の「### 作業内容」参照)。作業が進んだら`[x]`にして進捗が見える状態にする |
| 3 | 実施方法 | 実際に叩いたコマンド・手順(④の規則を参照) |
| 4 | 結果 | 数値・表・生成物など、出た結果をそのまま記載(②の規則を参照) |
| 5 | 分析 | 結果が想定通りだったか評価し、想定外なら**原因を推測で終わらせず特定**する(「原因調査→切り分け→結論」の順で記録。4日目④「なぜv2はCLARIFYを直せたのにFAST/DEEPが悪化したのか」、⑤「T4でCODEへの優先度上書きが発動しなかった」が型) |
| 6 | 改善策 | 分析で悪かった/想定外だったと分かった場合、**次にどう直すか**を具体的に書く。その場で修正して再検証まで行った場合は、修正内容と再検証結果も同じセクション(または直後のセクション)に追記する(4日目「system_prompt_v3で実際に修正・再実行した結果」、⑥「CODE_TRIGGERSの拡張」が型)。結果が良かった場合も「良かったので改善不要」と一言残す |
| 7 | 残課題 | セクション末尾に「残作業」「残課題」として明記(⑤の規則を参照) |

**3. ノート末尾**

「📌 次のステップ」として、次回ノートへ引き継ぐ項目を箇条書きでまとめる(完了した項目は取り消し線、持ち越しは次回ノートへのリンクを添える)。

## ② 実装の成果物一覧・出力先まとめを表にする

作業を進めるとファイルが増えていき、「何を」「どこに」「どうやって」作ったか後から追いにくくなる。そのため、ある程度作業がまとまったタイミングで**成果物一覧の表**をノート内に1セクション設ける。

**表に含める列(4日目③のフォーマットを踏襲):**

| 列 | 内容 |
|---|---|
| ファイル | `[[wikilink]]`形式で該当ファイルにリンク(コードブロック直書きではなくクリックで飛べるようにする) |
| 役割 | そのファイルが何をするものか(1〜2文) |
| 実行方法 | 実際に叩くコマンド(`python xxx.py`等)。単体実行しない部品ファイルは「単体実行はしない(〇〇からimportして使う部品)」と明記 |
| 出力先 | 結果がどこに保存されるか。**保存されない場合(標準出力のみ等)もその旨を明記する**(「ファイルには何も保存されない点に注意」のように太字/注記で強調すると後で事故らない) |

- プログラムファイル・プロンプト/テキストファイル・実装計画書など、種類が異なるものは表を分けてよい(4日目では3つの表に分割)。
- 「結果の数値がどこにも保存されていなかった」という事故が3日目→4日目で実際に発生した([[サポートAI作製計画/4日目Phi4ロジック設計.md]]の「⚠️『全問テストで18/20だった』という結果はどこにあったか」参照)。これを教訓に、**出力先が「標準出力のみ・ファイル保存なし」の場合は必ずその旨を明記し、後から再現・比較したくなったら保存機能を追加する**運用とする。

## ③ 新規作成か既存の修正かを明記する

ファイル名を挙げるときは、見出しや箇条書きの直後に**`(新規)`または`(修正)`**を付ける。

- 例: `**[[サポートAI作製計画/scripts/router_rules.py]]**(修正)`
- 例: `**[[サポートAI作製計画/scripts/openwebui_pipe/support_ai_auto_pipe.py]]**(新規)`

修正の場合は「変更前→変更後」が分かるように、可能な限り**Before/After比較表**を添える(4日目⑥の`CODE_TRIGGERS`修正のように、「項目・修正前・修正後・狙い」の4列表にすると差分の意図まで残せる)。

## ④ 実行方法(コマンド)を必ず記載する

作業内容の説明だけでなく、**実際に叩いたコマンドをそのままコピペできる形**(```powershell / ```bash コードブロック)で残す。

- どのディレクトリで実行するか(`cd`から書く)を省略しない。
- オプション引数がある場合は最低限使うものの意味を短く説明する(例: `--label`でラベルを変えられる、`--system-prompt`でプロンプトファイルを切り替えられる等)。
- 実行結果(出力の抜粋やサマリ)もコマンドの直後に載せ、「コマンド→結果」がセットで追えるようにする。
- テストコマンド(`python -m unittest ...`)は「何をテストしているか」を一言添えてから記載する(ロジックテストなのか実機検証なのかを区別する)。

## ⑤ その他、実際に効果があった書き方の癖

- **表が既にある場合は表で追記する**(散文で書き直すより差分が追いやすい)。数値結果の比較(正答率の推移など)は特に表向き。
- **「なぜ前回と数値が違うのか」のような疑問が起きうる箇所は、原因を推測ではなく特定してから注記する**(LLMのサンプリング揺らぎなのか、コード修正の影響なのか切り分けて書く)。
- **残課題・次回引き継ぎ事項は各セクションの末尾に「残作業」「残課題」として明記する**。ノート全体の最後にもまとめの箇条書きを置く。
- **想定外の挙動が出た場合は「原因調査→切り分け→結論」の流れをそのまま記録に残す**(結論だけでなく、どう切り分けたかも再現性のために残す)。
