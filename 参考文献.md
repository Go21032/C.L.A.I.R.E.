
# Gemma4推論高速化
1. https://ledge.ai/articles/google_gemma4_multi_token_prediction
2. https://youtu.be/K_qX648LrN8?si=02-M0zBfCC85UPdT
3. https://youtu.be/8AXb1h08YLk?si=vpoZXsVtGnv4ux_I
4. 

## 調査メモ:MTP(Multi-Token Prediction)/投機的デコーディングの現状(2026-08-07時点)

- **手法概要**: 軽量な「ドラフトモデル」が複数トークンを先読み予測し、本体(ターゲット)モデルがそれをまとめて並列検証することで、メモリバウンド処理を計算バウンドに転換し**最大3倍**の生成速度向上を狙う手法(投機的デコーディングの一種)。Google公式がGemma4向けにMTP-drafterを2026年5月5日に公開。
- **Ollamaでの対応状況**: [ollama/ollama PR #15980](https://github.com/ollama/ollama/pull/15980)で実装され、**mainにマージ済み**。Modelfileに`DRAFT`ディレクティブを追加する形で使う。
  ```
  FROM /path/to/gemma4/safetensors/target/dir
  DRAFT /path/to/gemma4/safetensors/assistant/dir
  ```
  ```
  ollama create --experimental -f Modelfile mymodel --quantize nvfp4 --quantize-draft nvfp4
  ollama run mymodel
  ```
- **⚠️ 現状の制約(重要)**: この機能は**MLX(Apple Silicon/Mac)向けに特化**しており、Linux/Windows + NVIDIA CUDA環境では「macOSが必要」というエラーが報告されている(NVIDIA GDX Sparkでの報告例あり)。**現在使用中の環境(Windows + RTX 5070 Ti、CUDA経由)ではまだ使えない可能性が高い。**
  - また効果が出るのは現状「unquantized bf16」モデル同士の組み合わせのみとされ、量子化済み運用(現行のQ4運用など)とは相性が未検証。
- **結論**: 現段階(このプランの1〜7日目)ではMTP/投機的デコーディングは**未導入・未検討**。技術自体はOllamaにマージ済みだが、CUDA/Windows対応はまだ課題が残っている段階。
- **今後のTODO**: OllamaのリリースノートでCUDA/Windows対応が明記されたら、Gemma4 26B-A4B用のdrafterモデルを用意し`monitor_ollama.py`で導入前後のtokens/secを比較する。

## 調査メモ:量子化・QAT・MoEによる高速化(動画③を踏まえて・2026-08-07)

参考文献3の動画(「ローカルLLM爆速進化の正体:量子化・QAT・MoE・高速化を10分超で深掘り」)の3テーマを、現行4モデル構成に当てはめて調査した。

### ① QAT(Quantization-Aware Training)モデルの活用

- Googleが[Gemma4のQATモデル](https://blog.google/innovation-and-ai/technology/developers-tools/quantization-aware-training-gemma-4/)を提供している(E2B / E4B / 12B / 26B MoEの各サイズ)。学習時に量子化を織り込んでいるため、**後付け量子化(現行のQ4_K_M等)より同じビット数でも精度劣化が少ない**のが利点。
- 例: Gemma3世代のQATではint4化でBF16比メモリ**約1/3**(1Bなら2GB→0.5GB)。Gemma4でも同様の圧縮効果が期待できる。
- **TODO**: 現行のGemma4 26B-A4B/Devstralが素の量子化(Q4_K_M等)なら、QAT版がOllama/Hugging Faceで配布されていないか確認し、あれば同精度でより高速な版に差し替える。

### ② ルーターモデル候補としてのGemma4 E2B/E4B

- ユーザー案の「Gemma4 E2B」はOllamaに`gemma4:e2b`として存在(実効パラメータ2.3B、E4Bは4.5B、共に128Kコンテキスト)。
- **⚠️ 罠が判明**: [E2B/E4Bベンチマーク記事](https://thekodelab.com/en/posts/gemma4-e2b-vs-e4b-benchmark/)によると、**E2Bはデフォルトでthinkingモード(内部CoT)が有効**になっており、ユーザーから見えない500トークン超の思考を毎回生成してしまう。この影響で分類・抽出等の短いタスクでは**E4BがE2Bの5〜10倍速い**という逆転現象が起きている(例: キーワード抽出でE2B 7.4秒 vs E4B 0.74秒)。
  - 対策: E2Bを使うなら`think: false`(Ollama API/Modelfileで明示)を必ず指定する。これだけで**約20倍高速化**する報告あり。
  - 指定を忘れると「軽量モデルにしたのにむしろ遅い」という本末転倒な結果になるため要注意。
- ルーターに使うなら、まずは
  1. `gemma4:e4b`(think無効化不要、短いタスクで実績あり)
  2. `gemma4:e2b` + `think:false`明示
  の両方を候補として、既存の`run_testset.py`(4分類20問セット)と`monitor_ollama.py`で実測比較するのが安全。

### ③ 他のルーター候補(小型・高性能な指示追従モデル)

Gemma系以外にも、分類・ルーティング用途でよく名前が挙がる軽量モデル:

| モデル | 規模 | 特記事項 |
|---|---|---|
| Qwen3-0.6B / 1.7B | 0.6B/1.7B | 多言語・構造化出力に強い。ただしQwen3系も`enable_thinking`のON/OFFがあり、Gemma4 E2B同様**thinking無効化を忘れると同じ罠にはまる**ので要確認 |
| Llama-3.2-1B-Instruct | 1B | ツールルーティング・分類・簡易抽出向けとして言及される定番小型モデル |
| Gemma3(無印) 1B/270m | 1B/270m | さらに軽量だが、4分類JSON出力のような構造化タスクでの精度は要検証 |

### ④ MoEの観点(現状の再確認)

- Gemma4 26B-A4B(有効4B/8-of-128エキスパート)・gpt-oss-20b は**既にMoE**であり、総パラメータの割に実推論コストは小さい(だからこそ26B/20B級でもVRAM16GBに収まり、Phi-4-miniやDevstralよりtokens/sが出ている)。
- **Devstral Small 2 24Bは非MoE(密モデル)**であることが、4モデル中最も生成速度が遅い(約24 tokens/s)根本原因である可能性が高い。QAT版や量子化レベルの見直し以外に、根本対策としては「同等の精度を持つMoE系コーディングモデルへの乗り換え」も選択肢として残る(今回は詳細未調査、次回検討)。

### 次のアクション

1. `gemma4:e4b` と `gemma4:e2b`(`think:false`指定)を`ollama pull`し、既存の`testset_v1.md`(20問)を`run_testset.py`で複数回実行して分類正答率を比較。
2. 同時に`monitor_ollama.py`でtokens/s・TTFT・VRAMを実測し、Phi-4-mini(CPU固定版)と横並び比較。
3. 精度(目安8割以上、できればv3プロンプトの実績である95%前後)と速度の両方で優位なモデルをルーターとして採用する。