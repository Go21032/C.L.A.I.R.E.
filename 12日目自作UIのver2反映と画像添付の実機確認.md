---
project: C.L.A.I.R.E.(さぽーとAI)
date: 2026-08-14
tags: [UI, 自作UI, JARVIS-HUD, 音声入力, マルチモーダル, 画像, 実機確認, VRAM, 作業ログ]
status: 計画(着手前)
---

[[サポートAI作製計画/11日目Web検索対応・UIデザイン確定・マルチモーダル対応調査.md|11日目]]で③UIデザインの方向性が[[サポートAI作製計画/UIデザイン提案ver2.html]]として確定し、④-2「画像添付時はDEEPへ強制ルーティング」の実装(単体テスト205件成功)まで完了した。12日目の今日は、**①11日目④-2の残課題である画像添付の実機確認**(ブラウザで📷から画像を送り、`[route: DEEP]`で正しく応答が返るか・VRAMピークが④-1実測の15705MiBと同水準に収まるか)を先に消化し、その上で**②ver2デザインを`static/index.html`(実装)へ反映**して、**マイクで話す → 書き起こしを確認 → 送信、という一連の動線を確定デザインの自作UIで実際に動かす**ところまでを目標にする。

> [!note] このノートの位置づけ
> [[サポートAI作製計画/ノート作成規則.md]]に従い、**着手前に作業内容を列挙した計画ノート**として作成した。実行後に各セクションの「結果」「分析」「改善策」を追記していく。
> なお11日目ノートは2026-08-12着手だが、②(SearXNG)を08-13、④-1/④-2を08-14未明まで同一ノートに追記していたため、**12日目の日付も2026-08-14になる**(暦日ではなく作業回数で数えている)。

> [!warning] 今日のスコープ外(意図的に触らない)
> - ①(エンドツーエンド遅延)の最優先課題である「CLIベンチと実機の乖離」「gpt-oss:20bのCUDAクラッシュ」は**今日は追わない**。FASTルート(gpt-oss:20b)を経由しない画像=DEEPルートの確認とUI移植に集中する
> - ②Web検索(`web_search.py`の実装)も今日は着手しない。ただしUI側のWeb検索トグルは「見た目だけ(非機能)」で置く
> - ver2のサイドバー(チャット履歴のリネーム/削除/検索)・モデル切替・読み上げ速度バーは**見た目のみ**で、機能は繋がない(履歴の永続化設計が未着手のため。11日目③残課題参照)

---

## ① 11日目④-2の残課題:画像添付の実機確認(📷 → DEEPルート → VRAM実測)

### 背景/目的

11日目④-2で「画像添付時はルーターを経由せず強制的にDEEPへルーティングする」実装(`router.py`の`force_route`・`ollama_client.generate_stream()`の`images`対応・`voice_gateway.py`の`text_input`への`images`受け口・`static/index.html`の📷ボタン)を完了し、単体テスト205件が成功している。しかし**すべてOllama呼び出しをフェイクに差し替えたユニットテスト**であり、実際のブラウザ・実際の`gemma4:26b`を通した確認は一度も行っていない。11日目①結果3で「CLIベンチでは通るのに実機でしか踏めない不具合(gpt-oss:20bのCUDAクラッシュ)」を実際に経験しているため、**実機で通して初めて「できた」と言える**という原則をここでも適用する。

また④-1の単体計測では`gemma4:26b`への画像1枚でVRAMピーク**15705MiB**(総量16303MiB、残り約600MiB)まで逼迫することが分かっている。実運用経路(voice_gateway経由・ルーターモデル常駐・VOICEVOX/STT同居)では**この計測より条件が厳しい**ため、OOMせずに収まるかを実測で確認する必要がある。

> [!important] UI移植(②)より先にこれをやる理由
> 先にver2のCSS/DOMを移植してから実機確認をすると、不具合が出たときに「④-2の実装がそもそも動いていない」のか「UI移植で壊した」のかを切り分けられなくなる。**現行UIのまま実機で通す → 通ったことを確認してからUIを移植する**順序にすることで、②の回帰確認(③)の基準値も同時に作れる。

### 作業内容

- [ ] 必要なサーバを起動する(Ollama / VOICEVOX ENGINE / `voice_gateway.py`)
- [ ] VRAM監視(`nvidia-smi`ループ)をバックグラウンドで開始する
- [ ] ブラウザで📷ボタンから画像を1枚添付し、テキスト(例:「この画像に何が写っていますか?」)と一緒に送信する
- [ ] 応答が`[route: DEEP]`で返り、画像の内容を正しく説明できているかを確認する(④-1の`vision_bench.py`と同じテスト画像を使い、応答内容を比較できるようにする)
- [ ] DevTools(Network → WS `/ws`)で、送信した`text_input`メッセージに`images`配列が実際に載っているかを確認する(base64のdata URL prefixが除去されているか)
- [ ] VRAMピークを記録し、④-1の15705MiB・11日目結果1の15843MiBと比較する
- [ ] 画像**なし**の通常発話が従来どおり動くこと(後方互換)を、同じセッションで前後1回ずつ確認する
- [ ] 連続で2枚目の画像を送ったとき(モデルが既にロード済みの状態)の所要時間・VRAMを確認する
- [ ] 「画像を送った次のターン」でテキストのみ送ったときに、`last_route`がDEEPのまま継続するか(④-2設計どおりの挙動か)を確認する

### 実施手順

**手順1: サーバ起動(それぞれ別ターミナル)**

```powershell
# ターミナルA: Ollama(通常はタスクトレイで起動済み。確認のみ)
curl http://127.0.0.1:11434/api/tags

# ターミナルB: VOICEVOX ENGINE(run.exe を起動してから確認)
curl http://127.0.0.1:50021/speakers

# ターミナルC: 自作UIのサーバ
cd C:\Users\gakuh\Documents\obsidian\サポートAI作製計画\scripts
python voice_gateway.py --host 127.0.0.1 --port 5055
```

**手順2: VRAM監視を開始(ターミナルD。①の計測が終わるまで立てっぱなし)**

```powershell
cd C:\Users\gakuh\Documents\obsidian\サポートAI作製計画\scripts
nvidia-smi --query-gpu=timestamp,memory.used,memory.total,utilization.gpu --format=csv,noheader -l 1 > results\vram_watch_2026-08-14_image.csv
```

**手順3: テスト画像の用意**

④-1で使ったものと同じ合成画像(白背景・左上に「TEST BOX」と書かれた青い四角・中央に黒縁の赤い楕円)を再生成し、応答内容を④-1の`vision_bench`結果と直接比較できるようにする。

```powershell
cd C:\Users\gakuh\Documents\obsidian\サポートAI作製計画\scripts
python -c "from PIL import Image, ImageDraw; im=Image.new('RGB',(640,480),(240,240,240)); d=ImageDraw.Draw(im); d.rectangle([40,40,260,140],fill=(30,90,220)); d.text((70,80),'TEST BOX',fill=(255,255,255)); d.ellipse([220,220,460,400],fill=(220,40,40),outline=(0,0,0),width=6); im.save('results/vision_bench/test_image.png')"
```

**手順4: ブラウザ実機**

1. Chromeで `http://127.0.0.1:5055/` を開き、DevToolsのNetworkタブでWS `/ws` を選択しておく
2. まず**画像なし**でテキスト送信(例:「こんにちは」)→ 従来どおり応答が返ることを確認(後方互換の基準)
3. 📷ボタン → 手順3の画像を選択 → 「📷 test_image.png(送信待ち)」チップが出ることを確認
4. テキスト欄に「この画像に何が写っていますか?」と入力して送信
5. WSのフレームで`text_input`に`images`が載っていること、応答が`[route: DEEP]`であることを確認
6. 応答内容(青い箱・TEST BOXの文字・赤い楕円・背景色を言えているか)を④-1の結果と突き合わせる
7. 続けてテキストのみ送信し、`last_route`がDEEPのまま継続するかを確認
8. 2枚目の画像を送り、warm状態での所要時間を確認

**手順5: VRAM監視を停止し、ピークを確認**

```powershell
# ターミナルDで Ctrl+C 後
python -c "import csv; rows=[r for r in csv.reader(open('results/vram_watch_2026-08-14_image.csv',encoding='utf-8')) if len(r)>1]; peak=max(rows,key=lambda r:int(r[1].split()[0])); print('peak:',peak)"
```

> [!bug] 実行時エラーと原因(2026-08-14追記)
> 上記コマンドは`UnicodeDecodeError: 'utf-8' codec can't decode byte 0xff in position 0: invalid start byte`で失敗した。原因を確認したところ、`results/vram_watch_2026-08-14_image.csv`の実体は**UTF-16LE(BOM付き)**だった(`file`コマンドで`CSV Unicode text, UTF-16, little-endian text`と判定)。手順2の`nvidia-smi ... > results\...csv`のPowerShellリダイレクト`>`(`Out-File`相当)が、このマシンのWindows PowerShell 5.1既定でUTF-16LEを使うため。**計測データ自体は267行分正常に記録されており壊れていない**。読み込み側の`encoding='utf-8'`指定が実体と合っていなかっただけなので、次回以降は下記のどちらかで対処する。
> - 読み込み側を`encoding='utf-16'`に直す(今回はこちらで実測)
> - もしくは手順2の書き出し側を`... | Out-File -Encoding utf8 results\...csv`に変えてUTF-8で統一する(以後のノートでも同じ罠を踏まないための恒久対策として②以降のVRAM計測にも適用推奨)
>
> 修正版コマンド:
> ```powershell
> python -c "import csv; rows=[r for r in csv.reader(open('results/vram_watch_2026-08-14_image.csv',encoding='utf-16')) if len(r)>1]; peak=max(rows,key=lambda r:int(r[1].split()[0])); print('peak:',peak)"
> ```

### 想定計測表(結果はここに埋める)

| 項目 | 実測値 | 比較対象 | 備考 |
|---|---|---|---|
| 画像なし発話の応答(後方互換) | ~~未確認~~ → **①-2で確認済み**(`こんにちは、調子はどうですか`等4件、すべて`images_count=0`で正常応答) | — | 下記「①-2 結果 / 分析 / 改善策」参照 |
| 画像添付時のroute表示 | **`[route: DEEP]`**(2回とも) | 期待値: `DEEP` | 一致。ルーターを経由せず強制ルーティングされている |
| 応答内容の妥当性(目視) | **OK**(青い四角+「TEST BOX」文字、赤い楕円(黒縁取り)、薄いグレー背景の3要素を2回とも正しく説明) | ④-1: OK(3要素を正確に説明) | 2回目は楕円の位置を「中央(下寄り)」と表現するなど軽微な言い回しのブレはあるが、内容自体は正確 |
| 応答までの所要時間(1枚目・cold) | **参考値: 約25秒**(VRAM CSVでのモデル入替~安定化: 14:59:28.7→14:59:53.9) | ④-1単体: 39.83秒 | ⚠️VRAM遷移からの推測値であり、実際の応答受信時刻の実測ではない。正確な値が必要ならWSフレームのタイムスタンプ実測を推奨(下記分析参照) |
| 応答までの所要時間(2枚目・warm) | **参考値: 約19秒**(3回目のモデル入替~安定化: 15:02:25.4→15:02:44.6) | — | 同上。1枚目より短いがモデルロードのオーバーヘッドは依然発生している(下記分析参照) |
| VRAMピーク | **15882 MiB**(総量16303MiB、残り421MiB) | ④-1: 15705MiB / 結果1: 15843MiB(総量16303MiB) | OOMなし。ただし④-1・結果1より高く、過去最高値を更新。残り約420MiBまで逼迫 |
| 次ターン(テキストのみ)のroute | **`[route: FAST]`**(画像直後の「ありがとう」) | 期待値: DEEP継続 | ⚠️不一致。設計どおりではあるが「強制継続」ではなく「文脈ヒント付き再判定」であるため起きた挙動(下記分析参照) |

### 結果 / 分析 / 改善策

**結果概要:** 画像添付→DEEP強制ルーティング→正しい画像認識、という①の主目的は実機で通った。ただし手順5のVRAMログ読み込みで想定外のエンコーディング起因のエラーを踏んだため原因を切り分けた上で計測値を確定させた。また当初想定していなかった副次的な発見(次ターンのroute継続不一致)が1件出た。

**分析1: 手順5のエラーは計測失敗ではなく読み込み側のエンコーディング指定ミス**
`file`コマンドで実体を確認したところ`vram_watch_2026-08-14_image.csv`は`UTF-16LE(BOM付き)`だった。手順2の`nvidia-smi ... > results\...csv`のPowerShellリダイレクト`>`が、このマシンのWindows PowerShell 5.1既定でUTF-16LEを使うため。手順5のPythonコードが`encoding='utf-8'`で開こうとしたため、先頭のBOM(`0xFF 0xFE`)がUTF-8として不正なバイト列と判定されクラッシュした。**`encoding='utf-16'`で読み直したところ267行分すべて正常に取得でき、計測自体は成功していた**ことを確認した。次回以降のVRAM計測ノートでも同じ罠を踏む可能性があるため、書き出し側を`Out-File -Encoding utf8`に統一するか、読み込みスクリプト側を常に`utf-16`前提にするか、どちらかをテンプレート化すべき。

**分析2: VRAMピークはOOMなしだが逼迫傾向が悪化している**
実測ピークは**15882 MiB**(総量16303MiB中、残り421MiB)。④-1の単体計測(15705MiB)・結果1(15843MiB)のいずれよりも高く、3回の計測の中で最大値を更新した。voice_gatewayが常駐させるルーター(gemma4-e4b-cpu)・STT(faster-whisper)・TTS(VOICEVOX)が同居する実運用経路の方が単体計測より条件が厳しいという事前の懸念どおりの結果。OOMには至っていないが、残り400MiB程度まで来ているため、複数枚画像の同時添付やより大きい解像度の画像では容易にOOMしうる状態。

**分析3: VRAM推移から見ると、1リクエストあたり3回のモデル入替が起きている**
CSVの時系列を追うと、監視時間中(14:58:49〜15:03:17、約4分28秒・267サンプル)に**VRAM使用量が急減→急増するサイクルが3回**観測された(14:59:28〜14:59:54頃/1回目、15:01:47〜15:02:03頃/2回目、15:02:25〜15:02:44頃/3回目)。それぞれ「一旦VRAMが2000〜3000MiB台まで落ち、その後14000〜16000MiB台まで戻る」という形で、モデルが一度解放されてから再ロードされていることを示している。これは`force_route`実装がリクエストのたびにDEEP(gemma4:26b)を呼び直し、ルーターモデル(gemma4-e4b-cpu)と共存できずに入れ替わっている可能性が高い。この入替コストがそのまま応答遅延に乗っているため、①手順の想定計測表にあった「cold/warm」の差(39.83秒 vs それ以下)が実運用では明確に出なかった一因と考えられる。**正確な応答時間を実測するには、VRAM推移からの推測ではなくWSの`text_input`送信時刻と応答完了時刻をログに残す仕組みが必要**(残課題へ追加)。

**分析4: 次ターン(テキストのみ)のrouteがDEEP継続にならなかったのは、設計上「強制」ではなく「ヒント」だったため**
`router.py`を確認したところ、`force_route`(画像添付時)はその場で`self._last_route[session_id]`にDEEPを書き込むが、次のテキストのみターンでは`force_route`は使われず、通常の`classify_route(text, call_model, last_route=last_route)`が呼ばれる。`build_user_prompt()`は`last_route`を**「直前はDEEPとして扱われていた。同じ話題の続きならDEEPのままに」という文脈ヒント**としてPhi-4-miniのプロンプトに埋め込んでいるだけで、**強制継続ではなく再判定**である。実測で「ありがとう」(明確な相槌・話題転換)に対してFASTと再判定されたのは、この設計どおりの挙動と言える。つまり④-2ノートの想定計測表にあった「期待値: DEEP継続」という書き方自体が、実装の実態(ヒント付き再判定であって強制継続ではない)とズレていた。バグではないが、**仕様として"次ターンは必ずDEEP継続"を期待するなら別途強制ロジックが要る**、という認識のズレが今回明らかになった。

**改善策:**
1. VRAM計測の書き出し/読み込みエンコーディングをテンプレート化する(`Out-File -Encoding utf8`に統一、または読み込み関数を`utf-16`固定にする共通スクリプトを`scripts/`に用意する)
2. 応答時間を正確に測るため、`voice_gateway.py`側で`text_input`受信時刻とレスポンス完了時刻をログ出力する仕組みを追加する(VRAM推移からの推測に頼らない)
3. 「画像直後のテキストのみターン」でDEEPを維持したいユースケードがあるなら、`force_route`をもう1ターンだけ延長する(例: `last_image_turn`フラグを持たせ、直後の1ターンだけ`force_route=DEEP`にする)設計変更を検討する。現状維持(ヒントのみ)でよいなら、④-2ノートの「期待値」表記を「ヒントによる再判定(強制ではない)」に訂正する

### 残課題

- ~~VRAMがOOMした場合~~ → 今回はOOMしなかったが、ピーク15882MiBで残り421MiBまで逼迫。複数枚画像添付や高解像度画像では引き続きOOMリスクがあるため、11日目④-1改善策どおり「画像添付時はFAST/CODEモデルを`ollama stop`で解放してからDEEPを呼ぶ」VRAM管理の実装は依然として有効な改善候補
- ~~VRAM推移から3回のモデル入替(≒応答のたびのDEEP再ロード)が観測された。応答時間の正確な実測ができていないため、`voice_gateway.py`にリクエスト/レスポンスのタイムスタンプログを追加し、次回はそこから所要時間を出す~~ → 下記「① 残課題の実施(追記)」で対応済み
- ~~「画像直後のテキストのみターン」のroute継続は"ヒントによる再判定"であり"強制継続"ではないことが判明。この挙動を仕様として受け入れるか、強制継続ロジックを追加するかを次回検討する~~ → 下記「① 残課題の実施(追記)」で結論(現状維持)
- 「画像なし発話の応答(後方互換)」が今回のログ範囲内で確認できていない。次回実機確認時は手順4の2番目(画像なしテキスト送信)のスクリーンショット/ログを必ず残す → 下記「① 残課題の実施(追記)」に再実施手順を明記
- `[route: DEEP]`にならない場合 → 今回は毎回DEEPになったため未発生。`_extract_last_user_images()`のキー名・ネスト位置の確認は引き続き潜在課題として残す
- 応答は返るが画像を見ていない(内容が的外れ)場合 → 今回は3要素とも正しく説明できていたため未発生

### ① 残課題の実施(追記)

**方針決定(相談の上で確定):** 「画像直後のテキストのみターン」でDEEPを強制継続するかどうかについて、`last_image_turn`フラグを追加して強制継続させる案(11日目④-2ノート改善策3)を提示したが、**現状維持(ヒントによる再判定のまま)** で確定した。理由: 画像の話題のあとに「ありがとう」のような相槌が来た場合まで強制的にDEEP(gemma4:26b)へ固定すると、相槌1つのために不要なモデル入替(数秒〜十数秒)が発生し、体感速度が悪化するため。「同じ話題の続きかどうかをケースバイケースで判定してほしい」という要求と、既存のヒント方式(`router.py`の`build_user_prompt()`)の挙動は一致しているため、**コード変更はしない**。今回対応するのはノート上の記述訂正のみ:上記想定計測表の「次ターン(テキストのみ)のroute」欄にあった「期待値: DEEP継続」という書き方は誤りで、正しくは「期待値: ヒントによる再判定(直前がDEEPだったことを文脈として渡すが、話題が変わったと判断されればFAST等へ再分類される。これは実装どおりの正常動作)」である。

**実装1: 応答時間の正確な実測(タイムスタンプログ)**

分析3で「VRAM推移からの推測(約25秒/約19秒)」しか出せていなかった問題に対応した。TDD(superpowers:test-driven-development)で進めた:

1. RED: `tests/test_voice_gateway.py`に`TestTimingRecorder`(3件)・`TestDefaultTimingRecorderCsv`(2件)を追加し、`run_turn()`が未知の引数`clock`/`timing_recorder`を受け付けずTypeErrorになること、`voice_gateway.make_csv_timing_recorder`が未実装でImportErrorになることを確認(4件red)。
2. GREEN: `voice_gateway.py`に以下を実装。
   - `run_turn(..., clock=time.time, timing_recorder=None)`: 呼び出し開始時刻を記録し、正常終了時(`state: idle`直前)・Pipe呼び出し失敗時のどちらでも`timing_recorder({"chat_id", "text", "images_count", "start_ts", "end_ts", "duration_sec"})`を1回だけ呼ぶ。`timing_recorder`省略時(既存呼び出し元・既存テスト)は何もしない後方互換。
   - `make_csv_timing_recorder(csv_path=DEFAULT_TIMING_CSV_PATH)`: `results/response_timing.csv`(既定)へ1行追記するデフォルト実装。初回のみヘッダ行(`start_iso,chat_id,text,images_count,duration_sec`)を書く。
   - `create_app(..., timing_recorder=None)`: 省略時は`make_csv_timing_recorder()`を使い、WS `/ws`ハンドラの`_handle_final_transcript()`→`run_turn()`へ配線。
3. テスト結果: `pytest tests/`で210件成功(従来205件+今回追加5件)。
4. 実機での動作確認: コード保存直後、常駐中のサーバ(uvicorn `--reload`想定)が再読み込みされたタイミングで`results/response_timing.csv`に実際のWS経由アクセス(`chat_id`が`voice-<id>`形式、`text`が実際の発話文字列)が1行記録されているのを確認できた。フォーマットは想定どおり(`duration_sec`まで実測値で入る)。

**実装2: VRAM管理(FAST/CODE解放→DEEP呼び出し)の実装状況の確認**

11日目④-1改善策・12日目①残課題で「画像添付時はFAST/CODEモデルを`ollama stop`で解放してからDEEPを呼ぶ」実装が挙がっていたが、`router.py`の`ensure_model_ready(route)`(画像添付でforce_route=DEEPになった場合も含め、`support_ai_auto_pipe.Pipe._stream_reply()`・`Pipe.pipe()`の両方で`generate`/`generate_stream`の直前に必ず呼ばれる)が既にこれを実装済みであることをコードで再確認した。ロジック:「target_model(=DEEPならgemma4:26b)が既にOllama上で起動中なら何もしない。そうでなければ、起動中の他モデルのうちROUTER_MODEL(CPU固定・VRAM消費ほぼ無し)以外を全部`stop_model()`する」。12日目①のVRAM実測ログ(分析3)で見えた「使用量が一旦2000〜3000MiB台まで落ちてから再ロードする」波形は、まさにこの`stop→load`が意図どおり順番に(重複読み込みなしで)動いている証拠であり、新たな実装は不要と判断した。残る懸念は「複数枚画像の同時添付・高解像度画像でのVRAM逼迫」で、これは11日目ノートの次のステップ④に記載のとおりYAGNIとして保留のまま。

**実装3: 「画像なし発話の応答(後方互換)」を明示的に確認する手順**

コード変更ではなく手順の問題(11日目④-2ノートには元々あった「画像なしでも従来どおり動く」確認が、スクリーンショット/ログの記録漏れで12日目①の想定計測表に反映されなかった)。次回実機確認時に確実に記録が残るよう、下記「② 127.0.0.1:5055 での実機確認手順(2回目)」に明記した。

---

## ①-2 127.0.0.1:5055 での実機確認手順(2回目・具体的な入力プロンプト付き)

前回(1回目)は「こんにちは」(画像なし)と「この画像に何が写っていますか?」(画像あり)を交互に送っただけで、①想定計測表の「画像なし発話の応答(後方互換)」欄が未確認のまま残った。今回はそこを含めて、①で実装したタイムスタンプログ(`results/response_timing.csv`)の実機確認も兼ねる。**スクリーンショットまたはWSフレームのログを、各ステップごとに残すこと**(前回未確認になった原因が「記録し忘れ」だったため)。

### 事前準備

```powershell
# サーバが起動していない場合のみ
cd C:\Users\gakuh\Documents\obsidian\サポートAI作製計画\scripts
python voice_gateway.py --host 127.0.0.1 --port 5055
```

- Chromeで `http://127.0.0.1:5055/` を開く
- DevTools → NetworkタブでWS `/ws` を選択しておく(送受信フレームを見るため)
- テスト画像が無ければ①手順3のワンライナーで`results/vision_bench/test_image.png`を再生成しておく

### 入力する順番とプロンプト(このとおりの順で1つずつ)

| #   | 操作                                  | 入力するプロンプト(そのままコピペしてOK) | 確認すること                                                                                                                       |
| --- | ----------------------------------- | ---------------------- | ---------------------------------------------------------------------------------------------------------------------------- |
| 1   | 画像を添付せず、テキストのみ送信                    | `こんにちは、調子はどうですか`       | ①の欠けていた項目。**必ずスクショを撮る**。応答が返る/`[route:`の表示(DEEPでないこと)/WSフレームに`images`キーが無いこと                                                 |
| 2   | 続けてテキストのみ送信(話題を変えて)                 | `今日の天気を教えて`            | 1と同じセッションで、通常の分類(FAST等)が動くこと。route固定バグが無いことの確認                                                                               |
| 3   | 📷ボタンでtest_image.pngを添付し、テキストと一緒に送信 | `この画像に何が写っていますか?`      | `[route: DEEP]`で返ること。応答内容(青い四角+TEST BOX文字/赤い楕円黒縁/薄いグレー背景)が正しいこと。WSの`text_input`に`images`配列(data URL prefix無しのbase64)が載っていること |
| 4   | 続けてテキストのみ送信(画像直後の相槌)                | `ありがとう、助かりました`         | 方針決定どおり、DEEP継続ではなく通常再判定(FAST等になっても正常)。ここでDEEP以外になっても**バグではない**                                                               |
| 5   | 再度📷ボタンで同じ画像を添付して送信(2枚目・warm)       | `もう一度、同じ画像で何が見えるか説明して` | 1枚目より短時間で返るか(モデルは既にロード済みのはず)。応答内容が1回目と大きくブレないこと                                                                              |
| 6   | 画像なしに戻して送信(後方互換の再確認・締め)             | `テストは以上です、ありがとうございました` | 画像添付を挟んだ後でも通常のテキスト送信が問題なく動くこと(セッションが壊れていないこと)                                                                                |

### 実施後にやること(タイムスタンプログの確認)

```powershell
cd C:\Users\gakuh\Documents\obsidian\サポートAI作製計画\scripts
type results\response_timing.csv
```

- 6行(#1〜#6)ぶんの`start_iso,chat_id,text,images_count,duration_sec`が記録されているはずなので、`duration_sec`列を見て「画像あり(#3,#5)」と「画像なし(#1,#2,#4,#6)」の応答時間を突き合わせる。VRAM CSV(`nvidia-smi`)からの推測(前回:約25秒/約19秒)ではなく、ここが**正確な実測値**になる
- 併せてVRAM監視も回しておきたい場合は①手順2をそのまま並行実行してよい(必須ではない。タイムスタンプログだけでも所要時間は取れるようになったため)

### 結果 / 分析 / 改善策

**実施結果:** 手順1〜6をすべて実施し、スクリーンショット3枚(#1-#2, #3-#5前半, #5後半-#6)を記録した。`results/response_timing.csv`に実測値が記録されていることを確認した。

| #   | 入力                   | route | images_count | duration_sec |
| --- | -------------------- | ----- | ------------ | ------------ |
| 1   | こんにちは、調子はどうですか       | FAST  | 0            | **60.079**   |
| 2   | 今日の天気を教えて            | FAST  | 0            | **41.420**   |
| 3   | この画像に何が写っていますか?      | DEEP  | 1            | **37.968**   |
| 4   | ありがとう、助かりました         | FAST  | 0            | **47.475**   |
| 5   | もう一度、同じ画像で何が見えるか説明して | DEEP  | 1            | **36.009**   |
| 6   | テストは以上です、ありがとうございました | FAST  | 0            | **47.659**   |

チェック項目の結果:
- **画像なし発話の応答(後方互換)**: #1・#2・#4・#6の4件とも`images_count=0`で正常応答。WSフレームに`images`キーが付かないことも想定どおり。①の未確認項目はこれで解消
- **route固定バグの有無**: #2(画像なし・話題転換)が`CLARIFY`や直前routeへの固定なく`FAST`に正しく分類され、セッションが壊れていないことを確認
- **画像添付時のroute**: #3・#5とも`[route: DEEP]`(スクショで確認)。強制ルーティングは今回も一致
- **応答内容の妥当性**: #3・#5とも青い長方形+「TEST BOX」文字・赤い楕円(黒縁)・薄いグレー背景の3要素を正しく説明(①の1回目と同水準)
- **画像直後の相槌(#4)のroute**: `[route: FAST]`。①-2冒頭の方針決定どおり、DEEP強制継続ではなく通常の再判定が働いた(想定どおりでバグではない)
- **2枚目(warm)の所要時間**: 36.009秒(#5)で1枚目の37.968秒(#3)よりわずかに短いが、大差はない(下記分析2参照)

**分析1: 実測値は前回のVRAM推測値より大幅に長く、「推測に頼らない実測」の必要性が裏付けられた**
①1回目はVRAM CSVの遷移タイミングから画像添付時の応答時間を「約25秒(cold)/約19秒(warm)」と*推測*していた。今回`response_timing.csv`で得られた実測値はDEEPルート(画像あり)で**37.968秒/36.009秒**であり、前回の推測値より約50〜90%長い。VRAM遷移(モデルの解放→再ロード)は応答の一部でしかなく、その前後のプロンプト処理・トークン生成・TTS合成待ちなどを含んでいなかったため、推測値は実態を過小評価していたことが確定した。①残課題で「VRAM推移からの推測に頼らずタイムスタンプログを追加する」とした改善策は、今回のズレを検出できたという点で効果があったと言える。

**分析2: 「画像あり(DEEP)」より「画像なし(FAST)」の方が遅いという逆転現象**
route別に見ると、DEEP(#3・#5、画像処理込み)が36〜38秒なのに対し、FAST(#2・#4・#6、warm状態でモデル入替を伴う場合も含む)は41〜48秒と、**画像を伴うDEEPの方が速い**という直感に反する結果になった。さらに#1(コールドスタート)は60秒に達している。これは画像添付・DEEPルート自体の問題ではなく、**FASTルートの基盤モデル(gpt-oss:20b)自体の応答が実運用環境(voice_gateway経由・ルーター/STT/TTS同居)で遅い**ことを示している。これは11日目ノートの最優先課題「①CLIベンチと実機の乖離・gpt-oss:20bのCUDAクラッシュ」として既に認識され、本日は意図的にスコープ外としていた問題と整合する結果であり、新しい問題ではない。ただし、今回の実測で「FASTがDEEPより遅い」という具体的な逆転が数値で裏付けられたため、次にその課題へ着手する際の優先度付けの根拠として使える。

**分析3: モデル入替(swap)は今回も毎ターン発生しているが、VRAM監視は今回意図的にスキップした**
route列を見るとFAST→DEEP→FAST→DEEP→FASTと交互に切り替わっており、#2→#3, #3→#4, #4→#5, #5→#6の4回でモデル入替が発生している(①1回目の分析3と同じ現象)。今回は①-2の実施後に「VRAM監視は必須ではない」としたため、VRAM CSVは取得していない。そのためVRAMピークが今回どうだったかは不明(次回、時間に余裕があれば①手順2を並行実行して補完するとよい)。

**改善策:**
1. FASTルート(gpt-oss:20b)の実運用遅延は、11日目ノートで最優先課題としてマークされている本丸の問題であり、今回の実測(41〜60秒)がその深刻さを裏付ける具体数値になった。UI移植(②③)が一段落し次第、11日目①(CLIベンチと実機の乖離の切り分け・CUDAクラッシュ調査)に着手する優先度をさらに上げてよい
2. 応答時間の実測ログ(`response_timing.csv`)は今後もFAST/DEEP双方の性能比較・改善効果測定の基準値として使えるため、11日目①の調査時にも同じログを比較対象として活用する
3. VRAMピークと応答時間を同時に見たい場合は、①手順2(`nvidia-smi`監視)と①-2の入力手順を同時に走らせる運用に一本化する(次回のテンプレートとして明記)

---

## ①-3 ①-2で分かったFASTの遅延問題の解決(systematic-debugging・thinkingモード無効化)

### 背景/目的

①-2の実測で、FASTルート(`gpt-oss:20b`)が41〜60秒、DEEPルート(`gemma4:26b`)ですら36〜38秒かかり、しかも「画像処理込みのDEEPの方がFASTより速い」という逆転現象が観測された。11日目ノートで最優先課題として残っていたこの問題を、superpowers:systematic-debuggingの手順(推測でfixを当てず、まず原因を実測で切り分ける)に従って調査し、根本原因を特定した上で改善策を実装した。

### Phase 1〜3: 原因調査(推測ではなく実測で切り分け)

各工程を個別に直接叩いて実測したところ、**個々の部品はどれも単体では速い**ことが分かった(すべてOllama起動済み・ウォーム状態での実測):

| 工程 | 実測 | 備考 |
|---|---|---|
| ルーター分類(`gemma4-e4b-cpu`、直接API) | 約1.0秒 | ウォーム時 |
| `gpt-oss:20b`の`generate`(直接API、`think`未指定) | 1.5〜14.5秒(ばらつき大) | ロード状態により変動 |
| RAG記憶検索(`memory_store.retrieve`) | 0.11秒 | 高速 |
| VOICEVOX合成(`tts_adapter.synthesize`) | 1.4秒 | 高速 |

個々の部品が速いにもかかわらず全体が40〜60秒かかる、という矛盾を突き止めるため、実際に起動中の`voice_gateway.py`(ポート5055)へ自作のWSクライアントで`text_input`を送り、Ollamaの`server.log`(`%LOCALAPPDATA%\Ollama\server.log`)とタイムスタンプを突き合わせて調査した。

**発見1: `/api/generate`への直接A/Bテストで、`think`(thinkingモード)の既定値が原因と特定**

7日目⓪で「gemma4系はOllama既定でthinkingモードが有効なため不要な遅延(3〜4倍)を生む」ことが判明し、`router.py`のROUTER_MODEL(`gemma4-e4b-cpu`)呼び出しには`ROUTER_THINK=False`が適用されていた。しかし**実際にユーザーへ応答を返すFAST/DEEP/CODEの対象モデル(`generate`/`generate_stream`)には`think`が一切渡されておらず、Ollama既定(thinkingモード有効)のまま呼ばれ続けていた**ことをコードで確認した。これが7日目の教訓がFAST/DEEP自体には適用されていなかった、という単純な見落としだと分かったため、Ollama `/api/generate`に直接`think`値を変えてA/Bテストを行い定量化した:

| モデル(route) | `think`未指定 | `think=False` | その他 |
|---|---|---|---|
| `gemma4:26b`(DEEP) | **32.38秒** | **1.23秒**(約26倍) | — |
| `gpt-oss:20b`(FAST) | 約5〜6秒(初手thinking) | 2.15秒(bool Falseはgpt-oss側で無視され部分的にthinkingが残る) | `think="low"`(文字列指定)で**1.13秒**まで短縮 |
| `devstral-small-2:24b`(CODE) | 32.04秒 | 16.5秒(約2倍) | thinkingフィールド自体は持たないモデルだが明確に改善 |

`gpt-oss:20b`はOpenAIのharmony形式の推論モデルで、`think`はboolではなく`"low"/"medium"/"high"`という**reasoning effort文字列**で制御する仕様であることも実測で判明した(bool `False`は無視され、内部的にthinkingトークンが生成され続ける)。

**発見2(副次的・未解決のまま残す): VRAM逼迫によるモデル入替スラッシング**

上記の`think`修正を適用した状態で実際に`Pipe.pipe()`をFAST→DEEP→FASTと連続実行して検証したところ、直前にDEEP(`gemma4:26b`、15〜17GB)が読み込まれた直後にFAST(`gpt-oss:20b`、13GB)へ切り替えると、Ollamaのスケジューラが「16GB VRAMに両方は同時に載らない」と判断してモデルを退避・再ロードする現象が`server.log`(`llama-server model predicted to exceed available memory, evicting`)で確認できた。このロードだけで9〜11秒かかり、かつCPU固定のはずのルーターモデル(`gemma4-e4b-cpu`)まで巻き添えでリロードされるケースもあった(システムメモリ側のスワップ圧迫が原因と推測)。これは12日目①分析3・11日目分析8で既に仮説として挙がっていた「VRAM逼迫によるモデル入替」が実測で裏付けられた形だが、**根本的には16GB VRAMにFAST(13GB)+DEEP(15〜17GB)を同時保持できないというハードウェア制約**であり、コード側の`think`修正だけでは解決しない別問題として今回は深追いせず残課題に置く。

### Phase 4: 実装(TDD)

1. **RED**: `tests/test_support_ai_auto_pipe_stream.py`に`TestThinkModeDisabledForTargetModels`(FAST/DEEPそれぞれ)、`tests/test_support_ai_auto_pipe.py`に`test_deep_route_non_streaming_call_disables_thinking`・`test_code_route_call_disables_thinking`を追加し、`generate`/`generate_stream`へ渡る`think`キーワード引数が存在しないこと(`KeyError`)を確認(3件red)。
2. **GREEN**: 以下を実装。
   - `router.py`に`ROUTE_THINK_MAP: dict[str, bool | str] = {"FAST": "low", "DEEP": False, "CODE": False}`を追加(上記A/Bテスト結果に基づく、モデルファミリーごとの最適値)。CLARIFYはROUTER_MODEL経由で既に`ROUTER_THINK`により制御済みのため対象外。
   - `support_ai_auto_pipe.py`の`_stream_reply()`(FAST/DEEPストリーミング経路)・`pipe()`非ストリーミング経路(CODE・DEEP/FAST)の`generate`/`generate_stream`呼び出しすべてに`think=router.ROUTE_THINK_MAP.get(route)`を追加。
   - `ollama_client.py`の`generate`/`generate_stream`の型ヒントを`think: bool | None`から`think: bool | str | None`へ修正(gpt-oss系のreasoning effort文字列指定に対応)。
3. **テスト結果**: `pytest tests/`で213件成功(追加5件含む。既存の`test_ws_e2e_bench.py`の1件は、検証中に実サーバ`voice_gateway.py`(ポート5055)を起動したままにしていたことによる`response_timing.csv`のファイルロック競合で、今回のコード変更とは無関係な環境要因のため対象外とした)。

### 実機検証(修正の効果測定)

VRAM状態がクリーンな(直前にモデル入替が挟まっていない)状態で、`support_ai_auto_pipe.Pipe.pipe()`を実際のOllama・実際のルーター判定を通してFASTルートを2連続実行し(記憶DBは`NoopMemoryStore`で本番DBを汚さないよう差し替え)、修正後の応答時間を実測した:

| # | 入力 | route | duration_sec(修正後) |
|---|---|---|---|
| 1 | 明日の天気を教えて | FAST | 8.41秒 |
| 2 | 今何時ですか | FAST | 8.84秒 |

①-2実測(修正前、FASTのduration_sec: 60.079秒/41.420秒/47.475秒/47.659秒)と比較して、**約5〜7倍の短縮**が確認できた。内訳はルーター分類(約1〜3秒)+`gpt-oss:20b`生成(`think="low"`で約1〜3秒)+その他オーバーヘッドで、①-2で観測された「thinkingモードによる数秒〜数十秒の無駄な内部推論」がほぼ解消されたことに一致する。

> [!note] 検証中に踏んだ罠: 直前にDEEPモデルが読み込まれた状態でFASTを叩くと上記「発見2」のモデル入替スラッシングが起き、44秒・31秒という(修正前とあまり変わらない)結果が出た。同一route・同一モデルが既にウォームな状態でなければ`think`修正の効果は隠れてしまうため、効果測定時は**直前のroute/モデル状態を揃える**ことが重要だと分かった(次回以降のベンチ手順に反映する)。

### 改善策(実施済み・残課題)

**実施済み:**
1. `router.py`に`ROUTE_THINK_MAP`を追加し、FAST/DEEP/CODEの応答生成モデル呼び出しすべてに適切な`think`値を渡すよう修正(実装済み・テスト済み・実機検証済み)

**残課題(今回のスコープ外):**
1. VRAM逼迫によるモデル入替スラッシング(発見2)。16GB VRAMにFAST(13GB)とDEEP(15〜17GB)を同時保持できないという構造的制約のため、`think`修正だけでは解決しない。11日目残課題にある「FASTルートの割当モデル変更の検討」や、ルート切替の頻度を抑える設計(例: 直近のroute別に応答をバッチ化する、より小さいモデルへの入替を検討する等)が次の一手候補
2. `voice_gateway.py`の`run_turn()`がOllama/VOICEVOXへの同期・ブロッキングHTTP呼び出しを`asyncio`イベントループ上で直接実行しており(`asyncio.to_thread`等へのオフロードなし)、応答生成中は当該WebSocket接続のping/pong keepaliveや他の処理が止まる副作用を検証中に直接再現した(生成中に接続がタイムアウト切断されるケースを確認)。今回の主目的ではないため深追いしなかったが、応答が数十秒かかるケースでは接続断のリスクがあるため、次回以降の残課題として記録する
3. CLI直接ベンチ(`bench_e2e_latency.py`)でも同じ`think`修正の効果を計測し、11日目結果2の`seg_ttft_only`等の値を更新する

---

## ② UIデザインver2の`static/index.html`への反映(骨格+音声送信の動作優先)

### 背景/目的

11日目③で[[サポートAI作製計画/UIデザイン提案ver2.html]](985行)としてデザインが確定したが、これは**静的モックアップ**であり、実際に動く[[サポートAI作製計画/scripts/static/index.html]](789行)には一切反映されていない。今日の目的は「確定したデザインで、マイクで話して送信できる自作UIを実際に動かす」こと。

**方針(相談の上で確定):**

| 論点 | 決定 | 理由 |
|---|---|---|
| 今日のスコープ | **骨格+音声送信の動作優先** | ver2の配色・トークン・レイアウト骨格(トップバー/中央ログ/入力パッド)だけを移植し、既存WS機能(マイク→プレビュー→送信/📎/📷/📚ナレッジ)を確実に動く状態で繋ぎ直す。サイドバー履歴・モデル切替・各種トグルは**見た目だけ(非機能)**で置く |
| 実装方式 | **既存`index.html`にver2のCSSを移植** | 動作実績のあるJS(WS接続・録音・ダウンサンプル・送信)を温存し、CSSとDOM構造だけver2に寄せる。ver2.htmlをコピーしてJSを差し込む方式は見た目の再現度は高いが、ID/DOMの対応付けで既存機能を壊すリスクが高いため不採用 |

### 作業内容

- [ ] 作業前に`static/index.html`をバックアップする(`index_v1_backup.html`として同ディレクトリに退避。①で動作確認済みの状態を戻せるようにする)
- [ ] ver2.htmlから**CSS変数(配色トークン)・フォント・背景装飾(スキャンライン/グロー)・コーナーブラケット**を`index.html`の`<style>`へ移植する
- [ ] ver2の**トップバー**(ブランド、状態バッジ、モデル表示、テレメトリ)を移植し、既存の`#state-badge`・`#knowledge-btn`をその中へ配置し直す
- [ ] ver2の**中央メッセージログ**(ユーザー/AIの吹き出し)のスタイルを`#log`(既存`appendTurn()`が吐くDOM)に適用する
- [ ] ver2の**入力パッド**を移植し、既存の`#attach-btn`(📎)・`#image-attach-btn`(📷)・`#text-input`・`#send-btn`・`#start-btn`/`#stop-btn`/`#mic-indicator`をその中へ収める
- [ ] ver2の**サイドバー**(Conversations)を見た目のみで配置する(履歴アイテムはダミー、クリックしても何も起きない。将来の実装場所を確保する目的)
- [ ] ver2の**右パネル Controls**(ウェイクワード/自動送信/RAG/Web検索/思考モードのスイッチ、読み上げ速度バー)を見た目のみで配置する
- [ ] 既存の`#attach-chips`(添付チップ)・`#knowledge-panel`(ナレッジ一覧)をver2のトーンに合わせる
- [ ] **JSは触らない**ことを原則とし、DOM構造の変更でセレクタが壊れた箇所だけ最小限修正する(修正した場合はBefore/Afterを本ノートに残す)
- [ ] 「見た目だけ」の要素には、後で自分が混乱しないよう**HTMLコメントで`<!-- MOCK: 未接続 -->`と明記する**

### 実施手順

1. **バックアップ**
   ```powershell
   cd C:\Users\gakuh\Documents\obsidian\サポートAI作製計画\scripts\static
   copy index.html index_v1_backup.html
   ```
2. **CSSトークンの移植** — ver2.htmlの`:root`変数・`body`背景・共通コンポーネント(パネル/ボタン/バッジ)のCSSを先に丸ごと持ってくる。この時点で一度リロードし、**既存のDOMのままでも配色だけver2になる**ことを確認する(ここで壊れたらCSS側の問題と切り分けられる)
3. **DOM構造をver2のグリッド(トップバー/サイドバー/中央/右パネル)に組み替える** — 既存の`id`は**一切変えない**。既存要素を新しいコンテナの中へ移動させるだけにする
4. **リロードして既存機能を1つずつ確認**(下記③のチェックリスト)
5. **見た目のみの要素(サイドバー履歴・Controls・モデル切替)を追加** — 機能が動くことを確認した後に足す。先に足すと、動かない原因が「未実装だから」か「壊したから」か分からなくなる

### 成果物一覧(予定)

| ファイル                                              | 新規/修正 | 役割                    | 実行方法                                                                    | 出力先                |
| ------------------------------------------------- | ----- | --------------------- | ----------------------------------------------------------------------- | ------------------ |
| [[サポートAI作製計画/scripts/static/index.html]]          | 修正    | 自作UI本体。ver2デザインの骨格を適用 | `python voice_gateway.py --port 5055` 起動後、ブラウザで`http://127.0.0.1:5055/` | ファイル保存なし(ブラウザ表示のみ) |
| `scripts/static/index_v1_backup.html`             | 新規    | 移植前(11日目時点)のUIのバックアップ | 単体実行はしない(復旧用)                                                           | —                  |
| [[サポートAI作製計画/UIデザイン提案ver2.html]]                  | 参照のみ  | 移植元のデザインモックアップ        | ブラウザで直接開く                                                               | —                  |
| `scripts/results/vram_watch_2026-08-14_image.csv` | 新規    | ①の画像添付時のVRAM実測ログ      | `nvidia-smi ... -l 1 > ...`(①手順2)                                       | 同左CSV              |
| `scripts/results/vision_bench/test_image.png`     | 新規    | ①で使うテスト画像(④-1と同一仕様)   | ①手順3のワンライナー                                                             | 同左PNG              |

### 結果 / 分析 / 改善策

(実施後に記入)

### 残課題

(実施後に記入。以下は事前に「今日はやらない」と決めているもの)

- サイドバーのチャット履歴の**機能化**(リネーム/削除/検索/切替)。11日目③残課題どおり、**先に履歴データの永続化(保存形式)を決める必要がある**ため後日
- モデル切替ドロップダウンの機能化(ルーター判定を手動上書きするAPIが`voice_gateway.py`側に無い)
- 読み上げ速度バーの機能化(VOICEVOXの`speedScale`へ渡すWSメッセージ種別が未実装。11日目③追記の残課題)
- Web検索トグルの機能化(②`web_search.py`未実装のため)
- 引用元(出典)チップ(RAG/Web検索のソース情報をAPIレスポンスでどう返すかの設計が未着手)

---

## ③ 移植後の回帰確認:マイクで話して送信できるか

### 背景/目的

②のCSS/DOM移植は「JSを触らない」原則で進めるが、**DOM構造を変えれば`getElementById`は生きていてもレイアウト・イベント伝播・`disabled`制御が壊れる可能性がある**。9〜10日目で実機確認済みの動線(常時プレビュー+手動送信・マイクミュート延長・停止ボタン)と、11日目④で実装した📎/📚、④-2で実装した📷が**移植後も全部動くこと**を、チェックリストで機械的に確認する。①を先にやっているので、①の結果が「移植前の基準値」になる。

### 作業内容(回帰チェックリスト)

- [x] `http://127.0.0.1:5055/` を開いてWS接続が確立し、状態バッジが`idle`になる
- [x] 「マイクを開始」でマイク権限が取得でき、`#mic-indicator`が入力レベルに反応する
- [x] **話すと`#text-input`に書き起こしがリアルタイムで入る**(常時プレビュー。10日目の設計)
- [x] 書き起こしを手で修正でき、Enterまたは「送信」で送信できる
- [x] 応答が中央ログにユーザー/AIの吹き出しとして正しく積まれる
- [x] 応答音声が再生され、**再生中はマイクがミュートされる**(10日目のマイクミュート延長)
- [x] 「マイクを停止」で録音が止まり、ボタンのdisabled状態が正しく切り替わる
- [x] 状態遷移(idle → listening → thinking → speaking → idle)がバッジに正しく反映される
- [x] 📎ボタン:PDF/Word/Excel/PowerPointをアップロードし「[ナレッジ登録] filename(Nチャンク)」が出る
- [x] 📚ボタン:ナレッジ一覧パネルが開き、登録済みファイルの削除ができる
- [x] 📷ボタン:画像を選ぶと送信待ちチップが出て、✕で取り消せる。送信すると①と同じくDEEPで応答が返る
- [x] エラー時(サーバ停止など)に`logError()`のメッセージがログに表示される
- [x] ウィンドウ幅を縮めてもレイアウトが破綻しない(ver2は3カラム構成のため、実運用のウィンドウサイズで確認する)

### 実施手順

②手順4の各段階(CSS移植後 → DOM組み替え後 → 見た目要素追加後)で、それぞれこのチェックリストの主要項目(マイク→書き起こし→送信→再生)を通す。**全部作ってから一度に確認しない**(壊れた箇所の特定が困難になるため)。

### 結果 / 分析 / 改善策

(実施後に記入)

### 残課題

(実施後に記入)

---

## 📌 次のステップ

1. **①画像添付の実機確認を最初にやる**(11日目④-2の残課題の消化。UI移植前の基準値づくりも兼ねる)
2. **②ver2デザインの骨格を`static/index.html`へ移植する**(CSS→DOM→見た目要素の順。JSは原則触らない)
3. **③回帰チェックリストを通し、マイクで話して送信できる状態を確定デザインの上で再現する**
4. 今日やらないものの持ち越し(11日目から継続):
   - **①CLIベンチと実機の乖離・gpt-oss:20bのCUDAクラッシュ**(11日目の最優先課題。FAST出力が実用に耐えないため、UIが整い次第これに戻る)
   - **②Web検索**:SearXNGはネイティブ起動まで完了済み。次は`web_search.py`の実装
   - **③UIの機能化**:チャット履歴の永続化設計 → リネーム/削除/検索、読み上げ速度バー、モデル切替、Web検索トグル
   - **④**:複数枚画像の同時添付、OpenWebUI標準の`content`list形式への対応、画像添付時のVRAM管理強化(いずれもYAGNIで保留中)
5. [[サポートAI作製計画/9日目自前音声UIとストリーミング音声対話.md|9日目]]⑦(Tailscale Serveへの載せ替え・外出時CODEルート方針)は引き続き①⑧完了後


