---
project: C.L.A.I.R.E.(さぽーとAI)
date: 2026-08-12
tags: [音声対話, ウェイクワード, VAD, WebSocket, キーボード入力, UI改善, 作業ログ]
status: ③⑦(常時プレビュー+手動送信方式)は実機確認済み・仕様通り動作。①③'(推論中ミュート・マイク停止ボタン)の実機確認のみ残課題
---

[[サポートAI作製計画/9日目自前音声UIとストリーミング音声対話.md|9日目]]で自前音声UI(`voice_gateway.py` + `static/index.html`)が完成し、外付けHDD接続+VOICEVOX起動という前提条件を理解した上で実機テストを行ったところ、**1つ目の質問には正しく応答するが、2つ目以降で文字化けが発生し認識精度が落ちる**という新たな不具合が判明した。本ノートはこの原因分析と、再発防止のための3つの機能追加(①マイクミュート範囲の拡大、②ウェイクワード対応、③キーボード入力)を計画する。

> [!info] ⑦で設計変更あり
> ①③(+マイク停止ボタン)は当初計画どおり実装した。②のウェイクワード方式は一度実装したものの、実機で使ってみて「無視された発話が消えて分からなくなる」体験の悪さが判明したため、本ノート後半の⑦で撤回し、「音声認識結果を常にテキスト入力欄へリアルタイム表示し、確認・修正してから手動送信する」方式に置き換えた。

> [!note] このノートの位置づけ
> [[サポートAI作製計画/ノート作成規則.md]]に従い、**着手前に作業内容を列挙した計画ノート**として作成した。実行後に「結果」「分析」「改善策」を各セクションへ追記していく。

---

## ⓪ 実機テストで判明した不具合(背景)

### 現象

- 1つ目の質問:正しく認識・応答された
- 2つ目以降の質問:リアルタイムの文字生成(逐次表示)自体は動いていたが、**推論が終わっていないタイミングで話した際に文字化けし、認識精度が悪化した**

### 原因(コードを確認して特定)

[[サポートAI作製計画/scripts/static/index.html]]のマイクミュート制御を確認したところ、**マイクは「音声再生中(`playNext()`実行中)」しかミュートされておらず、`state: thinking`(LLM推論中、まだ音声が1つも届いていない区間)は無防備でマイクが開いたまま**になっていることが判明した。

```
[ユーザー発話終了] → state:speaking送信 → state:thinking送信(run_turn開始)
                                              ↑ ここがミュートされていない
                          → LLM生成 → 文分割 → TTS合成 → 最初のaudioが届く
                                                              ↑ ここで初めてミュート開始
```

つまり「AIが考えている間」に次の発言をすると、その音声がそのままVADに拾われ、確定転写(faster-whisper)にかけられて次のターンとして処理されてしまう。この結果が「推論が終わっていない時に話すと文字化けする」という体感に一致する。

### 対策の方針

1. **①ミュート範囲の技術的な穴を塞ぐ**(根本原因の直接修正)
2. **②ウェイクワード方式を導入**(Hey Siri型。「ねえクレア、〜」と毎回言わない限りコマンドとして処理しない)。①だけでは「ミュートが外れている一瞬に環境音や独り言を拾って誤処理する」余地が残るため、意図的な呼びかけを必須にすることで二重に防御する
3. **③キーボード入力を追加**(認識結果が文字化けした場合に音声に頼らず修正・再送できる独立の入力経路)

---

## ① マイクミュート範囲の拡大(`static/index.html` 修正)

### 背景/目的

⓪で特定した根本原因の直接修正。`state`が`thinking`または`speaking`の間はマイク送信を止め、`listening`/`idle`に戻ったら再開する。

### 作業内容

- [x] `state`の値と再生キューの状態を一元管理する`updateMicMute()`相当の関数を作る(現状の`playNext()`内の`setMicMuted()`呼び出しと、新設する`state`ハンドラの両方から呼べるようにする)
- [x] `handleMessage()`の`"state"`ケースで、`value`が`"thinking"`または`"speaking"`のときミュート、`"listening"`/`"idle"`のとき解除(ただし②のarmed待ちUIとも整合させる)
- [ ] 実機で「2問目を推論中に話しかけても次のターンとして誤処理されない」ことを確認する(⑤へ持ち越し)

### 実施方法

`static/index.html`に`updateMicMute()`を追加し、`isPlaying`(再生キューの状態)と`currentState`(直近の`state`メッセージ)の両方を見て`setMicMuted()`を呼ぶよう一元化した。呼び出し元は次の3箇所:

- `playNext()`(再生開始時/再生キューが空になった時。以前は`setMicMuted(true/false)`を直接呼んでいた)
- `setState()`(サーバから`state`メッセージが届くたび。armed待ちUI(②)は別メッセージ`wake_word`なので競合しない)
- `startMic()`(マイク取得直後の初期化)

```javascript
function updateMicMute() {
  const shouldMute = isPlaying || currentState === "thinking" || currentState === "speaking";
  setMicMuted(shouldMute);
}
```

### 結果

コード変更のみ実施し、実機確認は未実施(⑤へ持ち越し)。ロジック自体は②③のユニットテスト(`python -m unittest discover -s tests`、194件)がグリーンであることは確認済みだが、`static/index.html`のJS部分は自動テスト対象外(9日目⑥と同じ理由でブラウザ実機確認が必要)。

### Before / After

| 項目 | 修正前 | 修正後 | 狙い |
|---|---|---|---|
| ミュートのトリガー | 音声再生中のみ(`playNext()`) | `state: thinking`〜`speaking`の全区間 | 推論中の発話が誤って次ターンとして処理される事故を防ぐ |

---

## ② ウェイクワード対応(`wake_word.py`新規 + `voice_gateway.py`修正)

> [!warning] このセクションは⑦で撤回・削除された
> 一度は実装・ユニットテストまで完了させたが、実際にUIを使ってみたところ「ウェイクワードなしで無視された発話が跡形もなく消える」体験の悪さが判明し、⑦でウェイクワード自動送信の仕組みごと撤回した。`wake_word.py`・`tests/test_wake_word.py`・`voice_gateway.py`の`decide_wake_action()`関連コードは削除済み。以下は経緯の記録として残す。

### 背景/目的

①だけでは「ミュートが外れている待機区間に、独り言や環境音が誤ってコマンドとして処理される」可能性が残る。**Hey Siri方式**(常時マイクONは維持しつつ、ウェイクワードを言うまで発話を無視する)を導入し、意図的な呼びかけを必須にする。

### 方式の確定

| 論点 | 決定 |
|---|---|
| 呼びかけの頻度 | **毎回必須**(Hey Siri方式)。1ターン免除やセッション免除はしない |
| 音声表現 | **日本語発音**(「ねえクレア」「ねぇクレア」等)。③STT実機比較結果④で「クレア」は安定して音写できる一方、英語発音「Hey C.L.A.I.R.E.」はスペルアウトが崩れやすいことが実測済みのため |
| 判定タイミング | VAD区切り後の**確定転写(faster-whisper)テキスト**に対して先頭一致判定 |
| ウェイクワード+コマンドが同一発話内(例:「ねえクレア、天気は?」) | ウェイクワード部分を除去した残りを即コマンドとして処理 |
| ウェイクワードのみで発話が終わった場合(例:「ねえクレア」のみ) | **armed状態**にし、次の発話(タイムアウト10秒)をウェイクワードチェックなしでコマンド扱いにする |
| ウェイクワードが検出できなかった場合 | コマンドとして処理せず破棄。UIに薄く「(聞き取りましたが無視:○○)」と表示する(何も起きない不安を解消するため) |

### 作業内容

- [x] `wake_word.py`(新規)を書く:
  - `WAKE_PHRASES`(「ねえクレア」等の許容パターンのリスト。句読点・空白を正規化してから比較)
  - `match_wake_word(text: str) -> WakeWordResult`(一致なし/一致+残りあり/一致のみ、の3値を返す設計にする)
- [x] `scripts/tests/test_wake_word.py`(新規)を書く:一致パターン・不一致・残りテキストの抽出・表記ゆれ(句読点・スペース)の正規化をテストする
- [x] `voice_gateway.py`を修正し、WebSocket接続ごとに`armed_until`(タイムスタンプ)を保持する
- [x] `on_final(text)`の処理を、①armed中なら即コマンド、②`wake_word.match_wake_word()`の結果で分岐(コマンド化/armed化/破棄)、に組み替える
- [x] 新規WSメッセージ種別を追加する:
  - `{"type": "wake_word", "status": "ignored", "text": str}`
  - `{"type": "wake_word", "status": "armed"}`
- [x] `scripts/tests/test_voice_gateway.py`にarmed状態遷移・タイムアウト・破棄時の挙動のテストを追加する
- [x] `static/index.html`で`wake_word`メッセージを処理し、`ignored`は薄いグレー表示、`armed`は「(ねえクレア…続けてどうぞ)」等のヒント表示にする
- [ ] 実機で「ウェイクワードなしの発話が無視される」「『ねえクレア、〜』が1発話でコマンドとして通る」「『ねえクレア』だけ→間を置いて質問、が通る」の3パターンを確認する(⑤へ持ち越し)

### 実施方法

設計どおり、判定ロジックをWebSocket/FastAPIに依存しない純粋関数として切り出した(9日目`run_turn()`と同じ方針)。

- **`wake_word.py`**: `match_wake_word(text) -> WakeWordResult(kind, remaining)`。`kind`は`"no_match"|"command"|"armed_only"`。表記ゆれ対策として、空白(半角/全角)・読点(`、`,`,`)・句点(`。`)を除いた正規化文字列で先頭一致だけ判定し、一致後の残りテキストは**元の文字列からそのままスライス**する(内部の句読点を壊さないように、正規化はインデックス対応表を作ってマッチング境界を探すためだけに使う設計)。
- **`voice_gateway.py`**: `decide_wake_action(text, armed_until, *, now=None) -> WakeDecision(action, command_text, armed_until)`をWebSocketから切り離した純粋関数として追加。`ws_endpoint`内の`on_final()`はこの関数の結果をWSメッセージ送出とarmed状態更新に反映するだけにした。armedは1回消費すると解除される(`armed_until=0.0`に戻る)。
- **`static/index.html`**: `handleMessage()`に`"wake_word"`ケースを追加。`ignored`は`.turn.ignored`(薄いグレー・小さめフォント)、`armed`は`.turn.armed`(アクセントカラー・斜体)で表示。

### 結果

`python -m unittest tests.test_wake_word tests.test_voice_gateway -v`で以下を確認(抜粋):

- `test_wake_word.py`: 一致(通常/表記ゆれ)・不一致・armed_only・残りテキスト抽出の14テストすべてパス
- `test_voice_gateway.py`の`TestDecideWakeAction`: command即時ディスパッチ・armed開始(タイムアウト付与)・無視・armed中の即時通過(判定バイパス)・armedタイムアウト後の通常判定復帰、の6テストすべてパス

実機確認(UIでの「ねえクレア」呼びかけ・armed表示・無視表示)は未実施。⑤へ持ち越し。

### WSメッセージ仕様(追加分)

```
{"type": "wake_word", "status": "ignored", "text": str}   # ウェイクワードなしで破棄
{"type": "wake_word", "status": "armed"}                   # ウェイクワードのみ検出、次発話待ち(10秒)
```

### 残課題(実装前に想定されるもの)

- ウェイクワード候補の言い回し(「ねえクレア」「ねぇクレア」「クレアさん」等)は実機での聞き取られ方を見て調整が必要になる可能性がある
- armed状態のタイムアウト値(暫定10秒)は実機の体感で調整する

---

## ③ キーボード入力(`static/index.html`修正 + `voice_gateway.py`修正)

### 背景/目的

音声認識が文字化けした場合に、音声に頼らずキーボードで修正・再送できる経路を用意する。ウェイクワード判定はバイパスする(ボタン操作自体が既に明示的な意思表示のため)。

### 作業内容

- [x] `static/index.html`のフッターに常時表示のテキスト入力欄+送信ボタンを追加する
- [x] `state`が`thinking`/`speaking`の間は送信不可にする(ターンの排他制御を音声と共通化する)
- [x] 送信時、WebSocketでJSON `{"type": "text_input", "text": str}` をサーバへ送る(既存はバイナリ音声フレームのみ受け付けていたため、テキストフレームの受信分岐を追加する)
- [x] `voice_gateway.py`のWS受信ループに、`message.get("text")`がある場合の分岐を追加し、`text_input`を受け取ったら`_handle_final_transcript()`相当の処理へ**ウェイクワード判定なしで**直接回す
- [x] 送信したテキストを他のユーザー発話と同じ見た目(`turn user`)でログに表示する
- [x] `scripts/tests/test_voice_gateway.py`に`text_input`経路のテストを追加する(ウェイクワード判定をスキップして直接コマンド処理されること)※実施方法参照。armed/ignoredと同じ理由でWebSocket自体の統合テストは追加せず、`decide_wake_action()`を経由しない設計であることをコードで保証する方針にとどめた
- [ ] 実機で「文字化けした音声認識結果を、キーボードで打ち直して送信できる」ことを確認する(⑤へ持ち越し)

### 実施方法

- **`static/index.html`**: フッターを2段構成にし(既存のマイク操作行はそのまま)、2段目に`#text-input`(テキスト入力欄)+`#send-btn`(送信ボタン)を追加した。`Enter`キー押下(IME変換中(`isComposing`)は除く)でも送信できる。`canSendText()`が`ws`接続中かつ`currentState`が`thinking`/`speaking`でないことを見て、`setState()`が呼ばれるたびに`updateTextInputEnabled()`で入力欄/ボタンの`disabled`を更新する(音声のマイクミュート判定(①)と同じ`currentState`を参照する一元管理)。
- **`voice_gateway.py`**: WS受信ループの`message.get("bytes")`が`None`のケースに`message.get("text")`の分岐を追加し、JSON parse後`type=="text_input"`なら`_handle_final_transcript()`へ直接`await`で回す。⑦の設計変更後は、これが**AIへコマンドを渡す唯一の経路**になっている(音声のSTT結果は⑦により`_handle_final_transcript()`を経由せず、常にテキスト入力欄への反映止まりになったため)。

### 結果

`text_input`経路が`_handle_final_transcript()`へ直接渡る構造になっていることをコードで確認済み。この経路自体(実際のWebSocket送受信)を検証する自動テストは今回追加していない(WebSocket自体の統合テストは9日目⑥から一貫して未整備で実機確認任せという既存方針を踏襲。`run_turn()`のユニットテストで内部ロジックはカバー済み)。実機での送受信確認は⑤へ持ち越し。

### WSメッセージ仕様(追加分)

```
# クライアント → サーバ(新規。既存はバイナリ音声フレームのみだった)
{"type": "text_input", "text": str}
```

---

## ③' マイクの停止ボタン(`static/index.html`修正・当初計画外の追加分)

### 背景/目的

本ノートの元の計画にはなかったが、実装依頼時に「マイクの停止ボタンを追加してほしい」との要望があったため、③と合わせて`static/index.html`に追加した。ウェイクワード必須化(②)でマイクは常時ONのままにする設計だが、誤動作・離席時などにローカルのマイクキャプチャそのものを止められる手段がなかったため。

### 作業内容

- [x] フッターに「マイクを停止」ボタン(`#stop-btn`)を追加する(マイク未取得時は`disabled`)
- [x] `stopMic()`:`micProcessor`/`micSource`の切断、`micStream`の各トラックの`stop()`を行い、マイクインジケータをOFF表示にする(WebSocket接続自体は維持し、サーバ側セッションは切らない)
- [x] 「マイクを開始」ボタンは再クリックで既存のWebSocket接続を再利用しつつ`startMic()`をやり直せるようにする(意図的切断でない限り再接続する既存ロジックと衝突しないよう`intentionalClose`はいじらない)

![[Pasted image 20260812013557.png]]

### 結果

コード変更のみ実施。実機での動作確認(停止ボタンでマイクランプが消え、送信も止まること)は⑤へ持ち越し。

---

## ④ 成果物一覧(実行後に確定。⑦の設計変更後の最終状態)

### プログラムファイル

| ファイル | 役割 | 実行方法 | 出力先 |
|---|---|---|---|
| [[サポートAI作製計画/scripts/voice_gateway.py]](修正) | ①state:thinking/speaking中のマイクミュート判定は変更なし(ブラウザ側の担当) / ③⑦テキスト入力欄からのtext_inputのみをAIへの唯一のコマンド化経路にする | `python voice_gateway.py --host 127.0.0.1 --port 5055` | なし(常駐サーバー) |
| [[サポートAI作製計画/scripts/static/index.html]](修正) | ①ミュート範囲の統合(`updateMicMute()`) / ③⑦音声認識結果(partial/final)をテキスト入力欄へリアルタイム反映+手動送信 / ③'マイク停止ボタン | `voice_gateway.py`が配信 | なし |
| [[サポートAI作製計画/scripts/tests/test_voice_gateway.py]](修正) | `run_turn()`のユニットテスト(既存分)。②で追加した`decide_wake_action()`関連テストは⑦で削除済み | `python -m unittest tests.test_voice_gateway -v` | 標準出力のみ |

`scripts/wake_word.py`・`scripts/tests/test_wake_word.py`は②実装時に新規作成したが、⑦の設計変更で不要になったため**削除済み**(git履歴には残る)。

---

## ⑤ 動作確認手順(ユニットテストのみ実施済み。実機確認は未実施)

- [ ] ①:2問目を推論中(state: thinking)に話しかけても、次のターンとして誤処理されないことを実機確認した
- [x] ③⑦:話している内容がテキスト入力欄にリアルタイムで書き起こされることを確認した(2026-08-12実機確認。「明日は何月何日かなぁ」等が入力欄にそのまま書き起こされた)
- [x] ③⑦:書き起こされた内容を送信ボタン/Enterで送れることを確認した(2026-08-12実機確認)
- [x] ③⑦:送信した内容が画面上部のログに自分の発言として表示されることを確認した(2026-08-12実機確認。「こんにちは」「C.L.A.I.R.E.、キミの声は誰のものだい?」が画面右上のユーザー吹き出しとして表示され、AIの応答も正常に返ってきた)
- [ ] ③:送信中(state: thinking/speaking)はテキスト入力欄からの送信がブロックされることを確認した
- [x] 新規ユニットテスト(`test_voice_gateway.py`)がすべてパスすることを確認した(`python -m unittest discover -s tests -v` で174件オールグリーン。②のウェイクワード関連20件は⑦で削除)
- [ ] ③':マイクの停止ボタンでマイク入力が止まり、再度「マイクを開始」で再開できることを実機確認した

(②のウェイクワード関連の確認項目は⑦で機能自体を撤回したため削除した)

### ⑦ 実機確認結果(2026-08-12)

「明日は何月何日かなぁ」「C.L.A.I.R.E.、キミの声は誰のものだい?」等の発話が、話している内容そのままテキスト入力欄にリアルタイムで書き起こされ、送信ボタンを押すと画面右上にユーザー発言として表示され、AIの応答(`[route: FAST]`のVOICEVOX音声付き回答)が正常に返ってくることを確認した。⑦で設計変更した「常時プレビュー+手動送信」方式は**仕様通りに動作**しており、②のウェイクワード方式で発生していた「無視されて消える」問題も、そもそも自動判定をしない設計にしたことで解消された。

---

## ⑥ 残課題・次回への持ち越し(記入用)

- ③⑦(常時プレビュー+手動送信方式)は2026-08-12に実機確認済み。仕様通り動作しており、②で問題だった「無視されて消える」体験は解消された。
- ①(推論中のミュート延長)・③'(マイクの停止ボタン)は未確認のまま残っている。次回実機で消化する。
- `text_input`経路(③)はWebSocketの統合テストを追加しておらず、コード構造で経路を保証するにとどめている。9日目⑥から一貫した既存方針だが、実機確認で問題が出た場合はテスト整備も検討する。
- ⑦で「partial_transcript/final_transcriptが届くたびテキスト入力欄を無条件で上書きする」設計にしたため、ユーザーが手動で編集中に次の発話(独り言・環境音の誤認識等)が入ると編集内容が上書きされてしまう可能性がある。今回の実機確認では問題にならなかったが、頻発するようならフォーカス中は上書きを抑制する等の対策を検討する(⑦「実施方法」に記載したトレードオフ)。

---

## ⑦ 設計変更:ウェイクワード自動送信を撤回し、常時プレビュー+手動送信方式へ(`static/index.html`修正 + `voice_gateway.py`修正)

### 背景/目的

①③実装後、実機でUIを操作した際のスクリーンショットをもとにユーザーからフィードバックを受けた。②で実装したウェイクワード方式では、ウェイクワードなしの発話は「(聞き取りましたが無視: ○○)」と画面上部に薄く表示されるだけで消えてしまい、**せっかく音声認識できていたテキストがそのまま失われる**体験の悪さがあった。実際のログでも「こんにちは」「ねえ、C.L.A.I.R.。こんにちは。」等、ウェイクワードの言い回しがうまく通らず無視され続けるケースが多く見られた。

この課題を受け、「ウェイクワードで自動判定する」のではなく「**音声認識結果は常にテキスト入力欄へリアルタイムに書き起こし、ユーザーが目で見て確認・修正してから明示的に送信する**」という、より確実な方式に設計変更した。これにより②のウェイクワード機構は不要になり撤回した。

### 方式の確定

| 論点 | 決定 |
|---|---|
| ウェイクワード自動送信 | **廃止**。「ねえクレア」を言っても自動ではコマンド化されなくなった |
| 音声認識結果(暫定・確定とも)の扱い | AIへは渡さず、常にテキスト入力欄(`#text-input`)へリアルタイムに反映するだけ |
| 話している途中(部分認識)の反映 | リアルタイムに反映する(発話が終わるのを待たない) |
| AIへの送信タイミング | ユーザーがテキスト入力欄の内容を確認・修正し、送信ボタン/Enterを押した時のみ(音声由来・キーボード入力由来を区別しない) |
| 画面上部のログへの表示 | 送信が実際に受理され、サーバが処理を開始した時のみ(`final_transcript`メッセージ) |

### 作業内容

- [x] `voice_gateway.py`:`on_final()`が`decide_wake_action()`を呼ばず、STT確定結果をそのまま`{"type": "partial_transcript", "text": text}`として送るだけに変更(AIへの自動ディスパッチを廃止)
- [x] `voice_gateway.py`:`decide_wake_action()`・`WakeDecision`・`WAKE_ARMED_TIMEOUT_SECONDS`・`wake_word`のimportを削除
- [x] `scripts/wake_word.py`・`scripts/tests/test_wake_word.py`を削除
- [x] `tests/test_voice_gateway.py`から`TestDecideWakeAction`とその依存importを削除
- [x] `static/index.html`:`handleMessage()`の`"partial_transcript"`ケースを、ログへの吹き出し表示からテキスト入力欄(`#text-input`)への反映に変更
- [x] `static/index.html`:`handleMessage()`の`"final_transcript"`ケースを「送信が受理された時だけログに表示する」に変更(STT側の確定結果表示ではなくなった)
- [x] `static/index.html`:`"wake_word"`ケースの処理・`.turn.ignored`/`.turn.armed`のCSSを削除
- [x] `static/index.html`:テキスト入力欄のplaceholderを「話すとここに書き起こされます。間違いを直して送信(Enter)」に変更
- [ ] 実機で「話した内容がリアルタイムに入力欄へ表示される」「間違いを直して送信すると画面上部に自分の発言として表示される」ことを確認する(⑤へ持ち越し)

### 実施方法

`voice_gateway.py`側は`on_final(text)`を1行に単純化し、`{"type": "partial_transcript", ...}`を送るだけにした(以前は`decide_wake_action()`の結果でコマンド化/armed化/破棄に分岐していた)。これにより、AIへの処理開始(`run_turn()`の呼び出し)は`text_input`メッセージ経由(③のキーボード送信ハンドラ)からしか起こらなくなり、**音声・キーボードどちらの入力でも「テキスト入力欄での確認 → 送信」という単一の確定経路**に統一された。

`static/index.html`側は、`partial_transcript`/`final_transcript`のどちらも受け取り次第`textInput.value`を書き換えるだけにし(区別する必要がなくなったため両方とも同じ扱い)、`final_transcript`という名前は「サーバが実際に処理を開始した(=送信が受理された)」ことの通知に意味が変わった(サーバ側で`_handle_final_transcript()`が送るタイミングが、STT確定時から送信受理時に移ったことに対応)。

### 結果

`python -m unittest discover -s tests -v` で174件オールグリーン(②のウェイクワード関連20件を削除した後の件数)。`voice_gateway.py`・`tests/test_voice_gateway.py`の構文チェック(`ast.parse`)、`static/index.html`内`<script>`のJS構文チェック(Node.jsの`new Function()`)もパス。実機での動作確認(リアルタイム書き起こし・修正・送信・ログ表示)は未実施で⑤へ持ち越し。

---

## 📌 次のステップ

1. ~~本ノートの①〜③を実装する~~ → 完了(コード実装・ユニットテスト174件パス)
2. ~~②ウェイクワード方式を実装する~~ → ⑦で撤回。常時プレビュー+手動送信方式に置き換え済み
3. 実機で⑤の動作確認手順を消化する(①③③'⑦すべて未実施。次回の最優先タスク)
4. 完了後、[[サポートAI作製計画/9日目自前音声UIとストリーミング音声対話.md|9日目]]⑦(Tailscale Serveへの載せ替え)・⑧(エンドツーエンド遅延実測)へ戻る
