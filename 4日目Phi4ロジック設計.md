[[サポートAI作製計画/3日目モデル切り替え・実コードテスト・振り分けロジック実装.md|3日目]]で①モデル切り替え検証・②Devstral実コードテストが完了した。積み残していた「Phi-4-miniの振り分けロジック実装」を今回で進める。

---

## ① Phi-4-miniの振り分けロジック実装(3日目から移設・参考/②で置き換え済み)

> [!info] ①と②の関係(混同注意)
> - ①は3日目からの積み残しタスクを**そのまま転記した初期案**(3分類: gemma4/gpt-oss/devstral)。
> - ②はクラウドのClaudeと壁打ちした結果、**分類方式を4分類(FAST/DEEP/CODE/CLARIFY)に変更した最終版**。
> - つまり①の「分類プロンプト設計」部分は②で**上書き・置き換え**されており、①をそのまま実装する必要は**ない**。
> - 一方、①にあった「ルータースクリプトの実装形態」「対象モデル未起動時の自動起動・切り替え」は②のタスクリストに明記されていなかったため、下記②のタスクに統合済み。
> - **結論: 実際に着手するのは②の「本日のタスク」のみでOK。①はそのまま実装せず、経緯として残してある。**

[[サポートAI作製計画/1日目設計とモデル選定.md|1日目]]の設計にある「リーダーエージェント(ルーター)」を実装する。2日目で`num_gpu 0`によるCPU固定化・`keep_alive -1`での無期限常駐まで確認済みなので、その上に判定ロジックを構築する。

### 作業内容

- [ ] 分類プロンプトの設計
  - 入力: ユーザーの質問文
  - 出力: `gemma4` / `gpt-oss` / `devstral` のいずれか1つ(+ 判定理由を任意で出力させ、デバッグしやすくする)
  - 例:
    ```
    あなたは質問を適切なAIモデルに振り分けるルーターです。
    以下のカテゴリから最も適切なものを1つだけ、他の文字を含めずに出力してください。

    - gemma4: 研究計画・生活相談・複数ステップの論理的な相談
    - gpt-oss: 雑談・即答が必要な質問・簡単な数学
    - devstral: プログラミング・コードのデバッグ・実装相談

    質問: {ここにユーザーの質問}
    回答:
    ```
- [ ] テスト用の質問セットを用意(各カテゴリ5問程度、判定が微妙な境界例も含める)
  - [ ] 明確にカテゴリが分かる質問(3カテゴリ×数問)
  - [ ] 複数カテゴリにまたがりそうな曖昧な質問(例:「このコードのアルゴリズムを勉強計画に組み込みたい」)
- [ ] 上記テストセットをPhi-4-miniに投げ、意図通りに分類できるかを記録(正答率を算出)
- [ ] 誤判定が多いカテゴリがあれば、プロンプトの表現・カテゴリ説明文を調整して再テスト
- [ ] 実装形態を決める
  - [ ] Python側でOllama APIを叩き、`keep_alive: -1`を付けてPhi-4-miniに判定させ、結果に応じて対象モデルを`ollama run`(または該当APIコール)する簡易ルータースクリプトを作成
  - [ ] 対象モデル未起動時の自動起動、既に別モデルが起動中の場合の`ollama stop`→切り替えも組み込む(3日目のスワップ検証結果を踏まえた待ち時間を考慮)
- [ ] 誤判定時のフォールバック(分類に失敗した場合のデフォルトモデル、再質問するかなど)を決める

### 完了条件

- [ ] テストセットに対する分類正答率が一定水準(目安: 8割以上)に達している
- [ ] ルータースクリプトが「質問入力 → 判定 → 該当モデル呼び出し → 応答取得」まで一気通貫で動作する
- [ ] 3日目のスワップ検証結果を踏まえ、体感の待ち時間が実運用で許容範囲に収まっている

---

## ② 本日の具体的な作業内容(クラウドのClaudeでのプロンプト設計検討を踏まえて・★実際に着手するのはこちら★)

上記①の「分類プロンプトの設計」を、クラウドのClaudeと壁打ちした内容(下記「検討内容の要約」参照)をベースに具体化して実装する。3分類(gemma4/gpt-oss/devstral)ではなく、**4分類(FAST/DEEP/CODE/CLARIFY)**へ設計変更し、ルールベースの事前フィルタも組み込む方針。

### 検討内容の要約

- 分類軸は「タスク種別」と「明示的指定」の2つ。4モデル構成に合わせてルーターの出力は以下4値に統一する。

| ルート       | 対応モデル                | 用途                           |
| --------- | -------------------- | ---------------------------- |
| `FAST`    | gpt-oss-20b          | 検索・雑談・簡易QA・単純計算              |
| `DEEP`    | Gemma4 26B-A4B       | 計画立案・スケジュール調整・プレゼン構成・複雑な指摘対応 |
| `CODE`    | Devstral Small 2 24B | コード生成・修正・レビュー                |
| `CLARIFY` | Phi-4-mini自身が返答      | 曖昧すぎて分類不能な場合の聞き返し            |

- Phi-4-miniのLLM判定の前段に、正規表現/キーワードマッチによる**ルールベース事前フィルタ**を挟む(「コード書いて」等の明示語は問答無用でCODE確定。VRAM 16GB制約上、誤って重いモデルをロードし直すレイテンシを避けるため)。
- Phi-4-mini(3.8B)への指示は、**理由づけを省略しJSON1行のみ出力**させる形式にする(CoTさせるとブレる)。出力形式: `{"route": "FAST" | "DEEP" | "CODE" | "CLARIFY"}`。
- Open WebUI側は「手動モード(ドロップダウンでモデル直接選択、ルーター完全バイパス)」と「自動モード(`さぽーとAI (Auto)`仮想モデル、Pipe機能でルーティング処理を挟む)」の2モードで共存させる。
- エッジケース2点への対応方針:
  - **会話継続中の切り替え**: 同一スレッド内は直近のrouteを保持し、毎ターン再分類しない(モデルスワップの頻発を防止)。
  - **複合タスク**(例: 「バグ修正しつつ開発スケジュールも整理して」): 初期実装では優先度ルール(CODE > DEEP > FAST)で単一ルートに倒す。タスク分割対応は将来検討。

### 本日のタスク

- [x] 分類プロンプトを3分類→4分類(FAST/DEEP/CODE/CLARIFY)に書き直す
  - [x] システムプロンプト本文をクラウドでの検討内容(上記JSON1行出力形式)をベースに確定し、`scripts/prompts/`配下に保存
  - [x] 各カテゴリの判断基準・判断のヒント(「〜を踏まえて」→DEEP 等)をプロンプトに反映
- [x] ルールベース事前フィルタを実装
  - [x] `CODE_TRIGGERS`(「コード書いて」「実装して」「デバッグして」「def 」「class 」```` ``` ````等)のキーワードリストを作成
  - [x] フィルタ一致時はPhi-4-mini判定をスキップして即`CODE`確定するロジックをルータースクリプトに追加
- [x] ルータースクリプト本体を実装(①から統合): PythonからOllama APIを叩き、`keep_alive: -1`でPhi-4-miniに判定させ、結果に応じて対象モデルを呼び出す
  - [x] 対象モデル未起動時の自動起動、既に別モデルが起動中の場合の`ollama stop`→切り替えを組み込む(3日目のスワップ検証結果を踏まえた待ち時間を考慮)
- [x] 4分類版のテスト用質問セットを用意(FAST/DEEP/CODE各5問程度+CLARIFY相当の曖昧な質問+境界例)
- [x] Phi-4-miniにテストセットを投げ、JSON1行が安定して返るか・分類精度を記録
  - [x] JSON形式から外れる出力が出た場合のパース処理(フォールバック)を実装
- [x] 会話継続時のroute保持ロジック(直近routeをセッション状態として保持し、文脈依存の発言では再分類しない)を実装
- [x] 複合タスクの優先度ルール(CODE > DEEP > FAST)を実装
- [x] CLARIFYルート時、Phi-4-mini自身が聞き返し文を生成して返す処理を実装
- [x] Open WebUIのPipe機能側の実装方法を確認し、`さぽーとAI (Auto)`仮想モデルとしてルーティング処理を組み込めるか検証(手動モード/自動モードの共存含む)(詳細は下記「⑦ Open WebUI Pipe連携の実装」参照。**ただしOpen WebUI自体が未導入のため、UI経由の実動作確認は次回に持ち越し**)

### 完了条件(本日分)

- [x] 4分類(FAST/DEEP/CODE/CLARIFY)のルーター実装が「質問入力 → (事前フィルタ) → Phi-4-mini判定 → 該当モデル呼び出し」まで一気通貫で動作する
- [x] テストセットに対する分類正答率を記録し、誤分類の傾向をログから確認できる状態にする(詳細は下記「③ 実装の成果物一覧・出力先まとめ」参照)
- [x] 会話継続時に不要なモデルスワップが起きないことを確認する(実機での通し確認完了。詳細は下記「⑤ 会話継続時のモデルスワップ実機検証」参照。ただしCODEトリガー正規表現のカバレッジ不足という新規課題を発見)

---

## ③ 実装の成果物一覧・出力先まとめ

②の作業で作成したファイルが増えてきたため、「何が」「どこに」「何を出力するか」を一覧化する。

### プログラムファイル(`scripts/`直下)

| ファイル | 役割 | 実行方法 | 出力先 |
|---|---|---|---|
| [[サポートAI作製計画/scripts/router_rules.py]] | `CODE_TRIGGERS`正規表現によるルールベース事前フィルタ。`match_rule_based(text)`が「コード書いて」「正規表現」等に一致すれば`"CODE"`を返す | 単体実行はしない(router.py/testからimportして使う部品) | なし(関数呼び出しのみ) |
| [[サポートAI作製計画/scripts/ollama_client.py]] | Ollama REST API(`/api/generate`, `/api/ps`)への薄いラッパー | 単体実行はしない(router.pyから使う部品) | なし |
| [[サポートAI作製計画/scripts/router.py]] | ルーター本体。CLIとして直接実行できる | `python router.py "質問文"` | **標準出力のみ**(`[route] ...`+モデルの実際の回答)。**ファイルには何も保存されない**点に注意 |
| [[サポートAI作製計画/scripts/run_testset.py]] | `testset_v1.md`の20問をrouter.classify_route()に通し、分類正答率を測定するテストハーネス | `python run_testset.py`(オプション:`--label <名前>`, `--system-prompt <プロンプトファイル>`) | `results/Router Classification/summary.csv`(実行ごとに1行追記)+`results/Router Classification/<label>_<timestamp>_detail.csv`(1問1行の詳細ログ) |
| `tests/test_router_rules.py` | `router_rules.py`のユニットテスト。当初6件だったが、⑥で`CODE_TRIGGERS`の口語表現対応を追加した際に9件追加し**計15件** | `python -m unittest tests.test_router_rules -v` | なし(コンソール出力のみ、`unittest`はファイルを残さない) |
| `tests/test_router.py` | `router.py`の分類ロジック・セッション管理のユニットテスト(12件、Ollama呼び出しはフェイク関数で代替。ノートに旧「13件」と誤記していたが実カウントは12件) | `python -m unittest tests.test_router -v` | なし(同上) |
| [[サポートAI作製計画/scripts/verify_conversation_swap.py]] | ⑤で作成。1つの`RouterSession`に複数ターンの会話を投げ、実際のOllamaで「Phi4呼び出しの有無」「`ollama ps`上のモデルスワップ有無」を記録する実機検証スクリプト | `python verify_conversation_swap.py` | 標準出力のみ(ファイル保存なし) |
| [[サポートAI作製計画/scripts/openwebui_pipe/support_ai_auto_pipe.py]] | ⑦で作成。Open WebUIのFunctions(Pipe)として読み込ませる想定の「さぽーとAI (Auto)」仮想モデル本体。`router.py`をimportしてチャットごとにセッションを保持しながらFAST/DEEP/CODE/CLARIFYへ振り分ける | Open WebUI管理画面のFunctionsに貼り付けて使う想定(このPCにはOpen WebUI自体が未導入のため、実機でのUI経由テストは未実施。詳細はファイル冒頭のdocstring・下記⑦参照) | なし |
| `tests/test_support_ai_auto_pipe.py` | `support_ai_auto_pipe.py`のユニットテスト(8件、Ollama呼び出しはフェイク関数で代替) | `python -m unittest tests.test_support_ai_auto_pipe -v` | なし |
| [[サポートAI作製計画/scripts/smoke_test_pipe.py]] | ⑦で作成。Open WebUIを介さず`Pipe`クラスを直接呼び出し、実際のOllamaで最後まで動くかを確認するスモークテスト | `python smoke_test_pipe.py` | 標準出力のみ(ファイル保存なし) |

### プロンプト・テキスト・Markdownファイル(`scripts/prompts/router_classification/`)

| ファイル | 役割 |
|---|---|
| `system_prompt_v1.txt` | 初回版の分類システムプロンプント。CLARIFYの誤判定が多かった(下記参照) |
| `system_prompt_v2.txt` | CLARIFY誤判定を踏まえてfew-shot例を追加した改訂版。CLARIFYは直ったがFAST/DEEPの一部がCLARIFYに巻き込まれる副作用があった(詳細は下記④参照) |
| `system_prompt_v3.txt` | CODEヒントを「依頼動詞+言語名/関数名」に限定し、FAST/DEEP側に反例few-shotを追加した改訂版。**現在`router.py`のデフォルト**(ユーザーが手動で切り替え済み)。X4型(「〜アルゴリズムを勉強計画に組み込みたい」)がCODEに誤判定される揺れが1回観測されたため、テスト文とは異なる表現(「A*アルゴリズムを勉強計画に組み込みたい」)の反例few-shotを追加で組み込み、以降の再実行で改善を確認済み |
| `testset_v1.md` | FAST/DEEP/CODE各5問+CLARIFY・境界例5問(計20問)のテストセットと、テスト実施記録(手動確認時のメモ+v1/v2/v3の正答率サマリ) |

### 実装計画書

| ファイル                                                             | 役割                                              |
| ---------------------------------------------------------------- | ----------------------------------------------- |
| [[サポートAI作製計画/scripts/plans/2026-08-04-router-implementation.md]] | ルータースクリプト実装時に使ったタスク分解済み実装プラン(TDD形式)。実施結果メモも追記済み |

### ⚠️ 「全問テストで18/20だった」という結果はどこにあったか

**結論: どこにも保存されていませんでした。** `run_testset.py`は最初のバージョンでは結果をコンソール(ターミナル)に出力するだけで、ファイル保存機能がありませんでした。18/20という数字は、Claudeが実行結果を見て`testset_v1.md`に手動で書き写した「要約」であり、20問それぞれの生ログはセッション終了とともに消えていました。

この状態は下記のとおり修正済みです:

- `run_testset.py`に`monitor_ollama.py`と同じ方式のCSV自動保存機能を追加した
- 保存先: `サポートAI作製計画/scripts/results/Router Classification/`
  - `summary.csv`: 実行1回につき1行(タイムスタンプ・ラベル・使用プロンプト・全体正答率・カテゴリ別正答率)を追記
  - `<label>_<timestamp>_detail.csv`: その回の20問それぞれの「質問・期待ルート・実際のルート・正誤・所要秒数」
- 修正後、v1プロンプトとv2プロンプトを実際に**再実行して記録を作り直した**。結果は以下(再実行のため、最初にチャットで見せた15/20・18/20とは数値がわずかに異なる点に注意。理由は次項参照):

| ラベル | 全体正答率 | detail_csv |
|---|---|---|
| system_prompt_v1 | 16/20 (80.0%) | `system_prompt_v1_20260804_221848_detail.csv` |
| system_prompt_v2 | 17/20 (85.0%) | `system_prompt_v2_20260804_221942_detail.csv` |

**数値が前回と微妙に違う理由(重要・LLM分類の非決定性)**:
1. v1の16/20(旧: 15/20)は、`router_rules.py`のCODE_TRIGGERSを先にv2用に修正済みだったため、v1プロンプトでもC4(正規表現デバッグ)がルールベースで先に正解してしまったことによる差(プロンプト自体の実力とは無関係)。
2. v2の17/20(旧: 18/20)は、**Phi-4-miniの出力が毎回完全に同じではない**(サンプリングに揺らぎがある)ため、同じ質問・同じプロンプトでも実行ごとに1〜2問結果が変わりうることが実際に確認された。今回はD5(「副業と本業の両立〜」)が新たにCLARIFYに誤判定された。

→ **今後、正答率を厳密に比較したい場合は複数回(例:3回)実行して平均を取ることを推奨。** `run_testset.py`は`--label`で実行ごとにラベルを変えられるので、`summary.csv`に複数回分の履歴が溜まっていく。

### 手動で全問テストを再現する方法(PowerShell)

Ollamaサーバーが起動していれば(`ollama ps`で確認可能)、以下だけで再現できる。事前にPhi-4-miniを個別に起動しておく必要はない(`keep_alive: -1`で自動ロードされる)。

```powershell
cd "C:\Users\gakuh\Documents\obsidian\サポートAI作製計画\scripts"

# デフォルト(system_prompt_v2.txt)で実行し、結果をresults\Router Classification\に保存
python run_testset.py

# プロンプトを明示的に指定して実行(v1との比較など)
python run_testset.py --system-prompt "prompts\router_classification\system_prompt_v1.txt" --label system_prompt_v1

# 同じ設定で複数回実行し、ブレを確認したい場合はlabelを変えて繰り返す
python run_testset.py --label system_prompt_v2_run2
python run_testset.py --label system_prompt_v2_run3
```

実行後、`results\Router Classification\summary.csv` をExcel等で開けば全実行の正答率推移を比較できる。個別の誤判定内容を見たい場合は、対応する `<label>_<timestamp>_detail.csv` を開く。

なお、ユニットテスト(`tests/test_router.py`等)は実際のOllama通信をフェイク関数に差し替えているため、Ollamaサーバーの起動有無に関わらずいつでも実行可能(高速・無料)。分類ロジック自体の正しさ(JSON解析・フォールバック・セッション保持等)を確認したいだけならこちらで十分:

```powershell
python -m unittest tests.test_router -v
python -m unittest tests.test_router_rules -v
```

---

## ④ ユニットテスト2種の説明 & v1→v2でFAST/DEEPが悪化した原因分析

### `python -m unittest tests.test_router -v` / `tests.test_router_rules -v` は何をするコマンドか

どちらも**Ollamaを一切呼ばない、ロジックだけの自動テスト**。「Phi-4-miniの分類が賢いか」ではなく、「router.py/router_rules.pyのコードが仕様通りに動くか」を毎回同じ入力・同じ期待値で機械的に確認するもの。`run_testset.py`(実際にPhi-4-miniへ20問投げる方)とは目的が別で、こちらは**高速・無料・何度実行しても結果が変わらない**(LLMを介さないため)。

| コマンド | 対象ファイル | 何をテストしているか | 実行結果(2026-08-04時点) |
|---|---|---|---|
| `python -m unittest tests.test_router_rules -v` | `router_rules.py` | ルールベース事前フィルタ`match_rule_based()`の正規表現マッチが正しいか。7件: 「コード書いて」等のキーワード一致→CODE、`def `/`class `/コードフェンス一致→CODE、無関係な文への非一致→None、複合タスク文でCODEが優先されるか、正規表現デバッグ文の一致 | **7 tests OK**(全件成功) |
| `python -m unittest tests.test_router -v` | `router.py` | ①LLM応答のJSONパース処理(正常JSON/空白混じり/壊れたJSONの正規表現復旧/不正な値へのフォールバック/ゴミ文字列へのフォールバック)、②`classify_route()`がルールベース一致時にLLMを呼ばずショートカットするか・一致しない時だけLLMを呼ぶか、③`RouterSession`の会話継続時route保持ロジック(1ターン目はLLM分類、2ターン目は前回routeを再利用してLLMを呼ばない、CODEトリガーが出たらセッションのrouteを上書き、セッション同士が独立、resetで状態がクリアされる)。Ollama呼び出し部分はフェイク関数で差し替えているので、Ollamaサーバー未起動でも実行可能 | **12 tests OK**(全件成功。ノート本文には「13件」と記載していたが実際にカウントすると12件だったので、この場を借りて訂正) |

→ 両方ともOK(成功)。つまり「Phi-4-miniの分類精度」ではなく「その前後を支えるPythonコード(JSON解析・フォールバック・セッション保持・ルールベースフィルタ)にバグがないこと」が保証された状態、という意味。ここが壊れていなければ、`run_testset.py`で観測される誤判定は**プロンプト(Phi-4-miniへの指示文)側の問題**だと切り分けられる。

### なぜv2はCLARIFYを直せたのにFAST/DEEPが悪化したのか

`results/Router Classification/summary.csv`に残っている実測値(v1×1回、v2×4回)で比較すると、傾向は再現性がある。

| ラベル | FAST | DEEP | CODE | CLARIFY |
|---|---|---|---|---|
| v1 | 5/5 | 5/5 | 6/6 | **0/4** |
| v2(4回平均) | **2.5/5** | **4.25/5** | 6/6 | **4/4** |

4回のdetail_csvを突き合わせると、間違え方はほぼ固定パターンで、ランダムなブレではなくプロンプトの構造的な問題だとわかる。

- **F3「おすすめのラーメン屋のジャンルを3つ挙げて」(FAST)→ 4回中4回CLARIFYに誤判定**
- **D5「副業と本業の両立が難しくなってきた。優先順位の付け方を一緒に整理して」(DEEP)→ 4回中3回CLARIFYに誤判定**
- **F4「Pythonのlen()関数って何をするものだっけ?」(FAST)→ 4回中4回CODEに誤判定**
- F5「最近疲れてるんだけど、なんか一言励まして」(FAST)→ 4回中2回CLARIFYに誤判定(ブレあり)

原因は大きく2つ。

**① CLARIFY寄りのfew-shot例だけを追加した「片手落ちの修正」(FAST/DEEPの巻き添え)**

v1→v2で追加したのは、CLARIFYの選び方を強調する一節と、CLARIFY専用のfew-shot例4つ(「あれ、どうすればいい?」「ちょっと相談があるんだけど」「これ」「このコードのアルゴリズムを勉強計画に組み込みたい」)。加えて次の指示を新設した。

> 質問の内容(何をしてほしいか)が具体的に書かれていない場合は、他の3カテゴリの特徴に少しでも似ていてもCLARIFYを選ぶこと。「近いから」という理由でFAST/DEEP/CODEに寄せてはいけない。
> 判断に自信が持てない(50%以上の確信が持てない)場合は、無理に4分類のどれかに決めず、必ずCLARIFYを選ぶこと。

これは「CLARIFYなのにFAST/DEEP/CODEに引っ張られる」誤判定(v1の0/4)を潰すには効いたが、**FAST/DEEPとCLARIFYの境界例に対する反例(「一見あいまいそうでも実はFAST/DEEPが正解」という例)を一つも足していない**。そのため、Phi-4-miniから見ると「具体的な行動やパラメータが書かれていない相談っぽい文 → CLARIFYへ倒せ」という圧力だけが一方的に強まった。
- F3(ラーメン屋のジャンルを3つ)は数値や固有名詞のような「厳密な具体性」がないため、「内容が具体的に書かれていない」に該当すると判断されやすい。
- D5(副業と本業の両立〜)は「ちょっと相談があるんだけど」に文体が近く、CLARIFYのfew-shot例と表面的なトーンが似ている。
本来はFAST/DEEPとして正しく分類してほしい「相談・依頼っぽい言い回し」の質問まで、CLARIFY判定の対象に巻き込まれてしまった。**CLARIFY過検出(over-triggering)**、つまり再現率(CLARIFY見逃し防止)を上げた代償として適合率(CLARIFYと判定して本当に正しいか)が下がった状態。

**② CODEカテゴリのヒント文自体に元々あった欠陥(v1から潜在、v2で顕在化)**

F4「Pythonのlen()関数って何をするものだっけ?」がCODEに誤判定される原因は、CODEカテゴリの説明文(v1・v2共通、変更していない箇所)にある。

> 判断のヒント: 「コードを書いて」「実装して」「デバッグして」「バグを直して」「レビューして」、**具体的な言語名・関数名・エラーメッセージ・コードスニペットを含む**

これは「コード生成・修正依頼と一緒に言語名/関数名が出てきたらCODE」という意図で書いたはずが、文面上は「言語名や関数名を含むだけでCODE」とも読めてしまう。F4は「len()関数って何をするものだっけ?」という**用語の意味を聞いているだけ**(本来はFAST)なのに、"Python" "len()関数" という字面がこのヒントに一致してCODE側に引っ張られる。

この欠陥はv1のプロンプトにも同じ文言で存在していたが、v1実行時はたまたま(Phi-4-miniのサンプリングのブレで)FAST判定になっていた。v2はCLARIFY関連の指示・few-shot例を大量に追加してプロンプト全体が長くなり、モデルが参照する文脈のバランスが変わったことで、この潜在バグが**4回中4回、安定して顕在化**するようになった。ノート内(145〜147行目)に書いた「Phi-4-miniの出力は毎回同じではない」という非決定性の話とも整合していて、「v1でこの欠陥が発現しなかったのはプロンプトが短く運良く踏み外さなかっただけで、直っていたわけではない」と解釈するのが妥当。

**まとめ**: v2の修正は「CLARIFYを増やす」方向にしか調整しておらず、(a) その副作用でFAST/DEEPの一部がCLARIFYに巻き込まれ、(b) 既存のCODEヒント文の曖昧さがプロンプト肥大化で表面化した、という2つの別要因が重なってFAST/DEEPの正答率を押し下げている。次の一手としては、
1. CODEヒントの「具体的な言語名・関数名を含む」を「コード生成/修正/デバッグの依頼と一緒に言語名・関数名が出てくる場合」のように限定し直す
2. F3・D5のような「相談・依頼口調だが実は具体的な要求があるFAST/DEEP」の反例few-shotを1〜2個ずつ追加してCLARIFYへの過剰な寄せを抑える
のセットで`system_prompt_v3`を作り、同じ20問セットを複数回(3回以上)再実行して平均で比較するのが良さそう。

### system_prompt_v3で実際に修正・再実行した結果

上記のまとめに沿って[[サポートAI作製計画/scripts/prompts/router_classification/system_prompt_v3.txt]]を作成し、3点を修正した。

1. **CODEヒントを「依頼動詞+言語名/関数名」の組み合わせに限定**: 「〜という**依頼動詞**と一緒に、具体的な言語名・関数名…が出てくる場合にCODEと判断する」と明記し、さらに「言語名・関数名が含まれているだけで依頼が伴わない場合はCODEにしない(FASTと判断する)」という**除外規定**を追加。
2. **FAST/DEEP側に反例few-shotを追加**: F3型(「〜を3つ挙げて」のような単発の一覧・推薦依頼)とF4型(「Pythonのlen()関数って何をするものだっけ?」のような用語の意味を聞くだけの質問)をFASTの出力例として明記。D5型(「副業と本業の両立〜優先順位の付け方を整理して」)をDEEPの出力例として明記。
3. **CLARIFYの選び方の一文を追加**: 「質問の中に『何を答えてほしいか』が一つでも明確に読み取れる場合は、表現が短く相談口調に見えても、その具体的な依頼内容に沿ってFAST/DEEP/CODEのいずれかに分類する」という**正の判定基準**を先頭に置き、CLARIFYへの過剰な寄せに歯止めをかけた(v2は「曖昧なら他カテゴリに寄せるな」という負の基準しかなかった)。

「わからない/教えて」をFASTヒントに追加する案(質問①)は採用しなかった。理由はCODEのヒント側を直接絞る方が根本原因に効き、単語追加によるモグラ叩きの副作用(CLARIFYが拾うべき曖昧な相談文までFASTに寄ってしまう)を避けられるため。

同じ`testset_v1.md`(20問)に対して`python run_testset.py --system-prompt "prompts/router_classification/system_prompt_v3.txt" --label system_prompt_v3_runN`を3回実行した結果:

| ラベル                   | 全体           | FAST | DEEP | CODE | CLARIFY                                    |
| --------------------- | ------------ | ---- | ---- | ---- | ------------------------------------------ |
| system_prompt_v3_run1 | 20/20 (100%) | 5/5  | 5/5  | 6/6  | 4/4                                        |
| system_prompt_v3_run2 | 20/20 (100%) | 5/5  | 5/5  | 6/6  | 4/4                                        |
| system_prompt_v3_run3 | 19/20 (95%)  | 5/5  | 5/5  | 6/6  | 3/4(X4「このコードのアルゴリズムを勉強計画に組み込みたい」がCODEに誤判定) |

3回合計で59/60(98.3%)。**FAST・DEEPは3回とも全問正解**(v2で崩れていたF3・F4・D5がすべて解消)。CLARIFYはX4(「コード」という単語が文中にあるため、CODE寄りに揺れる余地が残っている)が1回だけ揺れたが、v1(0/4)・v2(4/4だが今度はFAST/DEEPが崩れる)よりも全体としてバランスが良く、最有力候補になった。

> ⚠️ 実行時、`summary.csv`への追記が`PermissionError: [Errno 13] Permission denied`で失敗した(Excel等で開いたままだと書き込みロックがかかる)。詳細ログ(`system_prompt_v3_run1/2/3_*_detail.csv`)自体は正常に保存されているので実測データは残っているが、`summary.csv`の履歴には今回のv3の3回分が反映されていない。**次に実行する前に`summary.csv`を閉じておくこと。**

**残課題への対応(ユーザーによる追加修正・2026-08-04)**:
- `router.py`の`SYSTEM_PROMPT_PATH`は`system_prompt_v3.txt`に手動で切り替え済み。
- X4のブレ(CLARIFY⇔CODE)対策として、「勉強したい」という発言は前提として対象への知識がまだない(=コードそのものへの言及にはならないはず)という着眼点から、`system_prompt_v3.txt`の他カテゴリ出力例に以下を追加した。

  ```
  質問: A*アルゴリズムを勉強計画に組み込みたい
  回答: {"route": "CLARIFY"}
  ```

  意図的にテストセット本来の文言(「このコードのアルゴリズムを勉強計画に組み込みたい」)とは異なる表現を使っており、これは「テストの答えをそのままプロンプトに書き写す」形のリーク(それだと汎化性能を測れなくなる)を避けるための工夫。この追加は`system_prompt_v3.txt`のみに反映し、`v1`/`v2`は過去の実験記録として凍結したまま変更していない(テストセット`testset_v1.md`も変更不要、few-shot例とテスト問題は別物のため)。
  - 再実行結果(`system_prompt_v3_run3_20260804_231045_detail.csv`): **20/20 (100%)**。テストセット本来の表現のX4(「このコードの〜」)も正しくCLARIFYに戻り、別表現1件の追加で汎化が確認できた。

**残課題**:
- `summary.csv`を閉じた状態でv3をあと数回再実行し、100%が安定して再現するか(v2のときのようなサンプリングのブレが残っていないか)を継続観測する。

---

## ⑤ 会話継続時のモデルスワップ実機検証

②完了条件の最後に残っていた「会話継続時に不要なモデルスワップが起きないことを確認する」を実機で検証した。[[サポートAI作製計画/scripts/verify_conversation_swap.py]]を新規作成し、1つの`RouterSession`インスタンスに複数ターンの会話を順番に投げて、各ターンで①Phi-4-miniが実際に呼ばれたか、②`ollama ps`で見た実行中モデルが想定通りに切り替わったかを記録した。

### シナリオと結果

| ターン | 内容 | 判定route | Phi4呼び出し | 実行中モデル(前→後) | 所要時間 |
|---|---|---|---|---|---|
| T1 | 新規スレッド(thread-A)でDEEP相談(学習計画) | DEEP | あり(3.13s) | `[phi4-mini-cpu]` → `[gemma4:26b]`(**初回スワップ**) | 47.79s(生成45.73s、gemma4初回ロード込み) |
| T2 | 同スレッドの文脈依存の続き(「土日はもう少し軽めにしてほしい」) | DEEP | **なし**(0.00s) | `[gemma4:26b]` → `[gemma4:26b]`(**スワップなし**) | 22.06s(生成のみ) |
| T3 | 同スレッドのさらに続き | DEEP | **なし**(0.00s) | `[gemma4:26b]` → `[gemma4:26b]`(**スワップなし**) | 21.27s(生成のみ) |
| T4 | 同スレッドでCODEトリガー語を含めたつもりの複合タスク文 | DEEP(想定外) | なし | `[gemma4:26b]` → `[gemma4:26b]`(スワップなし) | 13.51s |
| T5 | 別スレッド(thread-B)でFAST質問(天気) | FAST | あり(15.52s) | `[gemma4:26b]` → `[gpt-oss:20b]`(**スワップ**) | 19.67s(生成17.11s) |

**T2・T3で検証したかった本題は成功**: 会話が続いている間(同一`session_id`)は、文脈だけでは単体分類しづらい短い続きの発言でも、Phi-4-miniを再度呼ばずセッションが保持していた直近route(DEEP)をそのまま使い、`ollama ps`上もモデルの入れ替えが一切発生しないことを実機で確認できた。T5では別スレッド(thread-B)に切り替えると、thread-Aの状態とは無関係に新規分類が走り、gemma4→gpt-ossへ正しくスワップすることも確認できた。3日目のスワップ検証結果(切り替え待ち時間11〜22秒程度)とも整合する挙動。

### 想定外だった点: T4でCODEへの優先度上書きが発動しなかった

複合タスクの優先度ルール(CODE > DEEP > FAST)を試すため、T4で「ついでにこの前渡したスクリプトのバグも直して実装しといて」という文を投げたが、CODEへ上書きされずDEEPのまま(=`router_rules.CODE_TRIGGERS`の正規表現にも一致せず、セッションの直近routeがそのまま使われた)という結果になった。

原因を切り分けたところ、**`RouterSession`側のロジックには問題がなく、`CODE_TRIGGERS`の正規表現のカバレッジ不足**が原因と判明した。

```python
from router_rules import match_rule_based
match_rule_based("ついでにこの前渡したスクリプトのバグも直して実装しといて")  # -> None(不一致)
match_rule_based("ついでにこの前渡したスクリプトのバグを直して")            # -> "CODE"(一致)
match_rule_based("ついでにこの前渡したスクリプトを実装して")                # -> "CODE"(一致)
```

`バグ(を)?(直して|修正)`は「バグを直して」「バグ直して」には一致するが、「バグ**も**直して」のように「を」以外の助詞が挟まると一致しない。同様に`実装して`は「実装し**といて**」(実装しておいて、のくだけた言い方)のような活用変化には一致しない。つまり**自然な口語表現の揺れに正規表現が追従できていない**ことが原因で、T4のような現実的な言い回しでは複合タスクの優先度ルールが静かに発動せず、DEEPのまま(=直前のモデルを使い続ける)という誤動作リスクがあることが分かった。

一方、`RouterSession`の上書きロジック自体は正しく動くことも別途確認済み:

```python
session = RouterSession()
session._last_route["thread-A"] = "DEEP"  # T1相当の状態を再現
session.get_route("thread-A", "ついでにこの前渡したスクリプトのバグを直して実装して", 未使用のcall_model)
# -> "CODE"(LLMを呼ばずに正しく上書き、session状態もCODEに更新される)
```

→ 正規表現さえヒットすれば、セッション保持中でもCODEへの優先度上書きは正しく機能する。**バグは「複合タスクの優先度ルール」の実装ではなく、それを起動する`CODE_TRIGGERS`の語彙カバレッジの狭さにある。**

### 残課題(新規発見分)

- `router_rules.py`の`CODE_TRIGGERS`を、「バグ.{0,3}(直して|直し|修正)」のように助詞の揺れを許容する形や、「実装(して|しといて|しておいて|してほしい)」のような口語活用も拾える形に広げる。
- 広げすぎるとFAST/DEEPの通常文を誤ってCODEに寄せてしまうリスクもあるため、修正後は`tests/test_router_rules.py`に「バグも直して」「実装しといて」等の口語表現のテストケースを追加してから、`testset_v1.md`相当の実運用に近い会話文でも再検証するのが望ましい。
- なお本検証中に`verify_conversation_swap.py`の標準出力がWindowsのコンソールで文字化けした(スクリプト側で`utf-8`への`reconfigure`をしていなかったため)。再実行する場合は`router.py`の`main()`同様、`sys.stdout.reconfigure(encoding="utf-8")`を先頭に入れるとよい。

---

## ⑥ CODE_TRIGGERSの拡張(⑤で発見した課題への対応)

⑤で見つかった「`バグも直して`」「`実装しといて`」のような自然な口語表現がCODEトリガーの正規表現に一致しない問題を修正した。

### 変更したファイル

**[[サポートAI作製計画/scripts/router_rules.py]]**(修正)

`CODE_TRIGGERS`の各パターンを、活用形を決め打ちする書き方から**動詞の語幹までで区切る書き方**に変更した。

| 項目 | 修正前 | 修正後 | 狙い |
|---|---|---|---|
| コードを書く依頼 | `コード(を)?書いて` | `コード(を)?書い` | 「書いて/書いてほしい/書いといて/書いておいて」等をまとめて拾う |
| 実装依頼 | `実装して` | `実装し` | 「実装して/実装したい/実装してほしい/実装しといて」等をまとめて拾う |
| デバッグ依頼 | `デバッグして` | `デバッグし` | 同上 |
| バグ修正依頼 | `バグ(を)?(直して\|修正)` | `バグ.{0,4}(直し\|修正)` | 「バグを直して」「バグ**も**直して」のように間に助詞が挟まっても拾う |
| レビュー依頼 | `レビューして` | `レビューし` | 「レビューして/レビューしてほしい/レビューしといて」等をまとめて拾う |
| 関数作成依頼 | `関数を(書いて\|作って)` | `関数(を)?(書い\|作っ)` | 助詞省略(「関数書いて」)や活用の揺れも許容 |

新たに一致するようになった具体例(修正前はNone=不一致だったもの):
1.  「ついでにこの前渡したスクリプトのバグも直して実装しといて」→ `CODE`
2. 「この機能、実装しといて」→ `CODE`
3. 「APIクライアントを実装してほしい」→ `CODE`
4. 「バグの修正をお願いします」→ `CODE`
5. 「このエラー、デバッグしといてくれる?」→ `CODE`
6. 「このPRレビューしてほしいです」→ `CODE`

一方、既存のFAST/DEEP/CLARIFY用テスト文言(「提案資料の構成を練り直して」等、「バグ」を伴わない「直して」)には引き続き一致しないことも回帰テストで確認済み(下記参照)。

**[[サポートAI作製計画/scripts/tests/test_router_rules.py]]**(修正・テスト追加)

上記6つの新パターンに対応するテストケースを9件追加(既存6件+新規9件=**計15件**)。「広げすぎて誤爆していないか」を確認する回帰テストも含めた。

### 実行したPowerShellコマンドと結果

```powershell
cd "C:\Users\gakuh\Documents\obsidian\サポートAI作製計画\scripts"

# 1. router_rules.pyのユニットテスト(拡張後の15件)
python -m unittest tests.test_router_rules -v
```

出力(実行結果、全件成功):
```
test_bug_fix_onegai_form_matches ... ok
test_bug_fix_with_particle_mo_matches ... ok
test_code_fence_matches ... ok
test_code_keyword_matches ... ok
test_compound_task_with_code_keyword_forces_code ... ok
test_debug_casual_form_matches ... ok
test_implement_casual_form_matches ... ok
test_implement_hoshii_form_matches ... ok
test_implement_keyword_matches ... ok
test_non_code_text_returns_none ... ok
test_python_def_matches ... ok
test_regex_debugging_keyword_matches ... ok
test_review_casual_form_matches ... ok
test_unrelated_kansei_form_does_not_match ... ok
test_unrelated_naoshite_does_not_match ... ok

Ran 15 tests in 0.000s
OK
```

```powershell
# 2. router.py側(セッション管理等)に影響がないことの回帰確認
python -m unittest tests.test_router -v
# -> Ran 12 tests in 0.001s / OK(全件成功、影響なし)

# 3. 既存の20問テストセットで正答率が落ちていないかの回帰確認(2回実行)
python run_testset.py --label code_triggers_v2_check
python run_testset.py --label code_triggers_v2_check2
```

出力先: `results\Router Classification\summary.csv`(2行追記)+`code_triggers_v2_check_<timestamp>_detail.csv` / `code_triggers_v2_check2_<timestamp>_detail.csv`。結果は**19/20(95%)**と**20/20(100%)**で、FAST/DEEP/CODEはいずれも3/3回とも満点。唯一のブレはX4(「このコードのアルゴリズムを勉強計画に組み込みたい」)がCLARIFY⇔CODEで揺れる、③④で既知だったLLM側の非決定性(`elapsed_s`が2.9秒前後=ルールベースではなくPhi-4-mini経由であることからも、今回の正規表現変更が原因ではないと確認済み)。

```powershell
# 4. 実際に今回の問題文がCODEとして拾えるようになったことの単体確認
python -c "from router_rules import match_rule_based; print(match_rule_based('ついでにこの前渡したスクリプトのバグも直して実装しといて'))"
# -> CODE (修正前はNoneだった)
```

---

## ⑦ Open WebUI Pipe連携の実装

②本日タスクの最後に残っていた「Open WebUIのPipe機能側の実装方法を確認し、`さぽーとAI (Auto)`仮想モデルとしてルーティング処理を組み込めるか検証」に対応した。

### ⚠️ 前提: このPCにはまだOpen WebUI自体が未導入

`pip show open-webui`・`docker ps`のいずれでも何もヒットせず、1日目ノートでも「Open WebUI等」という構想段階の記載のみだった。つまり**Open WebUIの管理画面を実際に開いてUI経由で動作確認することは今回できていない**。その代わり、以下の2段階で「Pipeとして実装したコードのロジック自体が正しいか」「実際のOllama連携込みで最後まで動くか」を実機で確認した。Open WebUIを実際に導入した後は、Functions画面にこのファイルを貼り付けて`さぽーとAI (Auto)`を選び、UI上のチャットで最終確認することが次回タスクとして残る。

### 作成したファイル

**[[サポートAI作製計画/scripts/openwebui_pipe/support_ai_auto_pipe.py]]**(新規)

Open WebUIの「Functions」機能が要求する規約(`Pipe`クラス、`Valves`によるユーザー設定、`pipe(self, body, __user__, __metadata__, ...)`という関数シグネチャ)に沿って実装した、`さぽーとAI (Auto)`仮想モデル本体。

主なポイント:
- `__init__`で`self._sessions: dict[str, RouterSession] = {}`を持たせ、**チャット(会話)ごとにRouterSessionを保持**する。Open WebUIはPipeインスタンスをサーバー起動中ずっと使い回すため、これで④/⑤で確認した「セッション保持ロジック」がOpen WebUI上でも機能する設計にした(前回お伝えした「CLIのようにプロセスを都度立ち上げる実装だとセッション保持が機能しない」という注意点への対応)。
- `_extract_chat_id()`: Open WebUIから渡される`__metadata__["chat_id"]`または`body["chat_id"]`から会話IDを取り出す(バージョン差異に備えて両方を候補にしている)。
- `_extract_last_user_text()`: `body["messages"]`から直近のuserメッセージ本文を取り出す。
- `pipe()`本体: `router.RouterSession.get_route()`でルート決定 → CLARIFYならPhi-4-mini自身に聞き返し文を生成させる → それ以外は`router.ensure_model_ready()`でモデルスワップの要否を判断してから対象モデルへ`generate()`する、という流れ。`router.py`の`main()`と同じロジックをOpen WebUI向けの関数シグネチャに載せ替えたもの。
- `Valves.show_route_debug_prefix`: 応答の先頭に`[route: DEEP]`のようなデバッグ用プレフィックスを付けるかどうかをOpen WebUI管理画面から設定できるようにした(既定はON)。
- 手動モード/自動モードの共存について: Open WebUIの仕組み上、Functionsで追加したPipeは「モデル選択肢が1つ増える」だけであり、既存の直接モデル選択(gpt-oss:20b等)の導線には影響しない。したがって本ファイルを追加するだけで手動モード/自動モードの共存要件は自動的に満たされる(Pipe側で特別な分岐は不要)。

**[[サポートAI作製計画/scripts/tests/test_support_ai_auto_pipe.py]]**(新規)

`support_ai_auto_pipe.py`のユニットテスト。`tests/test_router.py`と同じ方式で、Ollama呼び出し(`router.call_phi4`・`generate`・`router.ensure_model_ready`)をすべてフェイク関数に差し替え、Pipe自体のロジック(chat_idごとのセッション保持・CODEトリガーによる会話中の優先度上書き・CLARIFY分岐・エラー時のフォールバック・空メッセージガード・Valvesの設定反映)を検証する8件のテスト。

**[[サポートAI作製計画/scripts/smoke_test_pipe.py]]**(新規)

Open WebUIを介さず`Pipe`クラスを直接呼び出し、実際のOllama・実際のモデルで最後まで動くかを確認するスモークテスト。2つのチャット(`chat-smoke-A`/`chat-smoke-B`)に複数ターンを投げる。

### 実行したPowerShellコマンドと結果

```powershell
cd "C:\Users\gakuh\Documents\obsidian\サポートAI作製計画\scripts"

# 1. Pipeのユニットテスト(フェイク関数、Ollama不要・高速)
python -m unittest tests.test_support_ai_auto_pipe -v
```

出力(全件成功):
```
test_clarify_route_generates_followup_question_not_target_model ... ok
test_code_trigger_overrides_session_route_mid_conversation ... ok
test_debug_prefix_can_be_disabled_via_valves ... ok
test_different_chat_ids_are_independent_sessions ... ok
test_empty_message_returns_guard_message_without_calling_llm ... ok
test_first_turn_classifies_and_calls_target_model ... ok
test_llm_error_falls_back_to_clarify ... ok
test_second_turn_same_chat_reuses_route_without_calling_llm ... ok

Ran 8 tests in 0.002s
OK
```

```powershell
# 2. 実際のOllamaに対するスモークテスト(数分かかる。ログはファイルにも保存)
python smoke_test_pipe.py *> smoke_test_pipe_output.log
# PowerShellでは日本語文字化けを避けるため、事前に以下を実行しておくとよい:
#   $OutputEncoding = [System.Text.Encoding]::UTF8
#   chcp 65001
```

出力先: `scripts\smoke_test_pipe_output.log`(標準出力をそのまま保存。`run_testset.py`のようなCSV自動保存機能は付けていない=使い捨ての動作確認用スクリプトのため)。実行結果の抜粋:

| ターン | chat_id | 質問 | 判定route | 応答モデルの様子 |
|---|---|---|---|---|
| 1 | chat-smoke-A | 3ヶ月後の資格試験に向けて学習計画を立てて | `DEEP` | gemma4が学習計画案を生成 |
| 2 | chat-smoke-A | 土日はもう少し軽めにしてほしい | `DEEP`(セッション保持、Phi4呼び出しなし) | gemma4がそのまま続きの相談に応答 |
| 3 | chat-smoke-A | ついでにこの前渡したスクリプトのバグも直して実装しといて | **`CODE`**(⑥の修正で正しく上書き) | devstralに切り替わり「スクリプトを共有してください」と応答 |
| 4 | chat-smoke-B(新規チャット) | 今日の東京の天気を教えて | `FAST` | gpt-ossが応答(天気情報自体は取得不可と回答) |
| 5 | chat-smoke-B | あれ、どうすればいい? | `FAST`(セッション保持) | gpt-ossが「何に困っているか」を聞き返す |

**ターン3で⑥の修正効果を実機で確認できた**: DEEPで会話が続いていたスレッドの途中で「バグも直して実装しといて」という自然な口語文を挟んだところ、想定通りCODEへ上書きされ、devstralモデルに正しく切り替わった(⑤の時点ではこの上書きが発動しなかった)。

**副次的に見つかった仕様上の注意点(バグではなく設計上のトレードオフ)**: ターン5「あれ、どうすればいい?」は単体で見れば`CLARIFY`相当の曖昧な発言だが、直前のターン4(FAST)と同じ`chat_id`だったため、セッションが保持していたFASTがそのまま再利用され、Phi-4-miniには問い合わせずgpt-ossが応答した。これは②で決めた設計方針(「同一スレッド内は直近のrouteを保持し、毎ターン再分類しない」)通りの挙動であり不具合ではないが、**「話題が変わった直後の曖昧な一言」は前の話題のrouteを引き継いでしまう**という実運用上の特性として認識しておく必要がある。トピックが変わったことをユーザー自身が意識しているなら、`RouterSession.reset(chat_id)`を呼ぶ導線(例:Open WebUI側の「新しいチャット」ボタン=新しい`chat_id`になるので自然に別セッション化される)でカバーされる想定。

### 残課題

- Open WebUIを実際にインストールした後、Functions画面に`support_ai_auto_pipe.py`を貼り付けて`さぽーとAI (Auto)`が選択肢に現れるか、UI上のチャットで手動モード/自動モードが問題なく共存するかを確認する。
- `__metadata__`から取得する`chat_id`のキー名がOpen WebUIの実バージョンと一致しているか(バージョンによって`chat_id`ではなく別名の可能性がある)をUI導入後に要確認。ずれていた場合は全チャットが`"default"`セッションに丸められてしまうため、`_extract_chat_id()`の候補キーを実際のペイロードに合わせて調整する。

---

## ⑧ Open WebUIのインストールと動作確認手順

⑦で「このPCにはまだOpen WebUI自体が未導入」だったため、実際にインストールし、UI経由で最終確認するための手順をまとめる。

### インストールコマンド(PowerShell)

Dockerは未導入のため、pipでのインストールが最も手軽。

```powershell
pip install open-webui

# インストール確認
pip show open-webui

# サーバー起動(このターミナルは起動したまま維持しておく)
open-webui serve
```

起動すると `http://localhost:8080` でサーバーが立ち上がる。

### 確認手順

1. **サーバー起動確認**
   - `open-webui serve` 実行後、ターミナルにエラーが出ていないか確認する。
   - ブラウザで `http://localhost:8080` を開き、画面が表示されればOK。

2. **初回アカウント作成**
   - 初回アクセス時はサインアップ画面が出るので、管理者アカウント(メールアドレス・パスワード)を作成してログインする。
   - 以降はこのアカウントでログインする。

3. **Ollama接続の確認**
   - 個別に接続設定画面を開く必要はなく、**新規チャットのモデル選択ドロップダウンに`gemma4`・`gpt-oss`・`devstral`・`phi4-mini-cpu`などOllamaで動かしているモデルが一覧表示されていれば、それ自体がOllama API(`http://localhost:11434`)への接続成功の証拠**(2026-08-05実機確認済み。モデル一覧が出ない/取得エラーになる場合のみ`管理者パネル`→`設定`→`接続(Connections)`で個別確認すればよい)。

4. **Pipe(`C.L.A.I.R.E. (Auto)`)の登録**
   - `管理者パネル` → `Functions`(または`Workspace` → `Functions`) → 「+」で新規作成。
   - [[サポートAI作製計画/scripts/openwebui_pipe/support_ai_auto_pipe.py]] の中身を丸ごと貼り付けて保存する(サポートAIの名称は「C.L.A.I.R.E.」(クレア)に確定し、ファイル内の表示名も対応済み)。
   - 保存後、Function一覧でトグルを有効化(ON)にする。
   - 新規チャットのモデル選択ドロップダウンに `C.L.A.I.R.E. (Auto)` が追加されているか確認する(手動モードの既存モデルと共存していればOK)。

   > [!bug] 実機で発生したエラーと修正(2026-08-05)
   > 保存時に画面上に`ERROR: Error creating function`とだけ表示され、詳細は`open-webui serve`のターミナル側ログに出力されていた。
   > ```
   > ModuleNotFoundError: No module named 'router'
   > ```
   > **原因**: Open WebUIのFunctions機能は、貼り付けたコードを実ファイルとして保存するのではなく`exec(content, module.__dict__)`で文字列として動的実行する。そのため`support_ai_auto_pipe.py`内で使っていた`Path(__file__).resolve().parent.parent`(自分のファイル位置から`scripts`フォルダを逆算する書き方)が機能せず、`router.py`をimportできていなかった。ローカルでのCLI実行やユニットテストでは`__file__`が正しく機能するため、この問題はOpen WebUI上で保存するまで発覚しなかった。
   > **修正**: `support_ai_auto_pipe.py`の`ROUTER_SCRIPTS_DIR`の決定方法を、`__file__`に頼らずこのPC上の絶対パス(`C:\Users\gakuh\Documents\obsidian\サポートAI作製計画\scripts`)を直接指定する方式に変更した(`__file__`ベースの計算はローカル実行時のフォールバックとして残してある)。修正後、Open WebUIの`exec()`と同じ状況を再現するテストスクリプトで`router`のimportとPipeクラスのインスタンス化が成功することを確認し、既存のユニットテスト`tests.test_support_ai_auto_pipe`8件も全件OKのまま(ローカル実行への影響なし)であることを確認済み。
   > **教訓**: 「ローカルCLIから実行する前提のPythonファイル」と「Open WebUIのFunctionsとして`exec`される前提のファイル」では、`__file__`に依存した相対パス解決の扱いが異なる点に注意が必要。今後Open WebUI向けにファイルを新規作成する際は、`__file__`に頼らない絶対パス指定、または依存モジュールを1ファイルに統合する方式を優先的に検討する。

5. **手動モード/自動モードの動作確認**
   - 手動モード: ドロップダウンで `gemma4`・`gpt-oss`・`devstral` 等を直接選び、ルーターを経由せず直接そのモデルと会話できるか確認。
   - 自動モード: `C.L.A.I.R.E. (Auto)` を選び、質問を投げて意図通りのモデルに振り分けられるか確認する。
     - `Valves.show_route_debug_prefix` がONになっていれば、応答の先頭に `[route: DEEP]` のようなデバッグ表示が出るので、判定結果が一目で分かる。

   > [!info] `[route: DEEP]`はどこで・何をすると出るのか
   > **仕組み**: [[サポートAI作製計画/scripts/openwebui_pipe/support_ai_auto_pipe.py]]の`Pipe.pipe()`メソッド内、162行目付近に以下のコードがある。
   > ```python
   > debug_prefix = f"[route: {route}]\n" if self.valves.show_route_debug_prefix else ""
   > ```
   > `self.valves.show_route_debug_prefix`が`True`のときだけ、Phi-4-miniが判定した`route`(`FAST`/`DEEP`/`CODE`/`CLARIFY`のいずれか)を`[route: 〇〇]`という1行にして、実際の回答メッセージの先頭にくっつけて返す(78〜83行目の`Valves`クラスで定義、既定値は`True`=ON)。**Pipe側で特別な操作をしなくても、既定でONなのでC.L.A.I.R.E. (Auto)に何か質問を送れば毎回自動的に表示される。**
   >
   > **表示イメージ**(DEEPに振り分けられた場合):
   > ```
   > [route: DEEP]
   > (ここにgemma4:26bが生成した実際の回答本文が続く)
   > ```
   >
   > **ONかどうかを確認・変更したい場合**: Open WebUIの`管理者パネル`→`Functions`→`C.L.A.I.R.E. (Auto)`の行にある歯車(⚙)/バルブアイコンをクリックすると、`show_route_debug_prefix`のトグルが表示される(チャット画面右側のモデル設定からも同様に開ける場合がある)。ここをOFFにして保存すると、以降は`[route: 〇〇]`が表示されず、判定結果を意識せず純粋な回答だけを見たい場合に使える。動作確認中は基本的にONのままにしておくと、どのモデルに振り分けられたかが一目で分かって便利。

   > [!tip] どの質問を投げるべきか(`testset_v1.md`の質問 vs 別の質問)
   > **結論: 両方使うのがよい。使い分けの意図は以下の通り。**
   > - **`testset_v1.md`の質問(まず1〜2問)**: system_prompt_v3で3回連続100%近い正答率を確認済み(③④参照)なので、「期待するrouteが分かっている問題」としてUI上でも同じ結果になるかを確認する**回帰確認・ベースライン確認**に向いている。ズレていればUI連携側(Pipeの実装)に問題があると切り分けやすい。
   > - **`testset_v1.md`にない、自分で考えた新しい質問**: `testset_v1.md`はfew-shot例のプロンプト調整に使った「見たことのある問題」なので、これだけで確認すると**汎化性能(未知の言い回しにも対応できるか)を測れない**(③のX4の教訓と同じ理由)。実際にC.L.A.I.R.E.を日常使いする場面を想定した、自分の言葉での質問を必ず何問か混ぜること。
   > - 実際、⑤の会話継続検証や⑦の`smoke_test_pipe.py`のスモークテストも、`testset_v1.md`の文言をそのまま使わず「学習計画を立てて」「土日はもう少し軽めにしてほしい」のようなオリジナルの会話文で検証しており、今回のUI確認でも同じ考え方を踏襲するのがよい。

6. **会話継続・複合タスクの実機確認**
   - 同じチャット内で続けて質問し、直近のrouteが保持されて不要なモデルスワップが起きないか確認する(⑤で検証済みのロジックがUI上でも機能するか)。
   - 「バグも直して実装しといて」のような複合タスク文を会話途中に挟み、CODEへ正しく上書きされるか確認する(⑥の修正がUI上でも反映されているか)。
   - ここも5と同様、⑤の検証で使った文言(「学習計画を立てて」→「土日はもう少し軽めにしてほしい」→「ついでにこの前渡したスクリプトのバグも直して実装しといて」)をそのまま使ってよいし、自分で新しく考えた自然な会話の流れで試してもよい。むしろ普段実際に使いそうな言い回しで試す方が、実運用に近い確認になる。

   > [!example] 具体的な確認手順(⑤のシナリオをUI上で再現する)
   > 判定結果は上記の`[route: 〇〇]`表示で見えるが、「モデルが実際にスワップしたか/しなかったか」はチャット画面だけでは分からないため、`ollama ps`を別ウィンドウで併用する。
   >
   > **事前準備**: `open-webui serve`用とは別に、もう1つPowerShellウィンドウを開いておく(以降これを「確認用ターミナル」と呼ぶ)。
   >
   > 1. 確認用ターミナルで`ollama ps`を実行し、現在ロード中のモデルを確認する(通常は`phi4-mini-cpu`が`keep_alive -1`で常駐しているはず)。
   > 2. Open WebUIで**新規チャット**を開き、モデルに`C.L.A.I.R.E. (Auto)`を選択する。
   > 3. DEEP相当の相談文を送信する(例:「3ヶ月後の資格試験に向けて学習計画を立てて」)。
   >    - 応答が返ってきたら、先頭が`[route: DEEP]`になっているか確認する。
   >    - 確認用ターミナルで`ollama ps`を再実行し、`gemma4:26b`がロードされている(=スワップが起きた)ことを確認する。
   > 4. **同じチャット内で**続けて短い追加質問を送る(例:「土日はもう少し軽めにしてほしい」)。この文は単体では文脈がないと判定しづらい。
   >    - 応答の先頭が引き続き`[route: DEEP]`になっているか確認する(直近routeが保持されて再分類されていない証拠)。
   >    - 確認用ターミナルで`ollama ps`を再実行し、`gemma4:26b`のまま変わっていない(=モデルの再ロードが起きていない)ことを確認する。
   >    - ⚠️ 2026-08-05時点では⑨の修正により、2問目以降も毎回Phi-4-miniへの問い合わせが発生する仕様に変わっている(話題転換を正しく検知するため)。そのため「2問目の方が明らかに速い」という体感時間の違いは目安にならなくなった点に注意(詳細は⑨参照)。
   > 5. **同じチャット内で**複合タスク文を送る(例:「ついでにこの前渡したスクリプトのバグも直して実装しといて」)。
   >    - 応答の先頭が`[route: CODE]`に変わっていることを確認する(⑥で修正したCODE_TRIGGERSの正規表現がUI上でも機能している証拠)。
   >    - 確認用ターミナルで`ollama ps`を再実行し、`devstral-small-2:24b`へスワップしたことを確認する。
   > 6. 最後に**別の新規チャット**を開き、FAST相当の質問を送る(例:「今日の東京の天気を教えて」)。
   >    - チャット(=`chat_id`)が変わったことで、直前のCODE routeとは無関係に新規で分類が走ることを確認する(`[route: FAST]`になり、`ollama ps`で`gpt-oss:20b`へスワップする)。
   >    - これにより、別スレッドの状態が混ざらず独立してセッション管理されていることも合わせて確認できる。
   >
   > この一連の流れは⑤の`verify_conversation_swap.py`によるスクリプト検証(T1〜T5)と全く同じシナリオをUI上で手動再現するものなので、結果が食い違えば「ロジック自体は正しいがOpen WebUI連携部分に問題がある」と切り分けられる。

7. **既知の要確認事項(⑦の残課題)の確認手順**

   ⑦で残っていた「`__metadata__`から取得する`chat_id`のキー名が実際のOpen WebUIバージョンと一致しているか」を、以下の手順で確認する。目的は、`support_ai_auto_pipe.py`内の`_extract_chat_id()`が本当に正しい会話IDを取得できているか(=全チャットが`"default"`に丸められて会話ごとのroute保持が壊れていないか)を実機で確かめること。

   1. **デバッグ用の一時コードを追加する**
      ローカルの[[サポートAI作製計画/scripts/openwebui_pipe/support_ai_auto_pipe.py]]を開き、`pipe()`メソッドの先頭(`user_text = self._extract_last_user_text(body)`の直前あたり)に、一時的に次のようなデバッグ出力を追加する。
      ```python
      print(f"[DEBUG] __metadata__ keys: {list(__metadata__.keys()) if __metadata__ else None}")
      print(f"[DEBUG] __metadata__: {__metadata__}")
      print(f"[DEBUG] body keys: {list(body.keys())}")
      print(f"[DEBUG] body.get('chat_id'): {body.get('chat_id')}")
      ```
   2. **Open WebUI側のFunctionsを更新する**
      上記デバッグコード入りの内容を、Open WebUI管理画面の`Functions`→`C.L.A.I.R.E. (Auto)`の編集画面に丸ごと貼り直して保存する。
   3. **実際にチャットを送ってログを確認する**
      `C.L.A.I.R.E. (Auto)`を選んだ状態で何か1つメッセージを送信する。送信後、`open-webui serve`を動かしているPowerShellのターミナルに戻り、上記`[DEBUG]`で始まる行を探す。
   4. **`chat_id`に相当する値がどこにあるかを特定する**
      `__metadata__`の中身、または`body`のキー一覧を見て、実際に会話を一意に識別できそうな値(UUIDのような文字列など)がどのキーに入っているかを確認する。現状のコードは`__metadata__["chat_id"]` → `body["chat_id"]` → 見つからなければ`"default"`、の優先順位で探しているので、
      - `__metadata__`に`chat_id`というキーがそのまま存在していればOK(修正不要)。
      - `chat_id`という名前ではなく、例えば`id`や`conversation_id`、あるいは`body["metadata"]["chat_id"]`のようにネストした場所にあった場合は、ズレているので次のステップで修正が必要。
   5. **ズレていた場合は`_extract_chat_id()`を修正する**
      実際に見つかったキー名・階層に合わせて、`support_ai_auto_pipe.py`内の`_extract_chat_id()`の候補を追加・修正する(例: `metadata.get("chat_id")`の次に`metadata.get("id")`も候補として見に行く、など)。
   6. **動作確認する**
      - 同じチャット内で2回連続メッセージを送り、2回目でデバッグログの`chat_id`(または相当する値)が1回目と同じであること。
      - 新しいチャットを開いて別のメッセージを送り、今度は異なる値になること。
      この2点が確認できれば、会話ごとにセッションが正しく分離されている(=④/⑤で検証したroute保持ロジックがUI上でも機能する状態)と判断できる。
   7. **デバッグコードを取り除く**
      確認が終わったら、手順1で追加した`print(...)`の4行を削除し、Open WebUI側のFunctionsにも削除後の内容を再度貼り直して保存しておく(デバッグ出力を残したままにするとログが汚れるため)。

---

## ⑨ 実機(Open WebUI)で発覚した「会話が長引くとrouteが固定され続ける」バグの修正(2026-08-05)

⑧の手順5・6の実機確認中、`chat_id`自体は同じチャット内で一貫していた(正常)一方、**同じチャット内で異なるカテゴリの質問を続けて送ると、最初に判定されたrouteにその後ずっと固定され続ける**という不具合が見つかった。具体的には、FASTの質問がCLARIFYに、DEEPの質問(「基本情報技術者試験の勉強スケジュールを作成せよ…」)がCODEになり、`ollama ps`でも`devstral-small-2:24b`が意図せず動き続けていることを実機で確認した(各応答の内容自体は悪くなかったが、モデルの選択が明らかに間違っていた)。

### 原因(根本原因)

[[サポートAI作製計画/scripts/router.py]]の`RouterSession.get_route()`は、元々「同じ`session_id`(chat_id)なら2ターン目以降は一切Phi-4-miniに問い合わせず、直近のrouteをそのまま返す」という実装だった。

```python
if session_id in self._last_route:
    return self._last_route[session_id]
```

これは4日目ノート「⑤ 会話継続時のモデルスワップ実機検証」で確認した「同じ話題の短い続きの発言(例:『土日はもう少し軽めにしてほしい』)で無用なモデルスワップが起きない」という要件を満たすために入れた設計だったが、**⑤で検証したのは「同じ話題が続くケース」だけで、「同じチャット内で全く違う話題に切り替わるケース」は検証していなかった**。実際のOpen WebUI利用では、1つのチャットの中で複数の異なる話題を話すのはごく普通の使い方であり、その場合でも最初の1問目の判定結果に永久に固定されてしまう、という設計上の見落としだった。

`route(基本情報技術者試験の質問)`を、直前のセッション状態を使わずに単体でPhi-4-miniへ投げ直すと正しく`DEEP`が返ることも確認済みで、Phi-4-mini自体の分類性能(system_prompt_v3、③④で確認した高い正答率)には問題がなく、**「2ターン目以降は再分類しない」というRouterSession側のロジックだけが原因**と特定した。

### 対処(修正内容)

「2ターン目以降は一切再分類しない」という仕様そのものが原因だったため、**毎ターン必ずPhi-4-miniに問い合わせる**方式に変更した。ただし単純に「直前のroute情報を一切使わず、常に真っさらな状態で判定する」方式に戻すと、今度は「土日はもう少し軽めにしてほしい」のような単体では文脈の読めない短い続きの発言が、Phi-4-miniの単体判断でFAST/CLARIFY等の別カテゴリに揺れてしまう(検証時に実際に発生)ため、それも避けたい。

そこで、[[サポートAI作製計画/scripts/router.py]]の`classify_route()`に`last_route`引数を追加し、直前のrouteを「会話の文脈」としてPhi-4-miniへのプロンプトに軽く添える方式にした(`build_user_prompt()`関数として実装)。

```python
f"[会話の文脈] このスレッドはここまで \"{last_route}\" として扱われています。\n"
f"今回の発言が同じ話題の続きであれば \"{last_route}\" のままにしてください。\n"
"今回の発言の内容そのものが、他のカテゴリに明確に該当する具体的な依頼・質問である場合は、"
"その内容に従って正しいカテゴリに判定し直してください。\n\n"
f"質問: {text}"
```

`RouterSession.get_route()`側も、「2ターン目以降はスキップ」ではなく「直前のrouteを`last_route`として渡した上で毎回`classify_route()`を呼ぶ」ように変更した。これにより:
- 話題が続いている/単体では曖昧な短い発言 → 直前のrouteを維持しやすくなる(⑤の要件を維持)
- 話題が明確に変わった発言 → 内容に応じて正しく再分類される(今回のバグを解消)

という両立を狙った。トレードオフとして、2ターン目以降も毎回Phi-4-mini呼び出しのオーバーヘッド(3〜15秒程度)が発生するようになる(以前は0秒だった)。

### 検証結果

**ユニットテスト**: [[サポートAI作製計画/scripts/tests/test_router.py]]に、話題が変わった場合に正しく再分類されることを確認する`test_topic_change_within_same_session_is_reclassified`を新規追加(修正前のコードでは実際に失敗することをまず確認してから修正)。既存の「2ターン目はLLMを呼ばない」という旧仕様を前提にしていたテストも、新仕様(毎ターン呼ぶ・文脈を渡す)に合わせて更新した。[[サポートAI作製計画/scripts/tests/test_support_ai_auto_pipe.py]]側にも同様の話題転換テストを追加。

```powershell
python -m unittest tests.test_router tests.test_router_rules tests.test_support_ai_auto_pipe
# -> Ran 37 tests in 0.002s / OK(全件成功)
```

**実際のOllama・Phi-4-miniでの検証**: 実機で問題になった質問を使い、直前のroute状況を再現して確認した。

| 検証内容 | 結果 |
|---|---|
| 問題の質問を文脈なし(1問目相当)で分類 | `DEEP`(想定通り。Phi-4-mini自体の分類能力は問題ないことを確認) |
| 問題の質問を`last_route="CODE"`(誤って固定されていた状況を再現)で分類 | `DEEP`(3回中2回)/`CODE`(3回中1回) |
| 問題の質問を`last_route="FAST"`で分類 | `DEEP`(3回とも) |
| 「土日はもう少し軽めにしてほしい」を`last_route="DEEP"`で分類 | `DEEP`(想定通り、話題継続として維持) |
| 「今日の東京の天気を教えて」を`last_route="DEEP"`で分類 | `FAST`(想定通り、話題転換として正しく再分類) |

⚠️ `last_route="CODE"`のケースで3回中1回`CODE`のまま揺れたのは、③④で既知の**Phi-4-miniのサンプリング非決定性**によるもの(前回のroute名がプロンプト中に文字として出てくること自体が、多少CODEへ引っ張られるバイアスになっている可能性がある)。「絶対に100%固定されない」保証はできないが、**修正前は3問中3問とも100%固定され続ける不具合だったのに対し、修正後は大部分のケースで正しく再分類され、通常の分類非決定性(既存のsystem_prompt_v3でも既に許容していた水準)の範囲に収まった**、というのが今回の改善の実質的な効果。

### 補足: 手動モードでPhi-4-miniの常駐が途切れる件について

「モデルを手動で切り替えるとPhi-4-miniが常駐しなくなる」という点は、上記のroute固定バグとは別の話で、**コード側の不具合ではなく、Open WebUIの仕組み上の制約**。[[サポートAI作製計画/scripts/ollama_client.py]]の`generate()`は`keep_alive=-1`をデフォルトにしているが、これは`router.py`/`support_ai_auto_pipe.py`(=C.L.A.I.R.E. (Auto)経由)からOllamaを呼ぶ場合にだけ適用される設定。**手動モードでモデルを直接選んだ場合、Open WebUIは私たちのコードを一切経由せず、Open WebUI自身がOllamaに直接リクエストを送るため、`keep_alive=-1`が効かない**。加えて、devstral(24B)やgemma4(26B)のような大きいモデルを手動で読み込むと、システムのメモリ確保のために既存の常駐モデル(Phi-4-mini含む)が退避されることがある。これはOllama側のリソース管理の挙動であり、次にC.L.A.I.R.E. (Auto)経由で質問すればPhi-4-miniは自動的に再ロードされる(数秒程度)ため、実害としては「手動モードで色々試した直後の自動モードの1問目だけ、Phi-4-miniの再ロード分だけ少し遅くなる」程度。今回は特に修正はせず、仕様として認識しておく。

---

## ⑩ CODEルート(devstral)にファイル作成・実行機能を追加(2026-08-05)

⑨の修正確認中、CODEルートに振り分けられたdevstralが正しいモデルで応答している一方、「実際にPC上でファイルを作成してコードを実行する」ところまではできず、代わりに手順とコード例をテキストで返すだけだった、という指摘を受けた。これはバグではなく、そもそもこれまでのPipeが**単なるテキスト生成(Ollamaの`generate` API呼び出し)のみ**で、ファイル操作やコマンド実行の手段を一切持っていなかったため。今回、Claude Codeのように「毎回確認を求めるモード」と「完全自動モード」を選べる形で、この機能を新規実装した。

### 設計方針

- devstralに、ファイル作成・実行が必要な依頼だと判断した場合は`<ACTION path="..." run="true/false">`というブロック形式で応答するよう専用のシステムプロンプトで指示する(単なるコードレビュー・質問応答の場合はブロックなしの通常回答でよい)。
- 安全のため、書き込み先は必ず専用の作業フォルダ[[サポートAI作製計画/scripts/workspace]](`scripts/workspace/`、初回書き込み時に自動作成)配下に限定する。`".."`による親ディレクトリ脱出や別ドライブの絶対パス指定は拒否し、`router.py`等の既存スクリプトを誤って上書きされないようにする。
- 実行は「作成した`.py`ファイルを`python`コマンドで実行する」ことのみをサポートし、任意のシェルコマンド実行は許可しない。タイムアウト(既定30秒)を必ず設ける。
- `Valves.code_execution_mode`で3モードを切り替え可能にした:
  - `"confirm"`(既定・推奨): devstralが提案したファイル作成/実行内容を提示し、次の発言で「はい」「実行して」等の肯定的な返事があった場合のみ実際に書き込み・実行する(Claude Codeの確認モード相当)。話題が変わるなど肯定的でない返事の場合は保留中の提案を破棄し、通常の分類フローに戻る。
  - `"autonomous"`: 確認なしで即座に書き込み・実行する。
  - `"off"`: 提案(ACTIONブロックを含むテキスト)を見せるだけで、一切書き込み・実行しない(今までの挙動)。

### 実装したファイル

| ファイル | 役割 |
|---|---|
| [[サポートAI作製計画/scripts/code_executor.py]](新規) | ACTIONブロックの解析(`parse_action`)、安全なパス解決(`resolve_safe_path`/`UnsafePathError`)、ファイル書き込み(`write_action_file`)、Python実行(`execute_python_file`、タイムアウト対応)を提供する実行エンジン。Ollama通信は一切行わず、単体でテスト可能な形に分離した |
| [[サポートAI作製計画/scripts/openwebui_pipe/support_ai_auto_pipe.py]](修正) | `Valves.code_execution_mode`を追加。CODEルート時のみ`CODE_ACTION_SYSTEM_PROMPT`を付けてdevstralを呼び、`_handle_code_reply()`でACTIONブロックの検出・モード分岐、`_pending_actions`辞書でchat_idごとの確認待ち状態を管理、`_run_action()`で実際の書き込み・実行を行う。あわせて、⑧で`chat_id`検証用に追加していたデバッグ用`print(...)`4行(役目を終えたもの)をこのタイミングで削除した |
| [[サポートAI作製計画/scripts/tests/test_code_executor.py]](新規) | `code_executor.py`のユニットテスト13件(ACTIONブロック解析、パストラバーサル拒否、ファイル書き込み、実行結果取得、タイムアウト) |
| [[サポートAI作製計画/scripts/tests/test_support_ai_auto_pipe_code_execution.py]](新規) | Pipe側の統合テスト6件(confirm/autonomous/offモードそれぞれの挙動、確認後の実行、話題が変わった場合に保留アクションが破棄されること、ACTIONブロックがない場合は今までどおりの通常回答になること)。一時ディレクトリを`support_ai_auto_pipe.WORKSPACE_DIR`に差し替えることで、テスト実行中に実ファイルシステム(本番の`scripts/workspace/`)を汚さないようにしている |

### 検証結果

TDD(テストファースト)で実装し、実装前にテストが正しい理由で失敗することを確認してから実装した。

```powershell
python -m unittest discover -s tests
# -> Ran 56 tests in 1.139s / OK(全件成功。既存49件+今回追加19件)
```

さらに実際のOllama・devstralに対して、ユーザーが実機で入力した依頼文とほぼ同じ文面("scriptsフォルダに新しくPythonファイルを作製し、コードを実行するとHello Worldとコマンドラインに出力されるようにコードを実装して")で`Pipe.pipe()`をエンドツーエンドで実行し、以下を確認した(`confirm`モード、一時ディレクトリをworkspaceとして使用):

1. 1問目: devstralが`<ACTION path="hello.py" run="true">`ブロックを含む応答を生成し、`[C.L.A.I.R.E.] 上記の内容で hello.py を作成し、実行してよろしいですか?`という確認メッセージが返る。この時点ではファイルはまだ作成されない。
2. 2問目(「実行して」): `hello.py`が作成され、実際に`python hello.py`が実行され、標準出力に`Hello World`が返ってくることを確認。

### 既知の制約・今後の課題

- devstralへの指示（システムプロンプト）で「pathはファイル名のみ、ディレクトリを含めない」ことを明記しているが、これはプロンプトによる誘導であり100%守られる保証はない(Phi-4-miniの分類同様、LLMの非決定性の影響を受ける)。`resolve_safe_path()`によるworkspace外への書き込み拒否は、プロンプトが破られた場合の最後の砦として機能する。
- `_is_confirmation()`による肯定判定は単純な部分一致方式(「はい」「実行して」等の単語リスト)。誤検出を避けるため単独の"y"のような紛らわしい語は含めていないが、逆に想定外の言い回しの肯定的な返事は拾えない可能性がある。実運用で気になる場合は語彙を追加する。
- 現状は「1回の応答につきACTIONブロック1つ」のみ対応。複数ファイルの同時作成や、作成済みファイルの修正(差分編集)には対応していない。
- 実行対象は`.py`ファイルの`python`実行のみ。他言語やシェルコマンドの実行には対応していない(安全性のスコープを絞るための意図的な制限)。


### 直接コードを実装することについて
- 指定したパスにpythonファイルを作製しコードを実装してくれた。
- 許可モードの時、普通のチャット画面で「はい」と入力しても同じ質問を繰り返すだけなので、「続けて応答」というボタンを押してから「はい」と入力しないと実装されないのが懸念点。めんどくさいから。これは音声でやり取りするのが主流なので音声会話の時はづなるのかが不安
---

## 📌 次のステップ
1. ~~4モデルをOllamaで実際にダウンロード・動作検証~~
2. Phi-4-miniの振り分けロジック(プロンプト設計・実装) ← 今回実装中
3. RAGの記憶DB構築
4. Cloudflare Tunnelのセットアップ
5. STT/TTSパイプラインの組み立て
