---
project: C.L.A.I.R.E.(さぽーとAI)
date: 2026-08-08
tags: [外部アクセス, Tailscale, ゼロトラスト, セキュリティ, STT, TTS, Whisper, VOICEVOX, AivisSpeech, ストリーミング, 音声対話, 作業ログ]
status: 完了(①完了 / ②タスク1のみ完了。タスク2・3は[[サポートAI作製計画/9日目自前音声UIとストリーミング音声対話.md|9日目]]へ持ち越し)
---

[[サポートAI作製計画/7日目Obsidianノート取り込みとCloudflareTunnel.md|7日目]]でObsidianノートの記憶DB取り込み(777チャンク)まで完了したが、**⑥のCloudflare Tunnelは「独自ドメインの取得に費用がかかり0円制約に反する」ため中断**し、代替として**Tailscale**を採用する結論だけを出した状態で終わった。今回は①その外部アクセス環境のセットアップを実際に行い、②ロードマップ最終項目である**STT/TTSパイプラインの組み立て**に着手する。

> [!note] このノートの位置づけ
> 本ノートは7日目⑥・⑨「残課題・次回への持ち越し」を起点に、[[サポートAI作製計画/ノート作成規則.md]]に従って**着手前に作業内容を列挙した計画ノート**として作成した。実行後に「結果」「分析」「改善策」を各セクションへ追記していく。

---

## ① 外部アクセス環境の構築(7日目⑥の残作業。Cloudflare Tunnel → Tailscaleへ方針変更)

### 背景/目的

ロードマップ「次のステップ 6.」の積み残し。現状C.L.A.I.R.E.は自宅PCの`localhost:8080`でしか使えず、外出先やスマホから使えない。

7日目⑥でCloudflare Tunnelを試みたが、**`cloudflared tunnel login`がブラウザ側で「Cloudflareで管理しているドメイン」の選択を要求し、ドメイン未所持のため`cert.pem`が生成されず`tunnel create`が失敗**した。ドメイン取得には年数百〜千数百円の費用が発生するため、**費用0円**の制約に反すると判断して中断。「無料・セキュア・ポート開放不要・固定URL」の4条件で代替案を再調査した結果、**Tailscale**を採用する(調査結果の比較表は7日目⑥の結果欄を参照)。

> [!danger] 認証なしの公開は絶対にしないこと(7日目から引き継ぐ最重要事項)
> C.L.A.I.R.E.のCODEルートには**任意のPythonファイルを作成してその場で実行する機能**(4日目⑩のACTIONブロック / `code_executor.py`)がある。認証なしでインターネットに公開することは、**自宅PCに対する任意コード実行の踏み台を全世界に開放する**ことと同義。
> **Tailscale Serve(非公開)を使う限りこのリスクは構造的にゼロに近い**が、`tailscale funnel`(公開)へ切り替えた瞬間に7日目と同じ脅威モデルに戻る。**本ノートではFunnelを使わない**。使う場合は必ずアプリ側認証の検証を先に行う。

### 方式の確定:Tailscale Serve(Funnelではない)

| 項目 | 内容 |
|---|---|
| 費用 | **0円**(Personalプラン:6ユーザーまで、ユーザーデバイス数は無制限。永久無料) |
| ポート開放 | **不要**(WireGuardベースのメッシュVPN。アウトバウンド接続のみでNAT越え) |
| 固定URL | **○**(`https://<端末名>.<tailnet名>.ts.net`。HTTPS証明書もTailscaleが自動発行・更新) |
| 到達できる人 | **自分のTailscaleアカウントでログインし、管理画面で承認済みの端末のみ**。URLを知っていてもtailnet外からは到達不可 |
| 代償 | スマホ側に**Tailscaleアプリのインストールとログインが必要**(ブラウザ単体では不可) |

Cloudflare Tunnel + Accessは「公開ゾーンに置いた上でCloudflare側で認証を弾く」方式だったのに対し、Tailscale Serveは**そもそも公開ゾーンに露出しない**。認証の設定を1つ間違えると全世界に開放される、という事故の余地が構造的に小さいため、セキュリティ面ではむしろ上位互換と判断する。

### 前提条件(着手前に必要なもの)

- [ ] Tailscaleアカウント(GitHub/Google/Microsoftアカウントでサインアップ可、無料)
- [ ] 自宅PC(Windows)にTailscaleをインストール
- [ ] スマホ(iOS/Android)にTailscaleアプリをインストール、**同一アカウント**でログイン
- [ ] Open WebUIが`localhost:8080`で起動していること(実際のポートは要確認)

### 実施手順(予定)

```powershell
# 1. Tailscaleのインストール(winget)
winget install --id tailscale.tailscale
# ⚠️ 7日目⑥と同じPATH未反映問題が起きうる。インストール後は必ずPowerShellウィンドウを開き直すこと
#    (Windowsの環境変数はプロセス起動時にコピーされるため、既存ウィンドウには反映されない)

# 2. サインイン(ブラウザが開く。GitHub/Google等でログイン)
tailscale up

# 3. 自分のtailnet名・端末名(MagicDNS名)を確認する
tailscale status
#    → 例: claire-pc.tail1234.ts.net のような名前が割り当てられる

# 4. Open WebUI(localhost:8080)をtailnet内へ公開する
tailscale serve --bg 8080
#    --bg でバックグラウンド常駐。フォアグラウンドで挙動を見たい場合は --bg を外す

# 5. 現在の公開設定を確認(どのURLで何が見えているか)
tailscale serve status

# 6. 公開を止めたいとき(意図的にOFFにする手順。必ず控えておく)
tailscale serve --https=443 off
```

> [!warning] `tailscale serve` と `tailscale funnel` を打ち間違えないこと
> `serve`はtailnet内のみ、`funnel`は**インターネット全体への公開**。1単語違いで脅威モデルが完全に変わる。
> 実行後は必ず`tailscale serve status`で「Funnel: off」であることを目視確認する。

### 作業内容

- [x] Tailscaleアカウントを作成する
- [x] 自宅PCにTailscaleをインストールし、`tailscale up`でサインインする
- [x] `tailscale status`で割り当てられたMagicDNS名(`<端末名>.<tailnet名>.ts.net`)を記録する
- [x] `tailscale serve --bg 8080`でOpen WebUIをtailnet内に公開する
- [x] `tailscale serve status`で**Funnelがoffである(=インターネット非公開である)ことを確認する** ← 重要
- [x] スマホにTailscaleアプリを入れ、同一アカウントでログインする
- [x] **スマホのモバイル回線(自宅Wi-Fiを切った状態)から`https://<端末名>.<tailnet名>.ts.net`で実際に会話できることを確認する**
- [x] **Tailscaleアプリを切断した状態で同じURLへアクセスし、到達できないことを検証する** ← 最重要(認証の実効性確認)
- [x] Tailscale管理画面でデバイス承認設定(Device approval)を確認し、想定外の端末が勝手にtailnetへ入れない構成か検証する
- [x] Open WebUI自体のログイン認証が有効になっているか確認する(二重の防御)
- [x] PC起動時に自動でTailscaleが繋がる設定になっているか確認する(Windowsサービスとして常駐)
- [x] 7日目⑥の「実施手順」「作業内容」がCloudflare Tunnel前提のまま残っているため、本ノートを正とする旨を7日目に追記する

### セキュリティ上の残論点(実施時に必ず判断する)

- **CODEルートの外部からの利用可否**(7日目から継続):認証を掛けても、スマホから誤って「ファイルを作って実行して」と頼めば自宅PCでコードが動く。`code_execution_mode`(Valves)を外出時はOFFにする運用にするか、外部からはCODEを無効化するか方針を決める
- **Tailscaleへの依存**:Tailscaleのコントロールプレーン(認証サーバー)がダウンすると新規接続ができなくなる。既存接続は維持されるが、可用性の単一障害点であることは認識しておく
- **常時接続**:PCが起動しTailscaleが常駐している限り、自分の端末からは常に到達可能。意図的にOFFにする手順(`tailscale serve --https=443 off` / `tailscale down`)を記録しておく

### 結果 / 分析 / 改善策

3つ目のコマンド：100.115.166.90  go9029          gakuhari555@  windows  -
100.118.221.56  iphone-12-mini  gakuhari555@  iOS      offline, last seen 2m ago

4つ目：Success.
Available within your tailnet:

https://go9029.tail81b83d.ts.net/
|-- proxy http://127.0.0.1:8080

Serve started and running in the background.
To disable the proxy, run: tailscale serve --https=443 off

5つ目：https://go9029.tail81b83d.ts.net (tailnet only)
|-- / proxy http://127.0.0.1:8080

**機器承認(Device approval)**:管理画面(デバイス管理)の「新しいデバイスを手動で承認する」トグルは初期状態でオフ(=ログインさえできれば無条件でtailnetに参加できる状態)だった。オンに変更。既存デバイス(`go9029`・`iphone-12-mini`)への影響なし(既存デバイスは承認済み扱いのため引き続き利用可能)。以後、新規デバイスがtailnetに参加するには管理者(自分)の承認が必須になる。

**Open WebUIログイン認証**:シークレット/プライベートウィンドウで`https://go9029.tail81b83d.ts.net/`へアクセスしたところ、チャット画面ではなくログイン画面が要求されることを確認。認証は有効(二重防御が機能している)。

**PC起動時の自動接続**:`services.msc`で「Tailscale」サービスを確認。状態=実行中、スタートアップの種類=自動になっていることを確認。PC起動時に手動操作なしでtailnetへ接続される設定になっている。

**総括**:①は全項目完了。「無料・セキュア・ポート開放不要・固定URL」の4条件をすべて満たし、かつ二重防御(tailnet参加の可否 + Open WebUIのログイン認証)が実機で確認できた。Cloudflare Tunnelからの方針変更は結果的に正解で、**費用0円のまま7日目より強い脅威モデル**(公開ゾーンにそもそも露出しない)を得られた。改善は不要。

### 残課題

- **外出時のCODEルートの扱いが未決**(セキュリティ残論点1がそのまま残っている):スマホから誤って「ファイルを作って実行して」と頼めば自宅PCで任意コードが動く状態は変わっていない。`code_execution_mode`(Valves)を外出時OFFにする運用にするか、リクエスト元IP(tailnet内の端末IP)で分岐して外部からはCODEを無効化するかを決める必要がある。**9日目の自前音声UIは「音声で気軽に指示できる」ぶんこの誤爆リスクが上がる**ため、9日目に方針決定まで持ち越す
- **9日目の自前音声UIもTailscale Serveに載せる必要がある**:現在`tailscale serve --bg 8080`はOpen WebUI(8080)だけを公開している。9日目に作る`voice_gateway.py`を別ポートで立てるため、**スマホから使うには同じ要領でServeの公開対象を追加する**(パスベースで振り分けるか、ポートを分けるか要判断)。またブラウザのマイク入力(`getUserMedia`)は**セキュアコンテキスト(HTTPS or localhost)でしか動かない**ため、Tailscale ServeのHTTPSを経由することが必須条件になる(この点でTailscale Serve導入は9日目の前提条件を先に満たしていたことになる)
- 7日目⑥への追記(本ノート①を正とする旨)は完了済み

---

## ② STT/TTSパイプラインの組み立て(ロードマップ最終項目)

### 背景/目的

[[サポートAI作製計画/1日目設計とモデル選定.md|1日目]]①のタスク一覧にある「音声対話:STT/TTSによるハンズフリー会話(低遅延・常時)」の実現。1日目④の全体設計では以下の位置づけになっている。

```
[音声入力] → STT(Whisper, ローカル)
        ↓
[ルーター(gemma4-e4b-cpu)] → FAST/DEEP/CODE/CLARIFY
        ↓
[RAG記憶DBに書き戻し] → TTS(Piper, ローカル) → [音声出力]
```

1日目②の採用技術は**STT: whisper.cpp / faster-whisper**、**TTS: Piper TTS**(いずれも完全ローカル・無料)。本セクションではこれを実際に動かし、音声で会話できる状態にする。

> [!important] 着手した結果、方式が2点変わった(詳細は次項)
> ① **TTSはPiperを不採用にし、VOICEVOX ENGINE / AivisSpeech Engine を採用候補とする**(声が機械的で要件を満たさなかったため)。
> ② **Open WebUIの音声設定に繋ぎ込むだけでは要件を満たせない**ことが判明したため、自前の音声UIを作る方針に変更し、本ノートは**部品編(声の確定・STT選定・ストリーミング化)まで**、UI本体は9日目に分割する。

また、[[サポートAI作製計画/4日目Phi4ロジック設計.md]]の懸念(743行目)として「CLARIFYの許可モードで『続けて応答』ボタンを押す必要があるのが、音声会話時にどうなるか不安」が記録されている。**音声で会話した際にCLARIFYルートが実用に耐えるかは本セクションで実測して判断する**。

### 着手して最初に判明したこと(方式決定の根拠)

まずOpen WebUIの内蔵TTS(下記の旧方式A)を有効化し、「応答の自動再生」トグルをONにしたところ、**音声は再生されたが声が機械的で用途に合わない**ことが判明した。これは本ノート初版のwarning「Piperの日本語音声モデルは要調査」で懸念していた品質問題が、内蔵TTS(ブラウザ/OSの`SpeechSynthesis` = Windows日本語音声)で先に顕在化したもの。

これを受けて、**これまで曖昧だった「音声対話」の要件を明文化した**。以降の方式選定はすべてこの6条件を満たすかで判断する。

| # | 要件 | 種別 |
|---|---|---|
| 1 | 音声モデルが豊富で、**人間味のあるかわいらしい若い女性の声**があること | TTS |
| 2 | 完全ローカルで使えること | 両方 |
| 3 | 完全無料で使えること | 両方 |
| 4 | 使用制限(回数・文字数)がないこと | 両方 |
| 5 | **発話している途中から逐次的に文字が出ること** | STT |
| 6 | 音声から文字への変換が速いこと | STT |

要件1は主観評価であるため、**「候補を実際に聞き比べて自分で選ぶ」工程を作業として明示的に置く**(タスク1)。要件5は後述の通り、Open WebUIの標準UIでは構造的に実現できないことが分かったため、方式そのものの見直しが必要になった。

#### TTSエンジンの再選定(Piperを不採用にする)

要件1を満たす「完全ローカル・無料・無制限」の日本語TTSは、実質2系統に絞られる。**Piperは日本語の声が機械的で要件1を満たさないため不採用**とする(1日目②の採用技術からの変更。ノート初版のwarningに対する結論)。

| 項目 | VOICEVOX ENGINE | AivisSpeech Engine |
|---|---|---|
| 声の方向性 | アニメ・キャラクター寄り(はっきりした演技的発声) | **自然な人間の話し声寄り**(Style-Bert-VITS2ベース) |
| 声の数 | 話者数十種 × スタイル(ノーマル/あまあま/ツンツン/ささやき等) | AivisHubから多数のモデルを無料DL。感情表現の幅が広い |
| 費用 / 制限 | 0円・完全ローカル・無制限 | 0円・完全ローカル・無制限 |
| HTTP API | `localhost:50021` | **VOICEVOX互換API** `localhost:10101` |
| GPU利用 | GPU版あり | 利用可 |

> [!important] 両者はAPI互換である ← 今回の設計の要
> AivisSpeech EngineはVOICEVOX ENGINE互換のHTTP APIを実装している。したがって**アダプタを1本書けば、接続先URLと話者IDを差し替えるだけで2系統を同じコードで聞き比べできる**。「キャラ寄り」と「自然寄り」の両方を試したいという要件が、追加コストほぼゼロで満たせる。

なお**OpenAI TTS APIは有償のため不採用**(0円制約)、**ブラウザのWeb Speech APIは音声がクラウドへ送られるためSTT/TTSとも不採用**(プライバシー要件)という初版の判断は維持する。

#### 発見1:既存Pipeは Open WebUI にほぼ依存していない

[[サポートAI作製計画/scripts/openwebui_pipe/support_ai_auto_pipe.py]]の依存関係を確認したところ、依存は`pydantic`と自前モジュール(`router` / `code_executor` / `ollama_client`)だけで、Open WebUI固有の仕組み(`__event_emitter__`等)は使っていない。`Valves`は素の`BaseModel`であり、`pipe()`のシグネチャは`(body, __user__, __metadata__, **_)`。つまり**素のPythonから直接呼べる**。

```python
# Open WebUIを介さずにPipeを呼ぶことができる(要検証だが依存関係上は可能)
p = Pipe()
p.pipe(body={"messages": [{"role": "user", "content": "..."}]},
       __metadata__={"chat_id": "voice-001"})
```

これにより、自前の音声UIを作る際に**Open WebUIのHTTPを1段挟む必要がなく、かつルーター/RAG/記憶のコードを二重管理する必要もない**(=同一ファイルを使い回せる)。長期記憶(LanceDB)はファイル共有なのでOpen WebUI側の会話とも引き続き共有される。`RouterSession`(直前ルートの保持)だけがプロセス別になるが実害は小さいと判断する。この方式を以降**案A′**と呼ぶ。

#### 発見2:現状の生成はストリーミングではない ← 音声化の最大の障壁

[[サポートAI作製計画/scripts/ollama_client.py]]の`generate()`は**`"stream": False`が固定**で、`pipe()`は生成完了後に全文を1つの文字列で返している。つまり**現状のままでは「文ができた先から順次読み上げる」ことが原理的に不可能で、全文生成が終わるまで音声が一切始まらない**。7日目時点の実機応答は20秒以内という記録があり、音声会話では致命的な待ち時間になる。

→ **`ollama_client`へのストリーミング対応追加が、音声UIの前提条件**である。既存の`generate()`は壊さず`generate_stream()`を追加する(後方互換)。`pipe()`の型注釈は既に`str | Iterator[str]`なので、Iteratorを返す経路は想定されている。副産物として**既存のテキストチャットの体感も改善する**(Open WebUI側にもトークンが流れるようになる)。

### スコープの分割(本日と次回)

要件5(逐次STT表示)を満たすには、Open WebUIの標準の音声入力(録音→停止→まとめて文字化)ではなく、**自前のWebSocketゲートウェイ + ブラウザUI**が必要になる。これは実装量が大きいため、依存関係に沿って2日に分割する。

**分割の根拠**:声もSTTも**UIなしで選定・実測できる**(wavファイル相手に測れる)。逆にUIを先に作ると「この声は遅い」「このGPUでSTTが動かない」と後で判明して手戻りする。特に本機は**RTX 5070 Ti = sm_120 (Blackwell)** で新しく、`faster-whisper`の土台であるCTranslate2が対応しているか要検証。ここが不発ならSTT方式ごと変わるため、**UIを書く前に潰すべき最優先リスク**である。

| | 内容 | UI |
|---|---|---|
| **8日目②(本日)** | **タスク1** 声の探索と確定 / **タスク2** STTエンジンの選定と逐次化の実測 / **タスク3** `ollama_client`のストリーミング対応 | 不要 |
| **9日目(次回)** | 自前音声UI:WebSocketゲートウェイ + ブラウザUI(逐次STT表示・VAD自動送信・逐次読み上げ)。案A′でPipeを直呼び | ここで作る |

次回UIで採用する方針も先に確定させておく(本日は実装しない)。

- **使用端末**:自宅PCのブラウザ(localhost)と、スマホのブラウザ(Tailscale Serve経由のHTTPS)の両方
- **会話の駆動**:常時マイクON + 無音検出(VAD)で自動送信。読み上げ中はマイクOFFとし、**バージイン(発話中の割り込み)は初版では実装しない**(スピーカー音を自分の声と誤検出するエコー問題を避けるため。YAGNI)

---

### タスク1:お気に入りの声を探して確定する

#### 作業内容

- [x] VOICEVOX ENGINEをインストールして起動し、`GET /speakers`で話者一覧が取れることを確認する
- [ ] AivisSpeech Engineをインストールして起動し、**同じ`GET /speakers`でAPI互換であることを実証する** → **9日目へ持ち越し**(VOICEVOXの東北ずん子で要件1を満たす声が見つかったため、急ぎではなくなった。ただしAPI互換の実証自体は設計の要なので9日目に消化する)
- [ ] AivisHubから「かわいらしい若い女性の声」に該当しそうな日本語モデルを数種DLして読み込ませる → **9日目へ持ち越し**(上と同じ理由)
- [x] `tts_adapter.py`(新規)を書く:エンジンURL・話者IDを引数に取り、テキスト→wavを返す部品
- [x] `tts_voice_survey.py`(新規)を書く:**両エンジンの全話者×全スタイルに同一の台詞を読ませ、wavを一括生成してファイルに保存する**
- [x] 生成されたwavを実際に聞き比べ、**採用する声(エンジン・話者ID・スタイル・話速)をノートに記録する**
- [ ] 採用した声で1文あたりの合成所要時間を実測する(逐次読み上げが成立する速度か確認) → **9日目へ持ち越し**(下記「読み上げ開始までの遅延」の切り分けに必須。9日目の最優先で消化する)
- [x] `openai_tts_shim.py`(新規)を書く:OpenAI互換`/v1/audio/speech`をVOICEVOX互換APIへ橋渡しする
- [x] Open WebUI → 設定 → 音声 → Text-to-Speech Engine を OpenAI にし、shimを指して**普段のチャットが採用した声で読み上げられることを確認する** ← **疎通は成功したが、読み上げ開始まで約1分の遅延が発生(下記)**

> [!note] shimはOpen WebUIのためだけのものではない
> 声の評価自体にOpen WebUIは不要(wavを直接聞くのが最速で確実)。shimの狙いは**9日目に自前UIが完成するまでの間、キーボードで打つ普段のチャットも良い声で読み上げてもらう**こと。中身の`tts_adapter.py`は9日目の自前UIでもそのまま使うので捨て仕事にならない。

#### 実施手順(予定)

```powershell
# 1. VOICEVOX ENGINE を起動する(既定ポート 50021)
#    ※ 公式配布物(VOICEVOX/voicevox_engine)を展開して run.exe を起動。
#      GPU版/CPU版のどちらを使うかは、LLMとのVRAM競合を見て判断する(下記論点3)

# 2. 話者一覧を取得して、話者ID・スタイルIDを確認する
curl.exe "http://127.0.0.1:50021/speakers" -o voicevox_speakers.json

# 3. AivisSpeech Engine を起動する(既定ポート 10101)。同じAPIが通ることを確認する
curl.exe "http://127.0.0.1:10101/speakers" -o aivis_speakers.json

# 4. 声の一括聞き比べ用wavを生成する
cd C:\Users\gakuh\Documents\obsidian\サポートAI作製計画\scripts
python tts_voice_survey.py --engine-url http://127.0.0.1:50021 --label voicevox
python tts_voice_survey.py --engine-url http://127.0.0.1:10101 --label aivis
#    --text で読ませる台詞を変更できる(既定はクレアらしい定型文)
#    出力先: scripts/results/voice_survey/<label>/<speaker>_<style>.wav

# 5. 声が決まったら shim を起動する(ポート5051。8080/50021/10101と衝突させない)
python openai_tts_shim.py --engine-url http://127.0.0.1:10101 --speaker <確定したID> --port 5051

# 6. Open WebUI → 設定 → 音声 → Text-to-Speech Engine: OpenAI
#    API Base URL: http://127.0.0.1:5051/v1  /  API Key: ダミー文字列で可
```

#### 結果 / 分析 / 改善策

`scripts/voicevox_speakers.json`(`GET /speakers`の生JSON)から`name`が`東北ずん子`の要素を抽出した結果：

```json
{
  "name": "東北ずん子",
  "speaker_uuid": "80802b2d-8c75-4429-978b-515105017010",
  "styles": [
    { "name": "ノーマル", "id": 107, "type": "talk" }
  ],
  "version": "0.16.0"
}
```

| 項目                                | 値                                          |
| --------------------------------- | ------------------------------------------ |
| エンジン                              | VOICEVOX ENGINE (`http://127.0.0.1:50021`) |
| 話者名                               | 東北ずん子                                      |
| `speaker_uuid`                    | `80802b2d-8c75-4429-978b-515105017010`     |
| スタイル名                             | ノーマル                                       |
| **話者ID(API呼び出し時の`speaker`パラメータ)** | **`107`**                                  |
| 話速                                | (実行後に記入。1文あたりの合成所要時間の実測とあわせて確定)            |

`speaker_uuid`は話者そのものの識別子、`id`(=107)が`/audio_query`・`/synthesis`等のAPIに渡す実際の話者ID。今後`openai_tts_shim.py`の`--speaker`引数や`tts_adapter.py`にはこの`107`を渡す。

声の品質については要件1(人間味のあるかわいらしい若い女性の声)を満たすと判断し、**東北ずん子・ノーマル(ID 107)を採用として確定**する。内蔵TTS(Windowsの`SpeechSynthesis`)の機械的な声とは明確に別物で、VOICEVOX採用の判断は正しかった。

---

#### 想定外の事象:読み上げ開始まで約1分の遅延が発生した ← 本日最大の発見

shim経由でOpen WebUIの「応答の自動再生」を試したところ、**採用した声で正しく読み上げられること自体は確認できた**が、以下の挙動が観測された。

| 観測した挙動 | 内容 |
|---|---|
| 回答テキストの表示 | 従来通り(生成完了時に一括表示) |
| **自動読み上げの開始** | **テキスト表示後、さらに約1分経ってから音声が鳴り始める** |
| 「読み上げボタン」を手で押した場合 | **即座に音声が流れる** |

##### 原因分析(切り分け)

「ボタンなら即時、自動再生だと1分」という差から、**TTSエンジンそのものが常時遅いわけではない**ことがまず分かる。切り分けると原因は2層に分解できる。

**(1) 手動ボタンが速い理由 = 合成が速いのではなく、合成済み結果を再利用しているだけ**

一度自動再生が走った後の音声データ(wav)はブラウザ側にキャッシュされている。手動ボタンはそのキャッシュを鳴らしているだけで、TTS合成をやり直していない。**「ボタンが速い」は性能の証拠にならない**(ここを取り違えると「TTSは十分速い」と誤診して原因を見失う)。

**(2) 遅延の本体 = 「全文完成 → 全文まるごと1回で合成」という直列構造**

現在の経路は次のようになっている。

```
[LLM生成が全部終わる] → [完成した全文をshimへ1回でPOST] → [全文を一括で音声合成] → [ようやく再生開始]
```

これは本ノート「発見2」で指摘した**`ollama_client.generate()`が`"stream": False`固定**であることの直接の帰結であり、**予想していた問題が実機で顕在化した**もの。加えてOpen WebUI側の自動読み上げも「メッセージ確定後に全文を1回TTSへ投げる」設計であり、部分テキストを段階的にTTSへ渡す仕組みを持たない。すなわち**LLM側とUI側の両方が「全文待ち」**になっている。

長文回答ほど`/audio_query`+`/synthesis`の所要時間は文字数に比例して伸びるため、数百文字の回答で数十秒〜1分に達したと考えられる。VOICEVOXをCPU版で動かしている場合、あるいはLLMとGPUを取り合っている場合(論点3のVRAM競合)はさらに悪化する。

##### この構成では要件を原理的に満たせない

「生成と同時に音声を流したい」という要求に対し、**現在のOpen WebUI標準UI + shimという構成は構造的に不可能**である。仮にタスク3(LLMのストリーミング化)だけを実装しても、Open WebUIの自動読み上げが「確定後に全文を投げる」挙動を変えない限り、音声側の体験は1ミリも改善しない。

これは奇しくも**本ノートで「要件5(逐次STT表示)のためには自前UIが必要」と判断した根拠が、TTS側にもそのまま当てはまる**ことを示している。スコープ分割の判断自体は正しかったが、**「shimは9日目までの繋ぎとして普段のチャットを良い声で読み上げてもらう」という当初の狙いは、遅延1分では実用にならず半分しか達成できなかった**。

##### 改善策(9日目で実施する)

文単位で「生成 → 合成 → 再生」をパイプライン化する。全文完成を待たず、句点が現れた時点で1文を確定させて即TTSへ流す。

```
[LLMトークン列] →(句点で切る)→ [1文確定] → [その1文だけ合成] → [再生キューへ]
        ↓ 並行して次の文を生成し続ける      ↓ 並行して次の文を合成し続ける
```

こうすると音声の開始条件が「全文生成+全文合成の完了」から「**最初の1文の生成+その1文の合成**」に変わり、体感遅延の支配項が劇的に小さくなる。必要な部品は次の3つで、いずれも9日目のスコープに含める。

| 必要なもの | 状態 |
|---|---|
| `ollama_client.generate_stream()`(タスク3) | 未着手 → 9日目 |
| `sentence_splitter.py`(トークン列を句点で切ってTTSキューへ) | 未着手 → 9日目 |
| 文単位で再生できる自前UI(Open WebUI標準UIでは差し込む余地がない) | 未着手 → 9日目 |

なお**暫定の緩和策として、shimに文分割+逐次合成を実装しても効果はない**。Open WebUI側が全文を1リクエストで投げてくる以上、shimがいくら内部で細かく合成しても、レスポンスとして1本のwavを返し終えるまでブラウザは再生を始められないため。**shim側での小手先の対処は行わず、9日目の自前UIで根本解決する**方針とする。

#### 残課題

- **1文あたりの合成所要時間が未実測**:上記の改善策が成立するかは「1文を数百ms〜1秒程度で合成できるか」にかかっている。ここが遅ければ文単位パイプラインにしても体感は改善しないため、**9日目の最優先で実測する**(タスク1の未消化項目)
- **遅延の内訳が未計測**:今回の約1分が「LLMの生成待ち」なのか「TTS合成待ち」なのかを、タイムスタンプで分解して確認していない。9日目に工程別に計測してから設計判断(GPU版VOICEVOXにするか等)を下す
- **VOICEVOXをCPU版/GPU版のどちらで動かしているかを記録していない**:論点3(VRAM競合)の判断材料になるため9日目に確認して記録する
- AivisSpeech EngineでのAPI互換実証、AivisHubからのモデルDL(未着手。9日目へ)
- 話速(`speedScale`)を未確定のまま(既定値のまま使用中)。合成所要時間の実測とあわせて9日目に確定する

---

### タスク2:STTエンジンの選定と逐次化の実測 → **未着手。[[サポートAI作製計画/9日目自前音声UIとストリーミング音声対話.md|9日目]]へ全面持ち越し**

> [!warning] 本タスクは8日目では着手していない
> タスク1の途中で「読み上げ開始まで約1分」の問題(タスク1の結果欄参照)の原因分析に時間を使ったため、タスク2は未着手のまま。**作業内容・実施手順は9日目ノートへ転記済み**なので、以降の記述は8日目時点の計画の記録として残す(9日目ノートを正とする)。

#### 作業内容(9日目へ転記済み)

- [ ] **CTranslate2 / faster-whisper が sm_120 (RTX 5070 Ti) で動くかを最初に検証する** ← 最優先リスク
- [ ] 動かない場合の代替(whisper.cpp の CUDA/Vulkan ビルド、CPU int8 実行)を検証する
- [ ] 日本語特化モデル(Kotoba-Whisper 等)と素のWhisper(small/medium)で**認識精度を比較する**
- [ ] `stt_bench.py`(新規)を書く:**同一のwavに対して各エンジン・各モデルサイズの所要時間と認識結果を表で出力し、ファイルに保存する**
- [ ] 固有名詞の認識を実測する:「C.L.A.I.R.E.」「LanceDB」「Ruri」「Tailscale」等(下記論点1)。崩れる場合は`initial_prompt`(語彙ヒント)で改善するか試す
- [ ] **逐次化の方式を決める**:一定間隔で直近バッファを再転写して暫定表示する方式か、部分結果をネイティブに返す軽量エンジン(Vosk等)を暫定表示に併用する2段構えか
- [ ] VADの方式を選ぶ(silero-vad / webrtcvad)。無音判定の待ち時間が体感遅延に直結するため、**閾値を実測して決める**
- [ ] 常駐時のVRAM/CPU使用量を測り、LLM(16GB上限)と競合しないことを確認する(ルーターはCPU固定である点に注意)

#### 実施手順(予定)

```powershell
cd C:\Users\gakuh\Documents\obsidian\サポートAI作製計画\scripts

# 0. まず sm_120 でGPUが使えるかだけを切り分ける(ここで落ちたら方式変更)
python -c "from faster_whisper import WhisperModel; WhisperModel('small', device='cuda', compute_type='int8_float16')"

# 1. ベンチ用の音声を録る(固有名詞を含む台詞を数本。台詞もノートに残す)
# 2. 各エンジン・各モデルサイズで所要時間と認識結果を比較する
python stt_bench.py --audio results/stt_bench/sample01.wav
#    出力先: scripts/results/stt_bench/bench_<日時>.md(表形式で保存する)
```

#### 結果 / 分析 / 改善策

**未着手のため結果なし。** → [[サポートAI作製計画/9日目自前音声UIとストリーミング音声対話.md|9日目]]①で実施する。

#### 残課題

タスク2の全項目([[サポートAI作製計画/9日目自前音声UIとストリーミング音声対話.md|9日目]]①へ)。特に**faster-whisperがsm_120 (RTX 5070 Ti) で動くかの検証は依然として最優先リスク**であり、9日目の最初に潰す。

---

### タスク3:`ollama_client`のストリーミング対応 → **未着手。[[サポートAI作製計画/9日目自前音声UIとストリーミング音声対話.md|9日目]]へ全面持ち越し**

> [!danger] 持ち越したが、優先度は8日目時点より上がった
> 本ノートには「9日目に回すと音声UIが『全文待ち』前提で組まれて初日から作り直しになるため、本日のうちにやる」と書いていたが、実際には着手できなかった。**タスク1で観測した「読み上げまで約1分」の原因そのものがこの未実装**であり、警告が的中した形になる。9日目では**自前UIを書く前に、まずこのストリーミング対応を完了させる**順序を厳守する。

#### 作業内容(9日目へ転記済み)

- [ ] `ollama_client.py`(修正)に`generate_stream()`を**追加**する(`"stream": True`でNDJSONを逐次読み、トークンをyieldする)
- [ ] **既存の`generate()`は変更しない**(後方互換。既存の全呼び出し元に影響を出さない)
- [ ] `generate_stream()`の単体テストを書く(トークンが分割されて届くこと、最後まで連結すると`generate()`と同等の結果になること)
- [ ] `pipe()`がIteratorを返せる経路を用意する(型注釈は既に`str | Iterator[str]`。**CODEルートはACTIONブロックの解析に全文が必要なため、ストリーミング対象外とする**)
- [ ] Open WebUI上で既存のテキストチャットが**トークン単位で流れて表示されること**を確認する(=既存機能の改善であることを実証)
- [ ] **最初のトークンが届くまでの時間(TTFT)を測る**。これが音声会話の体感遅延の下限になる

> [!warning] 既存コードへの変更を含むタスク
> タスク1・2は新規ファイルの追加だけだが、タスク3は既存の`ollama_client.py`と`support_ai_auto_pipe.py`に手を入れる。**必ず`generate()`を残す形で追加する**こと。9日目に回すと音声UIが「全文待ち」前提で組まれて初日から作り直しになるため、本日のうちにやる。

#### 実施手順(予定)

```powershell
cd C:\Users\gakuh\Documents\obsidian\サポートAI作製計画\scripts
python -m unittest tests.test_ollama_stream -v   # ストリーミングのロジックテスト
```

#### 結果 / 分析 / 改善策

**未着手のため結果なし。** → [[サポートAI作製計画/9日目自前音声UIとストリーミング音声対話.md|9日目]]②で実施する。

**分析(着手前に判明したこと)**:タスク1の遅延問題により、このタスクの位置づけが「音声UIの前提条件」から「**現に起きている不具合の根本原因**」へ変わった。8日目時点では「副産物として既存のテキストチャットの体感も改善する」程度の見込みだったが、実際には**これがないと音声対話が成立しない**ことが実測で確定した。

#### 残課題

タスク3の全項目([[サポートAI作製計画/9日目自前音声UIとストリーミング音声対話.md|9日目]]②へ)。加えて9日目では`sentence_splitter.py`(トークン列を文に切る部品)とセットで設計する必要があることが分かったため、9日目ノートでは両者を同一セクションにまとめている。

---

### 想定される論点(実施前の仮説)

1. **STTの誤変換がRAG検索を壊す可能性**:「C.L.A.I.R.E.」「LanceDB」「Ruri」などの固有名詞が音声認識で崩れると、7日目で構築したノート検索がヒットしなくなる恐れがある。実測して、必要ならWhisperの`initial_prompt`(語彙ヒント)で対策する
2. **遅延の積み上がり**:STT + ルーター判定 + RAG検索 + 生成 + TTS が直列に積み上がる。7日目時点で実機応答は20秒以内だったが、音声だと体感の許容値はもっと厳しい(数秒)。**どの工程が支配的かを分解して測る**ことを優先する。ストリーミング化(タスク3)により「全文生成完了」ではなく「最初の1文の完成」が音声開始の条件になるため、支配項が変わる見込み
3. **VRAM競合が3者間になった**:従来はWhisperとLLMの2者だったが、**TTSエンジンのGPU版を使うと3者で16GBを取り合う**。優先度はLLM > STT > TTSと考える(TTSは軽く、CPUでも十分な可能性が高い)。実測してGPU/CPUの割り当てを決める
4. **要件1が主観評価であること**:「かわいい」は数値化できないため、聞き比べたwavと採用理由をノートに残すことで**判断の再現性を担保する**。後で気が変わったときに、どの候補を既に聞いて外したかが分かる状態にする

### ②全体のまとめ

**進捗:タスク1のみ完了(それも一部未消化)。タスク2・タスク3は未着手で9日目へ全面持ち越し。**

- **採用した構成**
  - TTSエンジン+声:**VOICEVOX ENGINE(`http://127.0.0.1:50021`)/ 東北ずん子・ノーマル / 話者ID `107`**。話速は未確定(既定値)
  - STTエンジン+モデル:**未選定**(タスク2未着手)
  - ストリーミングの可否:**未対応のまま**(タスク3未着手)。`ollama_client.generate()`は`"stream": False`固定
- **「想定される論点」1〜4の答え合わせ**

| # | 仮説 | 結果 |
|---|---|---|
| 1 | STTの誤変換がRAG検索を壊す | **未検証**(タスク2未着手) |
| 2 | 遅延の積み上がり。どの工程が支配的か分解して測る | **部分的に的中、かつ想定より深刻**。分解計測はできていないが、**TTS工程だけで約1分**という支配的な遅延を実測。原因は「全文生成完了を待ってから全文を一括合成する」直列構造 |
| 3 | VRAM競合が3者間になる | **未検証**。ただしVOICEVOXをCPU/GPUどちらで動かしているかすら記録していないことが判明したため、9日目で先に現状把握する |
| 4 | 要件1が主観評価であること | **対処済み**。東北ずん子(ID 107)を採用し、エンジン・話者ID・スタイルを数値で記録した。ただし**聞き比べた他候補と外した理由は記録できていない**(再現性の担保としては不十分) |

- **9日目のUI実装に渡す前提条件**
  1. TTSの声は確定済み(VOICEVOX / speaker=107)。`tts_adapter.py`はそのまま再利用できる
  2. **ストリーミング未対応**のため、9日目は「UIを書く前にタスク3を終わらせる」順序が必須
  3. **STT方式が未決**のため、9日目は「faster-whisperがsm_120で動くか」の検証を最初に置く必要がある(ここが不発ならSTT方式ごと変わり、UI設計にも波及する)
  4. Tailscale ServeのHTTPSが使えるため、スマホのブラウザからでも`getUserMedia`(マイク入力)のセキュアコンテキスト要件を満たせる(①の副次的な成果)

---

## ③ 実装の成果物一覧(実行後に確定)

### プログラムファイル

| ファイル | 役割 | 実行方法 | 出力先 |
|---|---|---|---|
| `scripts/tts_adapter.py`(新規) | タスク1:VOICEVOX互換API(VOICEVOX / AivisSpeech両対応)を叩き、テキスト→wavバイト列を返す共通部品 | **単体実行はしない**(他スクリプトからimportして使う部品) | なし(呼び出し元に返す) |
| `scripts/tts_voice_survey.py`(新規) | タスク1:全話者×全スタイルに同一台詞を読ませ、聞き比べ用wavを一括生成する | `python tts_voice_survey.py --engine-url <URL> --label <名前>` | `scripts/results/voice_survey/<label>/<speaker>_<style>.wav` |
| `scripts/openai_tts_shim.py`(新規) | タスク1:OpenAI互換`/v1/audio/speech`をVOICEVOX互換APIへ橋渡しし、Open WebUIから採用した声を使えるようにする | `python openai_tts_shim.py --engine-url <URL> --speaker <ID> --port 5051` | なし(常駐サーバー。ログは標準出力) |
| ~~`scripts/stt_bench.py`(新規)~~ | タスク2:同一wavに対し各STTエンジン・モデルサイズの所要時間と認識結果を比較する | **未作成 → 9日目へ** | — |
| ~~[[サポートAI作製計画/scripts/ollama_client.py]](修正)~~ | タスク3:`generate_stream()`を追加する | **未実施 → 9日目へ**(現状`"stream": False`固定のまま) | — |
| ~~[[サポートAI作製計画/scripts/openwebui_pipe/support_ai_auto_pipe.py]](修正)~~ | タスク3:`Iterator[str]`を返す経路を用意する | **未実施 → 9日目へ** | — |
| ~~`scripts/tests/test_ollama_stream.py`(新規)~~ | タスク3:`generate_stream()`のロジックテスト | **未作成 → 9日目へ** | — |

> [!note] 8日目に実際に作られたのは上3つ(`tts_adapter.py` / `tts_voice_survey.py` / `openai_tts_shim.py`)のみ
> 取り消し線の4件は未作成・未修正。9日目ノートの成果物一覧を正とする。

### 設定ファイル

| ファイル | 役割 | 備考 |
|---|---|---|
| Open WebUIの音声設定(Settings → Audio) | タスク1:TTSエンジンをOpenAIにし、shimのURLを指定する | **GUI設定のためファイルとして残らない**。設定値をこのノートに書き写して記録すること |
| **採用した声の設定値**(エンジンURL・話者ID・スタイルID・話速) | タスク1:確定した声のパラメータ | **どのファイルにも自動で残らない**。スクリプトの引数として渡すため、**必ずこのノートの結果欄に数値で書き残す**こと。記録がないと再現不能になる |
| (Tailscaleの設定) | ①:`tailscale serve`の公開設定 | **vault外**。`tailscale serve status`の出力をノートへ貼り付けて記録する。**認証情報(秘密鍵等)は絶対にvaultへコピーしない**(Obsidian同期でクラウドに載るため) |

---

## ④ 動作確認手順(実施予定)

- [x] ①:`tailscale status`でMagicDNS名が割り当てられている(`go9029.tail81b83d.ts.net`)
- [x] ①:`tailscale serve status`で**Funnel: off**である(インターネット非公開) ← 重要(`(tailnet only)`表記を確認)
- [x] ①:スマホのモバイル回線から`https://<端末名>.<tailnet名>.ts.net`で会話できる
- [x] ①:**Tailscaleアプリを切断すると同URLへ到達できない** ← 最重要
- [ ] ②タスク1:VOICEVOXとAivisSpeechの**両方に同じコードで接続できた**(API互換の実証) → 9日目へ
- [x] ②タスク1:聞き比べ用wavが`results/voice_survey/`に生成された
- [x] ②タスク1:**採用する声を決め、エンジン・話者ID・スタイルをノートに数値で記録した**(話速のみ未確定 → 9日目へ)
- [x] ②タスク1:Open WebUIの普段のチャットが、採用した声で読み上げられる(shim経由の疎通)**ただし約1分の遅延あり**
- [ ] ②タスク1:1文あたりの合成所要時間を実測した → **9日目へ(最優先)**
- [ ] ②タスク2:**faster-whisperがsm_120で動くか/動かないかの結論が出た** → 9日目へ
- [ ] ②タスク2:同一wavに対する各エンジンの所要時間と認識結果が表でファイルに保存された → 9日目へ
- [ ] ②タスク2:固有名詞(C.L.A.I.R.E. / LanceDB / Ruri 等)の認識結果を記録した → 9日目へ
- [ ] ②タスク2:逐次表示の方式(暫定表示をどう作るか)とVADの方式・閾値を決めた → 9日目へ
- [ ] ②タスク3:`generate_stream()`のテストが通る → 9日目へ
- [ ] ②タスク3:**Open WebUIの既存テキストチャットがトークン単位で流れて表示される** → 9日目へ
- [ ] ②タスク3:最初のトークンが届くまでの時間(TTFT)を秒数で記録した → 9日目へ
- [ ] ②タスク3:既存の`generate()`を使っている箇所が**従来通り動く**(後方互換の確認) → 9日目へ

---

## ⑤ 残課題・次回への持ち越し(7日目から継続 + 記入用)

### 9日目に確定で持ち越すもの(本日②のスコープ分割による)

> [!important] 9日目ノートを作成済み → [[サポートAI作製計画/9日目自前音声UIとストリーミング音声対話.md]]
> 以下の項目はすべて9日目ノートへ転記済み。**作業する際は9日目ノートを正とすること**(ここは8日目時点での引き継ぎ記録)。

**本日の実測により新たに確定した持ち越し(最優先):**

- **読み上げ開始まで約1分かかる問題の解消**(タスク1の結果欄に原因分析を記載):原因は「全文生成完了 → 全文を一括TTS合成」という直列構造。**Open WebUI標準UI + shimの構成では原理的に解決不可能**であり、`generate_stream()` + `sentence_splitter.py` + 自前UIの3点セットで文単位パイプライン化する以外に手がない。9日目の中心課題
- **1文あたりのTTS合成所要時間の実測**:上記パイプライン化が成立する前提条件。ここが遅ければ設計ごと見直しになるため、9日目の最初に測る
- **タスク2(STTエンジンの選定と逐次化の実測)の全項目**:8日目では未着手
- **タスク3(`ollama_client`のストリーミング対応)の全項目**:8日目では未着手。**自前UIより先に着手する順序を厳守**
- **VOICEVOXのCPU版/GPU版の別を確認して記録する**(論点3のVRAM競合判断のため)
- **話速(`speedScale`)の確定**

**8日目時点で予定していた持ち越し:**

- **自前音声UIの実装**:WebSocketゲートウェイ + ブラウザUI。**案A′**(自前ゲートウェイが`support_ai_auto_pipe.Pipe`を直importして呼ぶ)で組む。要件5(逐次STT表示)はここで初めて満たされる
  - 対象端末:自宅PCのブラウザ(localhost)+ スマホのブラウザ(Tailscale Serve経由のHTTPS)
  - 駆動方式:常時マイクON + VADで自動送信。読み上げ中はマイクOFF。**バージインは初版では実装しない**
  - 部品構成の想定:`voice_gateway.py`(FastAPI+WebSocket)/ `vad.py` / `stt_engine.py` / `sentence_splitter.py`(トークン列を句点で切ってTTSキューへ)/ `tts_adapter.py`(タスク1で作成済みのものを再利用)/ `static/index.html`
- **エンドツーエンド遅延の実測**:発話終了→応答音声の再生開始まで。ルート別(FAST/DEEP/CODE)に工程を分解して測る。**UIができてからでないと測れないため9日目**
- **音声でCLARIFYルートを踏んだときの挙動確認**(4日目743行目の懸念。「続けて応答」ボタンが音声操作の妨げにならないか)。UI前提のため9日目
- **音声入力時にRAG記憶レイヤーが正常に働くかの確認**(STTの誤変換が検索精度を落とさないか)。本日タスク2で固有名詞の認識精度は測るが、**検索まで通した実測は9日目**
- **`RouterSession`がプロセス別になる問題**:Open WebUIプロセスと自前ゲートウェイプロセスで直前ルートの保持が別々になる。実害は小さいと判断したが、9日目に実機で確認する
- **VRAM競合の3者調整**:LLM / STT / TTSで16GBを取り合う。優先度はLLM > STT > TTSと仮定。UI稼働時の実測は9日目

### 7日目から継続

- **`max_distance=0.45`の厳格化**(7日目⑤で判明):明確に無関係な質問(「宇宙の年齢は?」等)でもdistance 0.32〜0.44でヒットし、正解質問群(0.21〜0.33)との分離が甘い。0.35前後への変更を検討する(単純な閾値変更だけでは0.32〜0.33付近の重複を解消しきれない点に注意)
- **検索結果のハルシネーション対策**(7日目⑤で判明):FASTルート(`gemma4-e4b-cpu`)が、検索で正しい文脈を拾えていても数値を一般知識で補完してしまうケースを確認。`format_context()`の指示文強化で直接引用を促す
- **7日目⑥のセクション整理**:Cloudflare Tunnel前提の手順が残っているため、本ノート①を正とする旨を追記する
- CLARIFYの「粘着」対策:`build_user_prompt`が`last_route="CLARIFY"`を渡すとルーターがCLARIFYを維持し続ける(6日目⑧-2で発見)。**②の音声対話で実害が出る可能性があるため、優先度が上がる見込み**
- Ruri v2 → v3-310m 差し替え検証(768次元で同一。接頭辞の変更を忘れないこと)
- RECALL_TRIGGERSの網羅性(6日目⑧-4):実運用で誤判定を見つけ次第、正規表現を追加する運用課題
- 記憶の肥大化対策(要約圧縮・削除ポリシー)の実装:7日目のノート取り込みで806件まで増加済み
- Profile層(`user_profile`テーブル)の実装(5日目⑩・6日目⑦の設計のみ済み)
- コードブロックを分断しないチャンク分割(6日目③からの持ち越し)
- **クロスOS対応**:`db_path`のWindows固有表記をやめ、環境変数やOS判定で解決する。着手条件=実際に別OSから使う必要が生じたとき
- 外出時のCODEルート(コード実行)の扱い方針(①のセキュリティ残論点)
- ノート再取り込みの運用方針:現状は`ingest_notes.py`を毎回全量再実行する前提。差分取り込みの最適化は所要時間が気になり始めてから
- (任意)Open WebUIの「タスクモデル」を`phi4-mini-cpu`等へ固定する(6日目⑨の最適化課題)
- (実行後に追記)

---

## 📌 次のステップ

1. ~~4モデルをOllamaで実際にダウンロード・動作検証~~
2. ~~Phi-4-miniの振り分けロジック(プロンプト設計・実装)~~
3. ~~RAGの記憶DB構築(土台)~~ → [[サポートAI作製計画/5日目RAG記憶DB構築.md|5日目]]
4. ~~C.L.A.I.R.E. (Auto) Pipeへの記憶レイヤー組み込み(検索+書き戻し)~~ → [[サポートAI作製計画/6日目RAG記憶レイヤーのPipe組み込み.md|6日目]]
5. ~~Obsidianノートの取り込み(ingest)~~ → [[サポートAI作製計画/7日目Obsidianノート取り込みとCloudflareTunnel.md|7日目]]
6. ~~外部アクセス環境のセットアップ(~~Cloudflare Tunnel~~ → **Tailscale**に変更)~~ → 今回①で完了
7. STT/TTSパイプラインの組み立て ← 今回②。**声の確定(VOICEVOX/東北ずん子/ID 107)まで完了**。STT選定(タスク2)とストリーミング化(タスク3)は**未着手のまま[[サポートAI作製計画/9日目自前音声UIとストリーミング音声対話.md|9日目]]へ持ち越し**
8. **読み上げ開始まで約1分かかる問題の解消**(今回②タスク1で実測して発覚)→ [[サポートAI作製計画/9日目自前音声UIとストリーミング音声対話.md|9日目]]。文単位の生成→合成→再生パイプライン化で根本解決する
9. 自前音声UIの構築(逐次STT表示・VAD自動送信・逐次読み上げ)→ [[サポートAI作製計画/9日目自前音声UIとストリーミング音声対話.md|9日目]]。案A′で`Pipe`を直呼びする
