# requirements.txt
#
# 15日目⓪(サポートAI作製計画/15日目音声による音楽再生(ytmusicapi+Brave).md)の
# 採用条件3: バージョンを上げるときは⓪と同じ監査(バイナリの有無・危険呼び出し・
# 通信先・書き込み箇所の検分)を再実行してからにすること。
#
# media_player.py が曲名→動画IDの解決(検索のみ・認証なし)に使う。
ytmusicapi==1.12.2
